# 変更履歴(CHANGELOG)

本ファイルはアプリ `index.html` の版ごとの変更記録である(README「技術メモ」から
2026-07-21 に分離 — ロードマップ P2 群)。新しい版が上。バージョンは `APP_VERSION`
(index.html)と package.json の major.minor が QA `version.sync` で同期検査される。

## v1.39-b1(β・開発中)

第88便(2026-08-08)— 初見レビュー3件(外部AIレビュー)の共通指摘「主張の境界が初見では
見えない」への対応スプリント1。**すべて表示専用**(物理・保存 JSON・エクスポート封筒は 1 bit 不変):

- **初見ガイド**(EXT-01)— 初回起動時(http(s) 配信のみ)に3枚カード「DFMとは/できること/
  しない主張」を表示。タイトルタップの About パネルからいつでも再表示可(QA `ui.firstvisit`)。
  第89便: 表示中に 🌐 ボタンで日英を切り替えられるようにした(アプリ全体の言語設定と連動)
- **パラメータ役割の3分類**(EXT-03)— 全パラメータに「物理仮定/実験条件/数値」の役割チップと
  凡例を表示。何を変えると「宇宙のルールが変わる」のかを明示(宣言 `PARAM_ROLES`・QA `params.roles`)
- **サンプル単位の「しない主張」**(EXT-04)— 実在天体名・実現象名を使う40サンプルに、
  タイトル直下の「⚠ …」行(nc_* 9種: dm/bh/galaxy/nebula/cosmo/cal/solar/thermo/toy)を表示。
  実在天体アナログの二段名称を兼ねる(宣言 `notClaim`・QA `ui.notclaim`)
- **Failure First**(EXT-09)— ☿水星で「Base DFM: FAIL(1PN前進を再現しない)→ E12 ON: PASS」を
  本文より先に表示。負の結果を隠さない方針の UI 化(宣言 `failureFirst`・QA `ui.failure-first`)
- **摂動注入の正式化**(EXT-11・第90便)— 創発標準試験⑤「摂動回復」のアプリ操作版。
  シミュレーションカテゴリに「摂動注入」(強さ 弱/中/強+スピン半数反転)。全運動量は
  質量重みつき平均差引で厳密保存・角運動量/エネルギーは帳簿外の外部介入として明示。
  乱数は決定論(エンジン API `injectPerturb` — QA `perturb.inject`)
- **標準試験チェックリスト**(EXT-07・第91便)— stdTests 宣言のあるサンプル(🥚🔥)の説明タブに
  6試験(ノックアウト/用量反応/多seed/Nスケーリング/摂動回復/時間窓)の PASS 表を表示。
  宣言専用メタ — 実測の正本は stdTestsRef のハーネス(QA `ui.stdtests`)
- **保存モニタの拡充**(EXT-14・第91便)— 閉鎖/開放系の自動判定行(開放要素の内訳つき)と
  エネルギー内訳行(E_kin/E_rot/E_int — 読み口 `energies()`)を保存量モニタに常設
  (QA `monitor.system-energy`)
- **プリセット選択のウィンドウ化**(第92便)— プルダウンをボタン(選択中サンプル名)+
  モーダルに変更。グループ/スケール/分類/E水準チップと検索語(名前 ja/en・ID・絵文字)で
  絞り込み(QA `ui.presetpicker`)
- **距離表示**(第92便)— 画面左下にスケールバー(1/2/5×10^k モデル単位)・右下に表示幅。
  スケール換算ON時は実単位(nm〜ly)併記。表示トグルあり(QA `ui.scalebar-conv`)
- **粒子詳細のスケール換算**(第92便)— 換算表示ON時、編集パネルに m(kg/M⊕/M☉)・R(m〜ly)・
  |v|(m/s・km/s)・Ω(1/s)の換算行を表示
- **トイ/現実準拠の区別**(第92便)— 宣言 `fidelity:"real"`(6件: ⚾♨️🧪🛰️ 土星D68・🌕)に
  📏現実準拠チップ。宣言なし=トイ(誇張アナログ)が既定。プリセット宣言 `scaleExp:{L,T,M}` で
  換算指数を較正可能に(QA `ui.fidelity` / `scale.preset-exp`)
- **「日常」タグの整理**(第92便)— 日常タグは「g_y が ≈9.8 m/s² に換算される較正」を規約化
  (⚾0.08・♨️0.03・🧪0.05 → いずれも ≈9.8 m/s²)。⏪時間の矢は天体スケールへ再分類
- **コア内訳(v2)の見切れ対策**(第92便)— パネル幅188px+3値行の縮小フォント+全文ツールチップ
  (QA `ui.corev2-fit`)
- **「すべて表示」をウィンドウ内へ移動**(第93便)— ファミリー変種トグルをヘッダから
  プリセット選択ウィンドウ内へ(状態は同期・ヘッダ側は非表示で維持)
- **スケールバーの配置調整**(第93便)— 表示幅を右下→左下2行目へ(右上の粒子詳細パネルと
  重ならない)
- **粒子詳細の行別スケール換算**(第93便)— m/Ω・R・vx/vy の各行直下に換算副表示
- **換算の慣習アンカー**(第93便)— プリセット個別較正(第92便 scaleExp)を撤回し、
  (スケール×パラメータ)の慣習表 `SCALE_FAMILIAR` へ移行: 光速は全スケールで
  「3.0 → 3×10^8 m/s」・日常の g_y/g_x/ε は値がそのまま m/s²・m(「9.8 で 9.8 m/s²」)。
  馴染み優先の表示規約であり単一次元系ではないことを説明に明記。fidelity:"real" は
  較正実体のある3件(🛰️・土星D68・🌕)に整理(QA `scale.familiar` / `ui.fidelity`)
- **時間倍率 → 時間倍率指数**(第93便)— 数値欄を指数 x(10^x 倍)表示へ。x=0.0 が現在の
  表示スケールでの「実時間」(公称60fps)。**保存値・スライダー・セーブ JSON は従来の倍率の
  まま**(表示変換のみ — 互換不変)
- **プリセット選択の視認性**(第94便)— 絞り込み各行に下辺セパレータを追加。E水準絞り込み
  「E0」は emergence 宣言なし(=E0 扱い)のサンプルも含むよう修正(QA `ui.presetpicker`)
- **粒子換算の画面下移設**(第94便)— 第93便のパネル内行別換算(#beConv 系)を廃止し、粒子の
  スケール換算値(m/Ω/R/vx/vy)は「粒子選択時に画面下に表示される詳細」の3行目に表示。
  「選択粒子の編集」を畳んだときは表示しない。スケールバーは詳細表示中その分だけ上へ退避し
  重ならない(QA `ui.scalebar-conv`)
- **「スケール」カテゴリ新設**(第94便)— 「表示」の直後に新設し、スケールバー表示(旧
  「スケールバー」トグル — 共通設定・既定ON)・スケール換算表示(共通設定・**既定ONへ変更**)・
  距離スケール指数(旧「表示スケール指数」)・新設の時間/質量/光速スケール指数を集約。
  4指数とも表示専用(物理不変)で、サンプルのロード時にスケールタグ既定へ戻る。第93便の
  慣習アンカー表(SCALE_FAMILIAR)は撤回し「タグ既定指数+スライダー上書き」へ一本化 —
  光速は専用指数 eC(既定 8.0 = 全タグで「3.0 → 3×10^8 m/s」)が担う(QA `scale.exponents`)
- **時間倍率指数 → 時間倍率へ戻す**(第94便・原仮定者指示)— 第93便の指数表示(texp)を撤回し、
  数値欄は従来の倍率表示へ復帰。表示専用の時間換算は新設「時間スケール指数」が担う(役割分離)
- **壁別反発係数**(第94便)— 箱境界の4面それぞれの反発係数を `world.wallRest:{bottom,top,left,
  right}`(各0..1)で宣言可能に。既定は従来の 0.95(**全面既定なら既存プリセットと bit 不変**)。
  「実験箱」カテゴリは箱境界のサンプルで常に編集可能になり(伝熱壁が無くても反発係数4行を表示)、
  実行中編集は ⏮・チェックポイント・セーブ(非既定面のみ保存)にも追随(QA `box.wall-restitution`)
- **HUD換算時間に時間倍率を反映**(第95便)— スケール換算のHUD行に「倍率N: 再生1s≈Xs」
  (再生1秒あたりの宇宙時間 = 公称60fps×倍率×10^eT)を併記
- **粒子換算の常時表示+改行**(第95便)— 画面下詳細のスケール換算を「選択粒子の編集」を
  畳んでいても表示し、見切れないよう2行(m/Ω/R と vx/vy)に分割(QA `ui.scalebar-conv`)
- **カテゴリ説明の全カテゴリ整備**(第95便)— 時空/スピン・熱/引きずり・測地線/実験箱/
  シミュレーション/グラフ/表示にも説明ⓘを追加(全カテゴリ完備 — QA `ui.wave95`)
- **実験箱カテゴリの条件表示**(第95便)— 編集対象が無い(箱境界でも伝熱壁でもない)
  サンプルではカテゴリごと非表示(QA `ui.54d-params` / `ui.wave95`)
- **カメラ追従の移動と非選択対応**(第95便)— 「表示」→「共通設定」へ移動。粒子が非選択でも
  「選択粒子」を設定でき、非選択の間は「なし」と同じ挙動で選択した瞬間から追従
  (自動解除は「天体#N」の対象消滅のみに限定 — QA `camera.follow-ui`)
- **⏮初めからの保持対象拡大**(第95便)— 「グラフ」トグル・「スケール」指数・「箱宇宙」の
  実行中編集は ⏮ でリセットしない。グラフトグルとスケール指数上書きはセーブにも保存/復元
  (`graphOverlays`/`scaleExps` — 旧セーブ不変。QA `ui.wave95`)
- **箱宇宙パラメータの A/B 対応**(第95便)— 書き込み先を編集対象側(editSim)へ変更し、
  膨張則あり/なし等の対照実験を A/B で組めるように(A/B開始時は A の編集値を B へ引き継ぐ。
  旧「両宇宙へ同値」方式は廃止 — QA `box.edit-ui` 更新)
- **「重力 G」の表示位置**(第95便)— 時空カテゴリ内で「光速 c₀」の直後へ移動(定義順のみ)
- **スケール規約の一律化・第1段**(第95便)— 光速スケール指数の既定を 7.0 に変更
  (「c₀=30 → 3×10^8 m/s」の一律規約)。日常タグ既定指数を 距離/時間 −2.5・質量 −2.5 に変更
  (g_y 0.031 → ≈9.8 m/s²)。⚾は相似変換で g_y=0.031・c₀=30 へ再調整(軌跡不変)、
  ♨️🧪は g_y=0.031 で**日常タグへ復帰**(♨️は+3.3%・🧪は0.05→0.031 — 分離持続を実測)。
  G=0 で光学・PN と結合しない分子系8件(🔥🎈📏🌈🧬🧊⛓️♻️)の c₀ を 30 へ統一(力学 bit 不変
  — ♨️🧪の意図的変更は零コスト基準ハッシュを世代判定つきで再採取)。**G>0 系の c₀=30/
  G×(30/c)² 統一は相似変換+QA再較正を要するため第96便以降の段階適用**(QA `scale.exponents`)
- **スケールタグの7分類化**(第98便A)— 5分類(分子/日常/天体/銀河/宇宙全体)を
  **7分類(分子/ビーカー/日常/惑星/恒星/銀河/宇宙全体)**へ(原仮定者指示)。旧「日常」の較正
  (L=T=M=−2.5・g_y 0.031→≈9.8 m/s²)は**ビーカー**(♨️🧪)が引き継ぎ、旧「天体」21件は
  惑星8件(アンカー据置 10万km/2.8h/0.17M⊕)と恒星13件(新アンカー ≈1AU/0.3年/0.5M☉ —
  外部レビュー提案を採択)へ分割。旧5分類の celestial 宣言は読込時に惑星へ移行(アンカー同一)。
  AI雛形も7種へ(QA `preset.scale-tier`/`scale.exponents`/`ai.scale-templates` 更新)
- **「日常」タグは⚾のみ・1単位=1m/1s/1kg の実値規約**(第98便B)— 日常アンカーを {0,0,0} とし、
  ⚾自由落下を**第94便パラメータへ復元**(g_y=9.8〔そのまま 9.8 m/s²〕・vx 33.2〔=33.2 m/s〕・
  timeScale 0.271。c₀ のみ一律規約の 30 — ⚾は光速非依存)。原仮定者指示+外部レビュー2件
  (Grok/Gemini「日常は 1m/1s/1kg が最も直感的」)の採択
- **外部レビュー指摘の反映**(第98便C)— 光速指数の名称を「c₀ 表示較正指数」へ改め、
  距離/時間指数から導く一般速度換算(10^(L−T))とは独立した参照較正であることをヘルプに明記
  (ChatGPT P1-1)。旧日常アンカーの密度根拠コメント(鋼球相当)を撤去(同 P1-3 — アンカーは
  表示専用の代表単位であり密度等の導出根拠にしない)
- **スケール一律化の整理 — 光速非依存サンプルは c₀=30 のみ**(第97便A)— 光線・減光(auto)・
  PN・光子・τ時計のいずれも主役でないサンプル24件(🪐🎯🕶️🌌系2・⏪🕊️🕸️🌠🌫️🌪️☀️🌋🌍🌕🛰️♾️🔄
  箱宇宙5・probeH)は、第96便の相似変換を巻き戻して **c₀=30 に変更しただけ**の状態にした
  (cLight は力学に入らないため運動は従来と bit 一致 — τ比のみ僅かに変わる。原仮定者指示
  「c₀を30に変更しただけで支障が無い場合はその状態にする」)。相似変換(k=30/旧c₀)を維持する
  のは光速が意味を持つ20件(💡🔭🛰⚫🪜🌱🥚☿🌌⏳系3・🎡幾何・⭐・🚋💨・時計系)のみ。
  QA の世代係数・零コスト基準もこの整理に追随(巻き戻し分は従来基準へ復帰)
- **カメラ追従の永続化**(第97便B)— 追従設定を他の共通設定と同様に保存(hp_camfollow)。
  「初めから」やサンプル切替でも解除しない(未設定ユーザーのみサンプル既定 camera.follow に従う)。
  追従先の粒子が融合したら**合体後の粒子へ引き継ぎ**(index 写像 onCompact — 選択粒子も同様)、
  消滅しても設定は解除しない(「なし」と同じ挙動で待つ。QA `camera.follow-ui` 拡張)
- **時間倍率・実効倍率の範囲拡大**(第97便C)— 時間倍率の設定範囲と実効倍率(時間倍率×簡易倍率の
  クランプ後)を **0.01〜100** へ拡大(旧 0.1〜16/実効上限12)。dt は不変で倍率はサブステップ数で
  刻む(ステップ会計の上限 24→200步/フレーム — QA `time.step-accounting`/`ui.wave96a` 更新)
- **desc・多seed 記録の再実測**(第97便D)— 相似変換を維持したサンプルの説明文実測値を c₀=30
  単位へ更新(⚫殻スピン 0.075→0.665・コアΩ 10→2.12/🌱Ω 1.125→約121〔比107.9 は相似不変〕/
  🐚単層対照 保持0.39・攪乱3.70)。多seed 正本 exp-4-85(🥚16seed)・exp-4-88(⏳16seed)を
  新単位で再採取(🕶️は巻き戻しにより旧記録が正のまま)
- **カメラ追従・HUD実効倍率・箱A/Bの修正3件**(第96便A)— ①カメラ追従が「選択粒子」設定でも
  粒子非選択の間はドラッグで画面を直接移動できるように修正 ②HUD スケール換算の時間倍率表示を
  「シミュレーションの時間倍率×フッターの簡易倍率」の**クランプ後の実効値**(計算で実際に使う値)へ
  ③箱宇宙パラメータも A/B 比較の「最後に編集したパラメータ」比較表示に対応(QA `ui.wave96a`)
- **スケール規約の一律化・第2段**(第96便B)— G>0 系の残り44サンプルへ**力学的相似変換**
  (k=30/旧c₀)を適用し、全55サンプルの光速を c₀=30 に統一した: G・bM×k²・η_rad/κs/Kcs/
  contract×k・明示速度/スピン/ω×k・timeScale÷k・validT 等の時刻÷k・tInt/内部E×k²・
  sourceRate×k³・箱 H0/freq×k(変換表: 旧c60→k0.5/c100→k0.3/c40→k0.75/c25→k1.2/
  c70→3/7/c6→k5)。**接触の法線ばね定数をパラメータ化**(`physics.contactK`/`contactCap` —
  既定 40/8 で従来と bit 不変・宣言時のみ有効)し、変換44件に ×k² を宣言して融合カスケード
  (🥚🌱☀️ 等)の相似性を保った。時間再パラメータ化に伴い QA の評価窓を世代判定つきで
  步数×(旧c₀/30) にスケール(W5C ユニット16件+インライン17箇所+零コスト基準の再採取)。
  放射冷却 Λ∝ηT^p は p≠1 で厳密相似でないため、熱系(☀️🌪️🕶️ 等)は再実測値で窓を較正した
- **「日常」タグの g_y=9.8 既定化**(第94便)— ⚾自由落下を**力学的相似変換**で実値化:
  gravityY 0.08→9.8・速度×√122.5≈11.068(vx 3→33.2 等)・timeScale 3→0.271・cLight 60→664。
  純粋な一様重力+瞬間壁反射はスケールフリーなので軌跡・τ比は従来と同一。♨️対流・🧪浮力は
  接触剛性・kRep クランプに律速され相似変換が成立しないため分子スケールへ再分類(統括裁量)。
  一様重力 g_y のスライダー範囲を ±5→±10 に拡大(CLAMPS と一致)

## v1.38.0(2026-08-08)

**更新前の注意**: β版とルート版は同一オリジンで localStorage(セーブ・APIキー)を共有する。
大きな更新の前にはセーブタブから JSON エクスポートでのバックアップを推奨する。

第80〜86便(2026-08-05〜07)で v1.38 を確定し、ルートへ昇格した(原仮定者指示「Releaseを
進める」・フルゲート root/beta ALL PASS)。**この版の主題は「支配天体を pinned から解放し、
コアを一本の仕様へ畳み、創発を測れる形にした」こと**である:

- **E6′-R 反作用**(第80便A)— フレーム引きずりの要求を速度へ直接書かず、自由対ごとの
  換算質量対称インパルスとして配る `physics.frameReaction:"pairReduced"`(opt-in・既定
  `legacy` は 1 bit 不変)。対の線運動量が構成的に厳密保存するので、**支配天体の `pinned` を
  外せる**ようになった
- **コアv2 一本化**(第81便A・**破壊的変更**)— コアv1(比率仕様 `coreMR`/`coreSR`/`coreRR`)を
  エンジンから廃止し、コアは J_core を主変数とする `core:{}`(コアv2)だけになった。
  **旧セーブ・旧プリセット JSON は読込時に移行式でコアv2 へ変換されるので失われない**
  (移行が保証するのは**初期状態一致**で、コアが時間発展する構成の軌跡一致は保証しない)。
  第86便で代表的な旧セーブを固定資産化し、移行の来歴をエクスポート封筒へ記録するようにした
- **創発の標準試験と E水準**(第82便B・第83便A・第84便A/B)— 多seed・スケーリング・摂動回復・
  時間窓の4試験を共通の型にし、サンプルごとに **E0〜E3 の水準**を実測で格付けした。
  創発モニタ(表示専用)と相図ランナー(アプリ内2変数バッチ掃引)を追加
- **光学輸送**(第82便A)— 体積吸収・波長依存(赤化)・散乱。透過率を 1 本の式に統一し、
  力学と光線の幾何は**厳密不変**(観測層だけが変わる)ことを機械固定した
- **ファミリー表示**(第82便C)— サンプルを系統(ファミリー)で束ねて一覧できるようにした
- **新サンプル**: 🥚自己形成ダークローター(E3)・🪜隠れ質量ラダー・🌆赤化など

第86便(2026-08-08): **v1.38.0 昇格**(第79便=v1.37.0 昇格と同手順)— beta→root コピー
(byte 同一)・APP_VERSION=1.38・package/lockfile 1.38.0・root SW=`dfm-release-v1.38`・
beta は次期プレビュー線 v1.39-b1 へ(BETA_BUILD/beta SW=`dfm-beta-v1.39-b1`)。
CITATION.cff を 1.38.0 へ同期。昇格により、root では beta 専用機能・プリセットを前提とする
QA が一斉に SKIP から実行へ変わり、perf の `EXTRA_SAMPLES`(informational)も root に実体が
揃って比較ゲートへ自動昇格する(第79便と同じ仕組み)。

- **フルゲート**: root **396/396**・beta **396/396** ALL PASS。両者の結果 JSON が記録する
  対象 HTML の SHA-256 は同一(= beta→root の byte 一致の機械証跡)。root/beta の差は
  `version.sw-sync`+`release.no-beta-identifiers`(root 専用2件)と
  `version.beta-label`+`version.beta-label-http`(beta 専用2件)だけになった
- **QA の追随(実測して期待値を分岐)**: `ui.aitab-no-hscroll` の `TARGET.startsWith('beta/')`
  ガードを外した。これは第33便当時「修正がまだ root に昇格していない」ための beta 先行ガードで、
  昇格で root にも修正が載った以上そのままでは **root 側が恒久的に休眠**する(第85便の
  🕶️休眠5件と同型の事故)。ガードを **AI追加タブの存在**の機能判定へ差し替え、昇格後の
  root/beta 両方ではみ出し要素0を実測して確認した。他の root/beta 分岐(両受理形・機能判定)は
  設計どおり自動で新しい側を取り、期待値の書き換えは不要だった
- 昇格後の root の SKIP は4件のみ: `behavior.phase`(第60便で廃止したサンプル)・
  `obs.attrs`(第61便/第81便で廃止された属性層を直に読む — 第85便で休眠を明示化)・
  `version.beta-label` / `version.beta-label-http`(beta 配信専用)

第86便(2026-08-07): **Release 前 P0 の残2件**(外部レビュー採択)— コアv1 廃止(第81便・破壊的
変更)に対する**旧セーブ移行の固定資産化**と、**移行の来歴記録**。既存のエクスポートは
**1文字も変わらない**(記録は移行があったプロファイルにだけ付く)。

- **P0-2 代表的な旧セーブの移行 fixture**(`tests/fixtures/`)。コアv1 の比率仕様
  (`coreMR`/`coreSR`/`coreRR`)で書き出された「実在しうる旧エクスポート」を、**当時の封筒
  形式のまま**固定資産として置いた。移行式はアプリ内(`legacyCoreToV2`)にしかなく、**入力側の
  代表例がどこにも固定されていなかった**ため、コアv2 の値域・既定値・`build` の初期化式を
  触ったときに「旧セーブを読むと初期状態が変わる」ことを検出できなかった。3種で移行式の
  分岐を網羅する:
  - `legacy-core-rigid-v2.json`(schemaVersion 2 = v1.37.0 root の封筒)— 🌍地球と月の編集
    セーブ。`coreSR=1.0` → `mode:"rigid"`。`coreRR` 未指定なので R_c は質量比からの既定式
  - `legacy-core-differential-v2.json`(同 2)— 🐚重殻ローター型。`coreSR≠1` →
    `mode:"differential"`。`single` の `radius`+`coreRR` 指定と、`disk` 群の代表値
    (平均質量・平均スピン)の**両経路**を通す
  - `legacy-core-cavity-v3.json`(schemaVersion 3 = 第80便の封筒)— 空洞コア。`coreMR<0` →
    `mode:"cavity"`。`radiusScale=1.5` で表示スケール倍率を掛ける経路も通す
- **QA `migration.fixtures` 新設**: 各 fixture を `validatePreset` で読み ①致命エラー0で
  コアv2 へ変換される ②警告は**移行の告知だけ**(値域クランプ等の想定外が1件でも出たら FAIL)
  ③変換後の `core:{}` が **QA 側に独立に書き下した移行式**の値と厳密一致する ④`sim.build` 後の
  **t=0 の全状態**(x,y,vx,vy,spin,m,pinned+`coreMd`/`coreMF`/`RcV`/`coreJ`/`coreOm0`)が
  理論値と一致する、を機械固定する。**1步も進めない** — 移行が保証するのは初期状態一致だけで
  (コアv1 の Ω_c は殻スピンに追従 Ω_c=coreSR·s(t)、コアv2 の J_core は独立変数なので、コアが
  時間発展する構成では軌跡が割れうる)、**検査もその範囲しか主張しない**という形で保証範囲を
  体現させた。移行の告知文が軌跡の但し書きを持っていることだけは併せて固定する
- **P0-3 エクスポートの migration 記録**: 旧キーから移行した構成であることは、正規化後の
  localStorage には残らない(コアv2 だけが保存されるため)。そこで**インポート時に来歴を
  記録**し(`hp_migration`)、エクスポート封筒へ
  `migration:{fromSchema:<元の schemaVersion>, legacyCoreConverted:true}` を書き出す。
  - **移行が無ければキーごと存在しない** — 封筒のキー順も含め既存エクスポートは1文字も不変
  - `schemaVersion` は **4 のまま**(`migration` は追加の任意キー)
  - 複数回インポートしたときの `fromSchema` は**最小(最も古い由来)**= 順序非依存
  - 封筒に `schemaVersion` が無い旧形式(単独プリセット・配列)は 1 とみなす
  - `validatePreset` の戻り値に `legacyCore`(旧キーから実際に移行したか)を追加した。
    警告文の文言に依存せず移行の有無を判定するための機械可読フラグ(移行ロジック自体は不変)
  - **インポート時の情報表示**: 移行が起きた場合、および移行記録つき JSON を読み込んだ場合に
    「この構成は旧コア形式から変換済み(初期状態は互換・軌跡は旧版と一致しない場合あり)」を
    表示する(ja/en)
- **QA `migration.export-record` 新設**: 移行なしの封筒キー順が従来どおりで本文に `migration`
  の文字列が出ないこと・コアv2 明示の新規構成では付かないこと・旧キー入り(schemaVersion 2)で
  付くこと・2→3 と 3→2 のどちらの順で重ねても `fromSchema` が最小になること・移行記録つき
  JSON の読込で記録を引き継ぐこと・情報表示が移行時にだけ出ることを機械固定する。
  既存の `preset.roundtrip-builtins` は不変(内蔵プリセットに旧キーは無い)

第85便(2026-08-07): **🕶️darkrotor 系の休眠 QA 5件を復旧した(門を廃止済み API から現行 API へ
置換)**。**アプリ側(root/beta の物理・プリセット・claims)は 1 bit も変えていない** — 変更は
`tests/qa.mjs` だけである。

- **休眠の原因**: QA の観測層の門が `!!(HP.sim && HP.sim.obsT)` を判定子にしていたが、`obsT`
  (観測温度係数)は**第61便で「全プリセット未使用・恒等1」として廃止された配列**である。
  以後この門は root/beta とも恒常 false になり、`behavior.darkrotor` / `behavior.darkrotorLong` /
  `behavior.darkrotor-pitch` / `darkrotor.uphi` / `darkrotor.allfree` の5件が
  **フル QA でも一度も実行されない**状態が続いていた(第84便B が発見・第84便のフルゲート結果
  JSON にもこの5件は1件も入っていない)。5件とも**休眠は門だけが原因**で、テスト本体は
  `obsT` を一切読んでいなかった。
- **置換**: 門を `typeof HP.obsTemp === 'function' && HP.sim.lSw` へ差し替えた。観測温度そのものは
  廃止されておらず `HP.obsTemp(s,i) = (Tint ? Tint[i] : ½·m·R²·spin²)·(1−lSw[i])` として残っている
  (旧式は末尾に `·obsT[i]` が付くだけで、`obsT≡1` だったので**値としても等価**)。したがって
  「観測温度系を持つビルドか」という判定子の意味は完全に置き換わる。
- **再較正は不要だった**: 5件とも**閾値を一切動かさずに root/beta 両方で PASS**。休眠期間中の変更
  (第74便の kRep 0.15 再構成・第81便のコアv1廃止・第84便Bの claims 再測定)による乖離は
  閾値マージンの内側に収まっていた。実測は root と beta で**完全に一致**(分岐不要):
  u_φ 比 3.850/2.198/4.030(閾値 3.50/2.00/3.66)/ 3000步 外縁 3.230・r90 238.3・偏差 1.85%・
  ローター平均|spin| 1.769・max|spin| 3.258・BHスピン 0.12590・恒星保持 100%・重心移動 0.387・
  t=0 総運動量 9.35e-6 / 6000步 腕A2 0.609/0.683/0.549/0.427・対照帯平均 0.157(<0.25)= 3.62倍・
  max|spin| 4.640・ローター半径偏差 5.02% / ピッチ角 中央値 28.20°(窓20〜35°)・後行 7/7点。
  claims の窓(`darkrotor.pitch-angle` 20〜35 / `darkrotor.arm-band-avg` 0.476〜0.644・対照
  0.133〜0.179)とも整合するので、**claims 側は一切触っていない**。
- **`qa.testid-live` を新設**(休眠検出の再発防止)。全 claims 横断で ①testId が `qa.mjs` に
  `add('<testId>')` として実在する ②そのテストが**このランで実際に results へ入った**
  (`!FAST` のときだけ判定 — `!FAST` ガード付きの重量テストは QA_FAST では正当に未実行のため)
  を機械検査する。`claims.sync` は claim の窓と説明文の数値の一致しか見ておらず、testId の先が
  実行されるかは誰も検査していなかった。**QA_FAST への時間増はゼロ**(page.evaluate 1回+文字列検索)。
- **obs.attrs だけは復旧していない**(明示 SKIP へ変更)。本体が第61便で廃止された `obsT`/`coreOT`
  属性層と第81便で廃止されたコアv1(`HP.sim.Rc`)を直に読むため、門の付け替えでは復旧できず、
  現行 API 向けの検査内容の再定義が要る(root/beta で期待値も割れる)。`obs.equivalence` は
  現行ビルドでも成立するので復旧した(「廃止済み旧キーを書いても力学は 1 bit も動かない」錨)。

第84便 統合裁定(2026-08-07): **🧬🧊⛓️ の E水準を E2→E1 へ再格付け**。第84便A の実測で
4件とも加熱床(伝熱壁)が唯一の熱経路の外部駆動系と確認されたため、♻️ を E1 とした線引き
(外部駆動下の自己組織化=E1)を一貫適用した。構造の創発と頑健性実測は不変 — 変わるのは
「閉鎖系」という分類の主張だけ。閉鎖系の創発(E2 以上)は現在 🕶️(E2)と 🥚(E3)。

第84便 A(2026-08-07): **創発の標準試験を熱の実験室の創発4件へ展開 — E水準を実測で格付けし直した
(結果は4件とも据え置き)**。第83便A が 🥚selfRotor で標準試験化した6試験(①ノックアウト対照・
②用量反応・③多seed・④粒子数スケーリング・⑤摂動回復・⑥時間窓)を、🧬emergent(三態)・
🧊emergent2(蜂の巣格子)・⛓️chain2(鎖)・♻️chaincycle(温度循環)へそのまま当てた。
**物理・エンジン・プリセットの物理値は 1 bit も変えていない**(変更は `descStruct` / `claims` /
実測ハーネス / 文書のみ)。①②は既存カバー(QA コメントの angK=0 対照実測・第83便B の相図
`exp-4-86`・第67便の壁温閾値)の棚卸しにとどめ、重複測定を作らずに③〜⑥だけを新規に測った。

- **実測ハーネス `tests/exp-4-87.mjs` を新設**(→ `tests/out/exp-4-87.json`)。16seed(7〜22)×
  4サンプル+代表3seed のスケーリング(n=56/112/224)・摂動回復・時間窓で **146 run**。
  独立な run なので**ブラウザ4本のタスクプール**で並列に流し、重い run から配る(LPT)ことで
  **実測 20.9 分**に収めた(単スレッド換算 58.5 コア分 = 見積 4457k 步・n=112 換算)。
- **結果: E水準は 4件とも据え置き**(🧬🧊⛓️=E2 / ♻️=E1)。**通らなかった試験も数値ごと記録**した:
  - **🧬 の多seed試験は原理的に空虚だった**: `grid` に `jitter`/`vScale` が無く `mMin===mMax` なので
    **seed を振っても粒子の初期状態が 1 bit も変わらない**(16seed が bit 一致)。格子間隔の 6% の
    位置ゆらぎ+微小初速を足した **③′ 初期条件ノイズ版**を別立てし、16seed で c̄ 1.98〜2.75
    (対照 bondK=0 の 0.125 と 15.9倍離れる)を得た。**確率的な初期条件を持たないサンプルでは、
    ③は「seed を振る」ではなく「初期条件に摂動を入れる」に置き換えないと意味を持たない**
  - **⑤ 摂動回復は4件とも不通過**。熱の実験室の4件は**加熱床だけが熱の出入口の開放系**なので、
    温度キック(T_int を k 倍)で結合が切れると粒子がばらけて床に触れる頻度が下がり、
    **回復の速さを決めるのは自己維持ではなく壁の冷却速度**になる(🧬 の c̄ は 30000步 = 構造形成
    時間の5倍でも回復率 0.17、⛓️ の2配位率は 0.24〜0.50)。短い窓で「戻らない」と結論しないよう、
    回復窓は一次測定の 9000〜12000步から **30000步へ較正し直した**うえでの結果である。
    いっぽう**角度の秩序は戻る**(⛓️ の120°角偏差は ×4 のキックで 102〜480% 崩れて 1000〜12000步で
    復帰・速度ノイズでは2配位率が動かず角偏差だけ 44% 崩れて 2000步で復帰)= **「つなぎ替えは
    戻らないが、まっすぐさは戻る」**。🧊 の蜂の巣は温度キック ×12(T̄ 0.38→4.5)まで崩れない
  - **③ は ⛓️ だけが 16/16 通過**。🧊 は対照(angK=0)と 16seed を通して一度も重ならない(角偏差
    12.3〜44.1° vs 73.1〜79.0°)のに、固定seedで較正した窓(25°未満)に収まるのは 6/16
    (36000步でも 8/16)— **現象は立つが到達度の seed 幅が窓より広い**、という別々の主張を分けて記録
  - **④ の方法論を訂正**: 断片形成系では**断片の大きさの分布が示強量**なので `最大連結/n` は n に
    反比例して落ち(⛓️: 0.375〜0.536 / 0.259〜0.598 / 0.210〜0.388)、成分数は n で割っても示強に
    ならない。④の判定は**示強量(c̄・配位率・角偏差)**で行い、成分側は落ちた条件として記録する。
    スケーリング設計そのものは第83便A の教訓(n が総質量と分離できない)を踏まえ、**箱の大きさと
    列数を √n で振って箱の粒子密度(実測 23.14/1e4 が n=56/112/224 で厳密一致)・格子間隔・
    加熱床までの隙間を保存**した
  - **① ♻️ の対照は連結の大きさでは分離できない**: angK=0 は鎖にならないが**等方の液滴として
    最大50粒の連結**を作る。判別するのは 180°からの角偏差(本則 7.2〜17.3° vs 対照 89.9〜102.6°)
  - **⛓️ ≡ ♻️(t≤288)**: ♻️ は ⛓️ と壁スケジュール(`T2`/`tSwitch`)以外が同一設定なので t=288 まで
    軌道が一致する。代表3seed で全秩序変数の一致を実測したうえで③④の run を共有した
- **`claims` に `kind:"multi-seed"` を4件追加**(`emergent.multi-seed-min-coordination` /
  `emergent2.multi-seed-max-honeycomb-angle-dev` / `chain2.multi-seed-min-chain-fraction` /
  `chaincycle.multi-seed-min-melt-ncomp`)。**QA `behavior.phase-multiseed` を新設**して先頭3seed
  (7/8/9)+対照1seed の縮約版で機械固定し、16seed の分布は `exp-4-87` が正本として持つ
  (第83便A の `behavior.selfrotor-multiseed` と同じ流儀)。**`!FAST` ガードなので QA_FAST の
  所要時間は 1 秒も増えない**
- **既存の固定seed claim の窓を16seed幅へ拡張**: `chaincycle.freeze-maxcomp` 25〜50 → **20〜80**
  (16seed 実測 26〜70粒)・`chaincycle.melt-ncomp` 50〜95 → **40〜95**(実測 48〜84成分)。
  第83便A で 🥚 に施した「**測っていない再現性を窓の狭さで主張しない**」処置と同じ趣旨
- 文書: `docs/PHYSICS.md`「創発水準(E0〜E3)と標準試験」に**熱の実験室4件の適用実績表**と
  上記5つの知見を追記し、§6 表の 🧬🧊⛓️♻️ 行を更新。4サンプルの `descStruct`(ja/en)にも
  同じ実測を追記した(**E水準タグは変更なし**)
第84便 B(2026-08-07): **創発の標準試験を天体系の複合2件へ展開 — 🕶️darkrotor を E2・
⏳nebulaBipolar を E1 と新規宣言**(統括裁定「標準試験の他サンプル展開」)。第83便A が 🥚selfRotor で
正本化した6試験を、E水準タグの付いていなかった既存サンプル2件に当てて実測で格付けした。
**昇格ありきにはしていない — 2件とも E3 にはならなかった**。**物理法則・エンジン・既存プリセットの
物理値は 1 つも変えていない**(変更は `emergence` タグ・説明文・claims・実測ハーネス・QA・文書のみ。
既存プリセットの挙動は bit 不変)。

- **水準は構成が先に決める**(実測の前に body 定義を機械的に数える): 🕶️は383粒子すべて自由
  (`pinned` 0・`boundary:"none"`・一様重力0・レールなし)= **閉鎖系**なので E2 の資格あり。
  ⏳は24 body 定義のうち **22体(赤道ダークアーク)が `pinned`** で、ローブを立てている
  「赤道を塞ぐ幾何」そのものが外部固定 = **閉鎖系ではない**ので、E2/E3 には定義上該当しない
- **実測ハーネス `tests/exp-4-88.mjs` を新設**(→ `tests/out/exp-4-88.json`・所要 **31.8分**。
  内訳: 🕶️ ③多seed 713.8s + 対照定義の突合 86.3s + ④スケーリング 372.6s + ⑤摂動回復 619.2s /
  ⏳ 60.4s + 25.2s + 29.6s)。①ノックアウト・②用量反応は既存 claims・説明文が持つので
  **再測定せず参照**した(⏳の②= kRep の掃引は未実施 — 正直な記録)
- **🕶️darkrotor = `emergence:"E2"`(E3 へは上げない)**:
  - **③多seed 8**(20260726〜20260733・6000步): 腕の帯平均 0.500〜0.569(中央 0.551)/
    対照(中心BH込み全スピン0)0.157〜0.265(中央 0.210)→ **増強比の最小 2.15倍**
    (中央 2.51・最大 3.62)。後行螺旋は **8seed × 7点の 56/56 すべてが後行**・ピッチ角
    中央値 28.2〜32.3°(中央 30.1°)。恒星保持 99.5〜100%・NaN 0・clamp 0・|ΔL|/L_scale ≤ 2.0e-6。
    **多seed の選定根拠**: 1本(383粒子×6000步)が実測 **38.7秒** で本則+対照の2本が要るため、
    16seed だと多seedだけで20.6分・全体で45分を超える。統括裁定の「過大なら8まで」に従って8とした
    (⏳ は1本 1.6秒なので裁定の目標どおり16seed)
  - **④粒子数スケーリング**(n=190/380/597): 帯平均 0.585〜0.591 / 0.538〜0.567 / 0.574〜0.578 と
    ほぼ不変・増強比 3.29/3.53/3.74倍・ピッチ 27.8〜31.4°。**`N_CAP`=600 のため「2倍」(760)は
    置けず、到達できる最大は 1.57倍**という限界を記録した。n は円盤総質量(自己重力)でもあるので
    1粒の質量を ×380/n して総質量を約170に固定した補正版も走らせたが 0.571(n=190)/ 0.557(n=597)で
    やはり不変 = n の効きは自己重力経由ではない。ただし n=190 では環帯の人数が13〜59に減り、
    最内帯はノイズ床 0.246 に対し A2=0.566(2.3倍)しか余裕がない
  - **⑤摂動回復 → 不通過**(step4000 で**恒星だけ**に速度ノイズ・注入群の運動量保存・9000步まで
    無摂動と比較): 速さRMSの5%は崩れ ×0.91 で 2/2 復帰するが、**10%では ×0.80 まで落ちて
    9000步でも ×0.88 止まり(0/2)**・20% ×0.82(0/2)・40% ×0.59(0/2)。恒星保持は 96.6〜98.7%
    (無摂動 97.9〜98.2%)なので円盤自体は壊れていない = **戻らないのは腕だけ**。速度ノイズが
    円盤の速度分散を上げ、この構成には分散を抜く経路が無いので m=2 応答が鈍ったまま戻らない、
    というのが素直な読みである。**この1試験が通らないので ③④ を通っていても E3 にはしない**
  - **⑥時間窓**: 帯平均が最終値の90%に達するのは 3750〜4000步(t=60〜64)。増強比は1500步で
    約1.8倍、3000步から先は 2.5〜3.7倍で頭打ち → 腕の窓 6000步は立ち上がりの外側
  - **多seed が明らかにした「立たなかった読み方」(第82便B 🥚の先例に倣った正直な記録)**:
    ①**展示している内蔵 seed は8seed中いちばん強い引き**である(増強比 3.62倍が最大値。
    対照の帯平均は 8seed で 0.157〜0.265 に振れ、内蔵 seed の 0.157 は最も小さい側)— 説明文の
    「3.6倍」は再現性の主張ではない。②**seed を通して再現するのは「腕の強さ」より「後行の
    対数螺旋という形」**である(対照は同じ計測で後行が 0/3〜4/6 とばらつき、ピッチも 15.9〜66.2°と
    定まらない)。③**対照の定義が2通りある**: ワンタップA/B(`abBody`・対向2ローターのみ spin=0)は
    中心BH の spin 0.12 を残すので帯平均 **0.208 / 0.327**、QA と説明文の 0.156 が使う
    「中心BH 込みで全 single spin=0」は **0.157 / 0.217** で 0.05〜0.11 の差がある
- **⏳nebulaBipolar = `emergence:"E1"`(昇格しないことが正しい着地)**:
  - **③多seed 16**(20260804〜20260819・6000步): 極方向比 0.537〜0.833(中央 0.675)→
    **最小でも等方 1/3 の 1.61倍**(中央 2.02倍)。脱出 34〜46体/60・中心の減光は16seed全部で
    1.000・アーク帯 0.918。圧力オフ対照(kRep=0)の脱出は **16seed で最大1体**(15/16 seed で厳密に0)
  - **④ガス数スケーリング**(n=30/60/120): 極方向比の中央値 0.750/0.677/0.653・脱出率 0.57〜0.77・
    対照はどの n でも脱出0。**但し書き**: n はガスの総質量でも半径30の円盤への充填密度でもあるので
    粒子数だけを独立に振れる設計ではない(アーク幾何と中心質量は n によらず不変なので
    「幾何が絞っているか」の側は分離できている)
  - **⑤摂動回復**(step3000 で**ガスだけ**に等方ノイズ → 注入より後に r>200 を跨いだ粒子だけの
    極方向比): 速さRMS 25%/50%/100% で 0.818/0.787/0.776(中央値)= 無摂動 0.714〜0.826 と同水準。
    200% で 0.647・400% で 0.526 まで薄まるが 400% でも 1.58倍の集中が残る。**ただしこれは
    「外から与えた幾何が摂動で壊れない」ことを示すだけで E2/E3 の根拠にはならない**
  - **⑥時間窓**: 脱出は 2500〜3250步で始まり 3500〜3750步で10体・極方向比は4000步以降ほぼ横ばい
    → `validT`=96(6000步)は立ち上がりの外側。pinned を含む系なので帳簿込みの角運動量は閉じない
    (|ΔL|/L_scale 最大 1.7e-2)— これも「閉鎖系ではない」ことの機械的な現れ
- **claims を2件追加(どちらも `kind:"multi-seed"`)+ 固定seed 窓は据置**:
  - `darkrotor.multi-seed-min-arm-ratio`(窓 **1.80〜4.20**・対照 0.10〜0.32)= 「seed 集合を
    通した腕振幅帯平均の増強比の**最小値**」
  - `nebulaBipolar.multi-seed-min-polar-fraction`(窓 **0.45〜0.90**・対照 0〜3体)= 「seed 集合を
    通した極方向比の**最小値**」
  - **既存の `kind:"fixed-seed"` 窓は動かさなかった**(根拠): 🕶️ `arm-band-avg` 0.476〜0.644 /
    その対照 0.133〜0.179、⏳ `polar-fraction` 0.5〜0.72 は**内蔵 seed 1本のドリフト検出用**で
    あって seed 幅ではない。実測の seed 幅はどちらもそれより広い(🕶️の対照 0.157〜0.265 /
    ⏳の本則 0.537〜0.833)ので、**seed 幅は新設の multi-seed claim 側に持たせ**、固定seed 窓は
    ドリフト検出器として狭いまま残した。この読み分けは説明文と PHYSICS.md にも明記した
- **QA を2件新設(どちらも `QA_FAST=1` では実行しない = FAST の所要時間は増やさない)**:
  - `behavior.darkrotor-multiseed`(W5c 並列ワーカーユニット `darkrotorMultiseed`・**内蔵 seed
    以外の2seed** 20260727/20260728 × 本則/対照 = 6000步×4構成 ≈ 155s)。内蔵 seed は既存の
    `behavior.darkrotorLong` が見ているので重複させない。8seed の分布は `tests/exp-4-88.mjs` が正本
  - `claim.nebulabipolar-multiseed`(**内蔵 seed 以外の3seed** 20260805〜20260807 × 本則/対照 ≈ 10s)。
    16seed の分布は同じく exp 側が正本
  - どちらも E水準タグ(E2 / E1)を判定式に含め、宣言と実測がずれたら落ちるようにした
- 既存 QA `emergence.tag` の宣言表に 🕶️=E2・⏳=E1 を追加。`claims.kind` の multi-seed は 2→4 件に増える
- `docs/PHYSICS.md`「創発水準(E0〜E3)と標準試験」節に**適用実績を2件追加**し、§6 の該当2行を更新
- **範囲外で見つかった問題(報告のみ・本便では直していない)**: qa.mjs の 🕶️ 区画は
  `HP.sim.obsT` の存在を門(`hasObs` / `w5cHasObs`)にしているが、**`obsT` は第61便で廃止された**
  (「全プリセット未使用〔恒等1〕だった」)ため、現行 beta・root ではこの門が恒常的に閉じている。
  結果として `behavior.darkrotor` / `behavior.darkrotorLong` / `behavior.darkrotor-pitch` /
  `darkrotor.uphi` / `darkrotor.allfree` が**フルQAでも実行されていない**(claims の
  `testId` はこれらを指したままである)。門の再開は閾値の再較正を伴う設計判断なので本便では
  触らず、第84便B の新テストだけが obsT に依存しないよう `w5cDrFree` / `w5cDrMulti` を
  `w5cHasObs` から独立させた(**既存ユニットの `enabled` 式は `w5cHasObs && …` のまま残して
  あるので、既存の有効/無効は 1 つも変わらない**)
第84便 C(2026-08-07・原仮定者指示): **サンプルのスケールタグは維持、プルダウン絞り込みのみ撤去**。
カテゴリ選択(`#presetGroupSelect`)にあった「スケール」optgroup(`scale:<tier>` — 分子/日常/天体/
銀河/宇宙全体でサンプルを横断絞り込みする第74便④の機能)を削除した。**分類チップ・表示換算の
既定指数・AI生成雛形(第74便①②③)は無変更**——scaleTier 宣言そのもの・チップ表示・
`scaleExpAnchorOf`・`AI_SCALE_TPL` は一切触れていない。`presetInFilter` の `"scale:"` 分岐と
`rebuildPresetSelect` 側のスケール横断ロジック(`scaleF`)も削除し、group のみでの絞り込みに戻した。
絞り込み用だった i18n キー `grpScale`(ja/en)も撤去(他用途なし — `#aiScaleTpl` 側は `scaleName_*`
を直接使っており無関係)。`presetFilter` は永続化されていない(localStorage 未使用)ため、旧
`"scale:"` 値が起動時に残留する経路は無いが、`rebuildPresetGroupSelect` の既存の選択肢外れ検査
(`![...gs.options].some(...)`→`"all"`)がこの場合も自動的に吸収する。

第83便 D(2026-08-07): **エクスポート往復の保全 — 説明文・質量の上限を実測較正し、往復ゲートを
新設**(統括裁定・原仮定者指示「範囲外の潜在問題: 対応を進める」)。第83便 C が「許容警告2種」
として**種類固定だけして未対応**にしていた2件を、根本から解消した。どちらも内蔵の読込経路
(`loadPreset`→`sim.build`)は検証子を通らないため画面上の実害は無かったが、**エクスポート →
インポートの往復**で情報が落ちる/物理が変質するという保全の穴だった。**既存プリセットの実行時
挙動は bit 不変**(上限緩和は「通らなかったものが通る」方向にしか働かない)。

- **① descStruct の喪失 → `description` 上限の実測較正**: `validatePreset` が `description` を
  **200字**へ切り詰めるため、`descStruct`(要約/観察/操作の**純分割**=連結が `description` と
  厳密一致することを検査する)を持つ内蔵**全件**で連結一致が壊れ、往復のたびに構造化説明が
  警告つきで削除されていた。→ 上限を `REACTION_DV_CAP` と同じ「**正常域最大の約2倍**」の流儀で
  較正した。**実測**(内蔵55件 × ja/en = 110本): `description` 最長 **4367字**(🥚selfRotor/en。
  以下 bhCore 3703・massLadder 3233・starcore 3117)、節別最長 summary **1501** / observe **3065** /
  control **691**。→ `DESC_CAP=16000(第83便A の追記を含む統合後の実測最長 7819 の約2倍)`(≈2×4367)、節別上限 `{summary:3000, observe:6000,
  control:1500}`(≈2×実測)。**荒らし対策の上限そのものは維持**する(節別上限も置いたので、
  1節だけを肥大させる入力も止まる)
- **② 物理の変質 → 質量上限の実測較正**: 🪜massLadder の `m:10000` が MASS 上限**5000**で
  クランプされ、**往復すると別の物理になっていた**。→ `MASS_CAP=20000`(内蔵最大 10000 の約2倍)。
  **高質量帯の実測**(2000步=t32・NaN と全クランプ計数 clampVN/SN/HN/AN/RN/angOvfN を監視):
  ①🪜massLadder の中心3体を一律 k 倍(k=1/2/3/5 → 最大 m=50000)②pinned 高質量中心+ケプラー
  リング48体(m=2500〜40000)③自由な同質量2体の相互周回(m=2500〜40000)— **いずれも NaN 0・
  全クランプ 0**。上限を律速しているのは数値安定性ではなく荒らし・誤入力の歯止めなので、流儀
  どおり内蔵最大の約2倍で止めた。**クランプ挙動そのもの(値域外は警告つきクランプ)は不変**。
  同じ上限を `abBody.patch.m` と `ring`/`disk` の `aroundMass` にも掛けた(質量域が割れていると
  「m=20000 の中心を回すケプラー円盤」の初速だけが黙って半分になるため。内蔵の `aroundMass`
  最大は 2500 なので既存プリセットは 1 bit も変わらない)
- **QA `preset.roundtrip-builtins` を新設**: 内蔵全件について、**`exportData` と同じ封筒に載せて
  文字列化 → 読み戻し → `validatePreset` → `build`** が「直接 build」と **bit 一致**すること、
  **descStruct が ja/en とも一字一句保たれる**こと、**質量クランプが 1 件も発動しない**ことを
  機械固定する。区画: **t=0 の全状態 bit 一致 + descStruct 保全 + 質量クランプ0 は常に全件**
  (実測0.13秒 — 正規化の取りこぼしはほぼすべて初期状態に出るので、ここが主検出面)。
  **400步の bit 一致**は QA_FAST では代表8件(🪜massLadder=質量上限・🥚selfRotor=最長
  description とコアv2 active・🕳bhCore=最長 summary/control・🌟starcore・🪐saturn=zonal E13・
  📏probeH=universeBox・📦freebox=measureBox・🌀boxrot=レール駆動。実測8.9秒)、フルQAでは
  全件(実測約136秒)。併せて**上限が健在であること**(9000字での切り詰め・節上限超過は切り詰め
  警告→descStruct 削除・m は20000で警告つきクランプ)も同項が固定する
- **QA `preset.validate-all-builtins` の許容警告リストを整合**: W1/W2 とも根本解消したので
  **許容リストは空**(内蔵の警告は0件が正規値)。「リストに無い警告が1件でも出れば FAIL」という
  検出器としての性質は維持し、受け皿の構造も残した
- 追随した露出箇所(grep 確認): AI システムプロンプトの値域一覧(`質量:0.01〜20000`)と
  description の字数目安、粒子編集欄 `#beM` のクランプ(±5000 → ±MASS_CAP。旧値だと
  🪜massLadder の m=10000 を編集欄で触るだけで半減していた)とその説明文
- エクスポート形式(`schemaVersion` 4)は変更していない。APP_VERSION / BETA_BUILD も不変

第83便 C(2026-08-07): **内蔵プリセット4件が `validatePreset` を通らない問題の根本解消**
(統括裁定・台帳項目)。第82便 A の QA `optics.default-bitequal` 実装中に、🍳galaxyDB・
🌑nebulaRotor・🐚nebulaShell・⏳nebulaBipolar が**元から**検証子を通らないことが判明し、
当該 QA では除外して回避していた。内蔵の読込経路(`loadPreset`→`sim.build`)は検証子を
通らないため実害は出ていなかったが、「全内蔵が検証子を通る」が本来の姿なので根本原因まで
遡って解消した。**実行時挙動は 1 bit も変えていない**(下記の機械確認つき)。

- **原因①(4件すべて)— validator の過剰検査**: `disk`/`ring` の `aroundMass` を**全 vMode で
  必須**にしていた。しかし `sim.build` はこのキーを `vMode==="kepler"` の枝でしか読まない
  (`b.vMode==="kepler" && b.aroundMass>0 && r>1`)。`vMode:"random"` の群(バルジ・ローター
  クランプ・高温ガス)は中心質量を宣言しようがないので致命エラーになっていた。
  → **validator を直した**: `aroundMass` は `vMode:"kepler"` のときだけ必須とし、他モードでは
  `vScale`/`bulkVx` と同じ「未指定=0 の任意キー」として受理する。**kepler での宣言漏れは従来
  どおり致命**のまま(黙って v=0 の非軌道にしない — 正当な検査は殺していない)。既存の
  ユーザー JSON・AI 生成 JSON は「今まで通らなかったものが通る」方向にしか変わらない
- **原因②(🌑/🐚)— プリセット側の宣言漏れ**: エンベロープ `ring` に `pinned` が無かった。
  `ring` の `pinned` はレール駆動(`railOmega`/`railH`)・壁タグ・`balanceFrame` の分岐を
  持つ**挙動キー**なので、必須にしている検査側が正しい。→ **プリセットを直した**:
  `pinned:false` を補完(`sim.build` は未指定を `b.pinned?1:0` で偽として読むので挙動は同一)
- **bit 不変の機械確認**: 全内蔵55件について「読込直後」と「固定 seed 1000步後」の全数値状態
  (位置・速度・スピン・固有時計・質量・pinned・レール・コアv2 の J/Ω/Md ほか sim 上の全
  TypedArray とカウンタ)を SHA-256 で突き合わせ、**修正前後で 55/55 が完全一致**(差分0)。
  さらに QA へ「生定義 build と `validatePreset` 正規化後 build が 400步後まで bit 一致」を
  4件ぶん常設した
- **QA `preset.validate-all-builtins` を新設**: 全内蔵が検証子を**致命エラー0**で通過。
  許容する警告は**種類を固定**する(W1 descStruct の純分割不一致=50件 — 検証子が
  `description` を200字へ切り詰めるため原理的に一致しない表示専用キーの削除 / W2
  `bodies[N].m` の値域クランプ=1件 — 🪜massLadder の m=10000 が MASS 上限5000に当たる)。
  この2種以外の警告が1件でも出たら FAIL(検査を緩めた副作用の検出器)。第83便C 未適用の
  対象(root 等)は自動 SKIP
- **`optics.default-bitequal` の除外リストを撤去**して全内蔵を対象に戻した(通らない内蔵が
  再び生じたら当該 QA でも FAIL する)
- 挙動不変のため docs の修正はない。APP_VERSION / BETA_BUILD も不変

第82便 A(2026-08-06): **光学輸送拡張 — 体積吸収・波長依存(赤化)・散乱**(台帳
「光学輸送拡張」・opt-in)。E8R は「光がどう曲がるか」だけを決めていたが、光線積分器の経路に
沿って**光学的深さ**を積む観測層を追加した。**力学・固有時計 τ・既存の減光(lightSweep)は
1 bit も変えない** — 効くのは光線とその表示だけである。

- **体積吸収**: `dτ_ref = kAbs·ρ_local·ds`。ρ_local は**コンパクト台**の局所質量核
  `Σ_j m_j·(3/(π a_j²))·(1−(d_j/a_j)²)²`(`d_j<a_j`・`a_j = 4·max(R_j, ε)`・∫ρ dA=Σm に規格化)。
  遠方 1/d の決定力 W をそのまま使うと「雲の外」が定義できないため、吸収には台のある核を
  別に立てる。台の外はちょうど 0 なので、どの天体の台にも入らない光線の τ は**厳密に 0**
- **波長依存(赤化)**: `τ(λ) = τ_ref·(λ_ref/λ)^pAbs`(λ_ref=550nm)。pAbs=4 でレイリー類似
  (青が強く減衰)・pAbs=0 でグレー吸収。光線の表示不透明度が透過率 T(λ)=e^{−τ(λ)} 倍になり、
  スペクトル表示(🌈)に「源スペクトル×T(λ)」と透過率曲線の読み口が加わった(表示・診断のみ)
- **散乱(初期スコープ)**: `fScat` は減衰分を吸収体の拡散光として**描画にだけ**足し戻す割合。
  光線の分岐(多重散乱)はしない
- **新パラメータは全て opt-in・既定で bit 同一**: `kAbs`(既定0)・`pAbs`(既定4)・`fScat`(既定0)。
  第80便 `frameReaction` と同じ流儀で既定値のキーは `validatePreset` が書き出さないので、
  既存プリセットの**パラメータ署名・エクスポート JSON は 1 文字も変わらない**。
  kAbs=0 では光線ループに追加演算を持ち込まない(ゼロコスト原則)
- **新サンプル 🌆暗黒雲の赤化(reddening・グループ「光」)**: 背景の高温星群の手前を冷たい
  暗黒雲が横切る。雲の 1 粒は光線源のしきい質量を大きく下回るので**曲がらずに減るだけ**の
  純粋な吸収体になる。実測(固定seed): 雲中心の τ_ref=2.429・透過率は青(450nm)0.0044 /
  赤(650nm)0.288・比 0.0154、雲の脇は厳密に 0.000、pAbs=0/2/4 の青赤比は 1.0000/0.151/0.0154、
  kAbs 用量反応は τ_ref/kAbs=0.714537 で 4 点厳密一致、透過後スペクトルのピークは 5.09 倍長波長側へ
- **QA 3件を新設**: `optics.dynamics-invariant`(吸収 on/off で軌道・スピン・τ・光線の幾何が
  bit 一致)・`optics.default-bitequal`(既定値のキーが署名・エクスポートに現れない/全内蔵の
  τ_ref≡0/光線キャッシュキーが既定で 1 文字も増えない)・`claim.reddening`。
  較正実測は `tests/exp-4-83.mjs`(→ `tests/out/exp-4-83.json`)
- 正本: `docs/PHYSICS.md` §5「光学輸送(体積吸収・赤化・散乱)」。実在の星間減光則(R_V 等)の
  再現主張ではない(Negative Claims)
第82便 B(2026-08-06): **創発の推進 — 🥚自己形成ダークローターの新設 / 創発モニタ / E水準タグ**
(統括裁定。外部分析の一致見解「創発の主張を強めるには、目標形状を初期条件に埋め込まず、
局所則から構造が**生成される**ことを、ノックアウト対照つきで測るべき」の実施)。

- **🥚selfRotor「自己形成ダークローター — 種から育つ暗い中心」を新設**(group 銀河・
  scaleTier galactic・sampleClass composite・emergence E2)。**目標の形を初期条件に置かない**
  のが設計の中核: 置いたのは一様円盤180粒(全部 m=1・spin 0・自由)+弱い正味回転
  (vMode:"rigid" ω₀=0.008)+ 各粒に同一の微小コア種
  `core:{mode:"differential", massFrac:0.05, radius:0.2, omega:2, Kcs:0}`(第80便の群 core 開放)
  だけで、中心天体・暗い核・ハローはどれも書いていない。働く局所則は自己重力(E4)・接触(E9)と、
  融合 `fusion:{dFrac:0.7}`+そのコア継承則(M_c′=M_c,i+M_c,j・R_c′=√(R_c,i²+R_c,j²)・
  J_c′=J_c,i+J_c,j — 第80便コアv2)だけ。pinned・境界・一様重力・伝熱壁はどれも無く、
  `balanceFrame:"barycentric"` で系の重心・重心速度を生成時に0へ揃えた閉鎖系。
  `lightSweep:"auto"`・validT=144(9000步)。
  - **実測(seed 20260806・9000步)**: 180粒 → **25体**(融合155回)・最大天体が全質量の
    **26.7%**(m=48)・束縛率0.75・帳簿込み |ΔL|/L_scale=6.7e-7・clamp 0・NaN なし
  - **ノックアウト①(融合オフ)**: 最大天体は1粒のまま **0.56%**(=1/180)・束縛率0.14
    → 中心天体は初期条件ではなく融合則の生成物
  - **ノックアウト②(コア種なし)**: 中心天体の J_core は**恒等的に 0**(ΣJ_core も 0)。
    本則では J_core=**0.0960 = 種48個ぶん**で、飲み込んだ質量48と**1粒単位で一致**する
    (継承則の総和がそのまま個数を数えている)。系全体の ΣJ_core=0.3600(=180×0.0020)は不変
  - **用量反応(G)**: G 2/4/6/8/12 → 質量比 1.7/6.1/12.8/26.7/44.4%(融合 7/62/125/155/173回)
    = 構造生成の駆動因は自己重力である、という分離
  - **暗さの生成**: 実効減光 lS_eff は t=0 で 0.000 → 中心 **0.589** / 周囲平均 **0.159**
    (コントラスト3.71倍)。Kt 12/14/16/20/24 では粒子数・融合回数・質量比・スピン・半径が
    完全一致し、変わるのは減光だけ(0.920/0.589/0.421/0.264/0.193)= 暗さは力学に一切効かない
  - **立たなかった主張も説明文に明記した**: (a)暗さそのものは融合の有無を判別しない —
    融合を切った対照でも密集した山の中心は lS_eff=0.999 まで暗くなる(周囲も0.834)。
    判別するのは「中心が暗く周囲は明るい」というコントラストの方。(b)引きずり kFrame=0 の
    対照は外縁回転に系統差を作らない(生き残りが中心集中して外縁帯の統計が立たない)—
    本サンプルは引きずりの主張をしない。(c)融合閾値 dFrac の用量反応は単調ではない
    (0.7/0.6/0.5/0.35 → 26.7/47.8/43.3/23.3%)
  - **claims 5件**(QA `behavior.selfrotor` が同じ量を実測して照合・`claims.sync` が説明文と照合):
    最大天体の質量比(窓0.14〜0.36・対照0.004〜0.008)/ J_core の継承個数(窓25〜65・対照0)/
    中心の減光(窓0.35〜0.80)/ 周囲の減光(窓0.09〜0.32)/ 継承則の総和恒等式
    J_c/J_seed ÷ m/m_seed = 1(窓0.999〜1.001)。窓はいずれも 3seed(20260806/07/08)の実測幅
    (質量比18.3〜30.0%・中心減光0.472〜0.589・J_core 0.0660〜0.1080)を内側に含む
  - 実測ハーネス `tests/exp-4-84.mjs` → `tests/out/exp-4-84.json`(3seed・G/Kt/dFrac 用量反応つき)
- **創発モニタ(表示専用グラフ)を新設**。「グラフ」カテゴリのトグル「創発モニタ」で、
  「構造がどれだけ立ったか」を4つの実測量の時系列として読む:
  ①**塊の数** = 接触距離ベースの連結成分数(i–j を繋ぐ条件は中心距離 d<1.2·(R_i+R_j))
  ②**最大塊の質量比** = 最大連結成分の質量 ÷ Σ|m|
  ③**スピン整列度** = Σ I_i s_i ÷ Σ I_i |s_i|(I_i=½|m_i|R_i²。+1=全部同方向・0=無相関・−1=全部逆)
  ④**回転支持比 V/σ** = ⟨v_φ⟩² ÷ σ²(系の重心・重心速度を基準に v_φ,i=(dx·dv_y−dy·dv_x)/r・
  v_r,i=(dx·dv_x+dy·dv_y)/r を取り、⟨v_φ⟩=質量重み平均・σ²=(σ_r²+σ_φ²)/2。1より大きければ
  回転支持が優勢。分散が数値的に0の完全剛体回転では上限 1e9 を返す)。
  **物理ループには一切触れない** — 計測関数 `emergenceStats(S)` は S を1バイトも書き換えない
  純粋な読み取りで、`sim.step` からは呼ばれず、表示 ON のときだけ描画パスから6フレームに1回
  走る(OFF ではスロットにも載らず1命令も走らない)。履歴は sim ではなく表示層のモジュール変数に
  持つ(sim のオブジェクト形状を変えない — 第77便知見)。i18n(ja/en)。
  QA `emergence.monitor`: OFF/ON の600步 x,y,spin ハッシュ一致(bit 不変)+ 解析照合
  (接触3体→1塊・整列度1 / 離れた2群→2塊・整列度0 / 剛体回転→V/σ 上限)
- **E水準タグ(最小形)を導入**。プリセット最上位キー `emergence:"E0"|"E1"|"E2"|"E3"` の
  パススルー宣言(sampleClass・scaleTier と同方針 — **自動判定しない**・物理には一切影響しない)と、
  説明パネルの sampleClass バッジの隣へのバッジ表示(ja/en ツールチップ付き)。
  E0=規定 / E1=外部駆動下の自己組織化 / E2=閉鎖系の創発 / E3=多seed頑健性確認済み。
  `validatePreset` は未知値を警告つきで削除する(バッジが出ないだけ)。宣言は既存の創発系4件
  (🧬emergent=E2・🧊emergent2=E2・⛓️chain2=E2・♻️chaincycle=E1〔床壁の温度スケジュール=外部駆動〕)
  と新設 🥚selfRotor=E2 に付け、他のプリセットには付けない(未宣言=バッジ非表示)。
  QA `emergence.tag`(宣言・チップ DOM・スキーマ往復)
- 既存プリセットの挙動は bit 不変(root/beta とも QA_FAST ALL PASS。beta 310/310・root 300/300)
第82便 C(2026-08-06): **サンプルのファミリー表示+旧コア移行警告の強化**(統括裁定「サンプルの
統廃合」・表示層のみ)。プリセットの削除・ID変更・並び替え・claims・QA・カテゴリ(group)・
スケール順(GROUP_ORDER)は一切変更していない。

- 内蔵53プリセットのうち11ファミリー(lens・saturn・earthmoon・galaxyrot・darkcenter・
  rotorform・boxinertia・boxexpand・fewbody・collide・emerge、計33件)へ `familyId`/
  `familyRole:"primary"|"variant"` を宣言のパススルーとして追加した(`sampleClass` と同方針。
  他42件のプリセット物理・descStruct 本文には触れていない)。
- サンプル選択プルダウンの既定表示を**ファミリー表示**に変更: 各カテゴリ(optgroup)内で
  「ファミリー宣言なしのプリセット+各ファミリーの代表(primary)」だけを表示し、代表名の末尾に
  「(+N)」(N=そのファミリーの variant 数)を付ける。variant は既定では選択肢から隠れる
  (内部のプリセット解決・保存 JSON・URL・claims・AI生成のベース選択は全件のまま — 表示だけを
  絞る)。既定表示は53件→31件(宣言なし20件+代表11件)。
- ヘッダーに「すべて表示」トグル(チェックボックス。既定OFF)を新設。ONで従来どおり53件全表示に
  戻り、variant は同じ optgroup 内で primary の直後に「└ 」プレフィクス付きでまとめて並ぶ
  (ファミリーがカテゴリを跨ぐ場合〔collide: counterring=天体の物語/merger=銀河〕は元の
  カテゴリに残る)。状態は表示専用で localStorage(`hp_show_all_samples`)に保存し、
  `sim.params`・保存 JSON には一切書き込まない。
- 説明パネルのタイトル直下に「この仲間:」行を追加。familyId を持つプリセット(primary・variant
  いずれも)では、同ファミリーの他メンバー名をタップして即座に切替読込できる(ファミリー表示中で
  隠れている variant にもここから到達できる)。
- i18n(ja/en)キーを追加: `showAllSamplesLabel`/`showAllSamplesTip`(トグルラベル)・
  `familyRowLabel`(「この仲間:」/"Family:")。
- 旧コア(`coreMR`/`coreSR`/`coreRR`)入り JSON インポート時の移行警告(`legacyCoreToV2`)を
  強化(ChatGPT レビュー P0-1 採択・変換ロジック自体は不変): 「初期状態は移行されるが、コアが
  時間発展する構成(differential/active/cavity の omega・Kcs・pump 等)では旧版と同じ軌跡には
  ならない場合がある」という限定を ja/en 両方の文言へ明示した。

第81便 D(2026-08-06): **descStruct の平易化(summary 中心・19件)**(原仮定者指示・レビュー
持ち越し項目)。内蔵53プリセットの `descStruct.summary`(ja/en)を、専門用語・記号が先に立つ
書き出しから「このサンプルで何が見られるか」を日常語で示す一文を先頭に置く書き方へ改めた。
初出の記号・略号(ψ=W/Kt・kFrame・レイリー・ベナール等)には短い言い換えを添え、既存の訳語
(決定力・決定フレーム・空間の引きずり・掻き出し=減光)に統一した。observe/control・claims・
descPattern・description(descStruct 連結の自動生成)・id/name/emoji/物理値/activeParams は
一切変更していない。変更したのは ⏱️gclock・🛰️grcal・☿mercury・📈boxexpand・🫧boxcomoving・
🫁boxbreath・🕊️freebox・🎡galaxyStd・💫galaxyGeo2・🍳galaxyDB・🐚nebulaShell・⚫bhCore・
🌕earthMoonFree・🪐saturn・🎯saturnLayered・🛰️saturnZonalD68・♨️convection・☀️starcore・
🌱starSeed の19件で、残り34件は既に平易と判断し変更していない。

第81便 A(2026-08-06): **コアv1(比率仕様)の廃止 — コアはコアv2 の一本へ**(原仮定者指示・
**破壊的変更**)。併せて⚫DFM版ブラックホールを**自由な中心**へ正式移行した。

- **⚠️ 破壊的変更: コアv1(`coreMR`/`coreSR`/`coreRR`)を廃止**した。エンジンから
  A8/E3 のコア差動項 `ω += (mc/m)·s·(sc/s−1)·f(Rc,d)`・E12v2(geoPN=2)の同項の勾配・
  2層減光の v1 分岐・状態配列 `coreMR/coreSR/coreRRv/Rc`・`hasCore` フラグを削除し、
  粒子編集パネルの **mc/m・sc/s・Rc/R の行**(と i18n 説明)も撤去した。
  コアは **コアv2**(`core:{mode,massFrac,radius,omega,Kcs,pump,contract,sourceRate,voidFraction}`
  — J_core を主変数とする独立サブシステム)の一本になる。
  理由: コアv1 の Ω_c は χ·s(t) と殻スピンに追従する従属量で、τ_cs による角運動量交換や
  収縮による J 保存スピンアップというコアv2 の物理を原理的に書けず、同じ物理の二重経路が
  A8・2層減光・E12v2 勾配の3箇所で保守コストになっていた
- **旧セーブ・旧プリセット JSON は引き続き読める**(移行式で自動変換・警告つき)。
  R = (radius>0 ? radiusScale·radius : radiusScale·rMul·√|m|)、
  R_c = (coreRR>0 ? coreRR·R : radiusScale·rMul·√|coreMR·m|) として
  coreMR<0 → `cavity{voidFraction:|coreMR|, radius:R_c, omega:coreSR·s}` /
  coreSR==1 → `rigid{massFrac:coreMR, radius:R_c}` /
  それ以外 → `differential{massFrac:coreMR, radius:R_c, omega:coreSR·s, Kcs:0}`。
  Ω_c は**読込時に1回だけ**評価するので、移行が保証するのは「同一初期状態」であって
  「同一軌跡」ではない(中心スピンが時間発展する構成では挙動が変わる)。新旧併記は
  従来どおり新形式優先+警告。群(ring/disk)は代表値(平均質量・平均スピン)で評価する
- **schemaVersion 4**: エクスポートはコアを `core:{}` **のみ**で書き出す
  (第80便の旧キー併記 `withLegacyCore` を廃止)。インポートは v≤3 を上の移行式で受理する
- **サンプルのコアv2 移行**:
  - 🌍地球と月 / 🌕地球と月(自由二体)— `rigid`(旧 sc/s=1.0)。差動を持たないので
    **移行前後で bit 一致**(2000步の位置・スピンハッシュ一致を実測確認)
  - 🎯土星の環(主星2層)— `differential{massFrac:0.18, radius:29.577, omega:0.0525, Kcs:0}`。
    主星は pinned で殻スピン一定なのでコアv1 と同じ差動が保たれる(差は数値丸めのみ)
  - ⏳双極星雲 — 中心 `differential{0.3, R_c=5, Ω_c=9}`・赤道アーク22体 `{0.3, R_c=2, Ω_c=7.5}`
    (素直な移行値)。アークは pinned なので厳密に同じ差動、中心だけが「追従→独立」の差を受ける。
    極方向比の実測は 0.610 → **0.622**(窓 0.5〜0.72 内・脱出 41→37体)
  - 🐚重殻ローター星雲 — `differential{massFrac:0.4, radius:1.15022, omega:16, Kcs:0}`。
    Ω_c は**較正値**で、素直な移行値(旧 coreSR20×初期殻スピン0.6=12)ではコア平均減光が
    0.789 まで落ちて主張(0.886)と claim 窓[0.8,0.95]を外れる(v1 は Ω_c=20·s(t) と殻スピンの
    成長に追従して暗さを維持していた)。3000步のコア平均減光を実測掃引し
    (Ω_c=12→0.789 / 16→0.901 / 20→0.942 / 24→0.955)、旧実測に最も近い Ω_c=16 を採用。
    新実測: コア平均減光 **0.901**・保持率 **1.000**(単層対照 0.481 は不変)
- **⚫DFM版ブラックホール — 自由な5層の中心**(原仮定者指示): 第80便の実験サンプル
  🕳️bhCoreFree の構成(中心 `pinned:false` + `physics.frameReaction:"pairReduced"` +
  `balanceFrame:"barycentric"`)を ⚫ の正式構成として採用し、**🕳️bhCoreFree は廃止**した
  (id `bhCore`・emoji ⚫・camera.scale 400 は不変)。実測(seed 20260805・6000步・中心天体基準):
  外縁増強 kF1/kF0=**1.5236**(pinned 対照 1.5215 と一致)・自走 殻 0.15→**1.330**・
  コアΩ 20→**4.24**・恒星保持 1.000・反作用/速度/スピン上限の発動 **すべて0**・
  帳簿込み総 L 保存 1.3e-6・重心相対 最大変位 **0.394**/最大速度 **0.0086**・
  コアなし対照 **1.045**(コアが主因)。固定中心との対照は粒子タップで「固定(pinned)」に
  戻して比較できる。claims は `bhCore.outer-boost`(1.37〜1.68)/`bhCore.shell-spinup`
  (1.20〜1.47)/`bhCore.recoil-bounded`(0.2〜0.8)
- **QA**: `core.twolayer`・`behavior.saturnLayered` はコアv2 の SoA を叩く形へ改修(root 等の
  旧ビルドでは従来のコアv1 経路を叩く二経路構成)。`core.cavity-no-negative-mass` は
  **移行式の機械検証(reimport 型 — 旧キー入り JSON を読ませて手書き cavity と1步 bit 一致)**を
  兼ねる形へ、`preset.group-core` は群の旧キー移行検査へ、`core.schema-roundtrip` は
  schemaVersion 4(旧キー併記なし)へ、`core.ab-body-edit` はコアv1 欄の撤去検査へ更新。
  `claim.bhcore-free` は ⚫ 向けに統合し、`claim.bhcore-selfdrive` は中心天体基準の測り方へ。
  実測ハーネス `tests/exp-4-81.mjs` の対象を ⚫bhCore へ差し替えた
第81便 C(2026-08-06・UI): パラメータパネルに「共通設定」カテゴリを新設し、「表示」から
描画品質・文字サイズ・スケール換算表示・表示スケール指数の4項目を移動(挙動・保存先は不変)。
カテゴリ折りたたみの summary に開閉マーカー(▸/▾)を追加(CSSのみ・全 catParams に適用)。
粒子編集パネルの「コア内訳(v2)」を details/summary の折畳み(既定 closed)へ変更。

第80便 A(2026-08-05): **E6′-R — 換算質量対称インパルスによるフレーム引きずり反作用**
(外部レビュー採択の設計。opt-in で追加し、既定 legacy の挙動は 1 bit も変えない)。

- **新方式 `physics.frameReaction:"pairReduced"`**(既定 `"legacy"`)— E6′ の要求
  **d**_i = k_F·Δu を**速度へ直接書かず**、自由対 i<j ごとに換算質量
  μ_ij = m_i m_j/(m_i+m_j) の対称インパルス **J**_ij = μ_ij(φ_ij**d**_i − φ_ji**d**_j) として配る
  (Δ**v**_i = +**J**/m_i・Δ**v**_j = −**J**/m_j)。相対速度の更新は要求どおり
  Δ(**v**_i−**v**_j) = φ_ij**d**_i − φ_ji**d**_j のまま、**対の線運動量が構成的に厳密保存**する
  = A13 の相互性を対単位で満たす。残余トルクは従来と同形の Δs = −n_ij/(I_i+I_j) で移譲。
  pinned 相手は無限質量コントローラ扱い(この極限では legacy と**厳密に同式** — 実測差 0)、
  背景 D₀+W_B 分と非正質量対はそれぞれリザーバ帳簿 / legacy 式へフォールバックする。
  詳細と受入条件は PHYSICS.md §5「E6′-R」
- **狙い**: 支配天体の `pinned` を外せるようにすること。legacy は m=2500 の中心が引きずられる
  側になると巨大な Δ**p** を作ってから軽い円盤へ返すため離散フィードバック利得が発振し、
  第71便の REACTION_DV_CAP はその安全装置でしかなかった(実測: 自由中心で外縁増強が崩れる)
- **新サンプル 🕳️bhCoreFree「DFM版ブラックホール — 自由な中心(実験)」**(beta のみ・実験扱い。
  ⚫bhCore は pinned 対照として一切変更せず並存)。実測(seed 20260805・6000步・中心天体基準の
  相対座標): 反作用/速度/スピンの安全上限が**すべて発動0**、外縁増強 kF1/kF0=**1.524**
  (⚫の同構成 pinned 対照 1.522 と一致)、自走は殻 0.15→**1.330**・コアΩ 20→**4.24**、
  恒星保持率 1.000、帳簿込み総 L 保存 1.3e-6、支配天体の重心相対 最大変位 **0.394**・
  最大速度 **0.0086**。同構成を legacy のまま自由にすると 1200步で速度上限が 12297 回発動し
  増強は 0.99(消失)・殻スピンは 0.61 で頭打ち
- **`balanceFrame:"barycentric"`**(最上位キー)— 全 bodies 生成後に系全体の重心位置・重心速度を
  差し引く**生成時1回の座標変換**(相対配置は不変・物理ループへの追加コスト0)。自由な支配天体の
  系で、有限個の乱数配置が残す Σm**r**≠0・Σm**v**≠0 を初期条件の側で断つ。pinned を含む構成では
  警告つきで無効化する
- **QA 3件**: `reaction.pair-momentum`(閉鎖系で自由対の P・L が**帳簿なしで**閉じる — 相対ずれ
  9.1e-7 / 5.0e-7・resP=resL=0)/ `reaction.pair-test-particle-limit`(固定中心では legacy と
  差 0・中心を自由にしても 4.5e-4 = 0.05%)/ `claim.bhcore-free`(🕳️ の固定seed受入)。
  実測ハーネスは `tests/exp-4-81.mjs` → `tests/out/exp-4-81.json`
- 既定 legacy 経路は無変更(分岐で囲っただけ)。O(N²) ループ内はローカル束縛のみで
  S.xxx プロパティ参照を持たず、要求バッファは pairReduced の宇宙でだけ遅延確保する
第80便 B(2026-08-05): **コアv2 の残段5件**(第77便の残課題)。
旧比率経路(coreMR/coreSR/coreRR)は 1 文字も変えていないので、既存プリセットの fixed-seed
実測値はすべて不変(beta 300/300 で確認)。

- **mode="active"(sourceRate)**: differential の全機能 + `sourceRate`(≥0)で
  `E_int += sourceRate·dt`。裁定「核融合は密度が増す過程の副産物」に従い **E_int を増やすだけで
  回転化しない**(回転の源は収縮 contract)。外部注入なので同額を新設の帳簿 **coreSrcE**
  (fusU/coreWork と同格・HUD「コア注入E」)へ積み、恒等式
  Σ E_int − Σ E_int(0) = coreSrcE を QA が機械固定(core.active-energy・実測相対差 5e-17)
- **mode="cavity"(空洞の分離)**: 旧 coreMR<0 に多義的に重ねられていた「空洞」を独立モードへ。
  `voidFraction`(0.01〜1)+`radius`+`omega` で指定し、**質量も慣性も厳密に 0**
  (J_core を持たないので Ω は規定値)。引きずりは −voidFraction·(Ω_c−Ω_s)·f(R_c,d) で、
  内部表現は coreMF=−voidFraction の**負の重み**(質量ではない — 負質量はどこにも生じない)。
  移行式 coreMR=−voidFraction・Ω_c=coreSR·s・R_c=coreRR·R で旧空洞と**同一状態からの
  1步が bit 一致**(core.cavity-no-negative-mass)
- **disk/ring 群への `core:{}` 開放**: 群に書くと全メンバーが同じコア設定を持つ
  (massFrac は各メンバーの質量に対する比・radius/omega は絶対量)。付けない構成は
  push するオブジェクトの形も従来どおりで、hasCoreV2=false のゼロコスト経路が生き残る
  (core.group-core・第77便知見①の「参照の形まで含むゼロコスト」を維持)
- **融合/分裂のコア継承**: 第77便の「非継承+J_core をリザーバへ退避」を保存則ベースの継承へ
  置換した。融合は M_c′=M_c,i+M_c,j・**R_c′=√(R_c,i²+R_c,j²)**(殻の R′=√(R_i²+R_j²) と同じ
  面積等価則)・J_c′ と E_int′ は**厳密和**(リザーバを経由しないので resL は動かない)。
  分裂は質量比例で分配し R_c,k=√(m_k/m)·R_c(融合の厳密な逆)。実測: 融合 J −1.5→−1.5・
  E_int 10→10・ΔresL=0、分裂 J 1.2→1.2・R_c² の和が 3e-8 で保存(core.merge-split)
- **schemaVersion 3**: エクスポートは core:{} を持つ body に旧キー(coreMR/coreSR/coreRR)を
  併記する(コアv2 を知らない読み手への round-trip 互換)。インポートは新旧両形式を受理し、
  従来どおり新形式優先+併記警告(core.schema-roundtrip)。buildChannel は追加しない
  (appBuild が既にある)

第81便 B(2026-08-06): **🪜隠れ質量ラダー(光度質量と力学質量の分離)+カメラの天体追従UI**
(原仮定者指示。第74便 exp-4-70 E3 の採択残のサンプル化と、描画層だけの追従カメラ)。

- **新サンプル 🪜massLadder「隠れ質量ラダー — 暗い中心の力学質量」**(銀河・composite)。
  減光 auto の重い中心核(m=**2500 / 5000 / 10000**・pinned・spin 0.5)を系間距離1440で3系並べ、
  各系に**同一半径180の測定リング**(恒星48体・全147粒子)を回す。実測(seed 20260806・6000步=t96):
  ①3中心とも実効減光 lS_eff=**1.000**(完全減光)で観測温度 T_obs は厳密に **0** = 光度から
  推定される質量は 0 ②力学質量 M_dyn=⟨v²r⟩/G は **2780 / 5448 / 10676**、比は **1.960**
  (5000/2500)・**3.840**(10000/2500)で真の比 2 / 4 を −2.0% / −4.0% で回復
  ③M_dyn/m=1.112 / 1.090 / 1.068 の超過は **E6′ 引きずり由来** — リングを円軌道に保つ初速は
  ケプラー値の 1.05 / 1.04 / 1.04 倍(半径 RMS 最小の実測較正)で、**kFrame=0 では3系とも
  厳密に 1.00 倍**へ戻る(超過 f²−1 の全量が引きずり)④**暗さは高スピンのせいではない** —
  中心スピンを 0.05 まで落としても減光は 1.000 のまま(掻き出しの分母 c_surf=c·e^{−2ψ} が
  深い井戸で 0.08 まで落ちるため)。逆に spin=2 では超過が 1.53 / 1.43 / 1.33 まで育ち、
  ラダーは 1.87 / 3.46 へ**圧縮**される(引きずりの寄与は軽い中心ほど相対的に大きい)
  ⑤減光は力学に一切効かない(lightSweep なし対照と bit 一致・M_dyn 完全一致 — 第70便 E2 の
  機械証明がこの構成でも成立)⑥系間距離を3倍にした対照でも比は 1.95 / 3.74(差 0.4% / 2.7%)。
  NaN・速度/スピン/反作用クランプはいずれも0回。実在のダークマター観測の再現主張ではない
  (Negative Claims)。claims 3件(`massLadder.dyn-mass-ratio-2` / `-4` / `.dark-centers`)と
  QA `claim.massladder`(固定seed受入・QA_FAST でも実行)。実測ハーネスは
  `tests/exp-4-82.mjs` → `tests/out/exp-4-82.json`
- **カメラの天体追従(表示専用)**: プリセットの新キー **`camera:{follow:<粒子index>}`**(任意)と、
  「表示」カテゴリの**カメラ追従**コントロール(なし / 天体#N〔プリセット既定〕/ 選択粒子・ja/en)。
  毎フレーム描画の直前に camX/camY を追従対象の現在位置へ合わせるだけで、**sim.params・粒子状態・
  保存 JSON には一切書かない**(表示スケール指数と同じ流儀)。追従中のパンは「**追従対象からの
  オフセット**」として積まれるので、対象が動いても構図が保たれる。対象が範囲外(融合等で消滅)に
  なったら自動で「なし」へ戻り、A/B比較中は選択された宇宙の対象を追う。物理ループには 1 命令も
  入れていない。validatePreset は `camera.follow` を整数のみ受理し、非整数・負値は警告つきで無視。
  QA `camera.follow-ui`(コントロールの存在・受理/拒否・camX=天体位置・パン=オフセット・
  自動解除・物理不変)。既存プリセットは follow なし=挙動不変
第83便 A(2026-08-07): **創発の標準試験化 — 🥚selfRotor を E2 → E3 へ昇格**(統括裁定)。
「見た目がそれらしい固定seedの1本」と「再現性を持つ創発」を分ける手続きを、宣言(E水準)と
実測(標準試験6件)の二本立てで正本化した。**物理法則・エンジンは 1 行も変えていない**
(変更はプリセット宣言・実測ハーネス・QA・文書のみ。既存プリセットの挙動は bit 不変)。

- **標準試験6件を正本化**(`docs/PHYSICS.md` §5「創発水準(E0〜E3)と標準試験」を新設):
  ①ノックアウト対照 ②用量反応 ③多seed ④粒子数スケーリング ⑤摂動回復 ⑥時間窓。
  E0〜E3 の定義(何を主張しているかの宣言)と、各試験が落ちたときに何を意味するかを表にした。
  **通らなかった試験も数値ごと記録する**ことを明文化(通らない記録は通った記録と同じだけ価値がある)。
  ⑤の摂動は実験操作であってアプリの機能ではない(ハーネスが状態を直接書く。速度ノイズは
  質量重みつき平均を差し引いて**系の全運動量を保存**させ、「外から蹴った」ではなく
  「内部をかき混ぜた」摂動にする)— アプリ側に摂動ボタンは実装していない
- **🥚selfRotor に③〜⑥を適用**(①②は第82便B で実施済み)。実測ハーネス
  `tests/exp-4-85.mjs` → `tests/out/exp-4-85.json`(所要 約8.5分):
  - **③多seed 16**(20260806〜20260821・9000步=validT): 最大天体の質量比 **18.3〜53.3%**
    (中央34.7%)。融合オフの対照は全seed **0.5556%**(=1/180 が厳密に保たれる — 融合が
    無ければ質量は動かない)なので、**最小でも対照の33.0倍**。同16seed の秩序変数
    (min/中央/max): 最大塊の質量比 0.972/0.997/1.000・スピン整列度 0.985/1.000/1.000・
    V/σ 3.1/13.4/20.8・中心の減光 0.472/0.646/0.919・周囲 0.126/0.189/0.235・
    J_core 0.0660/0.1250/0.1920(種33/62.5/96個ぶん)・束縛率 0.43/0.82/1.00・
    残り体数 17/25/37体(融合143/155/163回)。NaN 0・clamp 0・|ΔL|/L_scale 最大 6e-6。
    **減光コントラスト(中心/周囲)は本則 2.41〜5.30(中央4.07)vs 対照 0.40〜1.26(中央1.17)で
    16seed を通して一度も重ならない** — 第82便B が「暗さ単独では融合の有無を判別しない」と
    記録した点について、正しい判別量がコントラストの方であることが多seedで裏づけられた
  - **④粒子数スケーリング**(n=90/180/360・代表3seed): 質量比の中央値 12.2/26.7/31.7%・
    最大塊 0.944/0.994/1.000・整列度どれも1.000 = 特定粒子数の離散模様ではない。
    **但し書き(正直な記録)**: 本サンプルは円盤半径と1粒の質量を固定して n を振るので、
    n は同時に総質量(=自己重力の強さ)でもある。n=90 は G 半減側に効き、「対照(=1/n)の
    10倍超」という基準は3seed中2seedでしか満たさない(比 7.0/11.0/22.0 — 中央値では 11.0 で通る)
  - **⑤摂動回復**(t=120=7500步で注入・代表3seed): 速さRMSの**30%の速度ノイズでは
    最大塊の質量比も整列度も中心天体の質量比も1つも動かない**(掃引: 0.15/0.3/0.6/1.0 まで
    無傷、壊れ始めるのは**2.0倍から** — 15000步で最大塊 0.994→0.811、4.0倍で 0.583)。
    動いて戻るのは V/σ(28〜33%落ち→10步で復帰)と束縛率(≤11%落ち→200步で復帰)。
    整列度は速度ノイズでは**定義上**動かない(慣性重みつきスピン和)ので、粒子の約半数の
    スピンを反転させる摂動を別立てした: 整列度 1.000→**−0.907/−0.800/−0.232** から
    **10步(Δt=0.16 = 構造形成にかかった t=32〜48 の 0.4% 未満)で 0.976 以上へ復帰**し、
    反転させた中心天体自身のスピンも10〜200步で正へ戻る = **揃った回転は「一回限りの配置」
    ではなく、接触摩擦(E9/muF)と引きずり(A8/E6′)が毎步維持している到達点**である
  - **⑥時間窓**(代表3seed): 塊は1500步(t=24)でひとつに・整列度は2000步(t=32)で0.99超・
    質量比は2000〜3000步(t=32〜48)で最終値の90%に達して飽和。9000步(=validT 144)と
    18000步の質量比の差は **0.0000** → validT は立ち上がり後の安定域の内側にある
- **`emergence:"E2"` → `"E3"` へ昇格**(多seed・粒子数変更・摂動回復で頑健性を確認した創発)。
  説明文(ja/en)に上記の実測を追記。バッジ表示は既存の E3 ラベルがそのまま出る
- **claims を1件追加+3件の窓を更新**:
  - 新設 `selfRotor.multi-seed-min-mass-fraction`(**kind:"multi-seed"**・窓 0.10〜0.60・
    対照 0.004〜0.008)= 「seed 集合を通した最大天体の質量比の**最小値**」。窓の下限 0.10 は
    対照中央値 0.56% の10倍(5.6%)より上に取ってある
  - 3seed → 16seed の実測に合わせて窓を**広げた**: 質量比 {0.14,0.36}→**{0.14,0.60}**・
    J_core 種数 {25,65}→**{25,100}**・中心減光 {0.35,0.80}→**{0.35,0.95}**
    (周囲減光 {0.09,0.32} は16seed幅 0.126〜0.235 を既に含むので不変)。窓を広げるのは
    主張を弱める方向の変更だが、**測っていない再現性を窓の狭さで主張しない**ための処置である
- **QA `behavior.selfrotor-multiseed` を新設**(4seed × 本則/融合オフ = 9000步×8構成):
  質量比の最小値が claim 窓内・対照が全seed 厳密に 1/180・比が10倍超・秩序変数(最大塊≥0.9・
  整列度≥0.9)・減光コントラストが対照と重ならないこと を機械固定する。**重いので
  `QA_FAST=1` では実行しない**(FAST の所要時間は増やさない)。16seed の分布そのものは
  `tests/exp-4-85.mjs` が正本 — 既存の `kind:"multi-seed"`(`galaxy.outer-boost-ratio`:
  QA は固定seedの A/B 1組・5seed 統計は `tests/exp-scale66.mjs`)と同じ「QA は縮約版・
  exp が全数の正本」という流儀を踏襲した
- 既存 QA の期待値を E3 へ追随(`behavior.selfrotor` のバッジ判定・`emergence.tag` の宣言表と
  チップ `emg-E3`)。`claims.kind` の分布は multi-seed が 1→2 件に増える
第83便 B(2026-08-07): **相図ランナー — アプリ内で2変数バッチ掃引を回し、秩序変数を色マップに描く**
(統括裁定「創発の標準試験化」の相図部分)。第82便B の創発モニタは「1つの構成の時系列」しか
見せなかったが、これで「パラメータ平面のどこに相境界が立つか」をアプリの中だけで見られる。
**物理法則・エンジン数値経路は一切変更していない**(既存プリセットの挙動は bit 不変)。

- **プリセット宣言 `phaseMap`(opt-in)**: `{xKey, xValues:[…], yKey, yValues:[…], steps, metric, label}`。
  xKey/yKey は物理パラメータキー(CLAMPS のキー・同一キー不可)、metric は創発モニタの4つの
  秩序変数(`nCluster`=塊の数 / `maxFrac`=最大塊の質量比 / `align`=スピン整列度 / `vsig`=回転支持比 V/σ)から選ぶ。
  **宣言の無いプリセットでは UI そのものが出ない**(ボタン行も作らない = OFF 時ゼロコスト)。
  `validatePreset` は未知キー・非数・不正 metric・xKey=yKey を**警告つきで無視**(phaseMap ごと削除 —
  ボタンが出ないだけで挙動は不変)し、有効な宣言は値域(CLAMPS)と steps 上限 20000 へクランプする。
  sampleClass・emergence と同じ**宣言専用メタ**で、SYSTEM_PROMPT には載せない(AI 生成には開放しない)
- **実行はメインスレッド分割**(Web Worker は使わない — 単一 HTML でエンジンを worker へ運ぶのは
  侵襲的すぎるため第一版では不採用と裁定)。A/B 比較が複数 sim を併走させている既存機構(`makeSim`)と
  同じ足場で、**描画に一切繋がっていないスクラッチ sim インスタンス**をセルごとに1つ作って順に走らせる。
  UI を止めないため「時間予算 24ms を使い切ったら `setTimeout(0)` で継続」する分割にし、セルの途中
  (25步単位)でも切れるようにした。進捗(セル k/total)・いま走っているセルの枠表示・中断ボタンつき。
  掃引中は本編を一時停止する(rAF の描画・積分と CPU を奪い合わない — 実装がいちばん単純な形)
- **表示**: キャンバス上の全面パネル(「このアプリについて」と同じ流儀)に色マップを描く。
  x/y 軸のラベルと各軸の値・カラーバー+数値レンジ(min/max)・metric 名を出す。
  **セルをタップすると、そのパラメータ値を本編へ適用して読み込める**(適用前に確認ダイアログ)。
  結果はセッション内のみで**保存しない**(プリセットを切り替えると破棄)
- **物理不変**: バッチはスクラッチ sim だけを触り、**本編 sim と保存 JSON には一切書かない**
  (適用導線はユーザーの明示操作のみ)。`presetSig`・エクスポート JSON にも phaseMap は入らない
- **宣言した2枚**(軸は実測で選定):
  - **🧬emergent**: `kRep`(熱圧)× `gravityY`(一様重力)/ metric=`maxFrac` / 6×6・2500步。
    実測(exp-4-86): 値域 **0.107〜1.000**(幅0.893)。相境界(中点 0.55 の交差位置)は
    **g_y=0 で kRep≈11.4 → g_y=0.5 で kRep≈6.8** と、一様重力を上げるほど低い熱圧側へ寄る
    (押し付けが強いほど早く壊れる)。1枚 **73.1秒**(2.03秒/セル)。
    cHeat・γn は相図を動かさなかったので軸に採らなかった(実測で棄却)
  - **🥚selfRotor**: `G`(自己重力)× `D0`(背景決定力)/ metric=`align` / 6×6・1500步。
    実測(exp-4-86): 値域 **−0.859〜+1.000**(幅1.859)。G≦6 では整列度が負〜0付近(バラバラ)、
    G≧9 で +0.90〜+1.00 へ跳ぶ。相境界(中点 0.07 の交差位置)は **D₀=2〜8 で G≈5.3**、
    D₀=0.5 では **G≈1.4**・D₀=16 では **G≈3.8** と下がる(**D₀ 依存は単調ではない** — 実測どおり記す)。
    1枚 **86.3秒**(2.40秒/セル)。
    ※ 銀河標準🎡は kFrame×D₀ を実測したが創発モニタの4量がどれも動かない(整列度は中心天体の
    スピンだけで常に +1.00・V/σ は 0.11 で一定)ため対象に採っていない
- **QA `phasemap.smoke`**(軽量・QA_FAST でも実行): 宣言の受理と正規化 / スキーマの警告つき無視
  (未知キー・非数・不正 metric・同一キー・要素不足)とクランプ / UI 行とパネル・canvas の実寸 /
  2×2 の極小バッチが有限値のグリッドを返す / **本編状態のハッシュ(x,y,spin)と t・n が bit 不変** /
  分割実行版も 2×2 を完走し本編が pause に落ちる / セル適用は確認ダイアログ承諾時だけ params を書き換える
- **実測ハーネス `tests/exp-4-86.mjs`** → `tests/out/exp-4-86.json`: 2枚の基準マップ(本番グリッド)を
  headless で記録し、相境界の存在(min/max セル差の有意性)・行ごとの境界位置・決定性・所要時間を出す
- i18n(ja/en)。正本: `docs/PHYSICS.md` §6「相図ランナー」の段落

## v1.37.0(2026-08-05)

**更新前の注意**: β版とルート版は同一オリジンで localStorage(セーブ・APIキー)を共有する。
大きな更新の前にはセーブタブから JSON エクスポートでのバックアップを推奨する。

第74〜79便(2026-08-04〜05)で v1.37 を確定し、ルートへ昇格した(原仮定者指示「Releaseを
進める」・フルゲート root/beta ALL PASS・perf 全ゲート PASS)。**この版の主題は
「暗くて重い回転体(ダークローター)」の系統化**である:

- **ダークローターの正式定義**(第76便)— 「重い外殻+高速スピンの中心コア=引力は強いが、
  減光で光を出さない天体」。🐚重殻ローター星雲が束縛と暗さの両立を実演する
- **コアv2**(第77便)— 中心コアを **J_core を主変数**とする独立サブシステムへ昇格
  (`core:{mode,massFrac,radius,omega,Kcs,pump,contract}`)。自己重力圧縮による角運動量保存の
  スピンアップを主要因に、パワーボール原理(2Dアナログ)を副機構に持つ。🌱星の種ローター
- **⚫DFM版ブラックホール 5層**(第78便)— コアに貯めた角運動量が τ_cs で外殻へ流れ、
  **中心が真っ暗(lSw=1.00)なまま**外縁増強 **1.524**(可視の🎡標準 1.2646 超)。
  コアなし対照 1.046 で因果を分離。帳簿込み総 L 保存 9e-7
- **新サンプル7件**: 🍳ディスクとバルジ・🌑ローター星雲・🐚重殻ローター星雲・⏳双極星雲・
  🌱星の種ローター・⚫DFM版ブラックホール(+🍳の統制対照)
- **5段階スケール**(第74便)— 分子/日常/天体/銀河/宇宙全体の `scaleTier` 横断分類・
  スケール換算表示・AI追加のスケール別テンプレート(いずれも表示/UI 専用で物理不変)
- **2層減光形**(第75便)— 各層が自分の光を自分の表面速度で掻き出し、光度比で混合する

第79便(2026-08-05): **v1.37.0 昇格**(52A/63/73 と同手順)— beta→root コピー(byte 同一)・
APP_VERSION=1.37・package/lockfile 1.37.0・root SW=dfm-release-v1.37・beta は次期プレビュー線
v1.38-b1 へ(BETA_BUILD/beta SW)。CITATION.cff を 1.37.0 へ同期(1.27.0 から積み残していた)。
perf: root 未実装だった galaxyDB/nebulaRotor/nebulaShell/nebulaBipolar/starSeed/bhCore が
informational から自動で比較ゲートへ。

**同便で入った UI の整理**(原仮定者指示6件)+**README/PHYSICS の記述修正**
(ChatGPT Release前レビュー P1 の採択)— beta 単独ビルドを挟まず本版へ直接収録した:

- **パラメータのカテゴリ再編**: ①一様重力 g_x/g_y を「実験箱」から**「時空」**へ移動
  (境界条件ではなく法則側のパラメータであるため)②**「グラフ」カテゴリを新設**し、
  グラフウィンドウの表示切替6件(回転曲線・温度・スペクトル・膨張・相変化・機構スペクトル)を
  「表示」から分離して**「表示」の直上**に置いた
- **表示スケール指数の移動と直値入力**: 「シミュレーション」から**「表示」**へ移動し
  (換算表示は表示専用機能で sim.params に一切書かないため)、スライダーの範囲外も指定できる
  **直値入力欄**(clamp −30〜30)と、最寄りスケール段の表示行を追加
- **スケール換算値の可読性**: 換算表示行のフォントを 10px → **12px**
- **サンプルカテゴリのスケール順**: 熱の実験室 → 空間と時間 → 光 → 天体の物語 → 銀河 →
  箱宇宙 の順に統一(プリセット選択・カテゴリ絞り込み・AIベースサンプルの3経路)。
  `BUILTIN_PRESETS` 配列自体は**1件も動かさず**表示順だけを `GROUP_ORDER` で決める
  (セーブ・claims・QA の同一性を保つ)
- **記述修正(ChatGPT P1)**: ①README「1サンプル=1効果」→**「原則1サンプル=1主題」**
  (🌑🐚⏳⚫ のような複合型サンプルの存在を明示)②🌈放射冷却の「s³則」→**「T²則(pRad=2)」**
  (tint 移行〔第36便 D〕前のスピン熱時代の表記が残っていた。README・PHYSICS.md 両方)
  ③README に v1.37 の新サンプル6件を追記し、**現物はアプリのプリセット選択が正本**である旨を明記
- スモーク検査 `tests/smoke-79ui.mjs` を追加(UI 6件の機械確認 — root/beta 両方で 7/7 PASS)
- **perf: starSeed のノイズフロア割れを修正**(昇格が露見させた計測欠陥)。informational から
  比較ゲートへ上がった結果、3体しかない 🌱starSeed は既定 60frames の計測が **240步 ≈ 0.5ms** =
  タイマのノイズフロア以下で、しかも JIT が温まりきる前に測り終わることが判明した
  (**byte 同一の root/beta** でペア比 2.600・root 0.6ms 対 beta 4.0ms)。echo と同じ既定路線
  — 閾値を緩めるのではなく**計測時間を引き上げる** — で `FRAMES_OVERRIDE.starSeed = 30000`
  (≈120000步・約 240ms)とした。調査の再現用に `tests/probe-perf-floor.mjs` を追加
  (温まった定常は約 2.2µs/步・60000frames でも NaN なし・コアΩは 8.3e3 で頭打ち)
- perf 計測ノート: 初回実行で `perf.darkrotor` 1.144・`perf.freebox` 1.103 も FAIL したが、
  **再実行で 0.982 / 1.023 と回復**。本便は beta→root が byte 同一なので**真の比は厳密に 1.000**
  であり、差はすべて共有ランナーの負荷偏りによる計測ノイズと確定できる
  (第62便の CI 偽陽性・第78便の merger フレークと同型)

## v1.37-b6(beta 先行・2026-08-05)

第78便(2026-08-05): **DFM版ブラックホール 5層プリセット**(原仮定者指示「DFM版BH 5層プリセット:
進める」+3外部分析〔Gemini/ChatGPT/Grok〕の採択)。

- **⚫bhCore — DFM版ブラックホール(5層の中心)**(ChatGPT §9.2/§13 サンプル3 の採択):
  層0=コア(コアv2 の独立 J・初期Ω20)/層1=重い内殻(m2500)/層2=外殻・擬似地平面
  (2層減光 lSw=**1.00**)/層3=フレーム場(kFrame)/層4=降着円盤(赤道面ガス120体)+
  測定用の恒星ディスク200体。実測(seed 20260805・6000步):
  **①自走** — τ_cs(K_cs=0.02)で殻スピン 0.15→**1.332**・コアΩ 20→**4.2**(第76便 exp-4-77 で
  「欠落部品」と定量化した「コア→外殻」が実際に流れる)。
  **②暗いまま回す** — 中心は真っ暗(lSw=1.00)なのに外縁増強 kF1/kF0=**1.524**で、可視の
  🎡標準 1.2646 を超える。**③因果分離** — コアなし対照 **1.046**(コアが主因)/K_cs=0(自走オフ)
  1.576/kRep=0(層4圧力オフ)1.356。**④帳簿込み総 L 保存 9e-7**。
  層4は重力井戸に束縛され6000步の脱出は120体中2体 — **双極ジェットは立たない**と明記
  (実測できない主張はしない。ジェット機構は ⏳nebulaBipolar の担当)。
  QA `claim.bhcore-selfdrive`(実験 tests/exp-4-79.mjs・exp-4-80.mjs)
- **K_cs を緩和率[1/時間]へ再定義**: τ_cs=−μ·K_cs·(Ω_c−Ω_s)(μ=換算慣性)。第77便は
  f=1−e^{−K_cs·dt/μ} で時定数が μ に比例したため、BH 質量級(μ~10⁴)では K_cs=10 でも
  6000步で 5% しか緩和せず「コア→外殻」が実質不動だった。**時定数 1/K_cs が質量によらず一定**に
  なり、ノブとして意味を持つ。J の等量交換・散逸帳簿・保存則(T7)は不変
- **pinned 天体でも τ_cs を有効化**: pinned は「位置・速度を外部が規定」する印であって自転を
  凍結する意味ではない(pump は加速度が外部規定になるため従来どおり pinned で無効)。
  重い中心を自由にすると E6′③反作用の上限が大量発動して系が乱れる(実測: clampR=30300・
  🎡標準相当の増強が 1.2646→0.59 に崩れる)ため、DFM版BH は既存 🎡🍳 と同じ pinned 中心で組む
- **⚠️パワーボールの2D注記(Gemini 分析の採択)**: 実物のパワーボールは*回転軸の歳差運動*という
  3次元現象で加速する。本モデルは2Dでスピンが面直成分のスカラーのみのため**歳差そのものは
  表現できない** — pump は「軸が自由なら加速・固定なら減速」という**入出力の対応だけを写した
  2Dアナログ**でありジャイロ機構の再現ではない旨を PHYSICS.md へ明記(Negative Claims)
- **堅牢性(ChatGPT P1)**: QA `core.extreme-fuzz` — コアv2 の境界・不正値7件(massFrac/radius/
  omega/Kcs/pump/contract の 0・1e9・負値・不正 mode・必須欠落)で NaN/Infinity/負半径が
  出ないこと+**保存互換**(core:{} を落とした読み〔root 相当〕でも単層として走ること)を機械固定
- perf: `bhCore` を EXTRA_SAMPLES へ(root 昇格まで informational)
- perf 計測ノート: 本便の初回実行で `perf.merger` が 1.163 FAIL(ペア比 [1.163 1.157 1.169])。
  本便の差分は対ループ外(bhCore はデータ・K_cs 変更は v2 粒子ゼロなら通らないパス)で、
  第77便のローカル巻き上げも全5箇所に残存。**再実行では 0.932 で 14/14 PASS** となり、
  共有ランナーの持続的な負荷偏りによるフレークと判定した(第62便が記録する CI 偽陽性と同型 —
  交互ペア測定でも数分規模の負荷偏りは相殺できない)。再発が続くなら echo と同じ
  FRAMES_OVERRIDE 方式を検討する

## v1.37-b5(beta 先行・2026-08-05)

第77便(2026-08-05): **コアv2 — 中心コアの独立サブシステム化**(原仮定者指示「コアv2 実装:
進める」+裁定2件「スピン加速の主要因=自己重力圧縮(核融合は密度増の副産物)」
「パワーボール原理を参考にする」)。

- **core:{}(single 限定・opt-in)**: mode="rigid"/"differential"・massFrac・radius・omega・
  Kcs・pump・contract。**J_core を主変数**とする絶対量管理 — 殻 spin=0 でも独立回転するコアを
  表現できる(旧比率仕様では不可能だった形)。旧経路(coreMR/coreSR/coreRR)は**1文字も変更せず**
  既存プリセットは新コードを通らない(🌑3000步 lS̄ 小数16桁一致で機械確認)
- **収縮スピンアップ(主要因の機械化)**: contract で R_c が縮み、J 厳密保存で Ω=J/I が自動上昇
  (実測: Rc 3→1.15・Ω 2→13.7・J=81 不変)。回転エネルギー増は coreWork 帳簿へ
- **パワーボール項(2Dアナログ)**: 殻の振動加速度 a_osc=「手首の揺すり」。差動(軸自由)コアは
  τ_pb=pump·M_c·R_c·a_osc で加速(種スピン必須)・**rigid(軸固定)は同じ forcing で殻スピンが
  減速**(器具の挙動どおり)。J はリザーバ resL と等量交換・仕事は coreWork(HUD 表示)
- **τ_cs=−K_cs(Ω_c−Ω_s)**: コア⇄殻の等量 J 交換(解析的指数緩和・合計ドリフト 8e-6)—
  exp-4-77 で欠落と判明した「コア→外殻」の段が実装された
- **🌱starSeed — 星の種ローター**: 親天体+鏡像楕円軌道の2種(A=差動/B=軸固定)。実測
  (6000步): 種A の Ω 1.5→**161.8**・減光 0.14→**0.999**(明るい種が周回しながら暗黒天体へ)⇔
  種B は同じ揺すりで spin 3→**2.61** に減速。対照(pump/contract 0)は不変。帳簿込み総 L 保存
  3e-6(totals() が J_core を含む T7 拡張)。QA `core.v2-mech`/`claim.starseed-powerball`
- 防御的措置: 融合体はコアv2 を継承しない(消える J はリザーバへ)・破片も継承しない。
  A/B・チェックポイント・compact の全複製経路に v2 配列を追加
- **性能記録(perf ゲートが捕捉した回帰と修正)**: 初版は O(N²) 対ループ内で `S.hasCoreV2` を
  プロパティ参照していたため、v2 粒子を持たない既存サンプルでも比較ゲートが FAIL
  (perf.merger 1.161 > 1.10)。サブステップ冒頭で `const hasCoreV2=S.hasCoreV2` へ巻き上げ、
  対ループ・勾配・減光・コア更新の全分岐をローカル参照に統一して 1.001 へ回復(14/14 PASS)。
  **ゼロコスト原則(第65便 mechspec 以来)は「分岐が偽」だけでなく「参照そのものの形」まで
  含む**という設計記録

## v1.37-b4(beta 先行・2026-08-05)

第76便(2026-08-05): **ダークローター正式定義+DFM版ブラックホール仮説の機械検証**(原仮定者
指示「添付ファイルを参考に、改善を行う」ほか — Gemini/ChatGPT/Grok 3外部分析の採択)。

- **ダークローターの正式定義**(3分析採択): 「重い外殻+高速スピンの中心コア=引力は強いが、
  減光で光を出さない天体」を PHYSICS.md へ収載(旧定義〔🕳️単層〕を包含する上位定義・無次元の
  構成条件つき)。🐚 の説明文にも定義と「暗黒星雲・星の種(prestellar core 類似)は候補」の
  位置づけを追記(ja/en)
- **P0 修正(ChatGPT レビュー)**: PHYSICS.md ⏳の対照表記を kRep=0 へ統一(「ガススピン0対照」
  は E6′残余トルクのスピン再注入で対照にならない — 第75便の実測 32体脱出の知見を本表へも反映)
- **exp-4-77 — 「中心コアの高速スピンは銀河を回せるか」**(BH仮説の検証・🎡と同一ディスク・
  seed 20260727・6000步): (a)🎡標準 単層spin1.2 → 増強 **1.2646**(較正の厳密再現)/
  (b)暗い重殻中心 殻0.15+コア×20(lSw=1.00)→ **1.0651**(コアだけでは回らない — A8 コア差動の
  到達 f(Rc,d) が狭い)/(c)殻1.2+コア×20(lSw=1.00)→ **1.3909**(殻が回ればコアは増幅・
  **「真っ暗なのに標準以上に銀河を回す中心」が成立**)。欠落部品は「コア→外殻トルク結合」—
  設計は private CORE_V2_DESIGN.md(次スプリント正本)

## v1.37-b3(beta 先行・2026-08-04)

第75便(2026-08-04): **2層減光形+🐚重殻ローター星雲+⏳双極星雲**(原仮定者指示
「双極星雲: 進める」「光学輸送拡張: 類似現状の創発を試みる。星雲は引力が弱いと拡散して
しまうので、粒子の外殻は重く、粒子のコアが高速スピンして光学的な影響範囲は狭い想定」)。

- **2層減光形(lightSweep auto の主星2層対応)**: 各層が自分の光を自分の表面速度で掻き出し
  光度比で混合(殻=A8コア差動形を殻表面で評価/コア=差動スピン×Rc をコア深さの c_core と比較)。
  **A8 と同じく差動分 (coreSR−1) だけがコアから効き、coreSR=1(剛体回転)は単層と bit 等価・
  coreMR=0 は従来式と厳密同値**(全既存プリセット bit 不変 — 🌑3000步の lS̄ が小数16桁一致で
  機械確認)。QA `lsw.core-auto`(解析式一致+剛体等価+差動増分)
- **coreMR/coreSR/coreRR を disk/ring 群へ開放**(第70便 lightSweep 群対応と同型・
  未指定=従来等価・RNG消費不変)。QA `preset.group-core`(負値=空洞も通る)
- **🐚nebulaShell — 重殻ローター星雲**: 塵粒=重い外殻(m24〜36 — 束縛)+高速スピンコア
  (coreSR20 — 暗さ)。熱圧 kRep=0.3 の下の実測(exp-4-76・3000步): コア lS̄ **0.886**・
  保持 **0.963**(エンベロープ1.00)⇔ 単層対照(同等の暗さに spin≈6)は保持 **0.481** に自壊・
  エンベロープ|spin| 攪乱 3.77 vs **0.97**(×3.9)。「暗さのスピンを小さなコアへ隠す」ことで
  束縛と暗さが両立(第74便 🌑 の保持85%も改善)。QA `claim.nebulashell-stress`
- **⏳nebulaBipolar — 双極星雲**: 中心の重殻ダークローター(lSw=1.00)+赤道±50°の pinned
  重殻ダークアーク22個(暗黒帯 lS̄=0.92)+高スピンガス60個。🌋 と同じ「E5′圧力+幾何」の
  星雲版 — 実測(6000步): 系外到達41体の極方向比 **0.610**(等方1/3の1.8倍)・ローブ平均
  観測温度2.0・kRep=0 対照で脱出 **0**。副知見: ガスのスピンを0にするだけでは対照にならない —
  中心・アークの回転が E6′残余トルクでスピンを再注入し圧力が再点火する(実測32体脱出・
  説明文に明記)。QA `claim.nebulabipolar-polar`
- 設計記録: 初版は粒 rMul2 の過密で雲が爆散(保持7〜17%)・etaRad>0 は(1−lS)漏れから
  殻スピンを冷やし暗さが崩壊 — 🐚 は etaRad=0(rotorSolo と同方針)。exp-4-76.mjs に失敗込みで記録
- perf: `nebulaShell`/`nebulaBipolar` を EXTRA_SAMPLES へ(root 昇格まで informational)

## v1.37-b2(beta 先行・2026-08-04)

第74便(2026-08-04): **5段階スケール整理+ディスク/バルジ対比+ローター星雲**(原仮定者指示
「添付ファイルを参考に改善する」— Gemini/ChatGPT/Grok 3外部分析の採択実装)。

- **5段階スケール scaleTier(ChatGPT §8 採択)**: 全46内蔵プリセットへ `scaleTier`
  (molecular/everyday/celestial/galactic/cosmic = 分子8・日常4・天体18・銀河6・宇宙全体10)を
  宣言。**UI 分類・表示換算専用のメタデータで物理エンジンには一切使わない**(「スケールを物理
  入力にしない」)。カテゴリプルダウンにスケール絞り込み(横断・optgroup)、ヘルプに
  スケールチップを追加。QA `preset.scale-tier`
- **スケール換算表示+指数スライダー(原仮定者タスク)**: 「表示」にスケール換算表示トグル
  (次元をもつ G・c₀・g_x/g_y・ε・D₀・Kt だけ SI 換算 ≈値を併記+HUD 1行)、
  「シミュレーション」に表示スケール指数スライダー(分子10⁻¹⁰m〜宇宙10²³m を指数で連続変更・
  時間/質量指数はスケール代表点間の線形補間)。**表示専用 — 240步の物理 bit 一致を機械固定**
  (QA `scale.display-invariant`)
- **AI追加のスケール雛形(原仮定者タスク)**: スケール毎のテンプレート(分子=実験箱・日常=実験室・
  天体=軌道の舞台・銀河=回転円盤・宇宙全体=箱宇宙)を要望欄へ挿入+推奨ベースサンプル自動選択。
  送信文組み立て(aiUserContent)は不変。QA `ai.scale-templates`
- **🍳galaxyDB — ディスクとバルジ(ChatGPT §4.1 galaxyDiskBulgeAB 採択)**: 同一中心天体で
  回転支持ディスク190体 vs 分散支持バルジ190体。固定ID追跡(第71便の訂正手順)の実測
  (seed 20260804・6000步): σ比 **2.61**(バルジは力学的に熱い)・E6′③残余トルクの |spin| 強制
  **1.92倍**(バルジ側が強い・kFrame=0 対照は厳密0)・geoPN=2 では 0.87/1.05 へ縮小。
  QA `claim.galaxydb-contrast`(実験 tests/exp-4-75.mjs)
- **🌑nebulaRotor — ローター星雲(3分析の採択)**: 高スピン小ローターの階層クランプ
  (暗黒星雲類似)+低スピン放射エンベロープ(散光星雲類似)。実測: コア減光 lS̄ **0.767** vs
  エンベロープ 0.052(×14.9)・スピン0対照で散光化 0.025・掻き出し飽和が放射を止め暗さが
  自己維持(c₀=40〜60 に相転移の縁)。**現行減光は背景光の体積吸収ではない**(ChatGPT §5.2)—
  波長別吸収・散乱の光学輸送拡張は台帳へ。QA `claim.nebularotor-contrast`
- perf: `galaxyDB`/`nebulaRotor` を EXTRA_SAMPLES へ(root 昇格まで informational)

## v1.36.0(2026-08-04)

**更新前の注意**: β版とルート版は同一オリジンで localStorage(セーブ・APIキー)を共有する。
大きな更新の前にはセーブタブから JSON エクスポートでのバックアップを推奨する。

第64〜73便(2026-08-03〜04)で v1.36 を確定し、ルートへ昇格した(原仮定者指示「Releaseを
進める」・フルゲート root/beta ALL PASS・perf 全ゲート PASS)。**最大の変更は geoPN=2 —
v−u 統一測地線則(E12v2)**: フレーム輸送・渦度コリオリ(空間引きずりの正体)・1PN 補正が、
時計を遅らせるのと同じ ψ から導いた一本の作用由来式(DERIVATIONS §18)で決まる。
kFrame=0 で geoPN=1 と bit 等価・0/1 経路は 1 bit 不変(段階導入)。ほかに機構スペクトル診断
(T13)・💫geo2銀河(較正 1.0803)・🎡銀河標準(1.2646)・♻️温度循環・減光の力学不変の機械証明・
E6′③反作用の1サブステップ上限(帳簿化)・A9″ 温度の形式化を含む。
geoPN=2 の追加コストは実測 ×1.475(O(N²) の定数倍 — perf.geo2-overhead で恒久ゲート化)。

第73便(2026-08-04): **v1.36.0 昇格**(52A/63 と同手順)— beta→root コピー(byte 同一)・
APP_VERSION=1.36・package/lockfile 1.36.0・root SW=dfm-release-v1.36・beta は次期プレビュー線
v1.37-b1 へ(BETA_BUILD/beta SW)。perf: root 未実装だった galaxyStd/galaxyGeo2/chaincycle が
自動で比較ゲートへ。

## v1.36-b9(beta 先行・2026-08-04)

第72便(2026-08-04): **geoPN=2 性能試験+Release前レビュー(Gemini/ChatGPT/Grok 2026-08-04)の採択**
(原仮定者指示「geoPN=2 性能試験: 進める」「添付ファイルを参考に改善する」)。

- **geoPN=2 性能試験(perf.mjs 拡張 — 新ゲート perf.geo2-overhead)**: 🎡galaxyStd(geoPN=0)⇔
  💫galaxyGeo2(geoPN=2)は厳密同一の初期配置のため、同一ページ交互ペア比が統一則の
  追加コストそのもの。実測 **1.475**(ペア比 1.430〜1.485・ゲート ≤1.65)。N 掃引
  (100/200/381/600)でも 1.42〜1.55 で平坦 — **追加コストは同じ O(N²) の定数倍 ≈1.5**
  (∇u 解析勾配の対ループ集積)。perf 全11比較ゲートも b9 で再実行し全 PASS
  (root/beta ≤1.10 — ChatGPT レビュー P0-2「perf 証跡が b5 相当で古い」の解消)
- **反作用上限の帳簿QA強化(ChatGPT P1)**: clamp.reaction-cap に**帳簿込み角運動量**の検証を
  追加(病的構成の実測 |ΔΣL|/スケール=3.1e-5・窓2e-3・スピンクランプ0)。正規系の無発動確認を
  🕶️2000→**6000步**(腕の分離窓相当)+🌠merger **4000步**へ延長し、💫の 6000步 A/B にも
  clampRN=0 を piggyback で機械固定
- **コメント/実装の同期(ChatGPT P1)**: clampRN 初期化コメントの旧案(|Δv|≤0.8・|Δs|≤0.8)を
  現行仕様(**|Δv|≤16・速度のみ**)へ修正。上限値はマジックナンバーをやめ
  REACTION_DV_CAP=16 の名前付き定数へ(P2)
- **撤回済み主張の横断整理(ChatGPT P1)**: 「温度の力学選別」の残存を全文検索し、
  v1.36-b7 の該当段落と tests/exp-4-70.mjs ヘッダに**撤回注記**を追加(実験履歴は不変のまま
  誤読を防止)。アプリ本文・説明文には残存なしを確認
- **強場バッジの安心文言(Gemini 提案)**: ⚠強場トイ領域のツールチップに「エラーや不具合では
  ありません — 効果を画面内で見せるための誇張設定」を追記(ja/en)
- レビュー持ち越し(台帳へ): サンプル階層化(基本/詳細フィルタ — ChatGPT §7.5/Gemini 2.3)・
  AI 生成用テンプレサンプル(Gemini 2.2)・隠れ質量ラダーのサンプル化(ChatGPT §7.3)・
  チェックポイント/セーブ往復スモーク・v1.36 昇格手順(ChatGPT §9.2 — 原仮定者判断)

## v1.36-b8(beta 先行・2026-08-04)

第71便(2026-08-04): **③反作用の1サブステップ上限(P0)+温度選別の機構分解(第70便主張の訂正)**
(原仮定者指示「温度選別の機構分解: 進める」「極端質量比 P0 対策案: 進める」「恒星集団変数は
導入しない — バルジは光学的に低温だが力学的には熱いと解釈」)。

- **E6′③反作用の1サブステップ上限(P0・ChatGPT レビュー §9)**: 粒子あたりの反作用合計
  |Δv| を **16** に制限し、適用しなかった分の運動量・角運動量をリザーバ帳簿へ記録(T7 恒等式は
  帳簿込みで維持)。上限16の較正: 全内蔵プリセットの per-particle 最大 |Δv|/サブステップ =
  🕶️ 7.52・🌠merger 1.59・他 ≤1e-4 ⇔ 病的構成(D0=0・質量比1:10⁴)9406 — 千倍の分離。
  実測: 病的構成の発振が停止(惑星 v 最大 10.7・速度クランプ 1998→**0**・|ΔΣP| 帳簿込み 2e-3)、
  🕶️24000步は不発=既存挙動 1 bit 不変。スピン移譲への上限は**不採用**(🕶️の正規の汲み上げ物理に
  大量発動するため — 実測754k回)。発動計数 clampRN(QA clamp.reaction-cap)
- **⚠️第70便の訂正 — 「温度の力学選別(比1.25)」は分類アーチファクト**(exp-4-71 機構分解):
  ①正追跡(初期集団を固定)では選別なし — 高温群/低温群の平均半径比 **0.96**(両群とも同率で
  収縮)②第70便 E5 は**終状態の符号付き spin>1 で群を再分類**していた — ③残余トルクが全星の
  スピンを逆行へ汲み下げる(hot 2→−2.4・cold 0.2→−4.0)ため、終状態で spin>1 を保つのは外縁の
  生存外れ値だけ(9/380個)になり「高温星が外に残った」ように見えた。同一走行で E5 方式を再計算
  すると **1.2498(第70便の値を厳密再現)**・kRep=1 の「抑制」1.041 も同アーチファクト
- **機構分解の確定(パッチビルド A/B — 実験スクリプトが一時生成する診断コピー)**:
  スピン汲み下げの経路は **③残余トルクが唯一**(パッチB=移譲オフで spin 2.00/0.20 のまま不変)。
  星スピンの u 回転項(パッチA)は分離にもスピン進化にも無関係。汲み下げは中心ほど速い
  (半径ビン平均 spin: 中心 −4.76 → 外縁 −2.82 の単調勾配)
- **ディスク/バルジ温度の確定整理(原仮定者裁定の解釈を採用)**: DFM で出るのは
  ①光学: 減光の中心集中(lS̄ 0.23→0.017 — v1.36-b7 C1)②力学温度: **中心 σ_v=1.12 vs
  ディスク 0.62 — 中心は力学的に約1.9倍熱い**(「バルジは光学的に低温だが力学的には熱い」が
  DFM でも成立)③マクロスピンは中心ほど残余トルクで逆行へ強制され |s| はむしろ増える —
  集団の選別・移動は起きない。**恒星集団変数(age/Teff)は導入しない**(2026-08-04 原仮定者裁定 —
  現実の色分けの主因〔星形成史〕はモデル外として区別)

## v1.36-b7(beta 先行・2026-08-04)

第70便(2026-08-04): **💫geo2銀河+減光の力学不変QA+ディスク/バルジ温度の機構探索**
(原仮定者指示「添付ファイル〔Gemini/ChatGPT/Grok 分析〕を参考に改善」「減光で重いBH・
見えないダークローターを検討」「ディスク高温星/バルジ低温星の理由を探す」)。

- **💫galaxyGeo2(v−u統一測地線の銀河)新設**: 🎡galaxyStd と厳密同一の初期配置で geoPN=2 に
  切り替えた統一則の基準銀河(ChatGPT レビュー §8.1 の採択 — 既存 🎡/🌌 は legacy 比較基準として
  不変)。外縁増強 kF1/kF0 = **1.0803** を初期較正値として機械固定(QA claim.galaxygeo2-outerboost・
  窓1.05〜1.12+純度Π: 熱斥力/結合=厳密0・測地線6.7%〔1PN〕・引きずり19.3%〔輸送+渦度〕)。
  現実の銀河回転曲線への一致は主張しない
- **disk/ring 群への lightSweep 属性(ChatGPT P2)**: 群共通の減光(数値/"auto")を恒星集団へ
  一括指定できる(未指定=0 で従来と厳密等価)。AI 生成プロンプトにも記載
- **減光の力学不変性を機械固定(QA dimming.dynamics-invariant)**: 減光なし/auto/固定1 の3構成で
  軌道・スピン・固有時計・光線が **1 bit も変わらない**(etaRad=0・121粒子2000步)—
  「光学的に暗いが重力的には重い」天体(重いBH・見えないダークローター)の検討の前提の機械証明
- **隠れ質量ラダー実測(exp-4-70 E3)**: 減光 auto の中心核(m=2500/5000/10000・pinned)は
  T_obs=0(完全減光)のまま、力学質量(外縁 v²r/G)は実質量に比例(比1.40〜1.47 — E6′増強込み)。
  光度推定質量≪力学質量の「隠れ質量」状況がモデル内で成立
- **ディスク/バルジ温度の機構探索(exp-4-70 E4/E5+分解追試)**:
  ①光学(C1): 全星の真の温度が同一でも、中心ほど実効減光が強く(lS̄ 中心0.23→外縁0.017)
  見かけの温度が下がる — 「真は熱いのに冷たく見えるバルジ」が光学だけで出る
  ②力学選別(C2)**〔⚠️撤回済み — v1.36-b8(第71便)で「終状態の符号付きスピンによる再分類」の
  アーチファクトと判明。正追跡では選別なし(0.96)。本段落は実験履歴として残す — 訂正の正本は
  b8 の訂正項と tests/out/exp-4-71.json〕**: 高温星=ディスク残留・低温星=中心沈降(平均半径比1.25)が
  熱斥力なし(kRep=0)で創発 — 帰属分解の実測: E6′切り(kFrame=0)で消滅(0.98)・
  中心スピン0で消滅(0.99)・kRep=1 はむしろ抑制(1.04)・一様スピン円盤は全て同率0.81で収縮
  (スピン値に非依存)→ **「E6′引きずり×中心コヒーレントスピン×スピン混合集団」の三条件が
  揃ったときだけ出る相互作用効果**(詳細機構の分解は次スプリント)。
  ※現実の銀河の主因は星形成史(ディスク=若い高温星の生成現場/バルジ=古い低温星の蓄積 —
  Gemini/ChatGPT 分析)であり、本機構はモデル内の力学的類似現象として区別して扱う
- P1 修正(ChatGPT): lambdaPN の説明を geoPN≥1 へ(geoPN=2 の a₁ₚₙ(w) も同スイッチ)

## v1.36-b6(beta 先行・2026-08-03)

第69便(2026-08-03): **geoPN=2 — v−u 統一測地線則の実装(P4b)**(原仮定者指示「P4b: 進める」。
導出=第68便 P4a/DERIVATIONS §18。裁定3点〔着手・帳簿=反作用返し・段階導入〕の実装)。

- **geoPN=2 新設(E12v2)**: dv/dt = kF·[∂u/∂t+(u·∇)u+(∇×u)ẑ×w] + ∇U + a₁ₚₙ(w)、w=v−kF·u。
  フレーム輸送(legacy E6′ の連続極限)・渦度コリオリ(フレームドラッグの正体)・1PN が
  1本の作用由来式で出る。∂u/∂t+(v·∇)u は E6′ と同じ粒子追跡差分、((v−u)·∇)u と渦度は
  対ループで集積する解析勾配 J=(∇P−u⊗∇W)/W から。**0/1 の経路は 1 bit も不変**
  (集積は geoPN=2∧kFrame>0 のときだけ確保・実行 — 既定経路ゼロコスト)
- **帳簿=反作用返し(§18.4 裁定)**: 輸送3項の Δp は既存 E6′③パス(φ_ij 対分配+残余トルク+
  背景リザーバ・40A 倍精度)をそのまま流用。1PN は ∇U_j 因子の対反作用を自由源へ返し、残余
  トルクは③と同じ等角加速度でスピンへ移譲(pinned 源は E4 と同じ外部拘束=第1段階と同じ開放系)
- **V ゲート実測(tests/exp-p4b.mjs 新設・QA 3本で機械固定)**: ①bit等価 —
  geoPN=2∧kFrame=0 ≡ geoPN=1 が☿型1500步で全状態厳密一致(QA geo2.kf0-bitequal)
  ②共動連星 — 等質量自由源対+トレーサ(D0=0)を V=3 でブーストしても分離窓平均の相対差
  0.75%(QA geo2.comoving。ガリレイ共変性 — 現行 E12 の絶対 v は式レベル 34% 破れ)
  ③保存則 — 自由連星+惑星2(D0=0・3000步)で |ΔΣP|/Σm|v|=1.3e-6・|ΔΣL|/|L₀|=6.6e-6、
  geoPN=1 の開放 1PN(ΔL 比 1.7e-4)を対反作用で約26倍閉じる(QA geo2.conservation)
- **🎡galaxyStd リトマス初実測(裁定材料 — QA 固定なし)**: 外縁増強 kF1/kF0 は
  geoPN=2 で **1.0803**(legacy 1.2646 は同走で厳密再現)— 式由来の渦度コリオリ+輸送からは
  legacy 現象論の約 1/3 のドラッグが出る(tests/out/p4b-results.json)
- 周辺観察(P4b 起因ではない): D0=0+極端質量比(1:10⁴)では③反作用の φ 対分配が軽い側へ
  全量向かい発振→速度クランプ(legacy geoPN=0 でも同一に実測 — E6′③の既存特性)
- スキーマ/UI: geoPN 値域 0〜2(スライダー・CLAMPS・AI 生成プロンプト)。E12 拡張バッジは
  geoPN≥1。E7R 時計・E8R 光線は不変(u の評価は従来どおり)

## v1.36-b5(beta 先行・2026-08-03)

第67便(2026-08-03): **♻️温度循環デモ+P3 サンプル4分類**(原仮定者指示「残キュー: 進める」)。

- **♻️chaincycle(温度循環 — 鎖の凍結と解離)新設**: ⛓️と同一物理・同一初期配置のまま、床壁の
  温度スケジュール(0.6 → t=288 で 20)だけで状態が往復する — 冷却で鎖が自己組織化して凍結
  (実測: 最大連結36粒・組替≈0)、強加熱で端から解離して流体へ(65成分・最大6粒・組替0.33)。
  相・融点・潜熱は入力しない(E14″)。**壁温8では解けない(断片化止まり)ことも実測 —
  明確な解離温度の存在そのものが創発**。機構スペクトルで結合→熱斥力の主役交代(T13)が
  1画面で見える。QA claim.chaincycle(凍結・解離の構造の族+T̄交差を機械固定)
- **P3 サンプル4分類チップ(第64便裁定キュー)**: プリセット宣言キー sampleClass
  (principle=原理実証/composite=複合現象/calibration=現実較正/semantic=意味論表示)を
  新設し、全内蔵45件に一次案を宣言(原理実証29・複合現象12・現実較正3〔grcal/🌍/🛰️〕・
  意味論1〔boxredshift〕)。**🕶️ダークローターは「配置四重極・引きずり・熱斥力の複合系」として
  複合現象を明示**(裁定要件)。表示チップ専用・物理不変。QA p3.sample-class(全件宣言+
  スポット照合+スキーマ+DOM表示)

第66便(2026-08-03): **P2 標準サンプル+機構スペクトル拡張+⭐binary geo化**
(原仮定者指示「次スプリント候補: 順次進める」— 第64便裁定キューの順次実施)。

- **🎡galaxyStd(空間引きずりの基準実験 — 銀河標準)新設**: 🌌 の純化版(kRep=muF=γn=κs=0・
  恒星スピン0。初期配置は 🌌 と厳密同一)。E4+E6′(+弾性接触ばね)だけで外縁増強
  kF1/kF0=**1.2646**。源の帰属は分解実測(tests/exp-scale66.mjs 新設)で確定 —
  **中心スピンのみ 1.2646 ≈ 全スピン 1.2648/恒星スピンのみ 1.0215 ≈ ゼロ 1.0214** =
  主源は中心天体のコヒーレントスピン(恒星スピンの寄与ゼロ・集団公転単独は+2%)。
  QA claim.galaxystd-outerboost(比の窓1.15〜1.4+純度Π: 熱斥力/結合/測地線=厳密0 の機械固定)。
  標準サンプル対応の整理: 分子=⛓️🧊🧬(bondN 2/3/6)・天体=☿・銀河=🎡(PHYSICS.md T13)
- **機構スペクトルの半径ビン表示+支配機構表示(66C)**: sim.mechSpec(reset, radialBins) が
  ビン別スペクトルを返し、グラフ二段化(上=6チャネル横棒+支配上位2の自動表示/
  下=半径6ビンの100%積み上げ帯 — 左=中心・右=外縁)。🎡 では引きずり比率の距離減衰
  (中心34%→外縁2%)が構成比として直接読める。集積層は不変(ゼロコスト QA は 65A のまま有効)
- **⭐binary の geoPN=1 化(66D — 62B 裁定の第一候補の実施)**: 恒星間距離が 12000步
  120.1→120.7 でほぼ不変(legacy は E6′ 散逸で 137→240 へ緩む)・円盤保持 240/240・約42%軽量。
  おすすめA/B の軸を kFrame → **geoPN 1⇄0**(測地線⇄引きずり輸送)へ変更(kFrame=1 は温存 —
  geoPN=0 側が旧構成と厳密一致)。behavior.binary は既存窓のまま成立(較正実測 sep=120.1)

## v1.36-b3(beta 先行・2026-08-03)

第65便(2026-08-03): **機構スペクトル診断(P0)** — 第64便裁定の採用項目③を実装
(原仮定者指示「P0 機構スペクトル診断: 進める」)。

- **機構スペクトル(overlays.mechSpectrum/表示トグル)**: いま系を駆動している機構の内訳を
  実測表示する診断層。6チャネル(重力E4+E13+外場/測地線E12/熱斥力E5′/接触E9/結合E14″/
  引きずりE6′)の加速度寄与を粒子ごとにベクトル集積し、粒子平均RMSの正規化比 Π を右下グラフに
  横棒表示(インパルスは Δv/dt 換算。読み口 sim.mechSpec)。**物理へは一切フィードバックしない**
  (E14″ の「観測ラベルは物理へ還流しない」原則の全体拡張)。RMS正規化比でありエネルギー比では
  ない(凡例・説明に明記)
- **ゼロコスト設計**: overlay OFF(既定・全内蔵プリセット)では診断配列を確保せず、全サイトが
  単一分岐で素通り — 6プリセット(☿🌌🪐🧊♨️🕊️)の300步が実装前基準と bit 一致
  (QA mechspec.zero-cost)。**ON でも物理は 1 bit も動かない**(QA mechspec.on-unchanged)
- **T13 の実測妥当性**(QA mechspec.sanity): ☿=重力98.6%+測地線1.4%(接触/結合/引きずり=0)/
  🧊emergent2=結合58.8%が最大/🕊️freebox=熱斥力98.3%/🌌galaxy=引きずり13.8%が kFrame=1 で
  だけ立つ(kFrame=0 対照は厳密0) — スケール別支配機構が診断量として読めることを機械固定

第64便(2026-08-03): **スケールの整理スプリント** — 3外部分析(Gemini/ChatGPT/Grok)の
収束点を裁定し、「シンプルな要素からの創発」を意味論と実測で前進させた。

- **A9″(内部温度=ミクロスピン分散の正式化)**: T_int をミクロスピン揺らぎの二乗平均 σ_s²
  (⟨δs⟩=0)と定義(PHYSICS.md §2 A9″)。「熱はスピン」の原仮定を保ったまま、冷たい高速回転体と
  熱い静止体を両立する A9′ の意味論を完成させた — 数値・経路は tint と完全同一(新モードではない)。
  スキーマに同義の別名 `thermal:"microspin"` と body キー `microSpinRms`(tInt=rms² へ正規化・
  tInt 併記時は tInt 優先で警告)を追加。QA tint.microspin-alias が 300步 bit 等価を機械証明
- **T13(スケール別の支配機構)**: 分子=結合(E9/E10′/E14″)/天体=測地線(E4+E12)/
  銀河=空間引きずり(E6′)の整理を帰結として明文化(PHYSICS.md §3 T13)。スケールは
  物理入力にしない(エンジンにスケール切替キーは存在しない — 支配機構の入れ替わり自体が創発)
- **銀河外縁増強の源の分解実測(tests/exp-scale64.mjs 新設)**: 🌌galaxy の kF1/kF0=1.321 を
  分解 — 熱・接触を切りスピンを残すと 1.265・全スピン0の「集団公転のみ」では 1.021(kFrame
  用量反応は単調)。**主源はコヒーレントスピンが u に与える回転成分**で、集団公転単独の寄与は
  小さい(銀河標準サンプル設計の一次データ — 重要な否定的結果として記録)
- 物理は 1 ビットも不変(スキーマ受理層・文書・QA・実験スクリプトのみ。既存全プリセット・
  保存データは無変更で有効)

## v1.35.0(2026-08-03)

**更新前の注意**: β版とルート版は同一オリジンで localStorage(セーブ・APIキー)を共有する。
大きな更新の前にはセーブタブから JSON エクスポートでのバックアップを推奨する。

第53〜63便(2026-08-01〜03)で v1.35 を確定し、ルートへ昇格した(原仮定者指示「Releaseを
進める」・フルゲート root/beta ALL PASS・perf PASS)。**最大の変更は E14″ 創発相変化 —
「相」を物理入力にせず、結合予算・結合角・減衰・熱伝導・潜熱・構造観測量(ψ6/組替率/相前線)
がすべて結合状態から創発する**(🧬emergent/🧊emergent2/⛓️chain2)。

第63便(2026-08-03): **ワンタップA/B・チェックポイントの仕上げ+v1.35.0 昇格**(原仮定者指示)。

- **ワンタップA/Bのタイトル統一**: 相変化系(abQuick)・粒子patch系(abBody)とも
  「⚖️ ワンタップ対照A/B(B側: …)」の共通書式へ(i18n abQuickTitle)
- **粒子patch系の再適用**: A/B 中の「⏮初めから」で B 側の対象粒子へ patch を再適用 —
  「A/B比較を終了」するまでワンタップ対照が効き続ける(従来はリセットで対照が素へ戻っていた)
- **チェックポイントの A/B 対応**: A/B 中の 📍保存 は A+B 両側のスナップショットを持ち、
  ⤺📍復帰 も両側同時(同じ瞬間へ揃って巻き戻る)。A/B 終了で無効化(B の文脈が消えるため)。
  QA ui.63-ab-tools が3点とも機械検証
- **昇格作業**: beta→root コピー(全一致)・APP_VERSION=1.35・package 1.35.0・
  root SW=dfm-release-v1.35。beta は次期プレビュー線 v1.36-b1 へ。perf の echo ALLOW 1.45 を
  撤去(53D の記録どおり — root 昇格で byte 同一比較に戻るため)。root 未実装だった
  echo/freebox/emergent/emergent2/chain2 は自動で比較ゲートへ昇格(perf 6→11ゲート)

第62便(2026-08-03): **Release前修正(性能ゲートの交互ペア化)+UI整理**(原仮定者指示
「添付レビューを参考に Release に向けた修正/UI 4件」)。

- **perf ゲートを交互ペア測定へ改修(Release前レビュー P0-1)**: 旧「全root→全beta」固定順は
  計測が数分離れ、共有ランナーの負荷変動が片側に乗って公式CIで galaxy 1.153 FAIL(手元 0.961)
  の偽陽性を出した。root/beta の2ページを常駐させ、サンプルごとに**ペアで隣接計測×3セット
  (開始側は交互)→ ペア比の中央値**で判定する方式へ。全反復生値・環境メタデータ(CPU/
  Chromium/測定順/commit/CI Run ID)を JSON へ記録(P1-3/P0-4)。サンプル走査順は不変
  (第39便 39B の計測条件を維持)
- **UI(原仮定者指示4件)**: ①2種類のワンタップA/B(相変化 abQuick/粒子patch abBody)を
  「A/B比較」グループの**上**の共通行 #abQuickRow へ統一 — 形状は相変化系(btn)に揃えた
  ②時間倍率・チェックポイントを狭め「⏮初めから」「実行」を拡幅(46px 触域は維持)
  ③実行/再開の「▶」を「▶️」へ ④プリセットプルダウンのカテゴリ側を狭め(96→72px)
  タイトル側を拡幅
- **調査(62B)**: 空間引きずり(E6′/kFrame)vs 測地線モード(E12/geoPN)の実測比較 —
  詳細は private 側の裁定資料(デフォルト切替は本便では実施せず)

第61便 61A-61C(2026-08-02): **構造観測量の創発(ψ6・組替率・相前線)+実験ツール群+
obsT/coreOT 廃止**(原仮定者指示「創発化の残テーマ: 進める/レビューP1 可視化群の残り:
進める/『To×』『TcO×』の使用状況を確認し、使われていないなら廃止する」)。

- **61A 観測量の創発(表示専用)**: 相変化サンプルで 25 サブステップごとに **ψ6(六方晶
  秩序)**・**組替率 churn(幾何近傍集合の入れ替わり率 — 「液体=速い組替」の粘性
  シグネチャ)**・**高さ8分割の配位数プロファイル(相前線)** をサンプル。物理には一切影響
  しない(QA phasechange.observables: 液体 ψ6=0.67/churn=0.017・結晶 ψ6=1.00/churn=0・
  前線 床側>天井側 を機械固定)
- **61B 実験ツール(レビューP1 可視化群の残り)**: ①相変化グラフを**二段**へ(上=T̄+c̄・
  下=ψ6+組替率×10・左帯=高さ別 c̄ の前線ストリップ)②**チェックポイント**(📍保存/⤺📍
  復帰 — bit 一致復帰・プリセット切替と A/B 開始で無効化)③**ワンタップ A/B**(プリセット
  宣言 abQuick — 🧬は bondK=0・🧊⛓️は angK=0 の対照併走を1タップ開始)④**HUD にステップ数
  併記**(t=… (…步))。QA ui.61b-tools が機械検証。P1 案「蒸気クッション表示」は対象の
  相率 fg が第60便で廃止済みのため不採用(対象消滅)
- **61C obsT/coreOT の廃止**: 全内蔵プリセット・保存系・SYSTEM_PROMPT で使用実績ゼロを確認し
  beta から削除(観測温度は T_obs = (1−lightSweep)·T へ簡素化)。validatePreset は旧キーを
  警告つき無視(セーブ互換)。root(v1.34)は無変更

第54便 54A(2026-08-01): **気液相変化 — E14 に沸騰・蒸発・凝縮を追加**(原仮定者指示「気液相変化:
進める」)。

- **5区分エンタルピー**: phaseChange に boilT(沸点)+latentV(蒸発潜熱)を両方指定すると気相が
  有効(片方だけ・boilT≤meltT は警告して降格)。h 軸を固/融解/液/沸騰/気の5区分へ一般化し、
  気相率 fGas を追加。boilT 省略時は 53A の3区分と 1 ビットも違わない(QA enthalpy-exact が
  3区分退化+5区分を両検証)
- **気相の力学**: 引力係数の相率読み替え fs=1−fl−fg — 気化で結合も凝集も連続的に消え、
  E9 接触+E5′ 熱圧だけの自由気体になる。熱量関数は Û=bMelt·(fl+fg)+bCe·fg へ拡張
  (A12⁗): **蒸発は凝集エネルギーも支払う(実効蒸発潜熱 = m·L_v + bC)・凝縮は放出**。
  相依存熱伝導は3態合成 c_a=condS·fs+condL·fl+condG·fg
- **💨boil サンプル新設**: 床加熱で液体プールが沸騰し、二相共存の準定常へ漸近(実測 24000步
  fl≈0.34/fg≈0.66)。空間配置は膜沸騰(ライデンフロスト)の類似 — 密度浮力の無い本宇宙では
  蒸気が床とプールの間に溜まり、E5′ 熱圧で液を持ち上げ攪拌する(設計帰結の可視化・seed 3種で
  再現)。相変化グラフに T_b 破線+fg 線、実験ノブに boilT/latentV/condG(気相のあるプリセット
  のみ表示)
- **QA 4件新設+2件拡張**: schema-gas / vapor-plateau(沸騰プラトー |T−T_b|=0 厳密・壁熱量=m·L_v・
  残差<1e-6)/ boil(長時間挙動+帳簿)/ conductivity に固気対(解析比 0.1818)追加 /
  enthalpy-exact 5区分化。perf に boil 追加(informational)

第60便 60A-60C(2026-08-02): **E14″ 創発一本化 — 潜熱・相率・valence の廃止と減衰の創発**
(原仮定者指示「相依存パラメータ創発化の続き: 進める/古い仕組みを使ったサンプルは廃止して
構わない/不用になったパラメータは削除する」)。

- **60A 減衰の創発**: 帯内減衰を cD = bondDamp·(√(share_i·share_j)/S_max)·W へ —
  減衰も結合の濃さから創発(固体の結合振動は強く減衰・気体の掠め接触はほぼ素通り)。
  旧 visc(液相粘性の相率合成)は廃止
- **60B E14 の創発一本化**: phaseChange を {bondN(必須), bondK, bondRange, bondDamp, angK,
  condN, condS, condG} の8ノブへ。**エンタルピー正本(hInt)・融点/潜熱の構成則(meltT/
  latentF/boilT/latentV)・相率(fLiq/fGas)・相率ゲート引力(cohesion)・結合価リンク台帳
  (valence/chainP)・condL をすべて削除** — 熱力学は素の T_int(tint 経路)へ回帰し、
  **潜熱は結合の井戸から抜けるエネルギーとして創発**する(c̄ 低下中は T̄ の上がりが鈍る —
  相変化グラフを T̄+c̄ 表示に変更)。相のラベルは観測量(結合線・配位数)へ完全降格。
  旧スキーマのキーは validatePreset が警告つきで無視(セーブ互換: 読み捨て)
- **60C サンプル刷新**: 旧機構のサンプル7件を廃止(🧊形態アナロジー・🫠melt・❄️freeze・
  🔁meltcycle・⛓️chain・🕸️lattice・💨boil — 旧実装は git 履歴)。🧬emergent(壁T2/rate0.25 —
  res 6.3e-4・分子ガス30成分)と 🧊emergent2(rate0.15 — 角偏差17.5° vs 対照79.2°)を純創発化、
  **⛓️chain2「鎖の創発 — 予算2×角度180°」新設**(2配位率0.80・180°偏差13° vs 対照94.5°・
  クランプ0 — 結合価の台帳なしで鎖が自己組織化。bondN ノブ1つで鎖⇄蜂の巣網⇄塊)
- UI: 相変化ノブを8つへ再編(bondN/bondRange をノブ化)・白円弧/点線リング(相率表示)廃止・
  結合線と T̄+c̄ グラフへ一本化。PHYSICS §E14 を創発一本の記述へ全面改稿(旧仕様は履歴節)・
  EXPLAINER 3-6〜3-8 改稿

第59便 59A-59B(2026-08-02): **angK×bondN 併用+創発熱伝導 — 相依存パラメータの創発化第一弾**
(原仮定者指示「相に依存しているパラメータを結合状態などから創発する」「angK×bondN の併用設計:
進める」— 外部レビュー3件〔ChatGPT/Grok/Gemini〕消化)。

- **59A angK×bondN 併用**: 58A では排他だった結合角ポテンシャルを結合予算モードでも有効化。
  明示リンクが無いため**幾何近傍**(井戸帯内・最大12体/中心・あふれ計数0)の対に働く
  **符号付き線形 g** の角ポテンシャル U_ang=−angK·bondK·μ·W·W·g(cosθ)、
  g=1−(cosθ−cosθ*)²/0.5(θ*=2π/bondN — 予算3=120°)。理想角=井戸・詰め込み過ぎの角
  (60°等)=障壁で、障壁側が本質(sp² 混成/VSEPR 反発の最小類似)。位置だけの純関数=
  厳密勾配の保存力(数値勾配一致を実測)— 運動量・角運動量厳密保存・熱交換不要で帳簿は
  監査 U_ang 項で閉じる。棄却2件を実測記録: ①g²≥0 型は密充填が角エネルギーでも勝ち格子が
  開かない(角偏差79.9°)②予算重み型は share 交差項で res 0.21〜0.47 に劣化
- **59B 創発熱伝導(condN)**: 相率合成 condS·fs+condL·fl+condG·fg に代えて、伝導率を
  **いまの結合状態(滑らかな配位数 B)から導出** — c=condG+(condS−condG)·B/(B+condN)。
  孤立粒子=condG・密な結合網→condS・液体は中間。対の調和平均・Q̇_i=−Q̇_j は不変で熱は
  厳密保存のまま(レビュー3件が一致推奨した「最初に創発化すべき最安全なパラメータ」)。
  相率はこのモードでは**表示だけの観測量に降格**。2粒子接触系で解析値と一致(誤差~4e-8)
- **🧊emergent2 サンプル新設**(格子も分子も — 予算×角度): リンク台帳なしの2ルール
  (予算+角度)だけで、徐冷=蜂の巣格子(60000步・凝固率100%・角偏差24.9° vs angK=0
  対照65.6°)・加熱=分子ガス(7〜21成分)の両方が創発。熱伝導も創発版(condN=2)
- UI: angK ノブを bondN モードでも表示・condN ノブ新設・condG は「気相 or 創発伝導」で表示
- QA: schema-angK 改定(bondN 併用=U_ang 有効へ)+3件新設(schema-condN / angK-budget /
  emergent2)

第58便 58A-58B(2026-08-02): **結合角ポテンシャル=格子の焼き鈍し+パラメータ3件**
(原仮定者指示)。

- **58A 結合角ポテンシャル(angK)**: 「結合角で結合力が強まる」を保存力で実装 —
  角の中心とリンク先の対ごとに U_ang=−angK·bondK·μ·W_ij·W_ik·g(cosθ)²(理想角=2π/valence。
  結合価3→120°)。**力は U の厳密勾配**(径方向+角度を理想へ戻すトルク)で運動量・角運動量
  厳密保存・帳簿は監査の U_ang 項で閉じる(W は帯端0でリンク生成/切断と連続。深部形成・
  ノブ縮小の ΔU_ang は熱と等価交換)。最初に試した「K の強度変調」(トルクなし)は角偏差が
  47.5°→56.9° に**悪化**することを実測して棄却。**実測(予想の検証)**: 温かい液体を底壁で
  ゆっくり冷却 → 蜂の巣状の開いた格子が焼き鈍しで創発(角偏差 26.2° — angK=0 は 47.5° の
  密集塊。5/7員環欠陥の多結晶・res 3.9e-3・クランプ0回)。🕸️lattice サンプル新設・
  QA 2件新設(schema-angK / phasechange.lattice)
- **58B パラメータ3件**: ①「⏮初めから」で相変化ノブ・壁温・壁レートの実行中編集を保持
  (A/B 転写も両側で保持)②壁温・壁レート行を rad 面(♨️天井など)にも表示 — T>0 の編集で
  heat 面へ変換③時間倍率を「シミュレーション」カテゴリへ移動

第57便 57A-57C(2026-08-02): **結合価の全相適用+ノブ化・UI3件・パラメータ7カテゴリ再編**
(原仮定者指示)。

- **57A 結合価の全相適用**: valence≥1 で結合(bondK)も凝集も「相に関わらず」リンク上のみに
  (56B の凝固リンク消去は撤回 — リンク=結合そのもの)。valence は実験ノブ(0〜3・実行中変更可 —
  chainP はスロット幅3で常時確保・縮小時は井戸深さ分を吸熱してリンク解消)。初期結晶は build 時
  リンク済み。**valence=3 の検証**: 凝固で結合網はほぼ全分子3本(3配位率84%)の「グラフとしての
  格子」になるが、空間はハニカムに開かない(角度剛性なし — 導入は次便候補)。2往復QA更新
  (リンク残存・引き継ぎ組み替え・res は総流入基準0.3〜0.5%)
- **57B UI 3件**: タブ別スクロール位置管理/「⏮初めから」等の再構築でカテゴリ開閉を保持/
  details 内説明(「A/B比較とは」等)の文字色を本文色へ
- **57C パラメータ7カテゴリ再編**: 「詳細設定(全パラメータ)」の括りを廃止し、
  時空(ロック・Kt・c₀・G)/スピン・熱(kRep・muF・γn・κs・b_m・η_rad・p・C)/
  引きずり・測地線(D₀・kFrame・q・geoPN・λ_PN・α)/実験箱(壁4面の壁温・レート+g_x/g_y —
  壁レート0=影響なし・未設定面は編集で生成・描画は断熱表示)/相変化/シミュレーション/表示 に
  再編(壁行は相変化セクションから実験箱へ移設)
- QA: activeParams.ui を7カテゴリ版へ・ui.57c-labbox 新設(実験箱10行・未設定面の生成と復帰・
  開閉保持・タブスクロール配線)・chain 系ゲートを 57A 意味論へ改定

第56便 56A-56C(2026-08-01): **結合予算による三態の創発+レビューP0消化+熱の実験室の全面更新**
(原仮定者指示+外部レビュー2件〔ChatGPT/Grok〕)。

- **56A 結合予算(E14″)**: phaseChange.bondN(公称配位数)— 引力を相率でゲートせず、結合予算を
  現在の滑らかな配位数で分配(K=bondK·√(share_i·share_j)・share=min(2, N0/c))。**結合数が減ると
  残りの結合力が増し、結合相手は毎サブステップ幾何で再計算(液相で容易に入替)。熱圧(E5′)との
  競合だけで固体→液滴群→分子ガスが創発**(相のスイッチなし)。share 変化のポテンシャル変化は
  熱と等価交換して帳簿を閉鎖(res 4.85e-3 — 対策前 0.25〜0.95)。🧬emergent サンプル新設
- **56B レビューP0**: ①相状態の視覚チャンネル(白円弧=溶融度・点線=気相・結合線=固相格子/
  鎖/予算強度 — 温度色と独立・白黒でも判別可)②chain 2往復QA+凝固でリンク消去(A案裁定 —
  トポロジー記憶を残さない可逆仕様)③リンク深部再結合の井戸深さを熱へ計上(chain 残差
  1.9e-2→1.15e-2)④🧊を「形態アナロジー(潜熱なし)」へ改称+E14 導線
- **56C 熱の実験室 全面更新**: 並びを 熱移動(🔥🎈📏🌈♨️🧪)→ 形態アナロジー(🧊)→
  E14 相変化(🫠❄️🔁⛓️💨🧬)へ再編。kRep スライダー上限 5→20(CLAMPS と一致)
- QA 3件新設: schema-bondN / chain-second-cycle(2往復・4転移 res<7e-2)/ emergent(三態創発+
  帳簿)
- **56D ドキュメント**: 理系高校生向け解説(EXPLAINER_HIGHSCHOOL ja/en)を全面書き直し —
  「シンプルな決定力場モデルを仮定すると、様々な現実物理の類似現象が創発する。アプリで
  確かめてみよう」を軸に、創発カタログ(10現象×サンプル対応)と ✅確認できる/⚠️できない の
  境界を全項目に明示(論文1・論文2・最新アプリ準拠)

第55便 55A(2026-08-01): **分子間力の結合価 — 塊状の固相⇄鎖状の液相**(原仮定者指示)。

- **phaseChange.valence(1〜3)**: 液相の凝集を「鎖リンク」(各分子最大 valence 本の持続結合
  chainP)上に制限。固相 bondK は等方のまま — 固体=配位6の塊 ⇄ 液体=配位≤2の鎖という
  構造の相転移。リンクの生成・切断はどちらも帯端(w=0)でエネルギー連続。省略時は従来と
  1 ビット一致
- **実効潜熱の増大**: 融解が壊す結合網(配位6)に対し液相の凝集(配位2)が小さいため、
  組み替えコストが潜熱として現れ融解は等方凝集より遅い=「相変化に逆らうエネルギー」の実装
- **⛓️chain サンプル新設**: 壁スケジュールで 塊→鎖(全112粒・平均次数1.98・近傍数2.0 vs
  等方5.2・占有面積4倍)→塊 の往復。鎖リンクを白細線で可視化(表示のみ)
- **電荷は導入不要と判断**: 鎖は結合価だけで成立(導入する場合は E15 として別途設計 —
  PHYSICS §E14 に記録)
- QA 2件新設: schema-valence(値域・結合価厳守・リンク対称・null経路)/ chain(往復挙動+帳簿。
  res 1.9e-2 — リンク組み替えの帳簿外を含めて閾値内)。perf EXTRA に chain 追加

第54便 54D(2026-08-01): **アプリUI 7件**(原仮定者指示)。

- **前回プリセットの復元**: 起動時に最後にロードしていたプリセット(localStorage
  hp_last_preset)を復元。無ければ従来どおり 🪐(QA ui.54d-preset-nav)
- **プルダウンの階層化**: カテゴリ選択+絞り込み一覧の2段プルダウン。「全カテゴリ」では従来と
  同一の全件表示(optgroup 構造不変)。カテゴリ外の項目が選ばれたら自動で全カテゴリへ復帰
- **開いた説明の文字色**: .pdesc を本文色(--fg)へ(--dim は読みにくいとの指摘)
- **主役パラメータの詳細重複**: 「このサンプルの主役」のキーを詳細設定にも表示し、同一キーの
  行同士を相互同期(paramRowSync を配列化)
- **カテゴリ折りたたみ**: 詳細設定の中を 法則/シミュレーション の <details> で畳み、
  箱宇宙/相変化(E14)/表示 も同じカテゴリ折りたたみ(catParams)へ統一。節の説明は summary の
  ⓘ で開閉(開閉操作と説明を分離)
- **相変化ノブのスライダー対応**: 全行にスライダー(代表域)+直値入力(スライダー範囲外も
  従来の値域まで可 — 主パラメータ行と同じ二段構え)
- **壁温の色対応**: heat 壁の描画色を固定の橙から、実効壁温(T2/tSwitch スケジュール込み)の
  温度色(粒子と同じ tempColor スケール)へ。壁の色がそのまま壁温の読みになる
- QA 2件新設(ui.54d-preset-nav / ui.54d-params)+2件更新(activeParams.ui・
  display.timescale-both — カテゴリ版の構造へ追随)

第54便 54B(2026-08-01): **相変化パラメータのセーブ保存**(原仮定者指示)。

- セーブ item に phaseParams(相変化実験ノブの係数スナップショット)と twallHeat(heat 壁の
  壁温/レート)を追加 — 53D の「実行中のみ・セーブ非保存」制約を解除。旧セーブはキー無しで
  従来どおり(後方互換)
- 読込は実験ノブと同じ値域へクランプし、融点・潜熱系は h を正本のまま温度・相率を再導出。
  プリセット側に E14/壁が無いセーブだけの設定は適用しない(universeBox と同方針)。気相の無い
  プリセットへ boilT だけは載せない・気相ありは Tb>Tm 規則を維持(QA phasechange.saveparams)
- インポートの重複判定署名に phaseChange と fusion を追加(これらだけが異なる2プリセットの
  誤重複スキップを防止 — 第36便 integrator 等と同型の穴)

第53便 53D(2026-07-31): **相変化レビューの残項目(P1/P2)消化**。

- **相変化(E14)の実験ノブ**: パラメータ編集パネルに専用節 — 融点・潜熱・結合・凝集・減衰・
  相別伝導と、加熱/冷却壁の壁温・レートを実行中に編集可能(3レビュー共通の P1)。A/B 比較では
  編集対象側のみに効く=「潜熱あり/なし」「結合あり/なし」の対照実験がそのまま組める。
  融点・潜熱の変更は h を正本のまま温度・相率を再導出
- **壁温スケジュール+往復サンプル 🔁meltcycle**: thermalWalls に T2/tSwitch(heat 面・1段)。
  融解→壁切替(t=230)→凝固の往復で、両プラトーが同じ T_m に現れる(ヒステリシスなし)
- **A/B 複製の修正**: E14 状態(hInt/fLiq/係数/相変化履歴)を bit 一致で転写(従来は Tint のみで
  B 側の熱状態が壊れていた — レビュー QA 9.2)
- **QA 5件新設**: wallschedule / damp-residual(帳簿外散逸=bondDamp/2 則に 0.1% 一致 —
  既知限界②の定量固定)/ abclone / cycle / dt-convergence(dt 半減で熱力学量の差 ≤0.002)/
  multiseed(凝固の seed 非依存)。perf に meltcycle 追加(informational)
- localStorage の root/β分離は**見送りを記録**(appBuild+読込中止〔53B〕で誤読は遮断済み。
  分離は昇格時のセーブ移行を要し R-05 と衝突 — 台帳に判断理由を記載)

第53便 53C(2026-07-31): **E14×kRep — melt/freeze の既定に熱圧(E5′)を採用**(原仮定者の
観察「kRep があった方が相変化が感じられる」→ A/B 実測・考察→裁定「進める」)。

- 🫠melt/❄️freeze の physics.kRep を 0→1 に変更し、activeParams の筆頭へ。kRep=0 では液相が
  「温度の色以外は固体と区別できない」(ψ6=1.000・間隔不変・静止 = 無温度の凝集粉体)のに対し、
  kRep=1 で熱膨張(+6%)・持続微動・液相の無秩序(ψ6≈0.8)・**凝固と同時の秩序創発**(ψ6→0.97)・
  収縮・多結晶片(収縮亀裂の類似)が現れる。E5′ は温度が力学に入る唯一のチャネル
  (MD の運動項/SPH の EOS 圧に相当 — docs/PHYSICS.md §E14「E5′ との併用」)
- U_rep は A12″ の a 項が支払い帳簿は閉じたまま(melt 14000步 残差 1.8e-4)
- 53B の「静止液体の自己再結晶化」は kRep=0 構成の性質と判明 — 説明・PHYSICS を実態へ改稿。
  kRep=0 は「熱が力学から切り離された対照実験」として descStruct で案内
- QA: behavior 窓を kRep=1 実測へ更新。凝固の「剛な結合網」指標を単一クラスタ率から
  配位数(配位≥2率=1.00・平均配位4.36)へ変更(多結晶片は仕様)

第53便 53B(2026-07-31): **相変化サンプルの外部レビュー3件(第2ラウンド)対応**。

- **相変化グラフ overlay(phaseGraph)**: 平均温度T̄・平均液相率を融点T_mの破線と重ね描き —
  潜熱プラトーの因果(温度停滞中に液相率だけが進む)を直接読める。melt/freeze で既定ON
- **セーブ互換**: 元プリセットの無いセーブは読込中止(先頭サンプルへの無言フォールバック廃止)。
  エクスポートに書き出し元ビルド識別 appBuild を追加
- **❄️freeze の初期条件統制**: 初期配置の結晶秩序を除去(pitch7+jitter1.0+攪拌 — ψ6=0.79)。
  凝集液体の自己再秩序化(熱運動を持たない液体の性質)は動力学の創発として明記・機械固定
- **クランプ帳簿**: E14 の b 負側クランプ(clampBN)・引力の力上限(clampAN)の発動回数を計数。
  「核生成コスト様」の表現を限定(確率的核生成・臨界核・界面自由エネルギーの再現ではない)
- **QA 強化**: 融解前線の方向(高さ帯)・凝固の最大結合クラスタ率・ψ6 秩序・クランプ帳簿・
  セーブ互換を機械固定。grid に vScale(攪拌)追加。perf に melt/freeze の実測を追加(informational)

第53便 53A(2026-07-31): **相変化拡張 E14 phaseChange(beta 先行)**。外部レビュー3件
(Grok/Gemini/ChatGPT 2026-07-31)の提案を統合し、エンタルピー法の熱力学的相変化を導入。

- **E14 相変化**: プリセット最上位キー `phaseChange:{meltT, latentF, …}`(thermal:"tint" 必須・
  fusion 排他)。熱状態の正本を比エンタルピー hInt に置き、温度・液相率は導出量。融点で
  潜熱を持ち、温度プラトー・凝固の発熱・可逆性を再現(詳細: docs/PHYSICS.md §E14)
- **凝縮相の力学**: 固相結合 bondK・液相凝集 cohesion(平衡距離=接触距離の短距離引力・
  端点連続)+相率合成の減衰 bondDamp/visc(散逸は dQi へ — A9′ 換算則)
- **A12‴(結合エネルギーの帳簿)**: 熱量関数を E(h)=m·h+a·√T(h)+b·fl(h) に拡張し、融解は
  「指定潜熱+実際の結合エネルギー」を支払う(実効潜熱)。🫠melt 12000步の第一法則残差 3e-4
- **相依存熱伝導**: E10′ に condS/condL の調和平均倍率(固相は速く・液相は遅く)
- **body type "grid"**: 六方格子の決定論初期配置(相変化デモの統制条件。AI 生成にも開放)
- **新サンプル2件**: 🫠融解 — 潜熱のプラトー / ❄️凝固 — 冷却曲線(熱の実験室)
- **QA 8件新設**(phasechange.schema / zero-cost / enthalpy-exact / latent-plateau /
  conductivity / grid / ledger / behavior)。既存 tint プリセット4件の 300步 bit 一致を含む

## v1.34.0(2026-07-31)

**更新前の注意**: β版とルート版は同一オリジンで localStorage(セーブ・APIキー)を共有する。
大きな更新の前にはセーブタブから JSON エクスポートでのバックアップを推奨する。

第50・51便(2026-07-30〜31)で v1.34 を確定し、ルートへ昇格した(実機スモーク確認済み・
フルゲート root/beta ALL PASS・CI paper2 success)。主な追加は以下のとおり。

第51便(2026-07-31): 外部レビュー3件(リリース確認+論文2査読)の反映と総仕上げ。

- **サンプル改善4件**: 🌠merger を軌道順行の遭遇へ(潮汐尾 10%@6000步・因果対照は
  接近期/深接触期の時期内訳ごと機械固定)/📏conduction に前線・列熱の HUD 常時比較/
  🌫️collapse を塊が育つ構成へ(r50 144→48・冷却の負の主張は2点判定で維持)/
  🕸️cosmicweb の既定 H0=0.01(長時間でも複数ノット+フィラメント維持)
- **UI**: プルダウンの (kF=N) 表示を廃止し分類チップ「引きずり」を新設。説明タブの表示順を
  「タグ→タイトル→数値主張→本文」へ。🧭は「２つのH推定器(ハッブルテンション類似)」に改題し
  テンション類似比を HUD 常時表示。HUD の長行は画面幅で折り返し(見切れ防止)
- **説明文の構造化(4-78 C案・全展開)**: 全41プリセットを descStruct(要約/観察/操作)へ
  純分割変換。内部管理番号などのメタ表現を除去し、ダークローターに定義文を追加。
  QA desc.struct-sync / claims.sync(44件)が純分割と数値保全を機械固定
- **論文2 v0.8**: Methods 新設・Hubble 比較の Discussion 移設・dt/光線本数の収束実測・
  暗い天体の付録化・Code and data availability 新設(投稿前 Major revision 対応)

第50便(2026-07-30): レビュー起点の計測基盤6件+P0消化。

- 🧭probeH 新設(V29 を操作可能に)・観測面光束 ray.observer-flux・安全クランプ帳簿+
  発動0回ゲート・photon-abc 同時性閾値 <0.2 へ厳格化・freebox 形状/加速診断+G×kRep 4象限・
  claims 分類メタデータ(kind 5種)・exp-4-66.mjs(指数形 vs 反比例の判別実験)
- 🌠merger/♨️convection の負荷半減と初期配置・伝熱壁の色分け表示

第47・48便(2026-07-30): 4-86 対応/台帳整理/外部レビュー2巡(計6レポート)の消化。

- **4-86 消化(47A)**: pinned への伝導熱を外部熱浴リザーバ **pinHeat** へ記帳(案B)。
  自由粒子側の力学は 1 ビットも不変(conduction.pinned-zero-cost が修正前とハッシュ一致で
  機械証明)。E10′ 解析値との一致は相対差 0.0(conduction.pinned-bath)。
  第一法則検査・保存量モニタ・A/B 複製へ pinHeat 項を組込み
- **図生成スクリプトの喪失と再建(47F)**: 統合手順の欠陥(worktree の未追跡ファイルが
  git diff に含まれない)で tools/gen-figures2.mjs が失われたことを CI が検出 →
  コミット済み図JSONを仕様として再建。ゲート21件はバイト一致で再現。再建版を新正本とし
  図一式を再コミット(旧版との残差は非描画の副系列3点・ulp水準)。**CI paper2 ジョブは
  再建後 success**(図再生成照合+TeX 日英+未定義参照検査)
- **論文2 v0.5→v0.6**: 2巡のレビュー Major を全対応(参考文献12件/推定量バイアスの明確化/
  n_eff(φ_B)/q=1 限定/暗さ計測の限定/意味論=符号化/後期挙動の正記 ほか)。
  詳細は tex ヘッダの変更記録
- レビュー由来の新台帳 4-87〜4-92(probeH サンプル/観測面光束/クランプ帳簿/photon-abc
  閾値/freebox 診断/claims 分類)は private 台帳参照

第46便(2026-07-29): 裁定4件(26j帰属/論文2進める+ja修正/starcore進める/saturnZonalD68妥当)。

- **☀️starcore 新設(4-68c 完結)**: 「恒星の内部 — 融合を熱源とする」。thermal:tint+
  fusion{dFrac:0.35}・熱源=融合のみ/出口=E11放射のみ(γn=μF=kRep=κs=0 — Q=ΔH+radE が
  etaRad 非依存で閉じる帳簿設計)。fission 不採用(fusU ドリフト回避・燃料が尽きるまでの
  有限過程として設計)・validT=192。対照3種の要因分離(融合切りで放射75.5倍差・etaRad=0 で
  軌道 bit 一致・G 用量反応=点火は重力駆動)・dt 収束(0.016/0.008/0.004)・3seed 頑健。
  claims 3件+behavior.starcore+PHYSICS §6 同期+基準ショット初回記録
- **論文2 v0.4**: 図8点を機械生成(tools/gen-figures2.mjs — 第1論文と同方式の svg+pdf+json+
  数値ゲート21件)して本文へ組込み(en 7頁/ja 8頁)。**ゲートが v0.3 本文の誤りを検出**:
  掃き出しの向き(非脱出は s=0.6→0.7 で消える — 第37便の実装側訂正と整合)を訂正・
  外側増強 1.15→1.16(beta 実測)・外縁ブースト 1.332(release)/1.331±0.009(beta)併記。
  ja §V の言い回しも修正(原仮定者指摘)
- 発見(要裁定として台帳へ): κs>0 で pinned 天体へ流れた伝導熱が帳簿に載らない
  (E10′ の dQi が pinned をスキップ — starcore 設計中に 46S が特定。κs=0 構成では無影響)

第45便(2026-07-29): 決断事項9件(過去ロードマップ整備2件は private 側)。beta は **v1.34-b3** へ:

- **4-48 消化(45A)**: RAY_MASS_MIN/PN_MASS_MIN(固定40)を**偏向角基準**へ置換 —
  源条件 m ≥ (α_min/4)·Kt·max(R,ε)・α_min=0.02 rad(光線/1PN 使用8プリセットの源集合が
  既定+物理ロック双方で現行一致する較正窓 (0.0167,0.0207] の最大値)。E12(1PN)は時空係数を
  c²/G で読む(V18〜V20 成立の必要条件と実測特定)。既定表示は全プリセット不変・
  QA ray.alpha-threshold 新設・較正証跡 tests/exp-4-48.mjs
- **粒子分裂(fission・45B)**: `fusion:{dFrac, fission:{Tcrit, frac}}` — 融合の厳密逆写像
  (R₁²+R₂²=R²・軌道L恒等不変・スピンは格納後値から逆算・ΔE_split≥0 恒等保証・第一法則は
  1次方程式で厳密に解ける)。分離エネルギーは T_int が支払い、熱不足なら発火しない。
  再融合抑止は構造的(fuse→fission の順)+幾何(発火面5%外側+離反保証)の2重。
  QA 3件: fission.conservation(P/L≤1.5e-8 実測)/zero-cost(第44便と bit 一致)/
  cycle(重力落下のみで融合⇄分裂 FSFS 2サイクル・保存維持)
- **吸熱融合の調査と制限(45B)**: 吸熱境界は理論値 dd_crit=1/√2(等質量最小・掃引63構成で
  理論と100%整合)。fusU が下限なしの記帳枠であるため回転エネルギーの無制限汲み出しが
  可能になる — **dFrac ≤ 1/√2 に制限**(FUSION_DFRAC_MAX・validatePreset/エンジン門番の両方)
- **相転移3対の再現検証(45B)**: ①融合⇄分裂 = QA fission.cycle(対照: fission なしでは
  戻らない)②結合⇄解離 = exp(低温で接触振動束縛・加熱で解離・単調増加率1.00)
  ③凝縮⇄気化 = exp(既存物理・⟨d⟩比 0.30/3.65・可逆性0.93・残差の主因は既存 E9 減衰と
  要因分離)。②③は E9 積分誤差の敏感性により exp 止まり(理由記録)
- **4-85 消化(45C)**: 強場トイ領域バッジ「⚠強場トイ領域」(ψ_static>0.5・分類チップ行・
  ja/en・表示専用)。対象5プリセット(galaxy 3.33/agnjet 1.85/darkrotor・rotorSolo 0.74/
  saturnZonalD68 0.538)。QA ui.strongfield-badge 新設
- 論文2 v0.3(0.92-a 関係の厳密化+著者名確定)は同便の別コミット参照。
  **☀️starcore への申し送り**: 分裂使用時は dt 収束を取ってから定常を主張すること
  (dt=0.016 の「自己維持」領域は刻み幅アーティファクトと実測特定)+fusU の
  サイクルあたりドリフト(実測 −0.30/サイクル≈熱回転量の1.6%)を監視指標に

第44便(2026-07-29): 決断事項6件(4-48 検討/4-68c 融合導入/論文2タイトル(c)/論文2 PDF 日英/
0.92 出典採録/過去ロードマップ監査 — 監査結果は private 台帳)。

- **粒子融合機構(4-68c 前提・☀️starcore 再挑戦への布石)**: プリセット最上位キー
  `fusion:{dFrac}`(opt-in・thermal:"tint" 限定・既定 dFrac=0.35)。融合則は保存厳密 —
  質量加算・運動量厳密・角運動量厳密(軌道→スピン移譲・I=½mR²・Float32 格納丸めは
  回収帳簿 fusPx/fusPy/fusL が保持)・ΔKE+U_rep+U_spring→T_int(第一法則)・
  E4 重力対ポテンシャルと N≥3 の第三者相互作用差分はリザーバ fusU へ(これが無いと
  N≥3 で第一法則が破れることを実測で特定)。R'=√(R₁²+R₂²)(rMul 再構成で R=rMul√m
  写像を維持)。粒子削除は順序保存の前詰め圧縮(A/B 転写・zonal・光子参照・選択 index を
  写像で追随)。QA 3件新設: fusion.conservation(P≤1.6e-8/L≤1.2e-8/E≤2.4e-8 実測)/
  fusion.zero-cost(fusion 無しの4プリセットが基点と bit 一致)/fusion.chain(3体連鎖)。
  プリセット追加は次便(☀️starcore 構成案は設計提言済み — 燃料枯渇問題と分裂機構の
  要否を含む)
- 論文2 v0.2(タイトル(c)確定・D/Kt=(2/3)ln4 厳密形+強場注記・日本語全訳)と
  4-48 検討(RAY_MASS_MIN の偏向角基準化設計)は同便の別コミット参照

第43便(2026-07-29): 決断事項11件(4-51/60/66/67/72/73/74/75/76 の追認・整理は private 台帳に
記録)+新規3件の消化。beta は **v1.34-b2** へ昇番(SW キャッシュ同期):

- **claims の UI 表示(4-78 B案)**: 説明タブの分類バッジ直下に折畳み「🔎 数値主張(機械検証済み)」
  を新設(claims を持つ18プリセットのみ・ja/en)。各 claim を `metric: 期待域 min〜max(対照)`
  で列挙し QA(claims.sync)が毎回照合している旨を注記。表示専用(物理・保存 JSON 不変)。
  canvas 外 DOM のためスクリーンショット基準は無変更。QA ui.claims-display 新設
- **4-66 比較実験設計書**(docs/dev/EXP_4-66_PROBE_H_DESIGN.md): E8R 指数形 vs 厳密反比例の
  プローブ依存H判別実験(V29 拡張・D/Kt×a スキャン・反比例対照は後処理)。核心: 反比例化は
  プローブ依存Hを消す(比≡dP)— テンション類似は指数形の帰結。実装は次便
- **論文2 ドラフト v0.1**(paper/dfm-paper2.tex): 「箱宇宙」本文(RevTeX 4.2・4部+
  Negative claims 6項+検証対応表)。図8点は生成波待ちのプレースホルダ

第42便(2026-07-29): 決断事項「4-78 A案を進める+論文2 Sprint 0 を進める」の消化。
アプリの物理は無変更(claims はデータ層・V29 は検証コード — BETA_BUILD は b1 のまま):

- **V29 プローブ依存H(42B・論文2 Sprint 0 — HANDOFF_PAPER2_WRITE §2)**: D/Kt=0.92 較正の
  規定膨張箱(mode:"exp"・dPower=1)で、幾何プローブ H_geo=Δln a/Δt と速度減衰プローブを
  FLRW 型(w/c∝1/a)と読んだ H_w=−Δln R/Δt を3窓(a: 1→1.5→2→3)同時測定し、比 H_w/H_geo が
  解析式 2(D/Kt)·(1/a1−1/a2)/ln(a2/a1) と一致することを機械検証(実測: 比誤差最大 5.3e-4
  <1e-2・w保存 4.8e-4・a=2 点値 0.92=D/Kt)。verify.all に組み込み。執筆指示書 v1.1 で
  前提消化を記録 — 論文2は執筆開始可能に
- **claims 全展開(42A・台帳4-78 A案)**: 数値主張を持つ16プリセットへ claims 計30件を新設
  (+パイロット2プリセットに descPattern 追記)。スキーマに descPattern(ja 説明文から数値を
  抜く正規表現・キャプチャ群1つ)/descScale(%→0.01 等の換算)を追加(validatePreset で
  構造検証 — 不正はフィールドのみ warning 削除)。汎用同期QA `claims.sync` 新設 — 全プリセットの
  descPattern 付き claim を走査し「説明文の数値が expected 窓内か」を機械照合
  (claims.sync-pilot は残置・root は SKIP)。説明文と実測の乖離(🌈coolrace 型の較正移行漏れ)を
  恒久的に機械監視できるようになった。UI 表示(B案)は次便以降の裁定

第41便(2026-07-29): 外部AI 3系統の beta 動作確認レポート(ChatGPT/Grok/Gemini 2026-07-29)の
消化+論文2着手。3レポートとも Critical/High/Medium 検出なし(リリース判定 GO)。
実装対象は ChatGPT の Low 2件のみ(アプリ本体は無変更 — BETA_BUILD は b1 のまま):

- **QA version.beta-label-http 新設(台帳4-83・L3)**: 従来の 7z は file:// フォールバック表示
  しか直接検証していなかったため、ローカル HTTP サーバで `/beta/index.html` を実配信条件
  (http(s)+パス /beta/)で開き、DOM の版表示が BETA_BUILD と一致することを統合検査する
  (beta 対象時のみ・root は SKIP)
- **CI の Node 24 化(台帳4-84・L4)**: actions の Node 20 非推奨警告に対応し
  workflow の node-version を 20→24 へ(QA/perf とも。Node 24 での成立は CI 実行で機械確認)
- 論文2「箱宇宙」: 執筆指示書 v1.0(docs/dev/HANDOFF_PAPER2_WRITE.md)を新設 —
  骨子 v0.3(承認済み)の次段階。執筆前提の V29(プローブ依存H)が未実装(verify 欠番)で
  あることを機械確認し、Sprint 0 として指示書に明記

第40便(2026-07-28〜29): 台帳4-81「E6′③反作用の倍精度アキュムレータ化」/ 4-82「saturn 系の絶対負荷低減」/ 4-79「因果QAバッテリー第2弾」/ 4-77+4-80+4-78「A/Bワンタップ化(粒子patch)+英日説明の意味同期+数値claimの機械可読化(パイロット)」— 原仮定者裁定「進める」。

- **E6′ 反作用③の倍精度アキュムレータ化(4-81)**: ③ E6′ 反作用対ループの per-pair 反作用
  (`vx/vy/spin` への減算)を Float32 の状態配列へ直接書かず、Float64 の `accVx/accVy/accS`
  へ集積してサブステップ末に一括適用する。③のループは `vx/vy/spin` を一度も読まないので、
  これは**加算順序の変更のみ**で、対分配 φ_ij = w_j(r_i)/W(r_i)・残余トルクの等角加速度移譲・
  飽和クランプ・リザーバ帳簿の式は 1 文字も変えていない。
  第39便 39B(台帳4-73)が確定させた真因 — 微小増分が `|値|·2⁻²⁴` を下回ると丸めで消える —
  に対し、丸めの回数を「対ごと O(N²)」から「粒子ごと1回」へ減らすのが効果の本体。
  - **実測**: 🕶️darkrotor v6 の BHスピン掃引(S_bh=0.12/1/2/4/8 × 6000/12000/24000步 = 15点)の
    **全点で relL=|ΔL|/L_scale < 1e-3**、上界 7.0e-5(旧: S_bh=2/4/8 で最大 1.2e-2)。
    全内蔵プリセットのセンサス(2000步)でも kFrame>0 の全件が改善(🕳️rotorSolo 2.9e-4→3.9e-6・
    💥merger 1.6e-4→1.2e-6・🌟collapse 1.3e-4→1.4e-7・🕶️darkrotor 8.3e-6→3.0e-7)。
  - **主張禁止レンジの撤回**: v1.33(第39便)で明文化した「高スピン合成域では保存量を主張しない」
    という制限を撤回した(DERIVATIONS §17.9.4 に撤回注記・新実測は §17.9.6・PHYSICS 保存量モニタ節)。
  - **QA カナリアの反転処理**: `conservation.float32-canary` は 39B のコメントの指示どおり
    **回帰側へ作り替えた**(S_bh=4 の判定を relL>1e-3 から relL<1e-3 へ。実測 2.13e-3→3.51e-6)。
    ID は履歴追跡のため据え置き、意味の反転と根拠を detail とコメントに明記。倍精度化前の対象
    (root=v1.33 以前)には旧カナリアの判定が残るよう機能判定 `'accVx' in HP.sim` で分岐する。
    `conservation.float32-range`(既定域の回帰)は判定・閾値とも不変で PASS(5.14e-6→3.50e-6)。
  - **bit 非同一の帰結**: kFrame>0 のプリセットは軌道が bit 一致しない(意図的な数値経路の変更)。
    bit 基準を持つ QA の基準ハッシュを 🪐saturn・🌌galaxy・🕶️darkrotor の3件だけ貼り直した
    (`integrator.default-unchanged` / `sweep.numeric-unchanged` / `tint.zero-cost` — 理由コメント付き・
    対象ごとに新旧を切替)。**kFrame=0 の counterring/freebox/echo は基準そのままで一致** =
    変更が E6′ 経路の外へ漏れていないことの機械的証跡。スクリーンショット回帰は全件が
    基準内(最大 mAD=0.89 = 同一コミットの自然ゆらぎ 1.56 の範囲内)で、基準の貼り直しは不要だった。
  - **コスト**: `node tests/perf.mjs` の beta/root 比 🌌galaxy 1.002・🕶️darkrotor 0.985・
    🪐saturnLayered 0.971(8ゲート ALL PASS)。追加は O(N) の配列3本のゼロ埋め+一括適用のみで、
    O(N²) の内側は 1 演算も増えていない。
  - 証跡: `tests/exp-4-73.mjs` を新計装(丸めの計測点を一括適用ループへ移動・対象で自動切替)へ
    更新し再実測、`tests/out/exp-4-73.json` を更新(旧 39B の掃引値は `previous` キーに履歴併記)。

- **🪐/🎯 の絶対負荷低減(4-82 — 統括裁定「粒子数削減・timeScale は変えない」)**: 環粒子を
  **300→240**(3環帯 75/150/75 → **60/120/60**・総粒子 301→241)へ削減した。真の重さは
  n×steps/frame の対評価数なので、対コストは (241/301)²=0.641(理論 −35.9%)。
  **帯の半径域(104/132/190/212)・帯ごとの粒子比(1:2:1)・physics・timeScale=3 は一切変えていない**
  (方位角は ang=(i/n)·2π+ジッタなので帯は等方のまま。面密度だけが 20% 下がる)。
  - **絶対 ms 実測(60frames・独立ページ・3回の中央値)**: 🪐 1586.5→**1000.8ms**(−36.9%・
    26.44→16.68 ms/frame)/ 🎯 1581.4→**1050.3ms**(−33.6%・26.36→17.50 ms/frame)。
    `tests/perf.mjs` の順(♨️の後)でも root 1729.9→beta 1076.7ms(−37.8%)。目標 −30% を達成。
  - perf ゲートは beta/root 比の**上限**しか見ないので、beta だけ軽くなった結果
    `saturnLayered` の比は 0.858→**0.622** に下がる(想定内・8/8 PASS・ALLOW 追加なし)。
  - **再較正**: `behavior.saturnExp` は新構成(n=241)で t≈360 帯内97.1%/落下1粒(0.42%)/散逸0/
    平均|spin|0.115、t≈1200 でも帯内98.8%/落下1粒/散逸0(旧 n=301 は t≈360 97.3%/0.33%/0)。
    **判定の向きも閾値も据え置き**(余裕: 帯内 2.1 ポイント・落下 4.8倍・平均|spin| 4.4倍)。
    `behavior.saturnLayered` も t≈360 帯内96.7%/落下0.42%/帯中央値 114/144/198/剛体回転との差 0.036 で
    閾値据え置き。C/B/A の index 境界は固定値からプリセットの ring.n 由来の計算へ変更した。
  - **bit 基準の貼り直し**: 初期配置そのものが変わるので `integrator.default-unchanged` と
    `tint.zero-cost` の 🪐 行を新実測へ(判定子は環粒子数から直接数える `hasSat240`。
    galaxy/darkrotor は 4-82 で 1 ビットも動かないので 40A の基準のまま)。`core.twolayer` の
    n 期待値も固定 301 からプリセット定義由来へ。
  - **スクリーンショット基準**: `darkrotor-v6` と同じ方式で世代キーを分けた(beta は
    `saturn-r240` / `saturnLayered-r240` として新規記録・root の `saturn` 基準はそのまま生存)。
    🎯 を基準対象へ追加。**他13件の shot.regress は基準内(最大 mAD 0.41)**= 見た目の差分は当該2IDに限局。
  - 説明文(ja/en)と `docs/PHYSICS.md` §6 の 🪐/🎯 行に新しい粒子数と再較正実測を反映。

- **因果QAバッテリー第2弾(4-79 — ChatGPT精査 P2-4/5/7/8+ピッチ角)**: 内蔵サンプルの説明文が
  主張している因果を、同一 seed で**ノブを1個だけ**変えた対照走行で機械検証する QA を **6件**新設した
  (閾値はすべて 4-82 適用後の実測を分母にした余裕係数つき・根拠は各 add() のコメント)。
  **実測が主張を支持しなかった3点は、主張の側を弱めて説明文を修正した**(誠実性優先)。
  - `behavior.saturn-kframe-control`(3000步・新設): kFrame 1/0 で離心率RMS 0.142/0.063(**2.24倍**)・
    帯中央値 159.2/170.8。**引きずりは環を内側へ寄せ離心率をむしろ上げ、kFrame=0 でも環は 100% 保たれる** →
    説明文の「環は空間引きずりによる近点移動で形を保つ」を撤回し、
    「この条件で長時間安定な環が得られる」まで弱めた。
  - `behavior.saturn-muf-control`(6000步・新設): 3000步では muF=0.2/0/1.0 が**倍精度で完全一致**
    (氷粒半径 R≈0.18〜0.31 が環幅 104〜212 に対し極小=接触がまれ)、6000步でも平均|spin| 差 1.66%・
    離心率RMS 差 0.018% で**用量反応なし** → 説明文の「muF=0 にすると円形化が止まる」を撤回。
  - `behavior.merger-causal`(3000步・新設): 潮汐尾率 0%→3.70%/ kFrame の効きは**スピン加熱に集中**
    (平均|spin| 比 2.17倍〔beta〕・1.95倍〔root〕)/ **核間距離の差は 0.5% しかない**。
    説明文にこの内訳を明記した。muF の効きは 2 ビルドで 10.1% と 0.87% と一桁違うので、
    QA は**存在のみ**を固定し大きさ・向きは主張しない。
  - `behavior.collapse-cooling`(6000步・新設): etaRad 0.004/0 で温度は 0.944/1.090(比 0.866)と
    明確に効くのに、半質量半径 r50 は 133.13/132.65(**0.36%**)。12000步でも 2.1% で符号不定、
    etaRad を5倍にしても温度だけが単調に下がる → 説明文の「放射冷却による収縮」
    「etaRad/pRad が崩壊速度に効く」を**撤回**し、否定的結果そのものを QA で固定した。
  - `behavior.darkrotor-pitch`(新設・**追加走行コストゼロ**): 探索指標だったピッチ角測定
    (`tests/exp-4-72.mjs` の pitchFit)を既存の `behavior.darkrotorLong` の走行に相乗りさせて QA 化。
    6000步窓7点の中央値 **28.20°**(点別 24.2〜35.2°)・**7/7 点が後行**。判定は点別の最大が 35° 際どいため
    中央値で行う。root(4-81 未適用)では中央値 26.02° で第39便 39A の実測と**完全一致**した
    (=移植が原実装と同一であることの証跡)。説明文の「ピッチ≈26°」を実測に合わせて更新。
  - `shot.regress-saturnLayered`(新設): 🎯 をスクリーンショット回帰の対象へ追加。
  - QA 件数: beta **281/281 ALL PASS**(旧 275)/ root **278/278 ALL PASS**(旧 274)。
    🪐 の対照2件は閾値が 240粒構成で較正されているため、301粒のままの対象では自動スキップする。

- **4-77+4-80+4-78 パイロット(40B — 原仮定者裁定「進める」)**: 台帳4-77(A/B対照のワンタップ化)・
  4-80(英日説明の意味同期)・4-78(数値claimの機械可読化)を実装。beta のみ(root は未編集)。
  - **4-77 A/B対照のワンタップ化(粒子patch)**: 既存の「おすすめA/B」(v1.15・物理パラメータの
    1タップA/B開始)を、粒子への patch まで拡張する新設任意キー `abBody`
    `{label:{ja,en}, targets:[粒子index...], patch:{spin/m/vx/vy}}` で拡張した。🕶️darkrotor
    (対向2ローター=bodies順 index1/2 の spin→0・ラベル「ローターのスピン0(対照)」)/
    🕳️rotorSolo(中心コア index0 の spin→0)に宣言し、パラメータタブのA/B比較グループへ
    ワンタップボタンを新設(既存の物理パラメータ単位おすすめA/Bと並置。A/B開始・B側適用・
    終了の既存挙動と整合 — `abStart()` の第3引数として実装)。`validatePreset` が
    abBody(label/targets/patch — patch は spin・m・vx・vy のみ受理し既存の粒子値クランプと
    同一域にクランプ)を検証し、不正は丸ごと無視する。説明文(ja/en)の「スピンを0にすると〜」
    という手動手順の記述をワンタップボタンへの言及に更新。QA新設 `ui.ab-body-onetap`(1件):
    darkrotor でボタンを実クリック→B側対象粒子の spin が0・A側の粒子配列は不変・A側の
    物理ハッシュ(位置・速度・スピン・半径)も不変であることを機械検証。
  - **4-80 英日説明の意味同期**: ⭐binary の en 説明に A5(空間の引きずり)の言及を、
    ♾️fig8 の en 説明に kFrame=0 ニュートン退化対照+運動量・角運動量保存(T7)の言及を、
    ja と同水準で補完した(いずれも従来 en 側に欠落していた)。QA新設 `i18n.claim-sync`(1件):
    対象プリセット(binary/fig8 — 拡張可能な表構造)ごとに ja/en 双方に必須キーワード対
    (例 ja「引きずり」⇔en「drag」、ja「保存」⇔en「conserv」)が存在することを機械検査。
  - **4-78 数値claimの機械可読化(パイロット)**: プリセットの新設任意キー `claims`
    `[{id, metric, expected:{min,max}, control?:{patch,expected}, testId}]` を追加(**表示はしない
    — データ層のみ。UI表示は次便裁定**)。対象は 🌋agnjet と 🕸️cosmicweb の2件のみ(パイロット)。
    agnjet は極方向割合(既定0.75〜0.92域=実測0.839・対照kRep=0で0.45〜0.58域=実測0.542)、
    cosmicweb はδ²成長倍率(既定10〜16倍域=実測13.1倍・対照H=0で26〜36倍域)とボイド率最終値
    (既定0.50〜0.65域=実測57.9%・対照0.68〜0.82域=実測75%)を、既存の説明文実測値から転記した。
    QA新設 `claims.sync-pilot`(1件): claims の expected が説明文中の実測数値(正規表現抽出)・
    既存 `behavior.agnjet`/`behavior.cosmicweb` の閾値のいずれとも矛盾しないことを機械照合。
  - **文書同期**: `abBody`/`claims`/`validT` を docs/dev/HANDOFF_RUNTIME_LLM.md へ
    「beta先行の任意キー(SYSTEM_PROMPTには未開放)」として1行ずつ追記。docs/PHYSICS.md §6 の
    darkrotor/rotorSolo 行にワンタップA/Bの言及を追加。
  - QA 件数: beta **284/284 ALL PASS**(旧281・新設3件)/ root は今便で無編集(root フルQAで確認)。
    perf 8/8 PASS。

## v1.33.0(2026-07-28)

第40便(2026-07-28): 第35〜39便(2026-07-26〜28)を beta(`beta/index.html`)で検証のうえ
ルートへ昇格(原仮定者指示)。昇格は PWA 一式(index.html/sw.js)+版数の1コミットで実施し、
ルート SW のキャッシュキーは `dfm-release-v1.33`(β版は `dfm-beta-v1.33-b5` のまま —
内容はルートと同一)。昇格によりルートでも第35〜39便系 QA(温度を内部状態変数化した
tint/第一法則帳簿・伝熱する箱・🕶️darkrotor v6(中心BH+対向2ローター)+🌪️spinup・
「掻出」→「減光」改称+λ0スライダー・可視外記号色・平均温度グラフ色/温度バッジ・validT・
性能最適化 等)が有効化される(root/beta フルQA 実測・perf 8ゲート ALL PASS)。

第39便(2026-07-28): 決断事項(4-72〜4-76 すべて進める)+サンプル関連4件(rotorSolo記述/
熱伝導が速すぎ/放射冷却が動かない/saturn系が重い)。beta SW は `dfm-beta-v1.33-b5`。

- **🕶️darkrotor v6(4-72)**: ローター10体→対向2体へ再設計(38C 4-67実験の提案A)。周速再較正・
  総運動量ゼロ化。24000步の恒星保持 87.9%(旧38.7±13.0%)・σ_rドリフト1/4.5。有効窓は2本立て
  (恒星の安定=24000步/腕の分離=6000步)。誠実性: 2体配置自体がm=2四重極 — 対照にも配置由来の
  腕が残ることを明記しスピン起因分は用量反応で分離。4-53要因分離を新構成で再測定
  (kFrame=0→0.687・kRep=0→0.328)
- **🌪️spinup 新設(4-74)**: 完全閉鎖系の自己重力雲(kRep=0)の収縮とコアのスピン加速 —
  R_core×1/3.06・ω_core×8.50 対 予測×9.36(一致90.8%)。帳簿[0,0,0]・|ΔL|/L_scale 1.7e-6。
  防御線: 主張はコア限定/kRep=0では温度は力学に入らない受動トレーサ(冷却駆動とは主張しない)
- **4-73 真因確定**: 高スピン域の保存恒等式破れは E6′飽和クランプではなく **Float32丸め吸収**
  (説明率100%・クランプは対分配に無罪の実測1.8e-6)。DERIVATIONS §17.9 新設・主張禁止レンジ
  明文化・カナリアペアQAガード(conservation.float32-range/-canary)。倍精度化試作は台帳4-81へ
- **4-76+実機報告**: beta/root 比超過の正体は計測順のJIT退化(♨️→🎯の順序依存・除くと1.000)。
  bit同一最適化4種(I=½mR²事前計算・E10′遠方対足切り・③W_j巻き上げ・tint配列単型化 —
  39プリセット×2000步ハッシュ機械証明)で saturnLayered ≤1.013×6回 → 暫定ALLOW 1.20撤去・
  counterring FAIL→PASS回復・絶対値−18.6%。物理変更を伴う追加低減は台帳4-82へ
- **サンプル修正(原仮定者報告)**: 🌈coolrace etaRad 1e-5→1.6e-3(旧スピン熱較正のtint移行漏れ —
  「動かない」の是正。T²比率則は不変)/📏conduction kappaS 1.5→0.3(前線と上下列差が見えるように。
  熱運搬比の実測 1.9→2.5倍)/🕳️rotorSolo 減光欄の記述を38A仕様へ
- **説明修正(ChatGPT精査第2版 P1)**: saturnLayered(42%はモデル写像と明記・「地震学的≈1.01」削除)/
  boxredshift(宇宙論的赤方偏移とハッブル–ルメートル則を区別)/earthMoon(内核差動の固定比削除)/
  saturn(radiusScale→rMul表記)
- **UI(4-75)**: 温度モデルバッジ「温度=T_int」(tintプリセット)+有効時間窓 validT の
  HUD評価窓外マーカー(darkrotor/spinup=384・rotorSolo/cosmicweb=96・agnjet=64)

第38便(2026-07-28): 決断事項(4-48 用語/4-67/4-68c/4-69/4-70 右壁追認+温度グラフ色/
boxtrans 明記)+UI 5件+検証実験2件。beta SW は `dfm-beta-v1.33-b4`。

- **用語改称(4-48)**: UI表示名「掻出」→**「減光」**(en: sweep→dimming)。0〜1(1=完全減光)の
  意味を直感化し、③外来光線の「掃き出し」(DERIVATIONS §17)との混同を解消。JSONキー
  lightSweep・lS_eff 表記・QA API は互換のため不変(旧称は docs に注記)
- **光線の基準波長 λ0**: 「光線」トグル直下でスライダー化(可視光380〜780nm・5nm刻み)。
  直値入力は可視外も受理([0.001,1e6]nm)。既定を 550→580nm へ(旧固定表示色の黄に相当。
  保存値550のみ580へ移行)。可視外は記号色で可視化: γ線=白/X線=淡シアン/紫外=明紫/
  赤外=暗赤(rayBandColor — 光子・赤方偏移表示の wavelengthColor は4-63裁定のまま不変)
- **減光欄の改修**: ラベル幅と実効値スパンを整理して数値欄を拡幅。auto 中は数値欄を編集不可に
  して実効値 lS_eff を毎フレーム上書き表示
- **radiusScale の全粒子化**: 明示半径(radius)の粒子にも効くよう修正(R=radiusScale·radius・
  既定1で bit 不変・QA radius.explicit-scale 新設)。粒子個別のスケール機構は設けない
- **平均温度グラフ(4-70続き)**: バー・折れ線の色を温度に対応(粒子と同じ tempColor 写像)。
  右系列は破線で識別
- **サンプル説明の精密化(ChatGPT 全サンプル精査 P1)**: darkrotor「kFrame=0で腕が消える」を
  撤回し複合経路へ訂正(kFrame=0 でも約89%残存・kRep=0 で約54% — 4-53実測)/cosmicweb
  「主要な競合因子は2つ」/convection 非対称壁境界の明記/agnjet「等方の1/3よりなお高い」/
  boxtrans「1つだけ速さが違う粒子(上の時計)」= vx=2.1 の紐付け
- **検証実験(コード変更なし・証跡 tests/out/)**: 4-67 — 中心BH+対向2ローター構成で恒星保持率
  88.6±0.3%(現行10体 38.7±13.0%)と安定化を実証。BHスピンは上げるほど単調悪化(既定0.12が
  最良)で S_bh≥2 は保存恒等式が破れる(E6′飽和疑い — 台帳へ)。4-68c — 冷却→収縮→スピン加速を
  実証(コア ω×8.50 対 予測(R₀/R)²=9.36・ずれ−9.2%=コアの角運動量流出分と恒等一致)。
  帳簿[0,0,0]・|ΔL|/L_scale 1e-6台の完全閉鎖系
- 4-69(darkrotor ctrl 帯平均<0.15)は第37便実装を第38便裁定で確定。♨️右壁 rad・boxtrans
  vx=2.1 存置は原仮定者追認済み

第37便(2026-07-27): 決断事項(4-53/4-66やらない/4-68/4-69/4-70/4-71・移行調整追認・
第2論文骨子承認)+UI 6件+サンプル3件。beta SW は `dfm-beta-v1.33-b3`。

- **伝熱作用する箱(4-70)**: world.thermalWalls(面ごとの heat/rad/断熱・帳簿 wallEin/Eout/KE)。
  ♨️を pinned 全廃の壁駆動対流へ再設計 — 床加熱・天井/右壁放熱・左壁断熱+一様重力のみで
  24000步循環持続・定常熱流(「左右断熱」literal 構成は循環符号不定と実測し右壁 rad を採用 —
  要追認)。BOX_UNIVERSE §12-7
- **E5′ ポテンシャルの帳簿化(4-71)**: 対ポテンシャル U_rep を確定し(q>1)、T_int 変化時の
  ΔU_rep を熱から支払う A12″ を新設 — **融解潜熱が力学の帰結に**(プラトー実測・吸熱の73%が
  U_rep へ)。L17 は tint かつ q>1 で解消(THEORY_SYNTHESIS 改稿)。V30 改訂・V32 新設
- **新サンプル3件**: 🕳️ダークローター(単体 — 原仮定者指示): 暗さの②掻出(auto)と③光線
  掃き出しが別機構であることを1画面で分離実証(lS_eff 飽和 spin≈0.29 vs ③立ち上がり 0.6〜0.7・
  物理ロックで③消失)/🌋AGN類似(4-68a): 内縁加熱ガスの0.84が±y±30°へ(双極性2.5倍)/
  🕸️コズミックウェブ類似(4-68b): δ² 13.1倍・ボイド率58%・H の単調用量反応。
  ☀️恒星内部(4-68c)は「内部エネルギー源が無く定常放射は原理的に不成立」の実測により見送り
- **UI(原仮定者指示6件)**: 下部タブの safe-area 回避/掻出 auto のチェックボックス+実効値
  表示/光線の基準波長 λ0 選択(表示専用)/決定力マップに一様重力を反映(表示規約
  W_disp=W+(g·r)/G_eff)/描画半径 cap を全品質で撤廃(忠実表示)/**drawScale 廃止**
  (☿等の表示被りの解消 — 旧 JSON のキーは無視)
- **サンプル整理**: 📦boxtrans の vx=2.1 は設計値(箱1.2+固有運動0.9)と説明文に明記/
  ❄️snowline 廃止(挙動の正解が不明のため — 原仮定者裁定)/4-69: darkrotor ctrl 判定を
  帯平均<0.15 へ(8seed 根拠)
- **要因分離バッテリー(4-53)**: tests/exp-factors.mjs(4サンプル×7ノックアウト)。
  galaxy 初期実測: 増強1.322 は kFrame=0 で完全消失・kRep 寄与≈4%

第36便(2026-07-27): 決断事項4件(4-51/4-47/4-48/4-49)+外部AI 3系統の beta/root 差分
検証レビュー(ChatGPT/Grok/Gemini 2026-07-27)の P1 消化+パラメータ2件+UI 4件+
第2論文「箱宇宙」骨子(docs/dev/HANDOFF_PAPER_BOX)。beta SW は `dfm-beta-v1.33-b2`。
モデルルーティング改訂版(最上位=Fable/複雑=Opus/量産=Sonnet/簡易=Haiku)で実施。

- **温度を内部状態変数にした(A9′・台帳4-51)**: プリセット単位のオプトイン thermal:"tint" で
  粒子ごとに内部温度 T_int(保有熱 cHeat·m·T_int)。衝突・摩擦の散逸は T_int になり(E9′)、
  熱伝導(κs)は熱だけを運びスピンを動かさない(角運動量が自明に厳密保存 — 4-50 解消・V31)。
  圧力(E5′)と放射(E11)の源も T_int。熱の実験室7種+❄️を移行(🎈は「熱の圧力」へ改称)。
  非移行プリセットは 1 ビットも変わらない(QA tint.zero-cost)。V30(第一法則の閉性)新設。
  凝固潜熱は E9′ 経由で自然発生・融解潜熱は L17(U_rep 帳簿外)により未発生(裁定伺い)
- **粒子半径スケールの正規化(原仮定者指示)**: 全 body 型に rMul(半径倍率)を新設し、
  radiusScale≠1 だった内蔵27件の旧値を焼き込んで physics を全件 1 へ(29プリセット bit 一致)
- **掻出の自動算出モード(原仮定者指示)**: lightSweep:"auto" — lS_eff=min(1,|s|R/c_surf)。
  物理対応ロック+実在自転比で 3.4e-5(誠実性を機械固定)。独立の数値指定は現行どおり
- **UI(原仮定者指示4件)**: タイトル直下のバージョン表示(beta では v1.33-b2 — 同期QA付き)/
  決定力マップを対数正規化+10バンド量子化(等決定力線状の読み取り)/光線の色を
  E7R 時計率比の波長 λ=λ0·e^(ψ0−ψ) で表示(重力赤方偏移アナログ — 表示のみ)/
  AIタブの説明3件を折畳み
- **新キーの追随(差分検証レビュー P1)**: AIベースJSON(validatePreset 正規化方式へ)・
  インポート重複判定・セーブ重複判定・ドラフトが integrator/echoFlipAt/measureBox/drawScale/
  universeBox/photonEmit を保持。perf に echo/freebox(昇格までは情報記録)
- **ダークローター継続(4-47/4-48/4-49)**: 半径依存曲線 v/v_bar(r)(seeds.mjs 8帯)・
  暗さ3機構の分離実験(掃き出しは強場ψ効果で lightSweep と直交 — DERIVATIONS §17 登録)・
  u_req 逆問題の設計実験(前方予測は残差≈2%・設計は u_φ 飽和が律速 — 継続)

第35便(2026-07-26): 決断事項8件の消化(4-20/4-42/4-45/4-54/4-55/4-58後半/4-61/4-62)。
外部AI 3系統レビュー(ChatGPT/Grok/Gemini 2026-07-26)を参考資料として設計。beta SW は
`dfm-beta-v1.33-b1`。モデルルーティング(設計=Fable/物理=Opus/UI=Sonnet/機械=Haiku)で実施。

- **可逆積分器+時間の矢(4-62)**: プリセット最上位キー integrator("semi"=既定・現行と
  厳密等価/"leapfrog"=KDK)と echoFlipAt(全粒子の v・s を一度だけ符号反転+echoRMS 公開・
  HUD 表示)。新サンプル ⏪「時間の矢 — 巻き戻る宇宙と巻き戻らない宇宙」(空間と時間) —
  可逆構成+leapfrog で反転後 RMS 1.3e-3 まで巻き戻り、muF=0.3 で 10.8(戻らない)・
  semi 積分器で 0.92(709倍)。時間の矢は散逸から生まれる(T11 新設)。QA echo.* 系
- **自由な箱(4-61)**: 新サンプル 🕊️「箱宇宙 — 自由な箱(膨張の原因)」(箱宇宙) —
  壁64個を全て自由粒子とし膨張則 a(t) を E4 自己重力+E5′ スピン斥力から「解く」
  (BOX_UNIVERSE §14.2)。圧力駆動で加速膨張(実測 a_eff 2.18)・kRep=0(おすすめA/B)で
  減速膨張→再収縮。最上位キー measureBox(実測尺度因子 — universeBox と排他)+
  ring vMode:"hubble"(群重心基準)。完全閉鎖系(帳簿厳密0)。T12 新設。QA freebox.* 系
- **サンプル分類バッジ(4-54)**: 説明タブに classifyPreset の自動チップ(コア公理/拡張則/
  規定背景/意味論/比較チャネル/外部駆動/閉鎖系 — ja/en)。既存の外部要素箇条書きは不変
- **描画専用半径 drawScale(4-55)**: 最上位キー drawScale([0.4,4])— 描画のみに効き
  物理不変(位置ハッシュ bit 一致で機械証明)。全サンプル監査の結果、主役粒子が 2px 未満の
  8件のみ調整(gclock/grcal/mercury/projectile/boxcomoving/boxbreath/boxredshift/freebox)
- **QA補強(4-42)**: 専用挙動QAの無かった6サンプル(boxbound/blens/gas/pressure/
  conduction/phase)に説明文の主張を数値判定する behavior テストを追加(閾値は実測から
  1桁の余裕・コメントに実測値を記録)
- **スクリーンショット回帰(4-58後半)**: キャンバス限定キャプチャ→48×48 輝度グリッドを
  tests/baseline-shots.json(コミット基準)と比較(mAD≤10/255・差分セル≤15%・SHOT_ALLOW
  運用)。第32便の取得(前半)を比較(後半)へ完成
- **フルQA並列化(4-45)**: 重量テストを4ワーカー並列化 — beta フル実測 923s→163s
  (5.7倍)。QA_SERIAL=1 で従来直列へ戻せる。判定の同一性は id/pass 全件一致で機械確認
- **多seed統計(4-20)**: tests/seeds.mjs 新設(npm run seeds — 8seed×galaxy 外縁増強/
  darkrotor 腕振幅の統計。QA とは独立の探索スクリプト)

## v1.32(2026-07-26)— beta からルート昇格

第31〜33便(箱宇宙・意味論・光子の波長状態化・🌌再設計・🕶️腕のある銀河)を
beta(`beta/index.html`)で検証のうえルートへ昇格(原仮定者指示 2026-07-26)。昇格は
PWA 一式(index.html/sw.js)+版数の1コミットで実施し、ルート SW のキャッシュキーは
`dfm-release-v1.32`(β版は `dfm-beta-v1.32-b4` へ更新 — 内容はルートと同一)。
昇格によりルートでも第31〜33便系 QA が有効化される(version.sw-sync 含む)。

第31便(2026-07-25): **箱宇宙**(`docs/BOX_UNIVERSE.md` v1.2 — §8-2 承認済み実装+
実装前精査〔外部AI 4系統〕の理論訂正を反映)。beta SW は `dfm-beta-v1.32-b1`。
README のグループ構成同期(5→6)は昇格手順 step2 で実施する。

- **エンジン(beta)**: ①`universeBox` 連続背景 — 静的 D₀ を「位置・速度・尺度 a(t) を持つ
  動的背景」へ昇格(W_B=D·a^{−dPower}・u_B=V+Ω·J(r−c)+H·(r−c)。E3/E6′/E7R/E8R へ一貫参入、
  背景持ち分の反作用はリザーバ帳簿)。尺度モード static/exp/lin/sin。名目半径 L·a(t) を
  破線円で表示・HUD に a/H/W_B ②`ring.railH` — 膨張レール(a=e^{Ht} の解析再構成・
  ドリフトなし・pinned 限定)③`disk.vMode:"hubble"` — 共動初期条件(v=vScale·(r−c))
  ④理論解説に T10(箱宇宙)を追加。既存プリセットは全て挙動不変(universeBox 無しは
  ゼロコスト経路)
- **サンプル**: 新グループ「箱宇宙」6種 — 📦並進する箱(並進の内部不可知性)/🌀回る箱
  (回転利得 g=1/2+空間の支え)/📈膨張する箱(離散壁は膨張を半分しか運ばない)/
  🫧UniverseBox の完全共動と膨張時計(χ=r/a 保存+決定力希釈で時計が加速)/
  🪢束縛系は膨張に逆らえるか(膨張追随の φ_B 則)/🫁呼吸する宇宙と時計(時計率変調+
  一周期可逆性)
- **検証**: V23a/V24a(離散リングの内部フレーム利得 g(20)=0.5001 — 解析値 1/2 と一致)・
  V24b(UniverseBox 完全共動 — χ ずれ 1.5e-5)・V25(膨張感受率 n_net: 箱支配 1.61/
  局所支配 0.00・特異速度保存 8.3e-5)を verify.all に追加。QA に box.trans-clocks /
  box.rot-support / box.expand-sqrt / box.breath-return / box.schema-clamp を新設
- **理論訂正(BOX_UNIVERSE v1.2 §0.5)**: 旧「E3 から u=Hr が創発」を「u=g(r)·H·r、
  2D一様リング中心で g=1/2」へ訂正(構造由来 — ⟨n̂(n̂·r)⟩=r/2)。旧「束縛系は膨張しない」を
  「膨張追随は決定力の持ち分 φ_B で決まる(E6′ は Hubble 反摩擦 — 特異速度を保存し、
  実宇宙の 1/a 減衰と分岐)」へ置換。V23/V24/V25 は成功前提から測定型へ再定義
- **ドキュメント**: PHYSICS.md v1.10(§5 属性 universeBox/railH/hubble・§6 箱宇宙6種)・
  BOX_UNIVERSE.md v1.2(§0.5 訂正・§9 実装記録・§10 考察〔膨張の原因 4 モード分離/
  膨張と時間の共通原因則/時間の四層/時間反転の知覚不能性/光速の意味〕・§11 次期候補)

第32便(2026-07-26): **箱宇宙の意味論**(原仮定者裁定 — BOX_UNIVERSE v1.3 §12)と
膨張則の一般化・赤方偏移表示、**ダークローター銀河のピン無し化**(台帳4-47 Phase C)、
台帳 4-23/4-38/4-39/4-48/4-49/4-51/4-53/4-57/4-58 の裁定消化。beta SW は `dfm-beta-v1.32-b2`。
モデルルーティング(設計=Fable/物理=Opus/UI=Sonnet/機械=Haiku)で実施。

- **意味論(BOX_UNIVERSE v1.3 §12)**: 箱宇宙=一定範囲の外を単純化する近似(スケール任意。
  D₀・一様重力も概念上は箱のパラメータ)。箱の移動・回転は内部から検知できないとみなす
  (検知が要るなら、より大きな箱=入れ子)。拡大縮小だけは検知でき、特異速度は保存(v1.2 の
  「実宇宙GRとの分岐点」を仕様として追認)・光の波長は a に比例して伸びる(赤方偏移
  1+z=a/a₀ — 意味論としての採用。光子力学からの導出は将来課題)。T10 を同旨に改稿
- **エンジン(beta)**: universeBox に mode:"pow"(a=1+係数·t^指数+揺らぎ〔amp·sin〕 —
  「時間を底とした係数・指数・揺らぎ」で膨張則を編集)+全モード共通の H クランプ ±0.5 /
  赤方偏移データ層(boxHist 720点・boxRedshift())/ 単体レールの railCx/railCy 公開
  (4-57 — 未指定=原点で従来と厳密等価・AI生成にも開放)/ traceRay 終端状態
  end("stopped"/"maxSteps")+steps(4-48)
- **UI(beta)**: 膨張グラフ overlay boxGraph(a(t)/H(t) 時系列・現在値・赤方偏移カラーバー
  〔放出/観測マーカー・赤外/紫外端・「意味論であり導出ではない」注記〕— 🫧🪢🫁で既定ON)/
  パラメータタブに箱宇宙セクション(mode・H₀・係数・指数・揺らぎ・D の実行中編集。
  セーブ対象外の実験用)/ HUD に z / 回転曲線グラフの基準線(点線)を「中心質量ケプラー」から
  **全バリオン実加速度 v_bar(r)** へ(4-49 — 中心1質点では旧ケプラー線と厳密一致に縮退)/
  AI送信内容の事前確認パネル(4-58)+QA スクリーンショット取得(tests/out/*.png・非コミット)
- **サンプル**: 📦🌀📈から「箱の影響を受けない粒子(絶対静止)」を廃止(意味論 §12-2 整合 —
  📦の対照時計は「箱に対する固有運動」へ)/ **🕶️ v4 = 全粒子自由の閉鎖系**(中心BH m2000+
  二層ダークローター10体+恒星200体。総運動量≈0・12000步(t≈192)安定を機械検証。
  実測の設計則: ハロー総質量/BH質量≳0.9・D0=2 で E6′ リング不安定を抑制・radiusScale=1.5 で
  恒星スピン汲み上げを抑制。有限時間の閉鎖系デモ — 18000步以降はハロー軌道が緩やかに分散)/
  🪐の説明を「環は空間引きずりによる近点移動で形を保つ」へ(4-38)
- **QA**: V26(pow 解析一致)/ box.redshift・box.hist・box.graph-overlay・box.edit-ui・
  box.redshift-bar / rail.custom-center / darkrotor.uphi 再較正(r260 新設)+darkrotor.allfree+
  behavior.darkrotor(v4 10条件)+behavior.darkrotorLong(12000步・!FAST)/ curve.vbar 系 /
  ai.payload-preview / shot.capture / fewshot.bh-capture→**bh-nonescape**・
  claim.galaxy-flatten→**galaxy-outerboost** に改名(4-48 — 実態整合)

第33便(2026-07-26): **光子の波長状態化**(台帳4-63 承認「DFMでは光の状態は『波長』で
保持した方がシンプル」)と A/B/C 赤方偏移の実装、**🌌銀河の回転曲線の再設計**(AI生成
プリセット土台 — 台帳4-65a)、**🕶️腕のある銀河 v5**(台帳4-65b — 渦状腕の再現に成功)、
台帳 4-61/4-62 承認記録・4-64 消化。外部AI 3系統レビュー(ChatGPT/Grok/Gemini 2026-07-26)の
P0/P1 を裁定消化。beta SW は `dfm-beta-v1.32-b3`。

- **光子(4-63・BOX_UNIVERSE v1.4 §12-6)**: 光子は放出波長 λ_emit と放出時尺度 a_emit の
  2値を状態に持ち、λ_obs=λ_emit·a_obs/a_emit(波長保持則 — 波動力学からの導出ではない)。
  photonEmit スキーマ・E8R と場評価を共有する photonStep・到着ログ・波長色の描画。
  新サンプル 🔦「箱宇宙 — 光の赤方偏移(A・B・C)」— 原仮定者の補足数値をそのまま再現:
  t=0 に A が放った光は a≈4 で C に届き λ×3.9(z=2.92)、a=2 で B が放った光はほぼ同時に
  届き λ×2(z=0.96)— 放出時刻の異なる光の異なる赤方偏移の同時観測(QA box.photon-abc・
  帳簿厳密性<1e-9・static対照 z=0)。traceRay は85プローブ全精度比較で bit 等価
- **FLRW 摩擦比較モード**(universeBox.friction:"flrw" — BOX_UNIVERSE v1.4 §13): 標準宇宙論の
  特異速度減衰 dw/dt=−H·w の対照チャネル(DFM公理外・外部場バッジ・V27: |w|/|w₀| が 1/a と
  0.03%一致)。§13「観測データとの比較」新設(DES超新星時間伸長・CMB T(z)と構造一致/
  BAOとは無次元トイの形状比較のみ)
- **§12-5 の原仮定者修正(BOX_UNIVERSE v1.5)**: 旧「拡大しても光速に対する慣性移動速度は
  変化しない」を撤回 — 光速は場の決定力に反比例し、箱4倍拡大で場の光速は4倍・絶対時間に
  対する慣性移動速度は維持・**「光速に対する比」は1/4へ低下**。エンジン対応: w保存(V25)は
  そのまま正・局所光速 c₀e^{−2ψ} は W_B 希釈で上昇 → 比 w/c_loc は単調低下(V28 で機械固定)。
  **比率基準では実宇宙GRの減衰方向と整合** — v1.2以来の「分岐点」は絶対速度の基準の取り方に
  由来。厳密反比例化(E8R改訂)は弱場GR較正との両立が論点のため台帳4-66へ。
  🪢boxbound の説明を比率基準へ改稿
- **🌌銀河の回転曲線 v2(4-65a)**: AI生成プリセットを土台に再設計 — 中心 m2500(spin1.2・
  掻出0.8)+恒星380体・光線24本(中心の強い場に捕捉・巻き付く)。円盤半径 320→260 の最小修正で
  全ゲート通過(外縁増強 1.322・24000步で恒星保持100%・max|spin| 健全 — 全軸で旧構成超え)
- **🕶️腕のある銀河 v5(4-65b)**: 対向2ローターのスピンだけ 2.0 に上げた完全軸対称配置が、
  恒星円盤(380体)に m=2 の後行渦状腕(ピッチ≈24°)を立てる。スピン0の対照で腕が消え、
  スピン量に単調応答(用量反応 — 腕の主因はローターの空間引きずり)。有効窓 t≈96(6000步)の
  有限時間デモ。kRep=0.15(E5′が腕形成の律速 — 本サンプルで E5′ を主張しない)。
  QA behavior.darkrotorLong を 6000步・腕A2 4帯+軸対称対照の機械実証へ再定義。
  perf は意図的変更として ALLOW(darkrotor:1.30・理由コメント付き)
- **UI/保存**: 光子の波長色描画(点+グロー+尾)・boxGraph に到着ログ(色見本つき)と
  H クランプ注記 / セーブに universeBox を含め復元(4-64 — 旧セーブ後方互換)/
  AIタブの横スクロール修正(#aiBasePreset の flex 最小幅 — min-width:0 の1箇所)/
  📦の対照時計の固有運動を x 方向へ統一(vy 不使用 — 原仮定者指示)

## v1.31(2026-07-25)— beta からルート昇格

第25〜29便(第24次リリースレビュー・第25次性能レビューの裁定実装、サンプル再編・レール駆動・
AIベースサンプル・UI改善・処理の高速化)を beta(`beta/index.html`)で検証のうえルートへ昇格
(原仮定者指示 2026-07-25)。昇格は PWA 一式(index.html/sw.js)+版数の1コミットで実施し、
ルート SW のキャッシュキーは `dfm-release-v1.31`(β版は `dfm-beta-v1.31-b6` へ更新 —
内容はルートと同一)。昇格によりルートでも第25〜29便系 QA が有効化され、`version.sw-sync`
(SW キャッシュ=dfm-release-v{APP_VERSION} の厳密一致 — 第24次 P0-1 の明示ゲート)を追加。

- **サンプル**: カテゴリ再編(5グループ — 「銀河」新設: 🌌🕶️🌠🌫️)/ 内蔵の kFrame を 0/1 に
  統一(⭐0.5→1・🌠0.55→1・🌫️0.35→1)/ 🌠銀河合体の負荷 −61%(q=2 高速経路+円盤 150/120)/
  💥リング衝突の負荷 −48%(100+100)/ 🕶️ハロー20体をレール駆動化(単体 railOmega 新設 —
  自由N体の長時間飛散を解消。PHYSICS.md 属性表参照)/ ♨️に代表粒子の軌跡(循環の可視化 —
  物理不変。第27便の timeScale 3 は第29便で 2 へ復帰)/ ❄️改名「塵は冷えると固まる — 冷たい星と
  熱い星」(en: Dust Clumps When Cold)
- **高速化(第29便 — 第25次レビュー裁定)**: ♨️timeScale 3→2 復帰(CPU計算量 実測1.522倍の
  主因を除去)/ 描画半径の品質連動上限(正確=半径準拠のまま・自動 40px・軽量 30px。グローは
  正確/未縮退の自動のみ — 塗り画素∝rr² の暴走防止)/ レール粒子の索引化 railIdx(レール無し
  宇宙の毎サブステップ O(N) 走査を除去)/ 軌跡バッファの品質別上限(正確1500/自動800/軽量400点・
  A/B中は半減)+満杯後の毎push前詰めを一括刈り取りへ・描画は品質連動で間引き(末尾は必ず描く)/
  動的性能回帰ゲート `tests/perf.mjs` 新設(同一Chromium で root→beta を実測し代表6サンプルの
  中央値比 ≤1.10 をゲート — CI 独立ジョブ perf・perf-results.json を artifact 化)
- **AI生成**: ベースサンプル選択(任意 — 参考サンプルのJSONを土台に改変生成。選択ベースは
  AIプロバイダへ送信される旨を明示)/ 実行時 Opus 指定を claude-opus-5 へ
- **UI**: 選択粒子の編集の行構成を対で整理(m|mc/m・s|sc/s・R|Rc/R・To×|TcO×・掻出)+
  R/To× 行の入力枠拡大/粒子選択時の詳細表示を2行化(R 表示新設)/粒子サイズ表示を半径準拠に
  固定/グラフ・編集パネルの最小化/タイトルタップでアプリ説明(操作・法則要約を説明タブから
  移動。キーボード・aria 対応)/A/B説明の折り畳み/文字サイズ 小・標準・大(既定=標準 1.15)/
  radiusScale 既定 1.2→1(内蔵は明示値のため挙動不変)
- **設定の変化**: 新規ユーザーの文字サイズ既定 1→1.15(保存済み hp_uiscale は尊重)。
  旧 hp_sizemode は無視(半径準拠固定)
- **QA**: 第25〜29便で +22 項目(beta 先行 — 昇格でルートも有効化: ルート 141→164・beta 163。
  +1 はルートのみの昇格整合 version.sw-sync)。第28便: railOmega 直接回帰・
  粒子数キャップの静的ゲート・QA結果への targetSha256/appVersion/swCache 記録を追加。
  第29便: perf.convection-timescale / perf.rail-indices / perf.trail-cap / perf.draw-radius-cap +
  動的性能ゲート(tests/perf.mjs — CI 独立ジョブ・perf-results.json を証跡化)

## v1.30(2026-07-24)— beta からルート昇格

第19〜23便(ダークローター仮説の実機検証・スピン役割分離の実装→原仮定者裁定による廃止・
観測温度系・ダークローター v3・UI 改善)を beta(`beta/index.html`)で検証のうえルートへ昇格
(原仮定者指示 2026-07-24)。昇格は PWA 一式(index.html/sw.js)+版数の1コミットで実施し、
ルート SW のキャッシュキーは `dfm-release-v1.30`(β版は `dfm-beta-v1.30-b5` へ更新 —
内容はルートと同一)。

- **🕶️「ダークローターの銀河」新設(第19〜22便・v3)** — 高速回転ブラックホールをモデルに
  した「ダークローター」(高速スピンのコア+圧倒的重量の外殻)。中心の巨大ローター(半径
  R=45 明示指定・pinned)とハロー20体が決定フレーム u_φ を実測 ×1.5〜2.0 押し上げ、外縁回転を
  増強する(QA darkrotor.uphi / behavior.darkrotor・12000步安定較正)。第19便の探索実験
  (`tests/exp-darkrotor.mjs`)と3次の外部AIレビュー裁定が背景(docs/dev/REVIEW_2026072*)
- **観測温度系(第22便)** — `single` に obsT(観測温度係数: T_obs=obsT·(1−lightSweep)·T)・
  lightSweep(光掻き出し — 自星の光を外に出さない: 観測温度0・放射冷却も(1−lS)倍)・
  coreOT(コア観測温度比率)を追加。温度表示(色・ヒストグラム・スペクトル・HUD)を
  T_obs 基準へ切替(既定値では従来と bit 等価 — QA obs.equivalence)
- **半径系(第22便)** — `single` に radius(半径の明示指定 — 慣性・接触・引きずり減衰・温度・
  扁平率に一貫使用)・coreRR(コア半径比 Rc/R)を追加。扁平率 f=5s²R³/(4Gm) を粒子選択時に
  HUD 表示。設定に「粒子サイズ表示」(現状/半径準拠)を新設
- **スピン役割分離(sFrame/sTherm/sRep・coreTR)は不採用** — 第20〜21便で beta 実装・検証の
  うえ、第22便の原仮定者裁定によりトイモデル段階では廃止(観測層 obsT/lightSweep へ転換)。
  力学は v1.29 と同一
- **UI(第21・23便)** — 左上タイトルを「DFM Lab」へ短縮(ページタイトルは従来どおり)・
  「表示」各行と「選択粒子の編集」全ラベルにパラメータ名タップで説明表示・数値入力は
  タップで全選択・プルダウンのサンプル名に (kF=N) を自動付記・β版起動通知に版数表示
- QA: ルート/β とも **141 項目**(obs.attrs / obs.equivalence / darkrotor.uphi /
  behavior.darkrotor / preset.darkrotor を含む — 昇格によりルートでも全項目が実行される)

## v1.29(2026-07-23)— beta からルート昇格

第11次・第13次裁定の保留分と第11〜16便(横画面/主星2層/🌍公転版/現実値較正+🌕)を
beta(`beta/index.html`)で検証のうえルートへ昇格(原仮定者指示 2026-07-23・第18次レビュー
条件付きGo)。昇格は PWA 一式(index.html/manifest/sw.js)の1コミットで実施し、ルート SW の
キャッシュキーは `dfm-release-v1.29`(接頭辞 `dfm-release-` — β版は `dfm-beta-v1.29-b1` へ
更新)。ルート manifest の orientation を `any` へ変更(4-35 裁定の反映)。

- **🛰️「土星の近点移動(D68精密較正)」新設** — 🧭 と同一構成で較正係数 C=1.0003023
  (zonal.calib)を与え、D68 の近点移動を観測 38.243°/日 へ精密一致(第11次保留分)
- **🧭「土星の近点移動(現実較正)」を廃止し 🛰️ へ一本化**(第12便・2026-07-23 原仮定者
  指示)— 両者の差は較正係数のみ(0.03%)で画面では区別できないため。関連 QA は存在する
  方の id で実行(本昇格でルートも 🛰️ へ一本化)
- **近点検出器の UI 表示** — E13 中心天体のあるプリセットで、最内粒子の実測 Δϖ/周 と
  解析値を画面左上に併記(engine periDet。QA `zonal.peri-ui` が一致を機械検証)
- **AI 生成プリセットへの zonal 開放** — validatePreset が single の zonal を受理
  (偶数次 J2〜J12・|J|≤0.1・refR/calib クランプ)。SYSTEM_PROMPT に仕様を追記
- **未保存編集の自動ドラフト** — 編集を sessionStorage へ即時保存し、誤リロード後に
  復元を提案(第13次 P1-2 後半。タブ単位・タブを閉じると消える)
- **SW 登録失敗の画面表示**(第13次 P1-1 後半)
- QA 追加: `zonal.order-convergence`(J2k 打ち切りの単調減衰)・`zonal.radial-monotonic`・
  `zonal.dt-convergence`(dt/2 で Δϖ/周 一致)・`zonal.ai-open`・`zonal.d68-preset`・
  `zonal.peri-ui`。結果 JSON に CI run ID を記録(第13次 P2-2 後半)
- **QA 追加(第14次裁定 P2-1/P2-2)**: `draft.restore`/`draft.discard`(自動ドラフトの
  復元・破棄 E2E)・`pwa.sw-fail-ui`(SW 登録失敗の表示)。結果 JSON に
  sha/ref/headRef/baseRef を追加(PR 実行の合成マージ SHA 対策)
- **横画面対応(4-35 裁定)** — landscape かつ幅 900px 以上でキャンバス左・操作列/タブ/
  パネル右の2カラム(CSS grid・DOM 順不変)。manifest `orientation` を `any` へ変更
  (タブレット・PC 想定)。QA `ui.landscape` 新設(1024×768 で2カラム・390px で縦積み復帰)
- **主星2層エンジン(coreM/coreSpin)+🎯「土星の環(主星2層)」新設**(第12便・2026-07-23
  原仮定者指示)— single 粒子にコア質量 mc・コアスピン sc を追加(既定 0=単層と厳密等価・
  既存データと整合)。実効質量 m_eff=m+coreM を慣性・重力・W 源で一貫使用し、コアは半径
  Rc=radiusScale·√|coreM| の第2の引きずり核(A8)。coreM=0 の宇宙は分岐ごと素通り
  (m=0 高速化)。QA `core.twolayer`(2層分割の bit 等価・高速パス・コア効果・負質量)
- **粒子編集の拡張(第12便)** — ①A/B比較中も粒子をタップ選択・編集可能(タップした
  半面 A/B の宇宙を編集。パネルに (A)/(B) を明示 — QA `core.ab-body-edit`)②編集パネルに
  コアの mc/sc 欄を追加(2層情報の編集)③m は負値も受理(±5000。0除算保護つき —
  「もし負の質量があったら」実験の開放)④編集パネルに z-index を付与(狭い横画面でも
  下部UIに隠れず操作可能)
- **試作の廃止(第12便)**: 💍「土星の環(多層)」・🌙「土星の環(共鳴衛星)」は意図と
  異なったため廃止(4-31 実測較正の記録 — 円軌道速度がケプラー値より約6%速いこと・
  この規模では衛星による能動的な間隙維持が働かないこと — はロードマップに保存)
- **🌍 コアの現実値較正+🌕「地球と月(自由二体・物理比)」新設(第16便・第17次外部レビュー)** —
  🌍 のコアを観測値へ: 地球 mc/m=0.325(PREM 32.5%)・月 mc/m=0.0168(月レーザー測距の
  1.59〜1.77% 中央値)・sc/s=1.0 中立既定(現実の内核差動 ≈1.0000008 に対し 1.05 は過大 —
  レビュー指摘)。コア差動項ゼロ化に伴い ω 再実測(0.013908)で月 spin を追随。
  🌕 は重心系・全運動量0・kFrame=0 の純ニュートン閉鎖二体 — spin が軌道に影響しないため
  **現実の自転比 27.4× をそのまま使用**(ChatGPT 第17次の独立計算を実エンジンで追認:
  t=5000〔10.5周〕で半径 179.97〜180.02・重心移動<0.001)。QA `behavior.earthMoonFree` 新設。
  rotationRate/coreRR 等のエンジン分離案は台帳 4-43 へ(Release 前の大改修は回避)
- **🌍「地球と月」を公転版に再設計(第15便・原仮定者指示)** — 月が地球を公転する二体デモへ
  (空洞実験は廃止 — 空洞機構自体はエンジン+QA `core.twolayer` に残置)。質量比は現実の
  0.0123、月は**同期自転**(spin=公転 ω=0.013931 — 潮汐固定がスポークで見える)、**地球に
  2層コア**(mc/m=0.33・sc/s=1.05)。円軌道速度は E6′ 込みの実測較正(1.063×軟化ケプラー値
  — 4周で半径ドリフト0.16%・同期誤差0.31°/周)。地球自転は E6′ 安定域の較正値 0.05
  (現実比 27.32×ω では回転フレーム風が実効重力の約2倍になり月が墜落・自由二体は D0
  リザーバ移譲で重心漂流 — いずれも実測の上で pinned 展示系を採用)。
  QA `behavior.earthMoon` 新設(円軌道±2%・同期誤差≤2°/周・2周走行)・`core.hollow` は廃止
- **コア指定を比率仕様へ(第14便・原仮定者指示)** — coreM/coreSpin(絶対値)を廃止し
  **coreMR=コア質量比 mc/m(既定0)・coreSR=コアスピン比 sc/s(既定1=剛体回転=単層等価)**へ。
  m は総質量のままなので元になる数値を誤解なくそのまま使え(🎯 は m=1500 に復帰)、コアは
  推測値なので比率の方が調整し易い。**mc/m の負値=空洞**(殻に質量集中 — 差動引きずりの
  符号が反転)を許容し、**🌍「地球と月(空洞実験)」新設**(重力では空洞を見分けられないが
  引きずりで見分けられる、というシェル定理類似の思考実験。QA `core.hollow`)。
  環の安定 QA は **t≈360 まで**に短縮(`behavior.saturnExp`/`behavior.saturnLayered` —
  QA 時間短縮の原仮定者指示)
- **コア引きずりを差動形へ修正+🎯 既定値の現実較正(第13便・第15次外部レビュー P1)** —
  旧・加算形は χ=1.05 でも環付近の引きずりを+71〜75%にする説明齟齬があった(ChatGPT 指摘を
  実測追認)。修正後は ω_frame = s·f(R,d) + (coreM/m_eff)·(sc−s)·f(Rc,d) で sc は
  「コアの絶対角速度」、剛体回転(sc=s)は単層と厳密等価(QA の錨)。🎯 は m=1230+
  coreM=270(重元素集中≈18%・Rc/R=42% — Cassini 後の推定に整合)へ変更し、コアスピンが
  反作用を受けない固定内部回転源である旨(統制実験)を説明文・PHYSICS に明示。
  QA `behavior.saturnLayered`(t≈600 の帯保持+sc=0 との帯中央値差の有界性)を追加

## v1.28(2026-07-22)— beta からルート昇格

beta(`beta/index.html`)で先行検証していた以下をルートへ昇格(原仮定者裁定 2026-07-22)。
昇格は PWA 一式(index.html/manifest/sw.js/アイコン3点)の1コミットで実施し、ルート SW の
キャッシュ接頭辞は `dfm-release-`(β版 `dfm-beta-` と分離 — 第13次裁定 P0-2)。
QA `behavior.saturnExp` の安定実走は **t=600 まで**に短縮(原仮定者裁定 — 昇格でルートも
実行対象になるため。t≈1200 の較正実測は裁定記録第12次 §6.1 に保存済み)。

- **E13 帯状重力補正**+🧭「土星の近点移動(現実較正)」— Cassini 実測 J2〜J12、
  D68 で観測と −0.03% 一致(第11次裁定。QA `zonal.*` 4項目)
- **🪐「土星の環(実験)」** — 旧🪐と🧊(氷)を統合しデフォルト化。初期配置を t=300〜600 の
  実測安定分布に再設計、t≈1200 の長時間安定を QA `behavior.saturnExp` で恒久検証
  (第12次裁定+原仮定者調整)
- **PWA**(manifest/Service Worker/アイコン)・**OGP**・**未保存編集バッジ**(P2群)。
  SW のキャッシュ削除・照合を `dfm-beta-*` 名前空間に限定(第13次裁定 P0-2)
- **未保存編集の破棄確認**(プリセット/セーブ読込前)と **β版共有ストレージ警告**
  (第13次裁定 P1-2/P1-3)
- CI を root/beta マトリクス化し、beta 固有 QA(zonal/ice/saturnExp/pwa)を必須実行・
  対象別 artifact 保存(第13次裁定 P0-1)。PWA の HTTP 実動作 QA `pwa.sw-offline` を新設(P1-1)

## v1.27(2026-07-20)— 公開リリース

- GitHub Pages で公開: https://tty-imamura.github.io/dfm-simulator/(タグ `v1.27.0`)
- 公開前レビュー P0-1: ステップ会計を純関数 `stepBudget` に切り出し(実行した分だけ
  繰越から差引・上限クランプ)。QA `time.step-accounting` で直接検証
- QA 109項目 ALL PASS を公開条件として確認

## v1.26(2026-07-20)

- **物理対応ロック Kt=cLight²/G**(決断事項 4-12 承認。第10次レビュー
  ChatGPT P0-2 の後半)— パラメータタブに「物理対応(GR較正)」グループを常設。トグルオンで
  Kt を cLight²/G へ自動維持(Kt は導出値になり cLight/G の編集に追随。Kt の直接編集は導出値へ
  戻す)。オフでは「一般化トイ設定(GR較正条件外)」バッジを常時表示(偶然の条件成立時は成立
  表示)。G=0 等の値域外はクランプ+「対応は近似」表示。ロックは画面全体の UI 設定で、
  プリセット・セーブのスキーマは不変(既定オフ=全既存挙動不変)。E12 の 1/(G·Kt) 再定式化は
  ロック下で等価のため不採用(PHYSICS §5)。QA `physlock.kt-derive` を新設。


## v1.25(2026-07-20)

- **第10次外部AIレビュー(ChatGPT/Gemini/Grok)の裁定 — 説明整合スプリント
  (物理エンジン不変)** — ①README限界節の物質軌道1PNを legacy(非再現)/E12(限定条件下で再現)の
  二層に分離 ②**物理対応条件 Kt=cLight²/G を仕様化**(PHYSICS §5 新設 — Kt=時計・光/cLight=E12/
  G=重力が実装上独立であることを明示。☿・🛰の画面が誇張アナログで条件外である旨を説明文に常設)
  ③銀河の主張を論文v0.2と同水準の**「外縁速度の増強」へ弱化**(平坦化の断定を撤去 —
  README・PHYSICS T4・THEORY C5・アプリ説明/用語集を横断統一。🌌にD₀=調整値の注記)
  ④PHYSICS の見出し・正本宣言を現行化(A1〜A13・E1′〜E12)+§6サンプル表を全プリセットに拡張
  ⑤THEORY の C1 式数現行化・r_flat→r_bg 改名(クロスオーバー尺度)・廃止済み🪣参照の更新
  ⑥QAゲート新設: `docs.no-qa-count`(QA項目数の固定記載検出)+`docs.preset-table-sync`
  (PHYSICS §6 表と内蔵プリセットIDの機械同期)。

## v1.24(2026-07-19)

- **原仮定者指示スプリント(サンプル整理・実測再調整・UI・負荷)** —
  サンプル3種を廃止(🪣マッハのバケツ=箱宇宙 T10 との整合待ち・🕰双子の時計・
  💫引きずられる楕円=☿測地線1PNに統合)、🛰弱場GR較正の説明から水星非再現の旧記述を削除
  (☿E12 が扱う旨へ)、♨️対流のヒーターを床全面へ拡張(右下滞留=循環死の解消を機械掃引で確認)、
  🪐土星の環を安定時の実測分布に合わせた初期3環帯へ再設計+粒子数を適正化(処理負荷減)、
  🌠銀河合体の円盤を回転支持(vMode kepler)に修正(合体前に円盤が核へ崩落する問題の解消)。
  UI: タブパネル高さを「下部UI全体≈画面の約半分」に調整、選択粒子編集の入力枠を拡大
  (ラベル左余白の削減)、負値パラメータ・速度・スピンの入力を改善(iOSでマイナス入力可+
  全角マイナス受理)。負荷: 全対ループの√を対ごとに1回削減(数式同一)、A/B比較の温度正規化
  ソートを間引き+配列再利用。ドキュメントからサンプル総数の固定記載を撤去(QAで機械強制)。

## v1.23(2026-07-19)

- **原仮定者指示3件+描画品質(物理不変)** —
  A/B比較に**「編集対象: A/B」トグル**(パラメータ編集の効く側を切替。表示値も対象側)、
  **表示トグルをA/B両方に常時反映**(決定力マップ・光線・グラフ類・軌跡)、
  **時間倍率を「表示」グループへ移設**(A/B両方に反映)、
  **描画品質 自動/正確/軽量**(DPR・フィールド格子/更新間隔・光線本数・軌跡/温度間引きの
  3段階縮退。自動は fps<45 が3秒続くと1段階低下。正確=常に最高品質でQA・検証は不変)。
  QA 105項目(新規4件)。第5次AI模擬査読の裁定(全21指摘を原則採用 — 論文改稿は次スプリント)。
  MODEL_ROUTING v1.26 追補。

## v1.22(2026-07-19)

- **性能・UXスプリント Phase A(物理不変)** —
  停止中の**描画オンデマンド化**(操作がない限り再描画しない — iPhoneの電池消費を抑制)、
  **光線・決定力マップのキー比較キャッシュ**(源が不変なら再積分・再計算ゼロ。
  💡🔭🛰🌗は実行中もゼロ)、**サンプル別 activeParams**(パラメータタブに
  「このサンプルの主役」+詳細設定折りたたみ。全25種・全キー編集可能のまま)、
  **発散時の「⏪直前の設定に戻して初めから」ボタン**(約2秒毎の2段スナップショットから
  発散前のパラメータを復元)。QA 101項目(新規5件)。MODEL_ROUTING v1.25 追補。

## v1.21(2026-07-19)

- **第9次外部AIレビュー(ChatGPT/Gemini/Grok)の裁定実装** —
  **パラメータ名タップで説明表示**(全21項目 ja/en)、オーバーレイの**スロット配置**
  (🌠mergerで回転曲線と温度グラフが重なる問題を解消)、線の軌跡の**対象限定**
  (overlays.trailTargets:"sampled" — 🌠🌫️は核+代表16粒子のみ記録し描画・メモリ負荷を削減)、
  **光線の空間随伴項に kFrame を適用**(🌗spinlens で kFrame=0 が正しい対照実験になる)、
  ☿mercuryの説明を「式の検証(V18〜V20)」と「可視化用初期値」に区別+内蔵初期値そのものの
  機械検証を追加、⚾projectileの説明修正(斜方投射は下段)、🔥gas・🧊phaseの統制強化
  (G/kFrame=0)、温度正規化ソートの10フレーム化、パネル高50vh。
  QA 96/96 ALL PASS(21キー完全明示への更新+内蔵☿実条件・spinlens制御ほか新規7件)。MODEL_ROUTING v1.24 追補。

## v1.20(2026-07-19)

- **v2.0 第1段階 — E12 測地線1PN**(geoPN/lambdaPN/pnAlpha。
  既定 legacy で既存プリセットの挙動不変、geoPN=1 では物質への E6′ を無効化=二重計上禁止)+
  **☿水星の近日点前進**(計25種)。検証 V18/V19/V20 を verify.all に追加(全量 1.764°/周 vs
  解析 1.762°/周=誤差0.1%、時間率のみ=1/3.06)。QA 86/86 PASS。
  仕様は BOX_UNIVERSE §5・DERIVATIONS §15、MODEL_ROUTING v1.23。

## v1.19(2026-07-18)

- **UI改善12件(原仮定者の指示)** — A/B比較の全面刷新
  (開始で「今の状態」をAとしそのコピーをBに作成/終了でAを継続しBを破棄/
  パラメータ選択・B側の値・おすすめA/Bを廃止し、最後に調整したパラメータの値を両画面に表示)、
  表示に**線の軌跡トグル**追加+A/B比較でも線の軌跡を描画、実効速度=**時間倍率×プルダウン倍率**、
  パラメータ直値入力の値をそのまま表示、一様重力の表記統一(g_x/g_y)、
  セーブ名の初期値=プリセット名+サフィックス、インポートの内容重複判定
  (パラメータ同一はスキップ・名前被りはサフィックス)、保存一覧・生成済みプリセットに
  **JSONコピー**。QA 85/85 PASS。MODEL_ROUTING v1.19 追補。

## v1.18(2026-07-18)

- **第8次外部AIレビュー(ChatGPT/Gemini/Grok)の裁定実装** —
  ⚾自由落下と放物運動・🧪重力分離と浮力の新サンプル(計24種)、🧊phaseへ一様重力導入、
  🌫️collapseに初期回転(円盤形成)、♨️対流の要因分離(G/kFrame/etaRad=0)、
  🔥gasの統制実験化、**bulkVx/Vy**(ring/disk/boxの母集団並進速度 — 🌠mergerの
  「核だけ動き円盤置き去り」を修正)、内蔵プリセットの physics 18キー完全明示
  (QAで機械強制)、セーブタブのバックアップを保存一覧より上に配置。QA 76/76 PASS。

## v1.17(2026-07-18)

- **粒子タップ編集パネル**(m・スピン・速度・固定を即時編集)+
  **一様重力場 gravityX/Y**(Wに寄与しない外部力場。T7恒等式は帳簿で維持)+
  **♨️加熱の対流を復活**(gravityYベースで再設計。機械掃引11構成から採択、48000步の循環持続)+
  **🪐土星の環を3環帯化**(AI生成プリセット参考。残存100%)+UI改善(文字サイズ設定・
  パネル閉じるボタン・光線トグル/PC横長での光線描画修正・回転方向スポーク)+
  バックアップUX(ファイルから読込・ID重複リネーム・インポートの保存一覧登録・
  プルダウンに保存一覧カテゴリ)。QA 68項目。MODEL_ROUTING v1.18 追補。

## v1.16(2026-07-17)

- **🛰️弱場GR較正デモ**(GPS・光偏向・シャピロの現実較正数値を表示、
  地上時計0.920 vs 周回衛星0.971=重力項が運動項に勝つ実GPSの符号構造。QA 3項目追加)+
  **論文図生成パイプライン**(`tools/gen-figures.mjs` — HP フック駆動で `paper/figures/` に
  図6枚を svg/pdf/json で機械生成。V6/V8/V11 等の文書値と一致することをゲートで強制)。
  仕様は HANDOFF_IMPLEMENTATION 付録O、MODEL_ROUTING v1.17。

## v1.15(2026-07-17)

- **第7次提案(ChatGPT/Grok/Gemini レビュー3件)の裁定と整合性修正** —
  APP_VERSION 同期(+QA機械強制)、スライダー範囲を値域と統一(κs/Kt/etaRad — 内蔵値の
  範囲外問題を解消)、AI few-shot 例5 の質量上限超過を修正(m13000→5000・G×mで外場保存、
  対流挙動を24000步で再検証)、**粒子値クランプの警告化**(LLM生成値の無警告書き換えを排除)、
  **外部要素バッジ**(固定拘束・レール・熱浴の自動判定)、**おすすめA/B**(7プリセットに宣言)、
  **🌌平坦化の定量QA**(実プリセット kFrame=1/0 外縁速度比)。裁定表は付録N、
  MODEL_ROUTING v1.15。

## v1.14(2026-07-17)

- **♨️加熱の対流を撤去**(原仮定者の指示。計20種。調整系譜は付録K/M と
  git 履歴に残置)、**右下グラフの位置調整**(下端 8→28px — 選択粒子の情報行〔左下〕との
  重なりを解消)。MODEL_ROUTING v1.14。

## v1.13(2026-07-16)

- **A/B比較モード**(同一初期条件・1パラメータ差の上下同時再生。
  機械検証5項目+視覚確認)、**理論解説パネル**(用語集30項目 ja/en+説明文からの自動抽出。
  「説明文に法則参照を最低1つ」をQAで機械強制)、**サンプルパック5本を内蔵化**(計21種。
  presets/SAMPLE_PACK.json は役目を終え撤去)、**♨️対流の機械再調整**(過熱による壁沿い
  弾道循環=中央占有0〜7%の空洞化を特定 → ヒーター spin12→10 で解消〔13〜17%〕。
  過熱⇔凍結の窓は spin10 のみ — 掃引9構成の記録は付録M)。QA 54/54 PASS。

## v1.12(2026-07-16)

- **Phase 1 CI 基盤** — `tests/qa.mjs`(1コマンド機械QA 42項目・結果JSON)+
  GitHub Actions。**内蔵サンプル+3種**(⏱重力による時計の遅れ〔解析一致1e-5〕・🌈放射冷却
  〔s³則8.0/7.8〕・💥順行・逆行リング〔muF対照0.76/0.00〕— 第6次外部AI提案から機械検証の上
  採用)+**任意サンプルパック5本**(presets/SAMPLE_PACK.json)。インポート4形式自動判別・
  ID重複スキップ・schemaVersion 2・seed正式化・overlays trail/monitor・ファイル保存/共有。
  棄却: 剛体ジョイント系(エンジン外)・回転対流(重ガス凍結の既知知見)。裁定と実測は
  付録L=HANDOFF_IMPLEMENTATION、MODEL_ROUTING v1.12。

## v1.11(2026-07-16)

- **Phase 0 完了** — LICENSE(コード MIT+docs CC BY 4.0、著者
  Tetsuya Imamura)・CITATION.cff・**HANDOFF_PAPER_V2**(自己完結の論文執筆指示書。旧版は
  廃止注記)。方針は教育用トイモデルで確定(DERIVATIONS §14.3)。アプリ: **♨️対流を天井冷却に
  改良**(側壁冷却は粒子の23%が膠着 — 疎な天井冷却板で4%へ。機械比較5構成)、**AI生成の
  表現力拡張**(熱浴・放射冷却・対流・ブラックホール光子捕捉のレシピと few-shot 5例化。
  BH例は5/5光線の捕捉を機械検証。捕捉条件「Kt40〜60・スピン0〜0.5」は掃引で確定 —
  スピン過大は引きずり随伴で光が逃げる)。温度グラフの pinned 除外込みで全回帰 PASS
  (付録K=HANDOFF_IMPLEMENTATION、MODEL_ROUTING v1.11)。

## v1.10(2026-07-16)

- 第5次外部AIレビュー(文書整合監査)の裁定16件(DERIVATIONS §14)—
  旧C8近点式の全面改稿・公理個数の現行化・「完全閉性」主張域の併記(THEORY_SYNTHESIS v1.6 /
  PHYSICS v1.6)。アプリ: **日本語/English 切り替え**(UI・13サンプル説明・グラフまで)、
  **AIプロバイダ柔軟化**(Claude / OpenAI互換 / Gemini+モデルID自由入力。キーは既定で
  端末に保存しない)、**♨️対流を片側ヒーター+冷壁構成に再設計**(旧構成はガス自己重力で
  1塊に凍結 — 軽量粒子化で解消。maxFrac 0.99→0.26)、**🪐土星の環を安定化**(中心星固定+
  環を外側へ。環残存 55%→99.7%・ドリフト 49→0)。verify 11項目・13プリセット・few-shot・
  互換の全回帰 PASS(付録J=HANDOFF_IMPLEMENTATION、MODEL_ROUTING v1.10)。

## v1.9(2026-07-16)

- 第4次外部AIレビュー(熱力学較正)の裁定とサンプル全面刷新 —
  スピン圧 EOS(P∝ρ²T・q=2 の非示量性=L15)、E10′ カーネルの長距離性(L16)、
  U_rep 帳簿の限界(L17)を登録し、V10 の「完全閉性」主張域を kRep=0/固定スピンに限定
  (新検証 V10b: 固定スピン+kRep=2 で K+U_rep 保存、残差 1.6e-4。計11項目 PASS)。
  サンプルは旧16種を廃止し「1サンプル=1効果」の13種へ(新規5種: 🕰双子の時計・
  💫引きずられる楕円〔軌道トレイル描画を追加〕・🌗回る星のレンズ・🎈スピン圧・📏熱伝導)。
  全13種の挙動指標を機械検証(τ/t 解析一致・近点後退 −11.3°/周・非対称湾曲 0.75rad・
  熱コア膨張 3.2倍・伝導の距離依存 2.5倍など。付録I=HANDOFF_IMPLEMENTATION)。

## v1.8(2026-07-16)

- 第3次外部AIレビュー(現実観測による係数較正)の裁定 — 写像 Kt=c²/G を
  GPS 時計(+38.5 μs/日)・太陽縁偏向(1.7512″)・シャピロ遅延で検証(GPS 逆算との差 0.11%)。
  D4a(近点移動)の符号と半径冪を機械実験で訂正: 順行軌道+順行スピンで近点は**後退**
  (= GR のレンズ=サーリングと同符号。旧記述「順行前進」は誤りだった)、近傍の冪は
  a^{-1/2}/周(LT の a^{-3/2}/周 とは異なる)。裁定を恒久化する自動検証 V6 を追加し
  計10項目 PASS。新規 L14(宇宙論的 D₀ ≈ c²/G の規模問題)を登録。物理エンジン不変
  (DERIVATIONS §12)。

## v1.7(2026-07-16)

- **ψ 統一時空則**(原仮定者承認)— A7 を A7′ に改訂し、時計 E7R
  dτ/dt=√(N²−A²|v−u|²/c₀²)(N=e^{−ψ}、A=e^{+ψ}、ψ=W_ext/Kt)と光 E8R n=e^{2ψ} を
  単一ポテンシャルから生成。運動による時間遅れが加わり、kLens を廃止(旧セーブは受理して
  無視)。弱場写像 k_L·Kt=2 が構成的に成立し「時間遅れ:光偏向=1:2」の GR 比が校正なしで
  出る。lensing プリセットを Kt=150 に再校正(湾曲は旧比3倍+光子捕捉が見える)。
  新検証 V12(運動時間遅れ)/V13(重力+運動の合成、自己場除外の検出器兼務)/V16(双子時計、
  不連続なし)を HP.verify.all() に追加し計9項目 PASS。16プリセット・few-shot 4例・
  旧セーブ互換の回帰 PASS(付録H=DERIVATIONS §11)。

## v1.6(2026-07-16)

- 第2次外部AIレビュー3件(ChatGPT/Grok/Gemini)の裁定と、原仮定者指定の
  アナロジー「相対性理論の時間パラドックス=天動説の惑星逆行」の定理化(T9: 時間パラドックスの
  座標化)・A11 追補(優先フレーム=物質共動フレームの創発、A2 との整合)を理論文書に反映
  (DERIVATIONS §10。単一ポテンシャル ψ 統一 E7R/E8R は v1.7 実装候補として確定設計のみ。
  物理エンジン変更なし)。UI修正: 下部タブを閉じた際に描画バッファが更新されず
  グラフィックスが縦に伸びる不具合を修正(閉分岐にも resizeCanvas、ResizeObserver で
  #canvasWrap を監視)。修正前に縦伸び2.10倍を再現→修正後3状態一致、
  全16プリセット+HP.verify.all() 回帰 PASS(付録G=MODEL_ROUTING.md v1.6)。

## v1.5(2026-07-16)

- 整合性修正(付録F=旧 docs/HANDOFF_FIX_V15.md。役目を終えたため
  削除済み — 内容は git 履歴参照、裁定の要約は DERIVATIONS §9)— E1 を Plummer 型
  w=m/√(d²+ε²) に統一し A6(E4=G∇W)が厳密成立(V9。修正前 2.7e-1 → 2.4e-4)。
  接線摩擦のすべり散逸・E10′ 同期散逸を radE に計上し第一法則が完全閉性(A12′、V10。
  修正前 1.9e-1 → 2.5e-4)。T4 の因果を kFrame=0/1 の同一初期条件 A/B 比較で機械検証(V11)。
  三態デモを統制実験化(3群は spin 以外完全同一)、⭐を「原始星への降着」に改名。

## v1.4(2026-07-15)

- サンプルラインナップ改訂 — 選択UIを3グループ化し、
  「塊になると分離しない」欠陥を機械診断で特定して初期値を適切化
  (🌌中心崩壊とデモ窓の塊化を解消しケプラー初期化で回転曲線の平坦化が「見えて起こる」形に /
  🌀惑星の種が複数、軌道上に持続)。❄️雪線サンプル新設(冷たい星の環は小衛星に集積、
  熱い星の環は生まれかけた塊が融けて塊になれない=塊は熱で分かれることの対照実験)。
  回帰25項目込み計42項目 PASS(HANDOFF_IMPLEMENTATION.md 付録E)。

## v1.3(2026-07-15)

- サンプル3種(🎈🛞⭐)を追加。挙動指標(等温自由膨張・ボールミルの
  定常摩擦加熱と伝導の遅延勾配・崩落→閃光加熱→気体圏の形成→冷却釣り合い→真円度)で
  機械判定して安定化、回帰17項目込み計34項目 PASS(HANDOFF_IMPLEMENTATION.md 付録D)。

## v1.2(2026-07-15)

- 熱・放射系サンプル6種(🌈♨️🧊🌠💫💥)+放射スペクトルオーバーレイを追加。
  初期値は挙動指標(選択的冷却・温度成層と対流熱輸送相関・三態の温度序列・冷却収縮・
  降着と摩擦発光・衝突閃光と残光減衰)で機械判定して安定化、回帰10項目込み計35項目 PASS
  (HANDOFF_IMPLEMENTATION.md 付録C)。

## v1.1(2026-07-15)

- 保存則 V1(|ΔP|/P_scale, |ΔL|/L_scale < 10⁻³。実測 ~10⁻⁶)・
  分光温度計 V2・非対称レンズ V8 を `HP.verify.all()` で機械判定、
  D₀>0 のリザーバ勘定込み保存も確認、計12項目 PASS。
