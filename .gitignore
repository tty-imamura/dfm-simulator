node_modules/
# QA成果物は除外するが、提出版の qa-results.json は例外的にコミット可能にする
# (公開前レビュー P0-5: README の提出手順と .gitignore の矛盾解消)
tests/out/*
!tests/out/qa-results.json
!tests/out/qa-results-beta.json
# 第271便e(統括の検証項目 R8): **フル走行(QA_FAST でない走行)の保存物**。従来は短い走行が
#   qa-results.json を上書きするので、公開 tree には fast:true の結果しか残らなかった。
#   tests/qa.mjs は FAST でないときだけこの 2 本を別名で書く(QA_FAST は読み書きしない)。
#   QA `lint.qaFullResultsSaved` が「保存経路が `!FAST` の内側にあること」「この例外行があること」
#   「ファイルがあれば fast:false・件数・failed・target・targetSha256 が整合すること」を機械固定する。
#   **統括がフルゲート後にコミットする**(枝は生成しない)。
!tests/out/qa-results-full.json
!tests/out/qa-results-full-beta.json
!tests/out/perf-results.json
# 第251便b: QA 指紋キャッシュ(固定 seed 指紋 + 前回の判定/detail)。コミットして手元の
#           フルゲートで再利用する(CI は QA_CACHE=0 で従来どおり全実行)
!tests/out/qa-fingerprints.json
# 第249便b: 現実較正 31 本の棚卸し(第41報 W2)の結果正本 — QA `docs.calaudit-sync` が
# 「列挙 id = sampleClass:"calibration" の集合」と「判定語彙が 5 区分」を機械固定するのでコミットする
!tests/out/calaudit-w249.json
# 第271便a(第61報・AF16): 正本 calaudit-w249.json から**移した**重い診断
# (machineIndependence / stageHealth.rows / deprecatedKeys / predictionEligibleReasons)。
# **削ったのではなく分けた**ので、正本と 2 つで 1 組である(正本の `diagnosticsSplit` に
# 相対パスと SHA-256 がある)。QA `lint.calauditDiagSplit` が両方を読むのでコミットする
!tests/out/calaudit-w249-diag.json
# 第259便d: 条件不一致 8 行の **kFrame=0 対照走行**(tests/exp-w259d-kf0.mjs)と、🧶 h8・⚡ h16 の
# 精度検査点(tests/exp-w259d-precision.mjs)の実測正本。QA `behavior.calauditMapping` が
# 「8 行に対照条件の走行があること」「kFrame=1 の再現が棚卸しの記録と一致すること」を機械照合し、
# `docs.calibration-verdict-sync` が台帳 §3/§5 の数値と突き合わせるのでコミットする
!tests/out/kf0-w259d.json
!tests/out/precision-w259d.json
# 第260便d: 🧮 J1757−1854 の h8 検査点(tests/exp-w260d-precision.mjs)の実測正本。
# QA `docs.calibration-verdict-sync` が台帳 §5.4 の 4 段の数値(周期・deg/yr)と突き合わせるので
# コミットする(文書が測定から独立に動かないようにするため)。
!tests/out/precision-w260d.json
# 第261便d: 帳簿の**両分母**(従来 / 活動部分)・E_shell 契約の照合・coupleSink × inStep の 3 行の
# 実測正本(tests/exp-w261d-ledgernorm.mjs)。docs/PHYSICS.md 〔第261便d〕の表がこの値そのものなので
# コミットする(**QA はこのファイルを読まない** —— 読むのは器の中で走らせた値である)
!tests/out/w261d-ledgernorm.json
# 第261便c: 位相制限つき近点抽出器で測り直した ⚡🧮🩺🧶 の段(tests/exp-w261c-precision.mjs)・
# 段ずらし Richardson と門 5 つの機械判定(tests/exp-w261c-richardson.mjs)・収束先に対する
# 較正の試行と感度行列・e 写像(tests/exp-w261c-convcal.mjs)の実測正本。
# QA `docs.calibration-verdict-sync` ⑭⑮ が台帳 §5.6 の数値と、`behavior.calibrationForecast` ② が
# 「門 5 つを通った系は 0 件」と突き合わせるのでコミットする(文書が測定から独立に動かないようにするため)。
!tests/out/precision-w261c.json
!tests/out/richardson-w261c.json
!tests/out/convcal-w261c.json
# 第262便b: コア V2 → 親子コアの移行の実測正本(tests/exp-w262b-migrate.mjs)。
# docs/PHYSICS.md 〔第262便b〕 §2(遠方/近傍の表)・§4(内蔵 121 本の移行レポート)・
# §5(4 つの viewport の見切れ画素)・§6(有限性の拒否)の表がこの値そのものなのでコミットする
# (**QA はこのファイルを読まない** —— 読むのは器の中で走らせた値である)
!tests/out/w262b-migrate.json
# 第262便d: 太陽系 16 本の σ 接続の切断点分解(tests/exp-w262d-solarsigma.mjs)の実測正本。
#   docs/CALIBRATION_VERDICT_v1.44.md §2.3′ と docs/PHYSICS.md 〔第262便d〕④ の表がこの値そのもの
#   なのでコミットする(**エンジンは走らせていない** —— 入力は CSV と calaudit-w249.json だけである)
!tests/out/solarsigma-w262d.json
# 第263便c: 観測レコード(2026-09-14 intake)の会計(tests/exp-w263c-obsintake.mjs)の実測正本。
#   docs/CALIBRATION_VERDICT_v1.44.md §5.8 と docs/PHYSICS.md 〔第263便c〕の表がこの値そのもの
#   なのでコミットする(**エンジンは走らせていない** —— 入力は CSV と既存の結果 JSON だけである)
!tests/out/obsintake-w263c.json
# 第264便d: 観測レコード(2026-09-15 intake・**2 系統**)の照合と不足表 68 組の会計
#   (tests/exp-w264d-intakeA.mjs)の実測正本。QA `docs.intakeA-sync` が CSV の転写行数・
#   σ つき行・候補行・派生行・6 分類の合計をこの JSON と突き合わせ、
#   docs/CALIBRATION_VERDICT_v1.44.md §5.9 と docs/PHYSICS.md 〔第264便d〕の表がこの値そのもの
#   なのでコミットする(**エンジンは 1 步も走らせていない** —— 入力は CSV 正本だけである)
!tests/out/intakeA-w264d.json
# 第266便a: 観測レコード(2026-09-16 intake B・確認記録 + 取得回答 2 系統)の照合と転写の会計
#   (tests/exp-w266a-intakeB.mjs)の実測正本。QA `docs.intakeB-sync` が 3 つの CSV の転写行数・
#   verified へ上げた行数と X7 の 3 欄・訂正 3 件・照合 3 分類・LFBOT が門に繋がっていないことを
#   この JSON と突き合わせ、docs/CALIBRATION_VERDICT_v1.44.md §2.3⁗′ §5.11 と
#   docs/PHYSICS.md 〔第266便a〕の表がこの値そのものなのでコミットする
#   (**エンジンは 1 步も走らせていない** —— 入力は CSV 正本だけである)
!tests/out/intakeB-w266a.json
# 第267便a: 確認記録(2026-09-17・第 2 回)の突き合わせ(tests/exp-w267a-confirm2.mjs)の実測正本。
#   QA `docs.confirm2-sync` が「回答があり**原記載から換算で再現できた**行だけが verified になって
#   いること」「未回答の行の印が 1 bit も動いていないこと」「訂正 3 件の追認印」をこの JSON と
#   突き合わせ、docs/CALIBRATION_VERDICT_v1.44.md §2.3⁗″ §5.12 と docs/PHYSICS.md 〔第267便a〕の
#   表がこの値そのものなのでコミットする(**エンジンは 1 步も走らせていない**)
!tests/out/confirm2-w267a.json
# 第269便b: 確認記録(2026-09-17・第 3 回)の突き合わせ(tests/exp-w269b-confirm3.mjs)の実測正本。
#   QA `docs.confirm3-sync` が「原記載から value と σ が再現できた行だけに印が付いていること」
#   「同印写しは印そのものを動かさないこと」「タイタン環は値と σ を残したまま unverified であること」
#   「写す元が無い行の印が不変であること」「SPARC 10 点を verified にしていないこと」をこの JSON と
#   突き合わせ、docs/CALIBRATION_VERDICT_v1.44.md §2.3⁗⁗ と docs/PHYSICS.md 〔第269便b〕の表が
#   この値そのものなのでコミットする(**エンジンは 1 步も走らせていない**)
!tests/out/confirm3-w269b.json
# 第270便b: 確認記録(2026-09-18・第 4 回)の突き合わせ(tests/exp-w270b-confirm4.mjs)の実測正本。
#   QA `docs.confirm4-sync` が「原記載から value と σ が再現できた行だけに印が付いていること」
#   「X7 の 3 欄を埋めただけの行は印そのものを動かさないこと」「併置行の旧印が note に残っていること」
#   「421 の追認で印も値も動いていないこと」「X7 警告が 0 になったこと」をこの JSON と突き合わせ、
#   docs/CALIBRATION_VERDICT_v1.44.md §2.3⁗⁗′ と docs/PHYSICS.md 〔第270便b〕の表が
#   この値そのものなのでコミットする(**エンジンは 1 步も走らせていない**)
!tests/out/confirm4-w270b.json
# 第270便b(AE2): 観測 CSV の `record_id` 欄の生成と検査(tests/exp-w270b-recordid.mjs)の実測正本。
#   QA `lint.recordId` が「全行に ID・欠損 0・重複 0」「ID が素から作り直せる」「列追加で集計が不変」
#   「宣言の record_id が CSV に 1 件で当たる」「AE15(`;` を値に入れない)」をこの JSON と
#   突き合わせるのでコミットする(**エンジンは 1 步も走らせていない**)
!tests/out/recordid-w270b.json
# 第268便b: 層融合の ζ 合成則の**反例**の実測正本(tests/exp-w268b-zetamerge.mjs)。
#   docs/PHYSICS.md 〔第268便b〕§3 と docs/RELEASE_NOTES_v1.44.md の Negative Claim 42 の表が
#   この値そのものなのでコミットする(**QA `behavior.zetaMergeCounterexample` はこのファイルを読まない**
#   —— 読むのは器の中で走らせた値である)
!tests/out/zetamerge-w268b.json
# 第262便c: 門(4) の独立推定(tests/exp-w262c-independent.mjs —— leapfrog-KDK とエンジン外 RK4 参照器・
# 門 5 つの再判定)・📻 の E12 と標準二体相対 1PN の対照(tests/exp-w262c-e12ref.mjs)・
# ✨🌟 の e 写像(tests/exp-w262c-emap.mjs)・🩺 観測版 v2 の差(tests/exp-w262c-v2delta.mjs)の実測正本。
# QA `behavior.calibrationForecast` ②′ が independent-w262c.json を読んで「独立推定を入れても
# 通った系は 0 件」「門(4) を通った系列は 14 本」を機械固定し、docs/PHYSICS.md 〔第262便c〕と
# docs/CALIBRATION_VERDICT_v1.44.md §5.7 の表がこれらの値そのものなのでコミットする。
!tests/out/independent-w262c.json
!tests/out/e12ref-w262c.json
!tests/out/emap-w262c.json
!tests/out/v2delta-w262c.json
# 第262便a: NS の釣り合い探索の実測正本。
#   balance: 現行経路(geoPN=2)の釣り合い曲線 f(kFrame)(tests/exp-w262a-balance.mjs)
#   geotoy : geoPN=3 の D₀×η×lawVersion 掃引と D₀=0 の直線離脱(tests/exp-w262a-geotoy.mjs)
#   uigeopn: geoPN=3 を UI で選ぶと 2 へ戻る不具合の実機再現と、直ったことの確認(tests/exp-w262a-uigeopn.mjs)
#   docs/PHYSICS.md 〔第262便a〕の表がこの値そのものなのでコミットする
#   (**QA はこの 3 本を読まない** —— 読むのは器の中で走らせた値である)
!tests/out/balance-w262a.json
!tests/out/geotoy-w262a.json
!tests/out/uigeopn-w262a.json
# 第263便a: geoPN=3 × 部分引きずりの実測正本(tests/exp-w263a-dragoverlay.mjs)。
#   bit / ctrl(二重計上の対照 3 走行)/ sweep(30 点の根探索)/ valley(ω̇ の谷)/ dom(支配度の 4 軸)を 1 本にまとめたもの。
#   docs/PHYSICS.md 〔第263便a〕の表がこの値そのものなのでコミットする(**QA はこのファイルを読まない**)
!tests/out/dragoverlay-w263a.json
# 第264便a: NS の (k, f) 共同根と転用試験の実測正本。
#   kjoint  : 4 系の (k*, f*) 2 次元根探索・実測ヤコビアン(tests/exp-w264a-kjoint.mjs)
#   fixed07 : kFrame=0.7・f=1.7 を 4 系へ転用したときの 3 段残差と ω̇ 逆行の切り分け(tests/exp-w264a-fixed07.mjs)
#   predfit : 事前予測式候補 H1/H2/H3 の検定(tests/exp-w264a-predfit.mjs — 走らせずに JSON と CSV を読む)
#   obsdelta: 採用レコードと「判定に使う解」の差・builder 入力の出どころの機械照合(tests/exp-w264a-obsdelta.mjs)
#   docs/PHYSICS.md 〔第264便a〕の表がこの値そのものなのでコミットする(**QA はこれらを読まない**)
!tests/out/kjoint-w264a.json
!tests/out/fixed07-w264a.json
!tests/out/predfit-w264a.json
!tests/out/obsdelta-w264a.json
# 第265便d: LFBOT のトイ仮説の実測正本。
#   lfbot  : 保存則 3 段・本体の走行・対照 4 本・識別性(tests/exp-w265d-lfbot.mjs)
#   srcdecl: 判定に使う出典の宣言(Z10)の会計(tests/exp-w265d-srcdecl.mjs — **エンジンは 1 步も走らせない**)
#   docs/PHYSICS.md 〔第265便d〕と docs/CALIBRATION_VERDICT_v1.44.md §2.3⁗ の表がこの値そのものなので
#   コミットする(**QA はこの 2 本を読まない** —— 読むのは器の中で走らせた値と CSV 正本である)
!tests/out/lfbot-w265d.json
!tests/out/srcdecl-w265d.json
# 第265便a: 共同補正プロトコルの実測正本。
#   basis   : 基準走行(kFrame=1・f=1 と台帳 f)の 3 刻み+判定(tests/exp-w265a-basis.mjs)
#   kjoint2 : **収束先(Richardson 外挿値)に対して取り直した (k*, f*)** と rootCheck・無次元感度(tests/exp-w265a-kjoint2.mjs)
#   analogy : BH 連星 🎻🎐 と星団 🍇🫐 の基準走行・3 刻み・感度(tests/exp-w265a-analogy.mjs)
#   docs/PHYSICS.md 〔第265便a〕と docs/CALIBRATION_VERDICT_v1.44.md §5″ の表がこの値そのものなのでコミットする
#   (**QA はこれらを読まない**)
!tests/out/basis-w265a.json
!tests/out/kjoint2-w265a.json
!tests/out/analogy-w265a.json
# 第268便a: 📡 D68 の 3 段(h/h2/h4・同じ窓・同じ抽出器)の実測正本(tests/exp-w268a-d68.mjs)。
# QA `behavior.unitConvertedFirst` が「3 段が揃っている・見かけの次数が正・h 段が σ 接続器の換算後と
# 一致する」を機械固定し、docs/PHYSICS.md〔第268便a〕§3 の表がこの値そのものなのでコミットする
!tests/out/d68-w268a.json
# 第270便a: **停止条件の宣言**(AE9)の照合結果(tests/exp-w270a-stoprule.mjs)。QA
# `behavior.stopRuleDeclared` が「基点 f6c19b4 の 84 段と步数・近点数が同じ」「各段の步数上限が
# 宣言と t=0 の接触要素だけから再計算できる」「資源上限を超えた段が無い」を機械固定するのでコミットする
!tests/out/stoprule-w270a.json
# 第269便d: 星団 47 Tuc の**比較サンプル v1a**(内部診断)の実測正本(tests/exp-w269d-cluster.mjs)。
# QA `behavior.clusterCenterContract` が「原点基準の 12 値が第265便a の analogy-w265a.json と
# Object.is で同一であること」、`behavior.clusterV1a` が「10 列 × 3 段・NaN 0・monotone:false の温存・
# 47 Tuc の観測量に値の比較が 1 つも無いこと」、`docs.clusterV1bDesign` が「v1b の設計宣言 5 項が
# JSON と PHYSICS の両方にあること」を突き合わせるのでコミットする
!tests/out/cluster-w269d.json
# 第269便c: **比較サンプル v1** の実測正本(BH 連星 tests/exp-w269c-bh90.mjs・銀河 tests/exp-w269c-sparc.mjs)。
# QA `behavior.compareStates` が「全行が 7 語の語彙の中にあり `not-measurable` の行の値がすべて null」、
# `behavior.bh90Intervals` が 90% 区間の非対称の温存・`merged:false` の remnant 行・⏰ の対応未宣言、
# `behavior.sparc10` が契約 6 項・ビン端の事前宣言・打切り超え・p 値の不在をこの 2 本と突き合わせ、
# docs/PHYSICS.md 〔第269便c〕と docs/CALIBRATION_VERDICT_v1.44.md §5.14 の表がこの値そのものなのでコミットする
!tests/out/bh90-w269c.json
!tests/out/sparc-w269c.json
# 第271便d: 銀河の器の診断 3 本の実測正本(tests/exp-w271d-galaxydiag.mjs)。
#   AF5 準定常窓 variant の評価(事前基準は第270便e の宣言のまま・**窓なし**)/
#   AF7 `clampSN` の切り分け(dt/dt2/dt4 の作動率・飽和粒子の点呼・帳簿の動き・kFrame=0 の床)/
#   AF19 N・seed 系列(**engine の粒子総数上限 N_CAP=600** の実測と、上限直下の ×1/×1.5/×1.99)。
#   QA `docs.quasiSteadyVariant`・`docs.clampSNSeparation`・`docs.nSeedSeries` がこの JSON を読んで
#   「基準を緩めていない」「主判定窓 T=40 が不変」「上限 ±40 を変えていない」「SE=SD/√k・空帯は
#   分母から外す」を機械固定するのでコミットする(**主判定の正本は sparc-w269c.json のままである**)
!tests/out/galaxydiag-w271d.json
# 第134便: kF1 較正ハーネスの実測正本(論文3 再現性 — 外部レビュー P0 対応。
# 本文の中心値〔q*=6.16/8.25/21.8・drag −5.6e-10・第122便 q*=4.59〕を提出コミットから再生成可能に)
!tests/out/coreshell-results.json
!tests/out/kf1-results.json
!tests/out/kf1b-results.json
!tests/out/kf1c-results.json
!tests/out/kf1d-results.json
# 第136便: qLock 半径方向監査の実測正本(論文3 Cost 2 の可視化+将来 Fig.4 の生成元データ。
# QA ゲート behavior.qlockRadial が claims の説明文数値と本 JSON の同期を機械照合する)
!tests/out/qlockradial-results.json
# 第138便: 木星ガリレオ衛星 hold-out の実測正本(規則を一切再フィットしない事後外挿テスト。
# QA ゲート behavior.jupiter が claims の説明文数値と本 JSON の同期・事前登録窓 JW1〜JW3 の
# PASS・決定性(ビット同一)・収束を機械照合する)
!tests/out/jupiter-results.json
# 第139便: コア外殻フォロー実験(CW1 q 連続掃引 / CW2 Ω_c 内点極小 / CW3 Rc 用量)の実測正本
#   — 第135便 coreshell-results.json の続きで、事前登録窓と PASS/FAIL 判定を同 JSON が保持する
!tests/out/coreshell2-results.json
# 第152便: コア外殻第3実験(事前登録窓 XW1 しきい値非依存の q₅₀ / XW2 R/r 差分検証 /
#   XW3 経路固定 / XW4 決定性・共有点 bit 一致・kF0×kRep0 対照)の実測正本
!tests/out/coreshell3-results.json
# 第154便: コア外殻第4実験(第152便 XW2 FAIL の再設計 — 事前登録窓 YW1 R/Rc 同率スイープの
#   Δq₅₀ 差分検証 / YW2 エンベロープ半径差分 / YW3 R単独対照と core/base 引きずり比の記述 /
#   YW4 決定性・第152便との共有点 bit 一致・kF0×kRep0 対照)の実測正本
#   — 全7アームを**同一の11点格子**(q=1.0〜2.0・0.1 刻み)で実測しており、第152便の格子非対称を
#   設計で排した版。第152便の coreshell3-results.json は不変のまま並置する
!tests/out/coreshell4-results.json
# 第158便: コア外殻第5実験(第155便の振幅寄与込み予測式の**前向き検証** — 事前登録窓 ZW1 未実測2構成
#   (R,Rc)=(12.5,6.25)・(7.5,3.75) の q₅₀ を実測前に予測して照合 / ZW2 🐚 引きずりプローブを q 2点で
#   計装した記述 / ZW3 決定性・第154便との共有点 bit 一致・kF0×kRep0 対照)の実測正本
#   — 全3アームを**同一の21点格子**(q=1.0〜2.0・0.05 刻み)で実測し、基準アームも同格子で再実測した版。
#   予測ブロックは対象の実測前にこの JSON へ書き出され、正準化 SHA-256 で事後改変が無いことを機械照合する
!tests/out/coreshell5-results.json
# 第155便: コア外殻理論便(保持喪失の条件式 → 振幅寄与込みのアンカー式 → 2項複合の数値解)の解析出力
#   — **解析専用**(シミュレーション再実行なし)。入力は第152便・第154便の実測正本だけで、両者を
#   1 bit も書き換えない。窓は置かない(既発行の XW/YW 窓の付け替え・再判定もしていない)
!tests/out/coreshell-theory-results.json
# 第162便: 木星 hold-out の JUP365 変種(観測ソース依存性 — 事前登録窓 UW1 両ソースの周期差 ≤1% /
#   UW2 既存初期条件の基線再測が fact-sheet ±1% かつ JUP365 ±2% / UW3 JUP365 周期からケプラー逆算した
#   a' の再フィットなし変種が JUP365 ±1% / UW4 hold-out 結論の両ソース一致)の実測正本
!tests/out/jup365-results.json
# 第162便: 木星 hold-out の複数シード(初期位相配置の統計 — 事前登録窓 SW1 全8配置で JW2 相当保持 /
#   SW2 全8配置・全衛星で周期 ±1% / SW3 配置間ばらつきの記述)の実測正本
!tests/out/jupseeds-results.json
# 第163便: コア外殻第6実験(第158便 ZW2 が分離した 🐚 の平均場2項振幅から立つ q₅₀ 予測の**前向き検証**
#   — 事前登録窓 BW1 🐚 env×1/×2 の q₅₀ 予測と掃引実測の照合 / BW2 決定性 / BW3 旧式・1項アンカー式の
#   併記とアンカー ±0.05 摂動の感度)の実測正本
#   — 掃引格子はアームごとに 2項複合予測を中心とする21点(0.05 刻み)で、予測・採点定義・掃引格子は
#   掃引前にこの JSON へ書き出され、正準化 SHA-256 で事後改変が無いことを機械照合する
!tests/out/coreshell6-results.json
# 第164便: q_exact 変種の頑健性(qLock 則の厳密一致式へ q だけ置換した実較正4系の再走行 —
#   事前登録窓 QW1 主窓 / QW2 記述 / QW3 決定性。基線再測が kf1c/kf1d/jupiter の実測正本と
#   ビット一致することを機械証明した版。較正なし fit=[])
!tests/out/qexact-results.json
# 第165便: iso-g 対照(同一ループ利得 g を3経路で構成して amp を比べる — 事前登録窓 IW1 構成可能性 /
#   IW2 主窓(経路間 amp 相対差 ≤10%)/ IW3 記述(数値床宣言)/ IW4 決定性。§F の用量反応グリッドを
#   再走行して obscal-results.json とビット一致することを機械証明した版)
!tests/out/isog-results.json
# 第167便: 論文2 プローブ依存比 H_w/H_geo の感度分析(外部レビュー第4巡 P1-5)の実測正本
#   — 事前登録窓 PW1 正準構成が committed figures/p2fig6.json を bit 一致で再現 / PW2 5係数×3観測窓×
#   2ビニング=30構成の比を記述収載 / PW3 全構成で |比−1| > 数値床(プローブ依存は消えない)/
#   PW4 決定性。論文2 第V節C の小表・本文数値はこの JSON からの機械転記で、QA ゲート
#   p2sens.papersync が dfm-paper2.tex / dfm-paper2-ja.tex を読んで全項目をその場照合する
!tests/out/p2sens-results.json
# 第168便: コア外殻第7実験(🐚 q₅₀ 掃引の 0.025 刻み精細化と精細アンカーによる 2項複合予測の再照合
#   — 事前登録窓 EW1 格子整合 / EW2 主窓 / EW3 決定性。掃引格子はアームごとに coreshell6 実測 q₅₀ を
#   中心とする21点(0.025 刻み)で、格子・採点定義は掃引前に、予測は env×2 掃引前に SHA 封印する。
#   **盲検の前向き検証ではなく分解能向上フォロー**である旨を preRegistered.honestFraming に明記)
!tests/out/coreshell7-results.json
# 第169便: kf1 系3主張(🪨 水星近点・🌘 地球月2量・💿 土星環近点ドリフト)の dt 収束・測定窓感度の
#   実測正本(事前登録窓 KW1 転記照合(基線再測が kf1c/kf1d とビット一致)/ KW2 記述(dt×{0.5,2}・
#   窓×{0.5,2}・数値床併記)/ KW3 決定性)。error budget v2 の dtConvergence / windowSensitivity
#   成分の供給元 — tools/gen-error-budget.mjs が SRC_FILES から機械読取するのでコミット対象にする
!tests/out/kf1sens-results.json
# 第174便: コア外殻第8実験(🐚 平均場2項振幅の 4点最小二乗による解き直し — 事前登録窓 FW1 主窓
#   (4点振幅+精細アンカーの2項複合予測 vs coreshell7 env×2 実測 |差| ≤ 0.10)/ FW2 整合(部分集合
#   {1.5,2.0} の振幅が第158便 ZW2 収載値と bit 一致)/ FW3 決定性。**hold-out は存在せず新規掃引なし**
#   である旨を preRegistered.honestFraming に明記)
!tests/out/coreshell8-results.json
# 第179便: コア外殻第9実験(🐚 残差の構造対処 — t=0+ 解析値 vs 窓平均引きずりの系統。採点窓と同一の
#   3000步走行中に Ω_drag を時間分解記録し窓平均から振幅を解き直す。事前登録窓 GW1 記述(時間プロファイル
#   と t0/窓平均比)/ GW2 主窓(|予測−1.5300| ≤ 0.05 かつ 第174便 0.040928 より改善)/ XCHECK 転記照合 /
#   GW3 決定性。GW2 は窓平均 Ω̄ の符号反転により A_c<0 となり予測が立たず INCONCLUSIVE で収載)
!tests/out/coreshell9-results.json
# 第180便: q_exact 世代「採用直値」regime の実測正本(プリセット・ハーネスが実際に載せている
#   4桁丸め直値 6.1471 / 8.2358 / 20.4932 / 12.0586 での実較正4系の走行 — 事前登録窓 RW1 主窓
#   (既存窓の流用)/ RW2 直値同定(ROUND4(q_exact) と対象 HTML のプリセット宣言値の照合・
#   走行前ゲート)/ RW3 基線 bit 照合(kF0・q* 走行・全桁 q_exact 走行が kf1c/kf1d/jupiter/qexact
#   の実測正本とビット一致)/ RW4 記述(4桁丸めに由来する系統誤差)/ RW5 否定対照(採用直値を
#   +1% ずらすと直値同定が外れ観測量も動く)/ RW6 決定性)。error budget v3 の regime 層
#   (qExact 世代)の供給元 — tools/gen-error-budget.mjs が SRC_FILES から機械読取するので
#   コミット対象にする
!tests/out/qexact-regime-results.json
# 第166便: 誤差予算(error budget)の機械集計台帳 — 較正・ホールドアウト主張ごとの
#   {claim・出典 JSON の SHA-256・主観測量・事前登録窓・不確かさ成分(数値床/dt 収束/窓感度/
#   ソース差/シード散らばり)} を上の白リスト済み結果 JSON **からのみ**集計した出力
#   (tools/gen-error-budget.mjs・新規シミュレーションなし・手書き数値ゼロ)。
#   QA ゲート errorbudget.presence / errorbudget.consistency が、スキーマ・5主張の全行・
#   再集計一致・出典 SHA-256 の現物照合を機械検査するため、コミット対象にする
!tests/out/error-budget.json
# 第19便: ダークローター探索実験の実測生データ(裁定記録 REVIEW_20260723_DARK_ROTOR_* が参照)
!tests/out/darkrotor-results.json
!tests/out/exp-4-67.json
!tests/out/exp-4-68c.json
!tests/out/exp-4-72.json
!tests/out/exp-4-73.json
!tests/out/exp-4-74.json
# 第50便(台帳4-66): 判別実験の実測記録(論文2 fig6 の元データを兼ねる)
!tests/out/exp-4-66-results.json
# 第68便 P4a: v−u 統一則の検証実測(DERIVATIONS §18.3 が参照)
!tests/out/p4a-results.json
# 第69便: P4b(geoPN=2 実装)の V ゲート実測+🎡リトマス初実測(DERIVATIONS §18.6 が参照)
!tests/out/p4b-results.json
# 第70便: 💫galaxyGeo2 較正・減光力学不変・隠れ質量・ディスク/バルジ温度の実測(CHANGELOG v1.36-b7 が参照)
!tests/out/exp-4-70.json
# 第71便: 温度選別の機構分解+第70便アーチファクト訂正の実測(CHANGELOG v1.36-b8 が参照)
!tests/out/exp-4-71.json
# 第111便: 観測較正ハーネスの実測記録(CHANGELOG v1.40-b1 第111便A が参照)
!tests/out/obscal-results.json
# 第32便 W4(台帳4-58): QAスクリーンショットはCIアーティファクトのみ・コミット対象外
tests/out/*.png
# 公開前レビュー P0-5: 依存を固定して QA の再現性を担保(CI は npm ci)
# (旧方針「QA専用依存のためロックしない」は v1.27 で撤回)
# エディタ・OS・内部開発資産(公開リポジトリに含めない)
.claude/
.DS_Store
*.swp
.vscode/
.idea/
.env*
# LaTeX ビルド生成物(Notes.bib は revtex4-2 の自動生成)
paper/*.aux
paper/*.log
paper/*.out
paper/*.pdf
paper/*Notes.bib
# 第270便d: 親子コアの**独立 Q**(AD1・法則 (a))と LFBOT の **ν̄ 受理前上限帳簿**(AD2)・
#           `collapseR` の対照(AA11)の実測正本。
#   zetaq : 反例の保存・総当たり 2025 組・読戻し 3 経路・6 経路の更新則・**融合が起きる内蔵の洗い出し**
#           (tests/exp-w270d-zetaq.mjs)
#   numax : 受理前上限の表・恒等式 3 段・対照④・N_γ=0 の未定義・collapseR の対照
#           (tests/exp-w270d-numax.mjs)
#   docs/PHYSICS.md〔第270便d〕§2/§3/§4/§5 の表がこの値そのものなのでコミットする
!tests/out/zetaq-w270d.json
!tests/out/numax-w270d.json
# 第271便c(統括の検証項目 R2/R4/R5・AF17): 独立 Q の**書込経路の境界**の実測正本。
#   qsplit: J の帯で切らない 7 経路・非有限の原子的拒否・`Q:null`/明示 0 の三分・
#           (10,5)→J=0→J=10 の往復・`dfmCoreQ` の omega と sourceRate・2025 組の ζ_eff 分類
#           (tests/exp-w271c-qsplit.mjs — **同じ器が基点 html でも走る**ので旧列も実測である)
#   docs/PHYSICS.md〔第271便c〕§1〜§5 の表がこの値そのものなのでコミットする
!tests/out/qsplit-w271c.json
# 第270便c(AD9)/ 第271便e(統括の検証項目 R8): PSR J1946+2052 の**採用レコードを一組へ**揃えた
#   ときの専用再走(tests/exp-w270c-j1946adopt.mjs)の実測正本。転写の自己点検・採用点検・
#   🩺🪀🩹 の 4 段(h=0.016/1,2,4,8・20 近点窓)・共同根の 1 次元探索・presetSig を 1 本に持つ。
#   **第270便c の時点では tests/out/* に飲まれて公開されていなかった**ので例外行を足した。
#   QA `docs.j1946adoptPublished` が「meta.targetSha256 が現行 beta/index.html と一致すること」
#   「採用入力が CSV の DDFWHE 行そのものであること」「刻み・窓・段・共同根の探索条件が入っていること」
#   「宣言リテラルが採用レコードの転写から相対差 0 で戻ること」を突き合わせるのでコミットする。
!tests/out/j1946adopt-w270c.json
# 第272便a(第62報・原仮定者の仮説(第62報)「現実較正サンプルで起きている問題を一覧で整理」):
#   問題一覧の機械可読版(tests/exp-w272a-issues.mjs が正本 JSON から生成)。
#   docs/CALIBRATION_ISSUES_v1.45.md の件数と一致することを QA `docs.issuesSync` が機械固定するので
#   コミットする(**数えただけ**であって、直した記録ではない)。
!tests/out/issues-w272a.json
# 第272便b(第62報「カロン」): ❄️ 冥王星–カロンの**対照系列 C0〜C7/S(診断のみ)**の実測正本
#   (tests/exp-w272b-charon.mjs)。kFrame<1・geoPN=3・f≠1 は**器の中の診断コピーだけ**で、
#   内蔵 124 本は 1 bit も変えていない。列 × 段(h / h/2 / h/4)・走行前に固定した契約
#   (近点窓 20・ORB_MAX 60・判定は 2 周目・h 段 20,700,000 步)・observation は calaudit 正本からの引き。
#   QA `docs.charonSeries` が「meta.targetSha256 が現行 beta/index.html と一致」「9 系列 38 列」
#   「NaN 0」「C0 が棚卸し正本の同方向 1 周を再現」を突き合わせるのでコミットする
!tests/out/charon-w272b.json
# 第272便e(AG18): **訂正履歴の台帳**(paper/data/corrections.json)と観測 CSV の突き合わせ
#   (tests/exp-w272e-corrections.mjs)。台帳の record_id が CSV に在るか・各 revision の
#   `current` が現行値と一致するか・訂正の印を持つ行が 1 行残らず台帳に載っているかの実測。
#   QA `docs.correctionsLedger` がこの JSON の件数と違反 0 を突き合わせるのでコミットする。
!tests/out/corrections-w272e.json
# 第272便c(第62報「連星と引きずり」・**診断のみ・既定不変**): 3 本の実測正本。
#   ① nslock —— **潮汐ロック枝**(kFrame=0・f=1・λ_PN=1)の診断コピーを NS 4 系+恒星 2 系 ×
#      6 列 × 3 段(h/h2/h4・20 近点窓)で測った走行。**旧共同根(第265便a)の列も並べてある**。
#   ② twobody —— 2 天体思考実験のメッシュ応答(径方向/接線方向の変位に対する拡縮 H・回転 Ω・
#      伸縮テンソル)と、変位を初期条件として記帳した帳簿(全運動量・全角運動量・自転)。
#   ③ rpar —— AA7 の静止 2 体の相対加速度比 R∥ を、実装(HP.dfmMeshV2Solve)と
#      独立に導いた解析式の 2 列で並べた走行。
#   QA `docs.nsLockBranch` / `docs.twoBodyMesh` / `docs.meshV2Rpar` がこの 3 本を読み、
#   入力 hash(対象 html・観測 CSV)と契約(3 段・診断コピー・**内蔵 kFrame は {0,1} のまま**・
#   旧共同根の列を消していない・2D では面外の自転軸を持てない)を機械固定するのでコミットする。
#   **σ 倍は記録であって合否ではない。**
!tests/out/nslock-w272c.json
!tests/out/twobody-w272c.json
!tests/out/rpar-w272c.json
# 第273便b(統括の検証項目 R21・**診断のみ・既定不変**): ❄️ カロン C3 の**符号反転区間の探索**
#   (tests/exp-w273b-charonk.mjs)。**これは fit である** —— 零点は観測周期そのものに合わせて
#   挟み込んだ 1 自由パラメータで、「解」でも「最良値」でもない(meta.fit:true が刻んである)。
#   QA `docs.charonKScan` が fit の刻印・3 段・NaN 0・併合鍵・softening 宣言(AH20)・
#   宣言鍵の同値性(AH1)を機械固定するのでコミットする。**判定はしていない。**
#   ※ `charonk-w273b-probe.json` / `charon-w272b-probe.json`(短い走行)は**正本ではない**ので
#      除外のまま(器が正本と別ファイルへ落とす)。
!tests/out/charonk-w273b.json
# 第273便c(第63報・AH24/AG24): 2 本の正本。
#   ① nslockledger —— **NS 連星 4 系の lock 枝(kFrame=0・f=1・λ_PN=1)を台帳の 1 行にした記録**
#      (`tests/exp-w273c-nslockledger.mjs` が `nslock-w272c.json` から組む。ω̇/ω̇_obs・質量枝・
#      λ_PN・pnAlpha・採用段を併記する)。QA `docs.nsLockLedgerRow` が台帳 §5.19 と突き合わせる。
#      **記録であって判定ではない**(内蔵の値は 1 つも変えていない・係数 6 は導入しない)。
#   ② assessed —— **正式判定値の side table の来歴**(`tests/exp-w273c-assessedtable.mjs` が
#      判定器の正本から beta の表を機械生成したときの控え)。QA `ui.assessedValuePanel` が
#      ページの表と判定器の正本を直接突き合わせるので、この控えは**何を転記したかの記録**である。
!tests/out/nslockledger-w273c.json
!tests/out/assessed-w273c.json
# 第274便b(第64報・因果の向きの裁定・**診断のみ・既定不変**): 独立同期トルクの node 診断
#   (tests/exp-w274b-sync.mjs)。**k を外から固定し非同期から出発する**試験の実測正本で、
#   QA `docs.syncCausal` が meta の 3 規則(k を外から固定 / 非同期から出発 / Γ・K を kFrame で
#   乗じない)と R28 の 4 列判定を読み、`lint.provenanceMeta` が来歴を照合するのでコミットする。
#   **エンジンへは 1 バイトも接続していない**(内蔵プリセットは 1 bit も変えていない)。
!tests/out/sync-w274b.json
# 第274便e(第64報「DFM 版ブラックホールを設計する」・**設計と純関数だけ・エンジン未接続**):
#   ① bhcore —— 🎐 gw150914(実単位)の**宣言値**(m・分離・c₀・κ・D₀・ε)で、有限コア Φ_c・
#      局所光速 c_eff=c₀e^{−2κW}・**光の捕捉境界の定義候補** ṙ=u_r+c_eff の根・逃げにくさ・
#      合体帳簿(M/P/J/E の閉じと**残余が負なら拒否**)を測った値表
#      (tests/exp-w274e-bhcore.mjs)。**捕捉境界は GR の事象の地平面との同等性を示していない。**
#   ② armbar —— パターン腕 U_arm の力(A′ 項も ∂χ/∂r 項も落とさない)・横幅 σ_⊥ の小振幅収束・
#      棒(6 節点鎖)の Hessian の**ゼロモード 5 本と最小非ゼロ固有値**・観測写像(視線積分・
#      σ_LOS・AE5 の √2)の値表(tests/exp-w274e-armbar.mjs)。**「腕の創発」は主張していない。**
#   QA `docs.bhDesignSync` が設計書 docs/BH_DESIGN_v1.45.md と純関数の API を集合照合し、
#   `behavior.armBarPure` が上の量を純関数で再計算するのでコミットする(値表は記録であって判定ではない)。
!tests/out/bhcore-w274e.json
!tests/out/armbar-w274e.json
# 第274便c(第64報): 3 本の正本。
#   ① galaxyprof —— **🪁 の 1 步の負荷分解**(実ブラウザ Chromium・ms/步)。docs/PHYSICS.md〔第274便c〕① の
#      表がこの値そのものなので、来歴つきでコミットする。**inputs は現行 html だけ**で、比較した基点 html の
#      刻印は `meta.comparedWith` に残す(基点 html は走行後に消す一時ファイルである)。
#   ② needmesh —— **`dfmField` の API 同値**(源 16 種 × 選択肢 35 種 × 4 点 = 2100 例)と
#      `need:"mesh"` の契約。**「1 例でも違えば入れない」という同値最適化の根拠**なのでコミットする。
#   ③ galaxylite —— 🪁 既定 / 🎋 軽量コピー / 帯平均 variant の 3 者比較(3000 步・保持率・V_rms・ms/步)。
#      docs/PHYSICS.md〔第274便c〕④ の表がこの値そのものである。
#   QA `lint.provenanceMeta` の CANON に 3 本を足した。**判定はしていない**(数を出す器である)。
!tests/out/galaxyprof-w274c.json
!tests/out/needmesh-w274c.json
!tests/out/galaxylite-w274c.json
# 第274便d(第64報「実在天体に先立ち安定サンプルを用意する」・**診断のみ・既定不変**):
#   形状トイ(`physics.shapeToy` —— 指定した 3D 正規分布を定常分布に持つ**参照モデル**)の
#   完成判定の正本(tests/exp-w274d-shapetoy.mjs)。参照形状(定常共分散・任意視線の投影・
#   帳簿)/ 安定成長(成長残差と成長後ドリフトを分けた列+無成長対照)/ 復元性(5%・10%・
#   3 seed・N 3 段)/ 厳密離散化(F=e^{Bh} と級数の行列指数の差・恒等式 C=F C Fᵀ+Q_h)/
#   coupling 2 案 / 重力の対照(G を変えても指紋が同じ)を持つ。
#   QA `docs.shapeToyCriteria` がこの JSON と docs/PHYSICS.md〔第274便d〕の表を突き合わせ、
#   `lint.provenanceMeta` が来歴を固定するのでコミットする。**較正ではない**(観測量は 1 つも
#   入力していない)。「銀河が安定した」「腕が創発した」とは書かない。
!tests/out/shapetoy-w274d.json
# 第274便a(第64報・原仮定者の優先課題(2)): **kF0 棚卸し表の正本**。較正 37 本 + 診断系列を行にし、
#   kF1 側と kF0 側それぞれに 4 列(走行成立/構造安定/数値成立/観測成立 —— R28)を置く。
#   `tests/exp-w274a-kf0ledger.mjs` が正本 JSON から機械生成し、`docs/KF0_LEDGER_v1.45.md` を書く。
#   QA `docs.kf0Ledger` が表と正本を突き合わせ、`lint.provenanceMeta` が来歴を照合するのでコミットする。
#   **未測定は未測定と書く**(推定で埋めない)。ラベルは**候補**であって較正完了ではない。
!tests/out/kf0ledger-w274a.json
# 第275便a(第65報・判定器便): 3 本の正本。
#   ① presetaxes —— **対照の軸**(内蔵 128 本の宣言から k を除く 5 成分〔f・D₀・q・frameWeight・
#      bodies の SHA-256〕を抜いたもの。`tests/exp-w275a-presetaxes.mjs`)。**「f 固定・k のみの対照」の
#      定義そのもの**で、第274便a が f だけを見ていた誤分類(統括の検証項目 R33)を直すために要る。
#   ② kf0ledger-w275a —— kF0 棚卸し表の**改版**(列を 5 本にし、門を通っていない `|nσ|≤3` を
#      「観測成立」から「外挿残差」へ分けた)。`docs/KF0_LEDGER_v1.45.md` を機械生成する正本。
#   ③ kfgate —— **kFrame 二値の既定契約**(第65報 (1))の案A(拒否)/案B(丸めて警告)を
#      同じ html で切り替えて測った実測(`tests/exp-w275a-kfgate.mjs`)。
#   QA `docs.kf0Ledger` が ② と文書を突き合わせ、`lint.provenanceMeta` が 3 本の来歴を照合するので
#   コミットする。**判定は 1 つも動かしていない**(4 値・門・5 区分は第274便a の正本のままである)。
!tests/out/presetaxes-w275a.json
!tests/out/kf0ledger-w275a.json
!tests/out/kfgate-w275a.json
# 第275便c(第65報 (3)「粒子同士は総当たりで重力計算、座標変換が重なっても総当たり回数は同じはず →
#   なぜ重いか分析して改善」): **回数の会計と前後の ms/步**の正本(tests/exp-w275c-galaxyprof2.mjs)。
#   ① 回数 —— Math.sqrt/pow/hypot と Array.prototype.push を差し替えて**実際に呼ばれた回数**を数え、
#      `_core` だけの走行(geoPN=0)を引いてトイの分を出す。frameWeight を share/pull/pull3/pull4 に
#      振っても **(行,源) の累算回数が 1 つも動かない**ことがこの JSON の push 欄で読める
#      (= 原仮定者の「座標変換が重なっても総当たり回数は同じはず」の検算)。
#   ② 時間 —— 同じページで `HP.setGeoPrepEnabled(true/false)` を切り替えた A/B(Chromium)と、
#      同じ 1 步の純関数の写し(tests/lib-w275c-prepared.mjs)による node の ms/步。
#   ③ 步をまたぐ幾何キャッシュ —— **採らない**ことの根拠(指紋が変わる)を数で残す欄。
#   docs/PHYSICS.md〔第275便c〕の表がこの値そのものなので、来歴つきでコミットする。
#   QA `lint.provenanceMeta` の CANON に足した。**判定はしていない**(数を出す器である)。
!tests/out/galaxyprof2-w275c.json
# 第275便e(第65報 (8)「パワーボール効果(仮説)」・**エンジン未接続の node 模型**):
#   自転ベクトル s・向きのポテンシャル U=−½K(ŝ·r̂)²・散逸結合 Q の閉じた模型を 5 段
#   (無トルク → 向きの整列 → 散逸 → 供給源の帳簿 → 閉じた連星)で走らせた値表
#   (tests/exp-w275e-powerball.mjs)。**負の対照**(軸に垂直なジャイロトルクは τ·ŝ=0 で
#   自転を加速しない —— 統括の検証項目 R37)と、仮説 H1〜H7 の対応づけ(Failure First)を持つ。
#   QA `docs.powerballLedger` がこの JSON と docs/PHYSICS.md〔第275便e〕の表を突き合わせ、
#   `lint.provenanceMeta` が来歴を固定するのでコミットする。K・Q・Γ は**宣言された自由
#   パラメータ**であって観測から同定した値ではない。「合体を再現した」とは書かない。
!tests/out/powerball-w275e.json
# 第276便c(原仮定者の裁定(第66報)(3)「パワーボール」・**エンジン未接続の node 模型**):
#   ① 軸へなされた仕事を口座 bank に貯め、要求と η で**有限量だけ**自転へ移す移送チャネル
#      (反作用ローターへ −δ・残りは熱。**入力 0 なら加速しない・口座が尽きれば止まる**)、
#   ② **熱非負の受動散逸** τ_d=−γP_s g・Q̇=γ|P_s g|²≥0(第275便e ⑧ の否定結果の直し。
#      **新しい γ は旧 Q と別の係数**)と、宣言した **最小 L の停止条件**
#      (**捕捉・地平面・合体の判定には使わない**)、
#   ③ **歳差ロックの位相** ψ と共役運動量(U_lock=−K_lock cosψ・両側へ反作用)
#   の実測値表(tests/exp-w276c-powerball2.mjs)。QA `docs.powerballLedger` がこの JSON と
#   docs/PHYSICS.md〔第276便c〕の表を突き合わせ、`behavior.axisWorkLedger` が純関数の閉じを
#   その場で走らせ、`lint.provenanceMeta` が来歴を固定するのでコミットする。
#   η・γ・K_lock は**宣言された自由パラメータ**であって観測から同定した値ではない。
#   **「パワーボールの機構を証明した」「合体を再現した」とは書かない。**
!tests/out/powerball2-w276c.json
# 第275便b(第65報 (2)「D₀ は『何を背景とするか』でサンプル毎に変わる」): **D₀ の単位監査と
#   規則適用表**の正本(tests/exp-w275b-d0audit.mjs)。較正 37 本 + 🪁🎋 について
#   scaleExp から D₀ の SI 値を作り、規則で計算した D_bg(太陽: M☉/r・銀河: M_enc(R₀)/R₀)と
#   比 D₀/D_bg を置く。仮定(質量・距離・銀河の参考値)は出典つきで JSON の中に全部並べてある。
#   源分割不変性(太陽を置く/畳む)と 🌇 の短窓走行(kF0/kF1 × D₀ 4 点)も同じ JSON に入る。
#   QA `docs.d0audit-sync` が docs/PHYSICS.md の表と突き合わせ、`lint.provenanceMeta` が
#   来歴を照合するのでコミットする。**較正ではない**(D₀ の値は 1 本も変えていない)。
!tests/out/d0audit-w275b.json
# 第275便b: **複素決定力場の D₀ 非依存版**(tests/exp-w275b-meshnod0.mjs)。🪁 の t=0 配置で
#   旧経路(χ=W/(D₀+W))と新 lawVersion `complex-nod0` の 2 案(自己規格化 / 配置スケール)を
#   同じ評価点で比べ、**単位系を取り替えたときの χ の変化**を測った正本。**エンジン未接続**。
!tests/out/meshnod0-w275b.json
# 第275便b: ❄️ カロンの **D₀ 系列**(tests/exp-w272b-charon.mjs --D0 rule)。裁定「D₀ の補正は
#   冥王星とカロンで検証する」の走行で、kF{0,1} × D₀{宣言値・太陽からの距離・銀河内位置・1} を
#   h/h2/h4 の 3 段で測る。**第272便b の正本 charon-w272b.json は 1 バイトも触らない**。
#   **「D₀ 補正で ❄️ が成立」とは書かない**。
!tests/out/charond0-w275b.json
# 第275便d(第65報 (4)(5)(6)「安定サンプルの改善と中心天体版」・**診断のみ・既定不変**):
#   ① 形状トイの**安定・低分散・銀河らしさの判定**の正本(tests/exp-w275d-shapecrit.mjs)。
#      内蔵 6 本(🔮🥏🧵 と中心天体版 🎱📀🧹)の RMS 半径の偏り/ドリフト/表示窓の凹み・
#      逆行の割合・σ_z/σ_R・棒の幅と半長・回転曲線の形・摂動からの回復・重力の対照・
#      **中心天体の質量の対照**(m を変えても指紋が同じ)と、既定値を決めた**候補の比較**を持つ。
#      QA `docs.shapeToyCriteria` がこの JSON と docs/PHYSICS.md〔第275便d〕を突き合わせ、
#      `lint.provenanceMeta` が来歴を固定するのでコミットする。
#   ② **動的中心の診断**の正本(tests/exp-w275d-dyncenter.mjs —— **エンジン未接続**の node 模型)。
#      中心質量 × 中心の扱い(自由/位置固定)の格子で、重心ドリフト・ビリアル比・RMS 半径・
#      **中心天体自身の変位**を置く。「位置固定の参照点」がどこから正当化できるかの数である。
#   **どちらも較正ではない**(観測量を 1 つも入力していない)。「銀河が安定した」「腕が創発した」
#   「中心ブラックホールを実装した」とは書かない。
!tests/out/shapecrit-w275d.json
!tests/out/dyncenter-w275d.json
# 第276便a(第66報 (1)「背景決定力 D₀ と別途 背景複素決定力を用意する」): **宣言専用の背景**の 3 本。
#   ① tests/out/bgfield-w276a.json … 背景複素決定力 (W₀,A₀,∇W₀,∇A₀,∂ₜW₀,∂ₜA₀) の純関数
#      (tests/exp-w276a-bgfield.mjs)。源分割の同値・空間/時間微分の中心差分検算・不完全な背景の
#      拒否・旧経路 χ=W/(D₀+W) との一致(p=2 でビット同一)を置く。**エンジン未接続**。
#   ② tests/out/d0sites-w276a.json … D₀ の読み口の**機械監査**(tests/exp-w276a-d0audit2.mjs)。
#      識別子の出現を機械抽出し、用途と「W₀ へ置き換えてよいか」だけを宣言表で与える。
#      **重力・時計・光は置換禁止**。R39(q は D₀ を読まない)の実測も同じ JSON に入る。
#   ③ tests/out/bgpredict-w276a.json … 背景の**事前予測表**(tests/exp-w276a-bgpredict.mjs)。
#      シチュエーションごとに D₀・W₀・A₀ と微分を宣言した質量分布から計算し、内蔵 37 本の
#      単位系へ落とす。**概算条件からの計算であって特定時刻の環境値ではない**。
#   QA `docs.d0sites-sync`・`docs.bgpredict-sync` が docs/PHYSICS.md〔第276便a〕と突き合わせ、
#   `lint.provenanceMeta` が来歴を固定するのでコミットする。**較正ではない**(値は 1 本も変えていない)。
!tests/out/bgfield-w276a.json
!tests/out/d0sites-w276a.json
!tests/out/bgpredict-w276a.json
# 第276便e(第66報 (4) 後半「渦巻銀河に組み合わせ、棒渦巻銀河に発展させる」): 渦巻・棒の node 試作。
#   R44 のハミルトニアン(同じ速度場 u を読むラグランジアン)を純関数へ写し、
#   (i) 直線腕の横方向安定 → (ii) 親コアの円盤メッシュ → (iii) パターン腕(位相 φ_c と共役運動量 J_c)
#   → (iv) 棒の結合グラフ、の順に**測る前に宣言した完成門**へかけた値表である。
#   **エンジン未接続**(beta/index.html はこの lib を 1 度も読まない)。
#   「腕が創発した」「棒が自発形成した」「渦巻銀河を再現した」とは書かない ——
#   **腕は与えた位相結合の帰結であり、棒は与えた結合グラフである**。
!tests/out/galaxyproto-w276e.json
# 第276便b(第66報 (2)「plutoCharonReal の kF0 版に問題がないか確認し、問題があれば解決する/
#   カロン以外の衛星の影響も検討対象とする/『どの様な要因が加われば解決するか』を探る」・
#   **診断のみ・内蔵の物理入力は不変**):
#   ① ❄️ の **softening ε 感度系列**(tests/exp-w272b-charon.mjs --eps rule)。基準単位では
#      受理下限 ε=0.01 が 10 km に当たるので、**単位を変えた診断コピー**(L=5・T=2・M=20)で
#      1 km まで下げた列を併記する。要求値・適用値・クランプ警告をそのまま残す。
#      **第272便b の正本 charon-w272b.json は 1 バイトも触らない**。
#   ② **要因表**(tests/exp-w276b-charonfactors.mjs)。ε・a の丸め・G の丸め・GM の観測精度・
#      質量配列の Float32 量子化・小衛星 4 体・太陽の分解・周期の定義 —— の感度と採用可否。
#      QA `docs.charonFactors` が docs/CALIBRATION_VERDICT_v1.44.md §5.24 の表と突き合わせ、
#      `lint.provenanceMeta` が来歴を固定するのでコミットする。
#   **「観測一致を達成した」「残差ゼロの ε を採用した」とは書かない**(どちらも本便はしていない)。
!tests/out/charoneps-w276b.json
!tests/out/charonfactors-w276b.json
# 第221便: セッション内スクラッチ実測ハーネス(コミット対象外 — 実測値は claims/QA が機械固定する)
_scratch-*.mjs
_scratch-*/
_scratch-*.log
# 第276便d(第66報 (4)「shapeToyClusterCore・shapeToyDiskCore・shapeToyArmCore は、各粒子を中心天体の
#   スピンに影響を受けた DFM に準拠する法則で制御する様に調整する」): **Core 力学**の検算の正本
#   (tests/exp-w276d-corefield.mjs)。閉形式の伝播子 ↔ 独立な行列指数の突合・1 粒子 20,000 步の
#   保存量 H と正準角運動量の相対誤差・エンジンとの一致・内蔵 3 本(🎱📀🧹)の完成門(RMS 半径の
#   偏り/ドリフト・逆行の割合・σ_z/σ_R・棒の幅と軸方向 RMS・摂動からの回復)・**指紋の対照**
#   (中心スピン 0→1 / G 0→8 / 二重加算の門 / 規定運動の双子はスピンに不感)・背景 W₀ と中心質量の
#   掃引・**供給枯渇**を持つ。QA `docs.coreFieldCriteria` がこの JSON と docs/PHYSICS.md〔第276便d〕を
#   突き合わせ、`lint.provenanceMeta` が来歴を固定するのでコミットする。
#   **較正ではない**(観測量を 1 つも入力していない)。**追加ポテンシャルは新しい構成仮説**であって、
#   現行 DFM から一意に導出された法則ではない。「銀河が安定した」「腕が創発した」とは書かない。
!tests/out/corefield-w276d.json
# 第277便d(原仮定者の裁定〔第67報〕(3)「慣性移動は、複素決定力による、自分自身に対するゼロ距離の
#   座標変換」と統括の読み R51/R52): **エンジン未接続の純関数**の検算 2 本。
#   ① 慣性移動の仮定(自己並進写像 selfDrift・対角/非対角の分離)の検証と、**現行 beta/index.html の
#      自己項監査**(力学が読む u の和に自己源が入っていないことを行番号つきで機械抽出したもの)。
#      **否定対照**(自己項を分母へ入れる実装・L の退化)も同じ JSON に持つ。
#   ② 背景 ON/OFF の**誤差予算**(共動系変換 toComovingFrame つき・純関数の 2 体 RK4 積分)。
#      「ほぼ一様」と「効果が小さい」は**別**であることを数で出す表で、**判定ではなく予算**である。
#   QA `docs.selfInertia-sync`・`docs.bgbudget-sync` が docs/PHYSICS.md〔第277便d〕と突き合わせ、
#   `lint.provenanceMeta` が来歴を固定するのでコミットする。
#   **「慣性を導出した」「背景を無視してよいことを証明した」とは書かない。**
!tests/out/selfinertia-w277d.json
!tests/out/bgbudget-w277d.json
# 第277便b(原仮定者の裁定(第67報)(1)「空間メッシュ(複素決定力場)による引きずりは相対的に
#   動いていなければ発生しない/上記を踏まえて実装を修正し、DFM 版冥王星とカロンの較正を通す」):
#   **同一観測解の冥王星–カロン**の正本(tests/exp-w277b-charondfm.mjs)。
#   ① 入力の監査(2024 年の暦解の GM・Horizons の状態ベクトル・軌道面射影・接触要素・
#      **公表 3 値の互いの差**〔PLU060 −21.0σ・Horizons PR 元期A −222.3σ〕)、
#   ② 相対すべりの引きずり則の純関数検定(**零条件は厳密 0**・否定対照・保存・Q̇≥0・
#      **相互同期していない中性子星連星へ延長すると桁で外れる**否定対照)、
#   ③ エンジン実測(新 ID 2 本 × 3 段 + 1 次外挿・帳簿・決定性)、④ ε/自転/倍精度/κ/小衛星の系列、
#   ⑤ 要因表 v2。QA `docs.charonDfm` がこの JSON と html の bodies・PHYSICS/台帳の表を突き合わせ、
#   `lint.provenanceMeta` が来歴を固定するのでコミットする。
#   **「較正を完了した」「観測と一致した」「残差がゼロになる ε・a・f を採用した」とは書かない**
#   (どれも本便はしていない)。判定の語は門が返す 合/否/保留 の 3 つだけである。
!tests/out/charondfm-w277b.json
# 第278便b(R53・R54): 陰的中点法の検定と、カロン周期の 4 構成 × 3 刻み・第 2〜第 6 周の時間窓の正本
# (QA docs.charonTwoColumns が読む)
!tests/out/charonwin-w278b.json
# 第277便a(第67報 (1)・統括の検証項目 R47): **冥王星系の状態ファイルの整合検査と「別の量」の診断**
#   (tests/exp-w277a-plutostates.mjs)。paper/data/pluto-system-states.csv の 72 成分(6 体 × 2 元期 ×
#   6 成分)・単位・sigma 全空・record_id 一意の検査と、2D へ移すときの面外成分の最大値、
#   同じ暦から作った相対二体の a・P、重心基準の osculating a・PR、Buie 2012 の two-body P、
#   PLU060 の 400 年平均 P の**並び**を持つ。QA `docs.plutoStatesFile` がこの JSON と
#   docs/CALIBRATION_VERDICT_v1.44.md §5.25 を突き合わせ、`lint.provenanceMeta` が来歴を固定する
#   のでコミットする。**エンジンは 1 步も走らせていない**(入力は 2 つの CSV だけである)。
#   **どの a も P も採用値ではない**(定義の違う量を並べた診断である)。σ は 1 つも作っていない。
!tests/out/plutostates-w277a.json
# 第277便c(原仮定者の裁定(第67報)(2)「観測値の自転軸の傾きと、潮汐力と、パワーボール効果を
#   利用して、DFM 版中性子星連星の安定状態を検証する」): **中性子星連星の診断格子**の正本
#   (tests/exp-w277c-nsgrid.mjs)。相対すべり s=v−ω×r の零条件・潮汐の手(宣言)と供給元の減少・
#   有限口座(η・要求率)・受動散逸(λ)・小摂動 ±1° の (A) 相対平衡 /(B) 吸引状態 /(C) 過渡 の
#   機械分類・供給元の対照(内部モード口座 vs 軌道 E)・N3 の可逆対照・無減衰の局所試作を NS へ
#   延長した否定対照を持つ。QA `behavior.nsGridLedger` がこの JSON の生データから門の式を引き直し、
#   `docs.nsGridCriteria` が docs/PHYSICS.md〔第277便c〕と突き合わせ、`lint.provenanceMeta` が
#   来歴を固定するのでコミットする。
#   **較正ではない**(観測の傾きは**初期値としてのみ**使い、そこから係数を決めていない)。
#   「NS の平衡を実証した」「ロックすると加速する」「観測と一致した」とは書かない。
!tests/out/nsgrid-w277c.json
# 第278便d(統括の読み R56): **明示天体 ↔ 局所背景展開の一致試験**の正本(tests/exp-w278d-bgequiv.mjs)。
#   ❄️🌘📻 で外部源(太陽/銀河の点質量)を明示天体として置いた走行と、背景展開(凍結・時間追従・
#   値の一次補正・厳密評価・潮汐・反作用のはしご 12 段)として置いた走行の ON/OFF 差(周期・位相・位置 ——
#   1 公転・RK4・刻み 3 段)と、食い違いの項別の内訳、移流項 (V·∇) の数値検証、潮汐鍵の受理契約の
#   ページ/純関数の突合を持つ。QA `docs.bgEquivalence`・`docs.comovingAdvection` が docs/PHYSICS.md
#   〔第278便d〕と突き合わせ、`lint.provenanceMeta` が来歴を固定するのでコミットする。**エンジン未接続**。
#   **「背景を無視してよいことを証明した」「閾値を採用した」とは書かない。**
!tests/out/bgequiv-w278d.json
# 第278便c(統括の検証項目 R55): **pairSlip の符号修正の影響範囲**(tests/exp-w278c-slipaudit.mjs —— 最小対照 4 件・
#   入れ替え対称性・beta/index.html の 2D 実装〔ソースを文字列で評価〕との突合・lib-w277b との 1 步ビット突合・
#   slipRmsCircular との一致・格子の力学が pairSlip を経由しないこと)と、**H6 の共役変数模型**
#   (tests/exp-w278c-nsmode.mjs —— 内部モード Q・P・有限容量・角力積の記帳・θ 依存の係数表・歳差との結合・
#   摂動復帰・供給停止・別系の零点)の正本。QA `behavior.pairSlipSign`・`behavior.nsModeExchange` が読み、
#   `lint.provenanceMeta` が来歴を固定するのでコミットする。**較正ではない**・**エンジン未接続**。
!tests/out/slipaudit-w278c.json
!tests/out/nsmode-w278c.json
# 第279便c(原仮定者の裁定〔第69報〕・統括の読み R62/R63): **背景の閾値なし合成と速度分解 RHS の検算**の正本
#   (tests/exp-w279c-bgcompose.mjs —— html の純関数をソースから取り出して node で評価し、ページの HP.* とビット一致を数える。
#   (a) 直接計算 vs 分解→合成 (b) 座標変換の不変性 (c) 定常場の H 保存 (d) 境界 (e) 背景重み 1e−30 (f) 一様定常場)と、
#   **新契約での背景の誤差予算**の正本(tests/exp-w279c-bgbudget2.mjs —— ❄️📻 + 🌘 参考行の純関数積分 12 模型 × 刻み 3 段と、
#   ❄️📻 の診断コピーのエンジン走行 × 刻み 2 段)。QA `docs.bgCompose`・`docs.bgbudget2-sync` が docs/PHYSICS.md〔第279便c〕と
#   突き合わせ、`lint.provenanceMeta` が来歴を固定するのでコミットする。**「背景を無視してよいことを証明した」とは書かない。**
!tests/out/bgcompose-w279c.json
!tests/out/bgbudget2-w279c.json
# 第280便b(原仮定者の裁定〔第70報〕・統括の読み R69/R70): **表裏核の検算**の正本(tests/exp-w280b-sphereKernel.mjs —— html の純関数を
#   ソースから取り出して node で評価し、ページの HP.* とビット一致を数える。(a) 遠方 (b) 求積の収束と独立な 2 次元求積 (c) 表面近く
#   (d) q_eff (e) 現行核との振幅比 (f) 中心 (g) 微分)と、**地球–月の 1 表**の正本(tests/exp-w280b-emgrid.mjs —— 🌘 の初期状態から
#   重み・初速・q・法則・核を一つずつ変えた 10 行 × 窓 2 つ)。QA `docs.sphereKernel`・`docs.emgrid-sync` が docs/PHYSICS.md〔第280便b〕と
#   突き合わせ、`lint.provenanceMeta` が来歴を固定するのでコミットする。**弱場振幅(c² の抑制)は導出できていない。**
!tests/out/spherekernel-w280b.json
!tests/out/emgrid-w280b.json
# 第279便a(原仮定者の裁定(第69報)「各サンプルの状況確認」・統括の読み R59/R60): **内蔵 133 本の status(目的・状況・
#   較正・合わない量・見込み・根拠)と概要の正本**(tests/exp-w279a-samplestatus.mjs)。較正の語・合わない量・見込みは
#   calaudit-w249.json / charonwin-w278b.json から機械で作る。QA docs.sampleStatus-sync が docs/SAMPLE_STATUS_v1.45.md と
#   html の生成領域 sample-status と突き合わせ、lint.provenanceMeta が来歴を固定するのでコミットする。**判定はしない**(台帳の転記)。
!tests/out/samplestatus-w279a.json
# 第280便a(原仮定者の裁定(第70報)「mercuryReal: 観測値版で問題が出ている理由を調査する」・統括の検証項目 R66):
#   **水星の近点移動の不足の分解**の正本(tests/exp-w280a-mercury.mjs —— 判定器の抽出器をソースのまま評価し、
#   dt 4 段 × ε 3 段 × λ_PN 2 値の格子・kF0/kF1/geoPN=3/vMinusU の 1 表・🪨−☄️ の差)。QA docs.mercuryDecomp が
#   CALIBRATION_VERDICT §5.32 と PHYSICS〔第280便a〕に突き合わせ、lint.provenanceMeta が来歴を固定するのでコミットする。
!tests/out/mercury-w280a.json
# 第279便b: QA の確認順・並列化(W5b)・失敗注入・preflight の実測の正本(tests/exp-w279b-qaorder.mjs --record)。
#   QA `lint.provenanceMeta` の CANON に入っている
!tests/out/qaorder-w279b.json
# 第280便c(原仮定者の裁定〔第70報〕「近点移動の差分を精査する — geoPN=3 として進める」・統括の読み R65): **geoPN=3 の契約の検算**の正本
#   (tests/exp-w280c-geo3.mjs —— 正式の判定器 calaudit のページ側ヘルパをソースから取り出した同じ抽出器・同じ窓で、☄️ の一様な u=V の零試験・
#   ❄️ の太陽の背景・velocityMeaning の往復・二重計上の拒否・保存の往復・診断コピー 2 本の再現性・NaN 受理の修正を測る)。
#   QA `docs.geo3-sync` が docs/PHYSICS.md〔第280便c〕と突き合わせ、`lint.provenanceMeta` が来歴を固定するのでコミットする。**較正ではない。**
!tests/out/geo3-w280c.json
# 第279便b: 前回 FAIL の先行再実行・preflight が一時的に書く切り出し版(終われば消す — 途中終了の残りを拾わない)
tests/.qa-replay-*.mjs
tests/.qa-preflight-*.mjs
# 第280便e(原仮定者の裁定(第70報)「saturnZonalD(68): 観測値版で問題が出ている理由を調査する」・統括の検証項目 R67):
#   **📡 D68 の問題の分解**の正本(tests/exp-w280e-d68.mjs —— 同じ帯状ポテンシャルの独立計算・正式の抽出器と窓での
#   実エンジンの条件×刻み 3 段・診断コピー 🧷📎)。QA docs.d68Decomp・behavior.d68Consistent が読み、lint.provenanceMeta が
#   来歴を固定するのでコミットする。**診断の器であって門の判定ではない**。
!tests/out/d68-w280e.json
# 第280便d(第70報・R68): ❄️ の否の要因の鎖・入力を整えた二体・同一定義の表・零条件の 3 検査の正本
#   (tests/exp-w280d-charonInput.mjs)。QA docs.charonInput・docs.charonSameColumn が読み、
#   lint.provenanceMeta の CANON に入っているのでコミットする。途中経過(.partial)と --pilot の出力は無視する
!tests/out/charoninput-w280d.json
tests/out/charoninput-w280d.json.partial
tests/out/charoninput-w280d-pilot.json
# 第281便b(原仮定者の裁定(第71報)「銀河の引きずり(座標変換)の分布を確認する」・統括の読み R72/R73): **銀河連鎖便**の正本 2 本
#   (tests/exp-w281b-galaxychain.mjs —— 場の契約の 3 列の半径表・🎋 の連鎖の実測・R_edge・中心応答 / 純関数 tests/lib-w281b-chain.mjs の
#   有限予算の交換模型の帳簿)。QA docs.galaxyFieldContract・behavior.chainLedger・docs.galaxyChain が読み、lint.provenanceMeta が
#   来歴を固定するのでコミットする。**境界の発見でも較正でもない**(R_edge は診断量)。
!tests/out/galaxychain-w281b.json
!tests/out/chainledger-w281b.json
# 第281便d(原仮定者の裁定(第71報)「ナビエ・ストークス方程式の渦伸長が渦巻銀河の腕に似ているので参考にする」・統括の読み R75):
#   **渦伸長便**の正本(tests/exp-w281d-strain.mjs —— 2D で渦伸長項が 0 の数値・ひずみ率の純関数の単体試験・🎠 の初期場の
#   2 読み手の S/ω/伸び・galaxyproto/corefield の既存正本の読み)。QA behavior.strainPure・docs.vortexStretch が読み、
#   lint.provenanceMeta が来歴を固定するのでコミットする。**エンジン未接続・門ではない**。
!tests/out/strain-w281d.json
# 第281便c(原仮定者の裁定(第71報)「浮遊惑星の質量算出根拠を調べ、ダークローターを恒星質量程度に調整して当てはめる」・統括の検証項目 R74):
#   **条件付き質量台帳・η 対照・減光と偏向の分離**の正本(tests/exp-w281c-rotorledger.mjs —— Node だけで html を読む)。
#   QA docs.rotorLedger・preset.massLedger が読み、lint.provenanceMeta が来歴を固定するのでコミットする。**較正ではない**。
!tests/out/rotorledger-w281c.json
# 第282便d(原仮定者の裁定(第72報)⑥・統括の読み R81): **アナロジー便**の正本(tests/exp-w282d-analogy.mjs —— 場の契約の読み手の再現・
#   中心 spin の応答・🌚 galaxyAnalogyBH の台帳と回転曲線・🛞 の f=1 台帳。Node だけで html を読む・領域 hash あり)。
#   QA preset.fieldContract・behavior.centerSpinResponse・docs.analogyLedger が読むのでコミットする
!tests/out/analogy-w282d.json
# 第281便a(原仮定者の裁定(第71報)・AN16 採用・統括の検証項目 R71): **❄️ 対照系列の履歴列**(C2/C4/C7 の全段と、
#   h4 段を C0/C1/C6 に絞ったことで現行から外れた C3/C5/S の h4 段)の正本。tests/exp-w281a-charonsplit.mjs が
#   基点コミット 53aaa64 の charon-w272b.json から**値を 1 つも変えずに 1 度だけ**写した(`role:"history"`・`frozen:true`・
#   targetSha256 は写し元の走行が読んだ html の hash のまま)。**再生成しない・現行の結論に昇格させない**。
#   QA docs.charonSeries(履歴列の本数)と lint.regenScope(履歴の正本がどの現行正本の入力にもなっていない)が読むのでコミットする
!tests/out/charon-history-w272b.json
# 第281便a: 再生成の計画(tools/regen-plan.mjs --out)は手元の作業物(コミットしない —— 統括の chain が読む)
tests/out/regen-plan-w281a.json
