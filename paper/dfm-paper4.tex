% =====================================================================
% Determinacy-Field Mechanics, Paper 4 -- DRAFT (v0.4, 2026-08-24)
% v0.4 (第191便): Paper 4/5 split (原仮定者裁定 2026-08-24 「論文4/5分割: 採用」) --
%   theme re-scoped from "Emergence across scales" to CELESTIAL REPRODUCTION
%   (macro: gravity / dragging / morphology). Changes:
%   - The micro chapter (internal-spin thermalization; exp-4-90/91/93) and the
%     hidden-mass ladder move VERBATIM to the new Paper 5 (dfm-paper5.tex).
%   - NEW Sec. "Solar-system real calibration" (measured): the zero-fit
%     transcription family (venusReal / marsMoonsReal / plutoCharonReal /
%     uranusReal / neptuneReal + the jupiterGalilean hold-out), the free-centre
%     reaction-rule discovery (E6'-R), and the frame-dragging excess series
%     chi = W/(D0+W): +0.002% / +0.117% / +0.246%, with its measured
%     direction-independence (wave 188).
%   - NEW Sec. "A staged programme toward galaxy morphology (plan)": eight target
%     classes x (observation build with a declared analytic halo vs DFM build
%     with dark rotors), with pre-declared shared gates. Plan only.
% v0.3 (第122便): Renumbered Paper 3 -> Paper 4 (原仮定者裁定 2026-08-16 —
%   Paper 3 is reassigned to the reality-calibration theme, dfm-paper3.tex;
%   this emergence-across-scales draft moves to Paper 4 unchanged in content).
% v0.2 (第100便): Secs. IV (micro) and V (meso) drafted in full prose from the
%   landed harness records (exp-4-90/91/93 for micro; the wave-97 re-measured
%   exp-4-85 + selfRotor sweep records for meso, with an explicit generation
%   note for the pre-rescaling exp-4-89/92 robustness sweeps).
% Theme (v0.3): "Emergence across scales" -- adopted 2026-08-08; re-scoped to
%   celestial reproduction by the v0.4 split above.
% Fallback scope (pre-agreed, v0.3): drop Sec. IV (micro) -> meso+macro only.
%   [superseded by the v0.4 split -- the micro chapter now lives in Paper 5]
% Status: section skeleton with measured numbers from the harnesses
%   tests/exp-4-84/85/89/90/91/92/93.mjs (result JSONs recorded per run).
%   All figures are placeholders; prose is bullet-level and NOT final.
% Companion papers: Paper 1 (foundations, dfm-paper.tex), Paper 3 (reality calibration, dfm-paper3.tex),
%   Paper 2 (box universe, dfm-paper2.tex).
% =====================================================================
\documentclass[aps,reprint,amsmath,amssymb,nofootinbib,superscriptaddress,floatfix]{revtex4-2}

\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{booktabs}

\begin{document}

\title{Celestial Reproduction in Determinacy-Field Mechanics:\\
Zero-Fit Solar-System Transcriptions, Self-Forming Dark Rotors,\\
and a Graded Standard-Test Programme toward Galaxy Morphology}

% TODO(author): author block as in Papers 1--2.
\author{...}
\date{2026-08-24 (draft v0.4)}

\begin{abstract}
% --- What we claim (skeleton bullets; final prose TODO) ---
% 1. In a single relational toy universe (DFM, Paper 1), calibrated ONCE against
%    real ephemerides (Paper 3) and never refit, we pursue celestial reproduction
%    on the macro side (gravity, dragging, morphology) with one shared
%    methodology: an emergence grading E0--E3 and six standard tests.
% 2. Solar-system real calibration (measured): a family of zero-fit observation
%    transcriptions (Sun--Venus, Mars' moons, Pluto--Charon, Uranus' rings and
%    moons, Neptune--Triton; Jupiter's Galilean moons as the Paper-3 hold-out)
%    reproduces observed periods at the 1e-4..1e-3 level at kFrame=0, exposes a
%    free-centre reaction-rule hole (fixed as E6'-R, with a new drift-
%    extrapolation self-check), and yields a declared frame-dragging excess
%    series chi = W/(D0+W): +0.002% (wide) / +0.117% (chi=0.83) / +0.246%
%    (chi~0.92, close) -- measured to be independent of orbit direction.
% 3. Meso: a heavy, dark, co-rotating centre (the toy universe's own dark-body
%    candidate) forms from a uniform cloud without being placed; robust over
%    16 seeds, 4 particle counts, perturbations, and a factor-4 range of dt;
%    softening sensitivity is reported honestly.
% 4. Macro (application): the m=2 disk signature with its quadrupole
%    contamination controls; and a staged PLAN with pre-declared gates from
%    binaries to barred spirals (observation builds with a declared analytic
%    halo vs DFM builds with dark rotors).
% --- What we do NOT claim (fixed list) ---
%    no real dark matter / black holes / galactic spiral arms; calibration
%    agreement is not mechanism identification; no 3D extrapolation; no
%    cosmological abundances. (The micro sector -- spin thermodynamics and the
%    optics of dark bodies -- is Paper 5.)
TODO abstract.
\end{abstract}

\maketitle

% =====================================================================
\section{Introduction}
% [第93便: 本文起こし第2弾 — 序論。位置づけ文は論文1/2 の Negative claims と整合]

Determinacy-Field Mechanics (DFM) is a two-dimensional relational toy
universe in which a single scalar field -- the determinacy $W$ sourced by
all matter -- supplies the local inertial frame, gravity, clock rates,
and optics \cite{paper1}. It is an educational \emph{computational
thought experiment}: it is not proposed as a description of the real
universe, and none of the phenomena below are claims about real
astrophysical objects (Sec.~\ref{sec:negative}). Paper 1 established the
foundations and their weak-field benchmarks; Paper 2 confined the model
to a finite ``box universe'' and studied expansion, time, and light
\cite{paper2}; Paper 3 calibrated the weak-field sector against real
ephemerides under an explicit ``calibration agreement is not mechanism
identification'' philosophy \cite{paper3}.

This paper asks the macro question of the series: \emph{can a toy
universe that was calibrated once against real ephemerides (Paper 3)
reproduce celestial systems from transcribed observations -- with zero
per-system fits -- and how far up the ladder of structure can that reach
under pre-declared gates?} Our answer is methodological before it is
physical. We fix a grading scheme (E0--E3) and a battery of six standard
tests (Sec.~\ref{sec:standard}); we anchor the paper in a family of
zero-fit solar-system transcriptions whose disagreements are declared
rather than fitted away (Sec.~\ref{sec:solar}); we keep the meso-scale
self-formation of a compact dark rotor -- the toy universe's own
dark-body candidate -- under the same battery (Sec.~\ref{sec:meso}); we
treat macro-scale disk signatures strictly as an application
(Sec.~\ref{sec:macro}); and we state, as a plan with gates declared
before any measurement, the staged programme from binaries to barred
spirals (Sec.~\ref{sec:plan}). The micro sector of the former joint
draft -- internal-spin thermodynamics and the optics of dark bodies --
now lives in Paper~5 \cite{paper5}; the split line is mechanism-based:
Paper 4 = gravity, dragging and morphology (macro); Paper 5 = spin, heat
and optics (micro).

The guiding principle throughout is that \emph{a single beautiful run is
the weakest form of evidence}. Accordingly, the results below include
their own failures: an orbit that the legacy reaction rule destroys
within a single revolution (and the self-check that now catches that
class of failure before adoption), ring masses that have no source and
are therefore carried as declared test particles, a dragging excess of
order $10^{-3}$ that the observations do not show and that we declare
rather than fit away, a mass fraction that is honestly sensitive to the
softening length, and an alignment timescale that outgrows its
measurement window as the particle count rises. We consider these
records part of the result: they are what distinguishes a laboratory
from a gallery.

% =====================================================================
\section{Model additions relevant to structure formation}
% Minimal recap only (no re-derivation; cite Paper 1):
% - determinacy field W, gravity a = G grad W, frame dragging (A8/E6'),
% - internal temperature T_int = coarse-grained microspin variance (A9''),
%   heat energy E = C m T_int, conduction E10' with C*m weighting,
% - fusion + core inheritance (M_c' = M_c,i + M_c,j etc.), core v2 (J_core),
% - dimming lS_eff = min(1, |s| R / c_surf) (A11/E8R),
% - E6'-R pair-reduced reaction: free dominant bodies in a closed system.
TODO.

% =====================================================================
\section{A standard for emergence}
\label{sec:standard}
% [第93便: 本文起こし第1弾 — 方法論章。数値・定義はアプリ docs/PHYSICS.md と
%  ハーネス exp-4-84..93 の実測に一致させてある。査読向け英文推敲は次版]

The word ``emergence'' carries a burden of proof that a single striking
simulation cannot discharge. A fixed random seed, a particular particle
count, and a hand-tuned time window can make almost any dynamical system
produce one beautiful movie. The methodological core of this paper is
therefore a fixed standard, applied identically on every scale we study:
each claimed phenomenon must declare its order parameters in advance,
pass a battery of six standard tests, and carry an explicit grade.

\subsection{Grading}

We grade each phenomenon on a four-level scale, declared per sample and
never auto-assigned:

\begin{itemize}
\item \textbf{E0} -- a configured display: the pattern is placed in the
  initial conditions or enforced by an external element (pinned bodies,
  rails, baths).
\item \textbf{E1} -- a driven effect: the pattern appears while an
  external element does work on the system.
\item \textbf{E2} -- emergence in a closed system: the pattern forms from
  the declared local rules alone, with momentum and angular momentum
  closing on the ledger, but robustness has not been established.
\item \textbf{E3} -- robust emergence: E2, plus reproduction across seeds
  and particle counts and recovery from perturbations, established by the
  standard tests below.
\end{itemize}

The grade is metadata attached to the sample and shown in its user
interface; the measurement harnesses that justify it are cited next to
the grade. Nothing in the engine reads the grade -- it is a claim, not a
mechanism.

\subsection{The six standard tests}

\begin{enumerate}
\item \emph{Knockout control.} Remove one candidate mechanism (a fusion
  rule, a conduction coefficient, a core seed) and re-run under identical
  conditions. If the phenomenon survives, the mechanism was not its
  cause; if every candidate can be removed without effect, the
  phenomenon was configured, not emergent.
\item \emph{Dose response.} Vary the strength of the claimed driver
  monotonically and require the declared order parameter to respond
  monotonically. Non-monotonic responses are reported as such (an
  example: the fusion-threshold sweep of Sec.~\ref{sec:meso}).
\item \emph{Multi-seed.} Re-run under a fixed set of random seeds
  (sixteen in this paper) and report the full range of the order
  parameter, not its best value. Controls run under the same seeds.
\item \emph{Particle-count scaling.} Repeat at several particle counts
  (three to four octaves here) to exclude discrete artefacts of one
  resolution. Where the count is entangled with a physical quantity --
  in our meso-scale sample $n$ is also the total mass -- the entanglement
  is stated rather than hidden.
\item \emph{Perturbation recovery.} Disturb the formed state -- with the
  application's own momentum-conserving injector where possible -- and
  measure whether the order parameters return. A pattern that never
  recovers is a relic of the initial conditions, not an attractor.
\item \emph{Time window.} State the window in which the claim holds and
  what happens outside it. Timescales that grow with system size or
  coupling (both occur below) are reported as part of the result.
\end{enumerate}

Two reporting rules bind all six tests. First, order parameters are
declared before testing; switching the judged quantity after a failed
run is recorded in the harness header when it happens (it happened twice
across this paper pair -- in the meso study below, Sec.~\ref{sec:meso},
and in the micro study of Paper~5 -- and both
switches are documented with the failed first attempt). Second, negative
and partial results are published in the same format as positive ones:
each sample's user interface carries a ``what did not hold'' list, and
this paper reproduces those lists verbatim in
Sec.~\ref{sec:negative}.

Every test in this section runs headless from a version-controlled
harness (Sec.~\ref{sec:repro}); the numbers quoted in
Secs.~\ref{sec:solar}--\ref{sec:robust} are the outputs of those
harnesses and of the machine-checked QA gates named in the text, not of
interactive runs.

% =====================================================================
\section{Solar-system real calibration: zero-fit transcriptions}
\label{sec:solar}
% [第191便: measured section. Every number below is a committed value: the
%  built-in presets venusReal / marsMoonsReal / plutoCharonReal / uranusReal /
%  neptuneReal (beta/index.html), their source table
%  paper/data/solar-observations.csv (archived URLs per row), and the QA gates
%  behavior.venusReal / .marsMoonsReal / .plutoCharonReal / .uranusReal /
%  .neptuneReal and ai.obs-* (transcription path). Prose is first-pass.]

\subsection{The transcription contract}

Before any galaxy is attempted, the calibrated sector must survive
contact with systems it was never fitted to. The application carries a
deterministic transcription path: a table of observation records (each
row a single measured quantity with its source and the URL actually
consulted) is converted -- by declared, deterministic functions and by
nothing else -- into a running two-dimensional system. Per-sample real
units are declared as scale exponents constrained to the one-parameter
family $L-T=4$, $M+2T-3L=11$, which preserves the real constants
($G$, $c_0$, $\kappa=G/c_0^2$); the qLock falloff $q$ is the exact-form
value $q_{\mathrm{exact}}$ evaluated once at a declared reference orbit;
the shared correction $D_0=0.006$ is reused from the lunar calibration
of Paper 3 \emph{with zero per-system refits}; and initial speeds are
the real Kepler speeds with calibration factor exactly $1.000$. Each
build must pass its own self-diagnosis (a 400-step trial comparing
estimated against transcribed periods at $\pm1\%$) or it is rejected.
Idealizations -- two-dimensional equatorial placement, circularization
where no eccentricity is sourced, test-particle demotion where masses
fall below the value floor -- are declared per sample, never silent.

\subsection{The family, and what it measured}

Six systems are carried as built-in, machine-checked samples: the
Jupiter--Galilean system (the hold-out of Paper 3), Sun--Venus, Mars
with Phobos and Deimos (a declared test-particle transcription),
Pluto--Charon (a barycentric pair whose barycentre lies outside the
primary), Uranus with its main rings and five major moons, and
Neptune--Triton (a retrograde-moon pair). Three measured results
deserve statement here.

\emph{A structural discovery.} Free-centre two-body transcriptions run
under the engine's legacy reaction rule -- which presumes a pinned
dominant body as the reaction reservoir -- are destroyed within a
single orbit when $\chi = W/(D_0+W) \rightarrow 1$ (the Sun--Venus
transcription showed a 45\% radius wobble and no completed revolution).
The fix is structural, not a fit: barycentric pairs are built under the
pair-reduced reaction rule E6$'$-R, and the self-diagnosis gained a
drift-extrapolation check (two-body only) that projects the trial-window
period drift over one orbit and rejects the legacy build at a projected
$1.47\%$ -- a negative control that is itself part of the QA suite.
With the rule fixed, the Venus year lands at 224.706~d against the
observed 224.701~d ($+0.0020\%$).

\emph{A declared dragging excess, and its $\chi$ series.} At
$k_{\mathrm{frame}}=0$ the transcriptions reproduce the observed
periods at the $10^{-4}$--$10^{-3}$ level (Neptune--Triton: 5.87741~d
against the observed 5.876854~d, $+0.009\%$). Under the template
convention $k_{\mathrm{frame}}=1$ the motion-dragging term E6$'$ lifts
the periods by an amount the observations do not show, and we declare
it instead of absorbing it: $+0.002\%$ for Sun--Venus ($\chi\approx1.0$
but wide), $+0.117\%$ for Neptune--Triton ($\chi=0.83$),
$+0.246\%$ for Pluto--Charon ($\chi\approx0.92$, close) -- a series in
which both $\chi$ and orbital closeness matter. This series is the
measured base of the ``dragging versus observation'' tension that the
galactic programme (Sec.~\ref{sec:plan}) must confront honestly.

\emph{Direction independence.} The Neptune--Triton pair is built
retrograde, as observed: an orbital inclination above $\pi/2$
(Triton: $157.345^{\circ} = 2.746188$~rad, transcribed from the source
table) is carried deterministically as the orbit direction, flipping
only the tangential speeds -- the out-of-plane tilt is ignored and
declared. The measured periods and radius wobbles of the retrograde
build are identical, at measurement precision, to the prograde build at
both $k_{\mathrm{frame}}$ settings: in this system the dragging excess
does not depend on the orbit direction. We record this without
attributing a mechanism.

Two honest declarations remain: the Uranian ring masses are sourced
only as model-dependent estimates (occultation self-gravity models,
with the epsilon ring at $3.27$--$6.99\times10^{15}$~kg), which convert
to ${\sim}10^{-10}$ units per grain -- below the engine's value floor --
so the grains are carried at the declared floor (their radial structure is
still transcribed and retained to $0.0164$ units at
$k_{\mathrm{frame}}=1$ over the measured window, against $0.968$ units
in the $k_{\mathrm{frame}}=0$ control -- recorded, unattributed); and
the transcription path is two-dimensional by construction, so
inclinations enter only as the orbit direction.

\subsection{What this section claims}

The claim is deliberately narrow: a weak-field sector calibrated once,
plus a deterministic transcription contract, reproduces solar-system
periods at the $10^{-4}$--$10^{-3}$ level with zero per-system fits,
and its residual disagreements are declared, sized, and traced to a
named term. Calibration agreement is not mechanism identification
(Paper 3); nothing here claims the real solar system contains the
model's dragging term.

% =====================================================================
\section{Meso scale: the self-forming dark rotor}
\label{sec:meso}
% Central result. System: 180 identical grains, uniform disk, slow global
% rotation, per-grain micro core seeds; fusion + core inheritance; closed
% system (E6'-R; no pinned bodies).
% Measured (tests/exp-4-84/85.mjs; seed 20260806, t=144):
%   - 155 fusions -> 25 bodies; largest = 26.7% of total mass; bound 0.75.
%   - no-fusion control: largest stays 1 grain (0.56% exactly, all seeds).
%   - dose response in G: 1.7/6.1/12.8/26.7/44.4% (monotone).
%   - J_core inheritance identity: centre J_core = 48 seeds' worth = mass
%     swallowed, grain for grain; Sigma J_core invariant; no-core control = 0.
%   - darkness: centre lS_eff 0.000 -> 0.589 vs halo 0.159 (contrast 3.7);
%     dimming decouples from dynamics exactly (kappa sweep leaves dynamics
%     bit-identical).
%   - 16 seeds: mFrac 18.3--53.3% (median 34.7%); contrast 2.41--5.30 vs
%     control 0.40--1.26 -- ranges never overlap (Fig. 5).
%   - honest negatives: darkness alone does not discriminate fusion (control
%     centre also dims); kFrame=0 control shows no outer-rotation systematics;
%     dFrac dose response is non-monotonic.
% Robustness (this paper's additions):
%   - dt in {0.016, 0.008, 0.004} at fixed physical time: all order parameters
%     stay inside the 16-seed spread; |Delta L|/L_scale stays ~1e-6
%     (tests/exp-4-89.mjs). Fusion count drifts mildly (155->147->138).
%   - softening x0.5/x1/x2: formation survives (maxFrac 0.978--1.000, align
%     1.000) but mFrac IS softening-sensitive at x2 (fusions drop to 104--122;
%     mFrac min 11.1% below the 16-seed floor 18.3%) -- quantitative mass
%     fractions must be quoted at fixed softening.
%   - n = 90/180/360/720 (4 stages; tests/exp-4-85.mjs + exp-4-92.mjs):
%     formation complete at n=720 (maxFrac 1.000, control ratio 166--208x);
%     mFrac saturates (median 31.5% vs 31.7% at n=360). Alignment at n=720
%     converges more slowly (one seed 0.752 at step 9000 -> 1.000 by 13500):
%     the alignment timescale grows with n; the t=144 window was tuned at
%     n=180. Recorded as a time-window caveat, not hidden.
% Figures: formation snapshots; mFrac(t); J_core lineage; dimming vs halo;
%   16-seed distributions (draft exists); dt/softening/n robustness table -- TODO.
% [第100便: 本文起こし — 数値は第97便の c₀=30 単位系での全面再実測(tests/out/exp-4-85.json・
%  probe-selfrotor-sweeps)を正とする。skeleton コメントの旧世代値(t=144・155融合・
%  mFrac 18.3〜53.3 等)は再パラメータ化前の記録 — dt/softening/n=720(exp-4-89/92)だけは
%  旧単位系の記録のまま引用し、その旨を本文で明示する]

\subsection{System}

The meso-scale system is a uniform disk of 180 identical grains (mass
1, spin 0, all free), given a slow global rotation
($\omega_0$) and one identical microscopic core seed inside every grain
(5\% of its mass, $J_{\mathrm{core}}=0.0010$ each). Nothing else is
placed: no central body, no dark core, no halo, no pinned particles, no
boundary, no external field. The active rules are self-gravity (E4),
contact (E9), fusion (two grains merging when their centres come within
$d<0.7\,(R_i+R_j)$), and the conservation-law inheritance of the cores
($M_c' = M_{c,i}+M_{c,j}$, $R_c' = \sqrt{R_{c,i}^2+R_{c,j}^2}$,
$J_c' = J_{c,i}+J_{c,j}$). The centre of mass and its velocity are
zeroed once at build time; thereafter the system is closed (E6$'$-R),
and the conservation monitor closes momentum and angular momentum with
all reservoir ledgers at zero ($|\Delta L|/L_{\mathrm{scale}} =
3.5\times10^{-6}$ over the reference run, $\leq 1.4\times10^{-5}$ over
all sixteen seeds).

All numbers below are quoted in the application's uniform light-speed
convention ($c_0=30$), in which this sample was re-measured in full;
the reference run is seed 20260806 evaluated at $t=288$ (18000 steps).

\subsection{Formation, and what causes it}

In the reference run the 180 grains undergo 146 fusions down to 34
bodies, and the heaviest body grows into a centre holding 27.2\% of the
total mass ($m=49$), with bound fraction 0.94. The knockout control
(fusion off) leaves the largest body at one original grain -- a mass
fraction of exactly $0.56\% = 1/180$, for every seed -- with bound
fraction 0.15: the heavy centre is a product of the fusion rule, not of
the initial conditions. The causal role of self-gravity is isolated by
a dose response: varying only $G$ over $0.5/1/1.5/2/3$ moves the mass
fraction monotonically through $1.1/7.8/18.9/27.2/57.8\%$
($7/62/125/155/173$ fusions).

The core angular momentum accumulates through the inheritance path and
nothing else: the centre's $J_{\mathrm{core}}=0.0490$ is exactly 49
seeds' worth, matching grain for grain the mass 49 it swallowed (the
sum in the inheritance rule literally counts them); the system-wide
$\sum J_{\mathrm{core}}$ stays at $0.1800 = 180\times0.0010$ to the
end; and the no-core control gives $J_{\mathrm{core}}\equiv 0$.

The darkness grows too. The effective dimming (A11/E8R -- how much of
its own light a body sweeps out by spinning) starts at $0.000$
everywhere; by $t=288$ the central body has dimmed to $0.818$ while its
surroundings average $0.192$ -- a dark centre with a bright surround,
contrast 4.3. Dimming is set purely by the depth of the well
$\psi=W\kappa$ (with $\kappa$ the spacetime coefficient of Paper~1 --
not Einstein's gravitational constant $8\pi G/c^4$, and distinct from
the spin diffusivity $\kappa_s$) and never enters the dynamics:
sweeping $\kappa$ over
$1/12,\,1/14,\,1/16,\,1/20,\,1/24$ leaves particle count, fusion count, mass fraction,
spin and radius identical to the last bit while the dimming alone moves
through $1.000/0.818/0.574/0.349/0.251$. The grains start with zero
spin and end co-rotating unanimously (spin alignment
$0.000\rightarrow1.000$, clump count $166\rightarrow2$, largest-clump
mass fraction 0.994, rotation support $V/\sigma=11.2$).

\subsection{The six tests}

\emph{Multi-seed (test 3).} Over sixteen seeds the central mass
fraction spans 16.7--47.8\% (median 26.7\%) and is at least 16.7\% in
every seed, while the no-fusion control sits at 0.56\% for all sixteen:
the worst seed is 30 times its control. Across the same seeds:
largest-clump mass fraction 0.978--1.000, spin alignment 1.000 in every
seed, $V/\sigma$ 4.1--22.0, central dimming 0.513--1.000 against
surround 0.156--0.220, $J_{\mathrm{core}}$ 0.0300--0.0860 (30--86
seeds' worth), bound fraction 0.49--0.94, 26--42 bodies left
(138--154 fusions), no NaN, no clamping. The discriminating optical
quantity is the \emph{contrast} (centre over surround): 2.59--5.41
(median 4.11) in the main runs against 0.36--1.26 (median 1.16) in the
controls -- the two ranges never overlap.

\emph{Knockout and dose (tests 1--2)} are the fusion knockout, the
no-core control, and the $G$ dose above; a second dose response in the
fusion threshold $d_{\mathrm{frac}}$ ($0.7/0.6/0.5/0.35 \rightarrow
27.2/37.2/53.9/53.9\%$) is monotone-then-saturating \emph{in this
realization} -- in the previous parameterization the same sweep
measured non-monotonic ($26.7/47.8/43.3/23.3\%$). The very shape of
this dose curve is sensitive to the rounding realization, and we record
that rather than choosing the prettier curve.

\emph{Particle-count scaling (test 4).} At $n=90/180/360$ (three
representative seeds) the median mass fraction is 17.8/22.8/37.5\%,
largest-clump fraction 0.933/0.994/1.000, alignment 1.000 throughout.
An honest entanglement is stated rather than hidden: this sample keeps
the disk radius and per-grain mass fixed, so $n$ is also the total mass
-- $n=90$ acts like halving $G$, and the ten-times-control bar is met
there by only 2 of the 3 seeds (ratios 2.0/16.0/20.0).

\emph{Perturbation recovery (test 5).} Gaussian velocity noise injected
at $t=240$ through the application's momentum-conserving injector, at
30\% of the speed RMS, moves none of the order parameters; sweeping the
amplitude, the largest clump only starts to break at four times the
speed RMS ($0.994\rightarrow0.861$; at twice it stays 0.994). What is
disturbed and returns is $V/\sigma$ (down 39--61\%, back within
20--100 steps) and the bound fraction (down $\leq$15\%, back within
20--50 steps; one seed misses the recovery criterion inside the
measured window -- recorded). Velocity noise cannot touch the
alignment by construction, so a separate perturbation flips the spins
of about half the grains: alignment is driven from 1.000 to
$0.531/-0.212/0.084$ and recovers within 20 steps ($\Delta t=0.32$).
The co-rotating state is not a configuration but an attractor that
contact friction (E9) and dragging (A8/E6$'$) re-establish every step.

\emph{Time window (test 6).} The clumps merge into one by step 3000
($t=48$); alignment passes 0.99 by steps 4000--5000; the mass fraction
reaches 90\% of its final value between steps 4000 and 6000 and then
saturates -- at step 18000 (the declared window) and step 36000 it
differs by at most 0.6 points. The window sits inside the plateau, past
the rise.

\subsection{Numerical robustness, with a generation note}

Three further sweeps probe the integration rather than the physics.
They were recorded (\texttt{tests/exp-4-89.mjs},
\texttt{exp-4-92.mjs}; 2026-08-08) in the sample's \emph{previous}
parameterization, before its exact-similarity rescaling into the
$c_0=30$ convention (velocities and rates $\times k$, times $\div k$;
the continuum dynamics is identical by construction, the discrete
integration differs slightly, and the multi-seed distribution above was
re-measured after the rescaling). We quote them in their original
units, against the old-unit sixteen-seed floor of 18.3\%:

(i) \emph{Timestep}: $dt \in \{0.016, 0.008, 0.004\}$ at fixed physical
time leaves every order parameter inside the sixteen-seed spread with
$|\Delta L|/L_{\mathrm{scale}}\sim10^{-6}$; the fusion count drifts
mildly ($155\rightarrow147\rightarrow138$). (ii) \emph{Softening}:
$\times0.5/\times1/\times2$ -- formation survives everywhere
(largest-clump 0.978--1.000, alignment 1.000), but the mass fraction
\emph{is} softening-sensitive at $\times2$ (fusions drop to 104--122;
minimum mass fraction 11.1\%, below the sixteen-seed floor):
quantitative mass fractions must be quoted at fixed softening, and we
do so. (iii) \emph{$n=720$}: formation completes (largest-clump 1.000,
control ratio 166--208$\times$) and the mass fraction saturates
(median 31.5\% vs.\ 31.7\% at $n=360$), but the alignment timescale
grows with $n$ -- one seed reaches only 0.752 by step 9000 and 1.000 by
step 13500. The $t$-window was tuned at $n=180$; at $n=720$ it is a
caveat, and we record it as such.

\subsection{What the meso chapter claims}

A heavy, dark, co-rotating, angular-momentum-bearing centre forms from
a uniform cloud of identical grains in a closed system, with every
ingredient of the outcome traced to a declared rule by knockout or dose
response, reproduced across sixteen seeds and three particle counts,
recovering from perturbations, and stated with its two honest
sensitivities (softening; alignment timescale at large $n$). By the
grading of Sec.~\ref{sec:standard} this is E3: robust emergence --
\emph{of the toy universe's own dark rotor}, and of nothing in the real
sky (Sec.~\ref{sec:negative}).

% =====================================================================
\section{Macro scale: disk signatures (application)}
\label{sec:macro}
% Deliberately framed as APPLICATION, not headline result.
% (v0.4: the hidden-mass ladder moved to Paper 5 per the split line --
%  optics/bookkeeping is the micro side.)
% - m=2 disk signature: spin dose response 0->1->2->3 on top of the
%   quadrupole contamination of the two-rotor placement (spin-0 control keeps
%   a weak m=2); multipole-controlled variants listed as future work (EXT-15).
% - Wording rule: "what toy-model signature a self-formed/defined rotor
%   imprints on a surrounding disk" -- never "explains galaxies".
TODO.

% =====================================================================
\section{A staged programme toward galaxy morphology (plan)}
\label{sec:plan}
% [第191便: PLAN section -- no results are claimed here; every gate is declared
%  before any measurement, in the spirit of Sec. II.]

This section is a plan, stated with its gates before any measurement,
and it claims no results. Eight target classes are staged in order of
increasing structure: an ordinary-star binary, a binary pulsar, a
black-hole binary, a globular cluster (with a dwarf-spheroidal
discriminating insert), an elliptical, a lenticular, a spiral, and a
barred spiral. Each class is built twice from transcribed observations:
an \emph{observation-reproduction build}, which includes a declared
analytic dark-halo term wherever the standard analysis of that class
requires one (with the halo knockout as its control; the binaries serve
as negative controls, since no halo is expected at that scale), and a
\emph{DFM build}, which replaces the halo by the toy universe's own
dark rotors (Sec.~\ref{sec:meso}) and is graded by the same standard.

All stages share one gate battery, fixed in advance: zero NaN and zero
clamping; conservation ledgers closed; convergence under halving of the
timestep and of the softening length; multi-seed reproduction;
perturbation recovery; declared time windows; knockout controls (halo,
rotor, and $k_{\mathrm{frame}}$); monotone dose responses where a driver
is claimed; held-out data judged only after the build is frozen; and a
four-value claim label on every published number. Three non-goals are
declared now: black-hole binaries stop before merger and no waveform
matching is attempted; the binary pulsar's orbital decay enters the
observation build only as an external bookkeeping term, never as an
engine axiom; and bar or arm responses driven by paired rotors are
labelled forced-$m{=}2$ controls, not spontaneous formation. The shared
correction $D_0$ is never refit per galaxy.

Three prerequisites precede stage one: a generalized observation schema
for equal-mass binaries (free centres under E6$'$-R -- the reaction rule
whose necessity Sec.~\ref{sec:solar} established), an analytic halo kit
for the observation builds, and an error/held-out declaration extending
the per-sample parameter audit. The dragging-excess series of
Sec.~\ref{sec:solar} is carried forward as the standing tension every
stage must report against its own observations.

% =====================================================================
\section{Numerical robustness summary}
\label{sec:robust}
% Cross-scale table: dt convergence, softening sensitivity, n scaling,
% multi-seed spreads, time windows -- one row per claimed phenomenon,
% with the honest exceptions highlighted (softening-sensitivity of mFrac;
% alignment timescale at n=720; conduction timescale ~6.8e4 steps).
TODO.

% =====================================================================
\section{Limitations and negative claims}
\label{sec:negative}
% Fixed list (kept verbatim in spirit from Papers 1--2, extended):
%  1. 2D only; no quantitative extrapolation to 3D.
%  2. Order-unity frame dragging is counterfactual.
%  3. No Lorentz invariance; absolute simultaneity.
%  4. No causal field propagation (instantaneous action).
%  5. Dimming is model-specific (not radiative transfer).
%  6. Fusion is coarse-grained; inheritance rules are model definitions.
%  7. Dark rotor != dark matter identification.
%  8. DFM black-hole analogue != GR black hole.
%  9. No astrophysical abundance or formation-rate claims.
% 10. Transcriptions are 2D and equatorial: an inclination above pi/2 is
%     carried as the retrograde orbit direction ONLY; tilts, third-body
%     perturbations and epochs are idealized as declared per sample.
% 11. Calibration agreement is not mechanism identification (Paper 3); the
%     dragging-excess series is declared, not evidence for dragging in the
%     real solar system.
% (The thermal-analogy limitation moved to Paper 5 with the micro sector.)
TODO.

% =====================================================================
\section{Reproducibility}
\label{sec:repro}
% Manifest table: phenomenon -> preset id -> harness -> seed set -> window ->
% commit -> result JSON. All harnesses run headless in one command
% (QA_TARGET=... node tests/exp-4-XX.mjs); the app itself is a single HTML
% file with 396+ machine-checked QA gates.
% Harness list for this paper: exp-4-84 (knockout/dose), exp-4-85 (multi-seed/
% n-scaling/perturbation/time-window), exp-4-89 (dt/softening), exp-4-92
% (n=720). Solar-calibration section: the transcription path QA (ai.obs-*),
% the per-sample QA gates behavior.venusReal / .marsMoonsReal /
% .plutoCharonReal / .uranusReal / .neptuneReal, and the committed source
% table paper/data/solar-observations.csv (archived URL per row).
% (exp-4-90/91/93 -- the micro harnesses -- are cited from Paper 5.)
TODO.

% =====================================================================
\section*{Acknowledgments}
TODO.

% =====================================================================
% References: Papers 1-2 (self), plus the small canonical set used there.
\begin{thebibliography}{9}
\bibitem{paper1} TODO: Paper 1 reference.
\bibitem{paper2} TODO: Paper 2 reference.
\bibitem{paper3} TODO: Paper 3 reference (reality calibration).
\bibitem{paper5} TODO: Paper 5 reference (micro-scale phase transitions).
\end{thebibliography}

\end{document}
