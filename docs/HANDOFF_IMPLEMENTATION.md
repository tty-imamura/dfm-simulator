# 実装指示書: 仮定物理シミュレーションアプリ (index.html)

テンプレート1(設計→実装ハンドオフ)準拠。実装担当 Tier: Sonnet 4.6 相当。
物理式は `docs/PHYSICS.md` の E1〜E10 と既定パラメータ表を正とし、本書では繰り返さない。
**v1.1(2026-07-15)**: 物理改訂(A11/A12/A13、E5′/E6′/E8′/E9/E10′/E11)の実装差分は
**付録B** を正とする(DERIVATIONS.md §7 の添付・詳細化)。
**付録F: v1.5 整合性修正(E1′ 統一・帳簿完全化・V9/V10/V11・プリセット誠実化)→
`docs/HANDOFF_FIX_V15.md` 参照(書き戻し欄・追記を含め同書が正)。**
**付録G: v1.6 UI修正(タブ閉時のキャンバス縦伸び)→ `docs/MODEL_ROUTING.md`
「v1.6 改訂」内の実行指示書が正(根因・差分2箇所・受け入れ条件・QA結果)。**
**付録H: v1.7 ψ統一(A7′/E7R/E8R・kLens 廃止・V12/V13/V16)→ `docs/DERIVATIONS.md` §11 と
`docs/MODEL_ROUTING.md`「v1.7 改訂」が正(式・較正値・QA結果)。**
**付録I: v1.9 サンプル全面刷新(旧16種廃止→新13種・軌道トレイル・V10b)→ 本書末尾の
付録I が正。旧サンプル向けの付録C/D/E の挙動指標は歴史的記録となる(数値の継承元)。**

> **現行仕様の注意(2026-07-16)**: 本文(§1〜)は v1.0 当時の指示書であり、本文中の
> 「6プリセット」等の個数は当時の値。**現行の内蔵プリセットは16種(v1.12)**で、
> 最新仕様は末尾の付録(J/K/L)ほど新しい。全体の受け入れ条件は `tests/qa.mjs`
> (1コマンド機械QA)が正本。

## 目的(1段落)

ユーザー提示の仮定物理法則(相対空間・決定力・スピン=熱)を、iPhone の Safari で動く
単一 HTML ファイルのインタラクティブシミュレータとして可視化する。6つの内蔵プリセットと、
Claude API(Haiku 4.5)によるプリセット追加機能を持つ。迷ったら「iPhoneで指1本で触れて、
法則の帰結(整列・平坦回転・熱平衡・光の湾曲・引きずり)が目に見えること」を優先する。

## 成果物

- `index.html` — アプリ本体。**単一ファイル・外部リソース参照ゼロ**(CDN/フォント/画像不可)。
- 変更禁止: `docs/` 配下(実装中に矛盾を見つけた場合は実装を止めて文書改訂を要求する)。

## インターフェース契約

### 1. プリセットJSON(内蔵・LLM生成・共通スキーマ)

```jsonc
{
  "name": "土星の環",            // 必須 string ≤30字
  "emoji": "🪐",                // 任意 string
  "description": "説明…",       // 必須 string ≤200字
  "camera": { "scale": 170 },   // 必須。画面短辺の半分が scale ワールド単位に対応
  "world": { "boundary": "none", "size": 0 },  // 必須。boundary: none|box|circle。size=半幅/半径
  "physics": { /* PHYSICS.md の既定パラメータ表のキー(dt,substeps除く)。部分指定可、既定値にマージ */ },
  "bodies": [ /* 下記ジェネレータの配列。合計粒子数は600にクランプ */ ],
  "rays": { "n": 26, "spread": 0.85 },          // 任意。光線: 左端から n 本、縦に spread×高さへ分散
  "overlays": { "rotationCurve": false, "tempHistogram": false, "field": false }  // 任意
}
```

ジェネレータ4種(**全フィールド必須**。LLM出力も同形):

```jsonc
{ "type":"single", "m":1200, "x":0, "y":0, "vx":0, "vy":0, "spin":0.8, "pinned":false }
{ "type":"ring", "n":340, "cx":0, "cy":0, "rIn":60, "rOut":150,
  "mMin":0.05, "mMax":0.3, "spinMin":0, "spinMax":0,
  "vMode":"kepler",            // kepler|omega|none
  "aroundMass":1200,           // vMode=kepler時の中心質量 (v=√(G·aroundMass/r))
  "omega":0,                   // vMode=omega時: v = omega·r·direction
  "vNoise":0.06,               // 速度に±vNoise比のジッタ
  "direction":1,               // 1|-1 (反時計|時計)
  "pinned":false }             // true: 力を受けず、vMode=omegaなら円軌道レール上を回る
{ "type":"disk", "n":380, "cx":0, "cy":0, "radius":260,
  "mMin":0.5, "mMax":1.2, "spinMin":0, "spinMax":0,
  "vMode":"flat",              // kepler|rigid|flat|random|none
  "aroundMass":600,            // kepler用
  "vScale":1.6,                // kepler:倍率 / rigid:角速度ω / flat:速さ / random:速さ
  "direction":1 }
{ "type":"box", "n":120, "cx":-100, "cy":0, "w":180, "h":360,
  "mMin":1, "mMax":1, "spinMin":2, "spinMax":3, "vScale":2.5 }   // ランダム方向・速さ~vScale
```

### 2. 内蔵プリセット(6種、上記スキーマのJSオブジェクトとして定義)

| id | 主要設定(camera.scale / world / physics上書き / bodies / overlays) |
|---|---|
| saturn 🪐 土星の環 | 170 / none / {muF:0.6, gammaN:0.5, timeScale:4} / single(m1200,spin0.04) + ring(n340, 60..150, kepler around 1200, vNoise0.06) / なし |
| protodisk 🌀 原始惑星系円盤 | 260 / none / {muF:0.5, gammaN:0.6, kappaS:0.1, kRep:0.5, timeScale:4} / single(m400) + disk(n320, r220, m0.2..1.5, kepler around 400 ×0.75) / なし |
| galaxy 🌌 銀河の回転曲線 | 300 / none / {D0:1, muF:0.1, gammaN:0.1, kRep:0.5, softening:4, timeScale:4} / single(m600, spin0.05) + disk(n380, r260, m0.5..1.2, flat v=1.6) / rotationCurve |

> **改訂 2026-07-13(安定初期値)**: 大質量天体のスピンは「表面速度 s·R が軌道速度と同程度以下」
> になるよう選ぶこと(例: m=1200, R≈42 なら s≦0.05 程度)。大きなスピンはスピン引きずり(E2/E3)
> により周囲の軌道速度を超える回転流を作り、系全体を吹き飛ばす。
> 原始円盤は「弱い回転の剛体雲」ではなく「サブケプラー円盤(kepler×0.75)」で開始する方が
> 穏やかに収縮し、蒸発しない。
| gas 🔥 気体の熱平衡 | 240 / box size200 / {G:0.05, D0:50, kFrame:0.2, kRep:2, muF:0.8, kappaS:0.15, timeScale:2} / box左(n120, spin0, v0.3) + box右(n120, spin2..3, v2.5) / tempHistogram |
| lensing 💡 重力レンズと時間 | 300 / none / {Kt:150, timeScale:1}〔v1.7 再校正。旧 {kLens:0.004, Kt:30}〕 / single(m1500, pinned, spin0.5) + single(m500, x120, y-90, pinned) + rays(n26, 0.85) / field |
| mach 🪣 マッハのバケツ | 220 / none / {G:0.02, D0:0.5, timeScale:2} / ring(n14, r150, m80, omega0.012, pinned, spin0.5) + disk(n40, r80, m0.5, none) / なし |

数値は初期案。**QA(品質ゲート)で「帰結が目に見える」ことを基準に timeScale・半径・質量の調整可**。
それ以外の仕様変更は不可。

### 3. localStorage キー

| キー | 内容 |
|---|---|
| `hp_key` | Claude APIキー(平文。端末外に出るのは api.anthropic.com への送信のみ) |
| `hp_model` | 実行時モデルID(既定 `claude-haiku-4-5`) |
| `hp_saves` | 保存配列: `[{name, comment, savedAt(ISO), presetId, presetName, physics{...全キー}, cameraScale}]` |
| `hp_custom_presets` | LLM生成プリセット配列(検証済みプリセットJSON + `id:"custom_<epoch>"`) |

### 4. 実行時LLM呼び出し

`docs/HANDOFF_RUNTIME_LLM.md` のシステムプロンプト・スキーマ・段構えロジックを**そのまま**焼き込む。
エンドポイント/ヘッダ契約:

```
POST https://api.anthropic.com/v1/messages
content-type: application/json
x-api-key: <hp_key>
anthropic-version: 2023-06-01
anthropic-dangerous-direct-browser-access: true
```

### 5. デバッグフック(品質ゲートが使用)

`window.HP = { sim, loadPreset(id), presets, running() }` を公開する。
`sim` は `{n, x, y, vx, vy, spin, t}` を参照できること(TypedArray可)。

## 実装手順

1. HTML骨格 + CSS(ダークテーマ、`viewport-fit=cover`、`user-scalable=no`、safe-area対応、
   ボタン最小44px、`touch-action:none` はcanvasのみ)。レイアウト: ヘッダ(タイトル+プリセット
   `<select>`)/ canvas(伸縮)/ トランスポートバー(⏮ 初めから・▶/⏸ 実行停止再開・速度select)/
   タブバー(説明・パラメータ・セーブ・AI)+ パネル(max-height 42vh、スクロール)。
2. 物理エンジン: SoA(Float32Array)で m,x,y,vx,vy,spin,R,I,pinned,rail{cx,cy,r,ang,omega},
   uPrevX,uPrevY,tau。ステップ関数は PHYSICS.md §5 数値スキーマの順序どおり。
3. プリセットローダ: スキーマ→粒子生成(乱数は mulberry32、`seed` はリセット毎に固定し直す)。
   ring/disk/box の位置サンプリングは、既に置かれた大質量天体(m≥40)の内部を避けて
   リトライする(最大30回)。深い初期重なりは接触ばねの持続的な押し出しを生み不安定の原因になる。
4. 描画: 背景微星、粒子(色=|スピン|正規化: 青→白→橙→赤、半径=R・表示は30px上限+大天体グロー)、pinnedは輪郭付き、
   トレイル(オフ可)、fieldオーバーレイ(48×48グリッドのW値を淡色で、5フレーム毎更新)、
   光線(白→黄のポリライン、毎フレームE8を左端から再積分、ステップ長=scale/120、320step上限)、
   rotationCurve(右下 140×90px: 半径10ビンの平均接線速度の折れ線+ケプラー参照曲線)、
   tempHistogram(右下: 左半分/右半分の平均温度バー+時系列)、
   選択粒子の固有時計(タップで選択、τ/tの円グラフ)。ヘッダ下に t・N・fps 表示。
5. カメラ: 1本指ドラッグ=パン、2本指ピンチ=ズーム(タッチイベント、preventDefault)。
   マウス(ドラッグ+ホイール)も対応。
6. パラメータタブ: PHYSICS.md の表の全キー+timeScale を
   **スライダ+直値入力欄**の2系統で編集できるようにする。変更は**即時** `sim.params` に反映
   (再スタート不要)。
   - 桁をまたぐ量(G・D₀・Kt・timeScale)は**対数スライダー**(内部0..1000の指数マップ)、
     それ以外は微調整しやすい細かいステップの線形スライダー。
   - 直値入力欄はスライダー範囲外の値(0を含む)も受け付け、バリデータの値域(CLAMPS)でクランプ。
     不正入力は元の値に戻す。inputmode="decimal"。
   - 「粒子の初期配置(bodies・N)はプリセット選択/⏮で反映」の注記。既定値に戻すボタン。
7. セーブタブ: 名前+コメント入力→保存で `hp_saves` へ。一覧(名前・コメント・日時・元プリセット)
   に 読込(=physicsを適用し該当プリセットを⏮)・削除。全体をJSONでエクスポート(クリップボード)
   /インポート(textarea貼り付け)。
8. AIタブ: APIキー入力(type=password、保存ボタン、「キーはこの端末のlocalStorageにのみ保存」
   注記)、モデル選択(haiku既定/sonnet)、要望textarea、生成ボタン(生成中はスピナー+無効化)、
   結果メッセージ領域。成功→ hp_custom_presets へ追加、プリセットselectに「🤖名前」で追加し
   即ロード。カスタムプリセットの削除ボタン。
9. NaNガード: 毎フレーム先頭粒子群の座標をチェックし、NaN検出で自動停止+「数値が発散しました。
   パラメータを見直すか⏮してください」表示。
10. 説明タブ: 各プリセットの見どころ+操作方法+法則の要約(PHYSICS.mdの短縮版)を静的表示。

## エッジケースと対応(表)

| 入力/状況 | 期待動作 |
|---|---|
| APIキー未設定で生成ボタン | 「APIキーを設定してください」表示、リクエストしない |
| APIキー不正(401) | 「APIキーが無効です」表示 |
| 429 / 529 | 「混雑しています。しばらく待って再試行してください」 |
| オフライン/fetch例外 | 「ネットワークに接続できません」 |
| stop_reason≠"end_turn"(refusal/max_tokens) | 「生成に失敗しました(理由)」表示、登録しない |
| LLM出力がスキーマ検証NG | 失敗理由を添えて同モデルに1回だけ再試行→なお失敗なら Sonnet 5 へ→なお失敗ならエラー表示 |
| 粒子合計 >600 | 600に比例縮小(先頭ジェネレータ優先)し、その旨を表示 |
| 数値が範囲外 | バリデータがクランプ(範囲は HANDOFF_RUNTIME_LLM.md の表) |
| 直値入力が範囲外/不正 | CLAMPSでクランプ / 不正文字列は元の値へ戻す |
| ジェネレータ位置が大質量天体の内部 | 生成時にリサンプリングで回避(最大30回) |
| 保存名が空 | 「無題」+日時で保存 |
| localStorage書込例外(容量) | 「保存できませんでした(容量)」alert |
| インポートJSONが不正 | 「読み込めませんでした」表示、既存データ無変更 |
| 画面回転/リサイズ | canvasをdevicePixelRatio込みで再設定、描画継続 |
| ⏸中のパラメータ変更 | 反映され、▶で継続 |
| ⏮ | 現在のphysicsパラメータを**保持したまま**粒子を初期配置し直し、t=0、停止状態にする |
| bodies空のプリセット | 空宇宙を表示(エラーにしない) |

## 使用技術の制約

- Vanilla JS (ES2020)、ライブラリ・ビルド・CDN一切禁止。`<script>` 1ブロック。
- フォントはシステムフォント。絵文字はUnicodeのまま使用可。
- LLM/ユーザー由来の文字列は **textContent でのみ** DOMに入れる(innerHTML禁止)。
- コメントは日本語で、セクション見出し(`// ===== 物理エンジン =====` 等)を付ける。

## 正例・負例

```js
// ✅ 正例: LLM由来文字列の表示
li.querySelector('.name').textContent = preset.name;
// ❌ 負例: XSS経路になる
li.innerHTML = `<b>${preset.name}</b>`;

// ✅ 正例: E4 ソフトニング付き重力(対称なので j>i で半分だけ回す)
const d2 = dx*dx + dy*dy + eps2, inv = 1/(d2*Math.sqrt(d2));
// ❌ 負例: 距離0で発散 / 全対を2回計算
```

## 受け入れ条件(全て検証可能な形で)

- [ ] `<script>` 抽出後 `node --check` がエラー0
- [ ] headless Chromium(390×844)で6プリセット全てをロードし、各120フレーム進めて
      console error 0件・粒子座標にNaNなし(`window.HP` で検査)
- [ ] ▶→⏸→▶→⏮ の操作列が動作(⏮後 t=0・停止)
- [ ] パラメータスライダ変更が `HP.sim.params` に即反映
- [ ] 保存→リロード→読込 で physics が復元される
- [ ] AIタブ: キー未設定時にエラーメッセージ表示(実API呼び出しはQA対象外、バリデータは
      few-shot例4件が全て検証合格・壊した例が不合格になることを node で確認)
- [ ] 横スクロールが発生しない(document.scrollingElement.scrollWidth ≤ innerWidth)

## 禁止事項

- api.anthropic.com 以外への通信。外部へのAPIキー送信・埋め込みアナリティクス。
- LLM出力の eval / Function 化(JSONとしてのみ解釈)。
- docs/ の仕様と異なる式・スキーマの発明(発見した矛盾は文書改訂として報告)。
- プリセット調整の名目で physics の式(E1〜E10)自体を変えること。

---

## 付録B: v1.1 物理改訂の実装差分(DERIVATIONS.md §7 の添付)

対象ファイルは `index.html` のみ。式の正は PHYSICS.md v1.1(E5′/E6′/E8′/E9/E10′/E11)。
以下は実装上の確定事項であり、判断の余地を残さない。

### B1. パラメータ追加(4件)

`DEFAULT_PHYSICS` / `CLAMPS` / `PARAM_DEFS` に追加する:

| キー | 既定 | CLAMPS | スライダー | グループ |
|---|---|---|---|---|
| cLight | 60 | [1, 10000] | log, lo:5, hi:1000 | 法則 |
| bM | 1.0 | [0.001, 1000] | log, lo:0.01, hi:100 | 法則 |
| etaRad | 0 | [0, 1] | log, lo:1e-6, hi:0.1(0 は直値入力で設定) | 法則 |
| pRad | 4 | [1, 6] | lin, lo:1, hi:6, step:0.5 | 法則 |

実行時LLMプロンプト(SYSTEM_PROMPT)と HANDOFF_RUNTIME_LLM.md は**意図的に変更しない**。
LLM が新キーを出力しなくてもバリデータの既定値マージで補完されるため、few-shot 4例は
そのまま有効。LLM 出力に新キーが含まれた場合は CLAMPS の値域でクランプされる。

### B2. エンジン改修(sim.step の4フェーズ化)

PHYSICS.md v1.1 §5「数値スキーム」の順序どおり。実装の要点:

1. **対距離キャッシュ**: 全対ループで計算した d_ij を `S.pairD`(Float32Array、長さ
   n(n−1)/2、インデックス `i*n - i*(i+1)/2 + (j-i-1)`)に保存し、反作用対ループで再利用
   (sqrt の二重計算を避ける)。alloc 時に確保。
2. **E5′**: `f = kRep * muIJ * (omI*omI + omJ*omJ)`、`a_i += f/m_i * (dx,dy)`、
   `a_j -= f/m_j * (dx,dy)`。omI/omJ は E2 の ω_i(d), ω_j(d)(既存計算を流用)。
3. **E9 摩擦(符号・作用線の改訂)**: 実効腕 `Li = Ri*d/(Ri+Rj)`, `Lj = Rj*d/(Ri+Rj)`。
   `vs = (v_i−v_j)·t̂ − spin_i*Li − spin_j*Lj`、`jt = (muF/3) * mu * vs`
   (mu は換算質量。pinned 側は invM=0 として K=invMi+invMj で計算し、jt = muF*vs/(3K))。
   適用: `v_i −= jt*invMi*t̂; v_j += jt*invMj*t̂; spin_i += Li*jt/I_i; spin_j += Lj*jt/I_j`
   (pinned はスピン変更なし)。旧実装は vs の符号と Δs の符号が PHYSICS.md と不一致で、
   L を系統的に破っていた(修正後は V1 が合格する)。
4. **法線減衰の放射帳簿**: 減衰インパルス適用前後の並進運動エネルギー差を `S.radE` に加算。
5. **E10′**: 拡散重みを `I_j/(I_i+I_j)` に変更。共通係数
   `c = kappaS*(s_j−s_i)*gg/(I_i+I_j)` を作り `ds_i += c*I_j; ds_j −= c*I_i`
   (この形なら I_i·Δs_i = −I_j·Δs_j が浮動小数点でも厳密)。
6. **E6′ 反作用**: 粒子ループで Δp_i = m_i·kFrame·Δu(クランプ後)を `S.dpx/S.dpy` に保存。
   位置更新はまだしない。反作用対ループで各対 (i,j) につき双方向に:
   `phi = (m_j/(pairD+eps)) / (D0 + sumW_i)`; j が非pinned なら
   `v_j −= phi*dp_i/m_j` とし、残余トルク `n = dx*(phi*dpy_i) − dy*(phi*dpx_i)` を
   `spin_i −= n/(I_i+I_j); spin_j −= n/(I_i+I_j)` として移譲。j が pinned なら
   運動量 −phi*dp_i とトルク [r_i×phi*dp_i]_z をリザーバへ
   (`resPx −= phi*dpx_i` 等、`resL −= x_i*phi*dpy_i − y_i*phi*dpx_i`)。
   背景分 `phiBg = D0/(D0+sumW_i)` も同じ式でリザーバへ。最後の粒子ループで位置・境界・τ 更新。
7. **E11 冷却**: etaRad>0 かつ s≠0 のとき `T = I*s*s; lam = etaRad*T^pRad;
   dsC = lam*dt/(I*|s|)` を `min(dsC, 0.5*|s|)` にクランプして |s| を減らす。
   `radE += ½I(s_before²−s_after²); radL += I*(s_before−s_after)`。

帳簿変数は `S.resPx, S.resPy, S.resL, S.radE, S.radL`(build でゼロ初期化)。
検証恒等式: P+resP 一定、L+resL+radL 一定(D₀=0・pinnedなし・境界なしなら resP=resL=0)。

### B3. E8′ 光線の媒質随伴

光線積分を共有関数 `traceRay(S, x0, y0, cx0, cy0, maxSteps, dl, emit)` に括り出し、
描画(drawRays)と検証(V8)の双方から使う。1ステップ:
- 重い天体(m ≥ RAY_MASS_MIN)のみで n, ∇n, **u** を評価(既存の性能近似を維持)。
  **u** は E3 と同形: `u = Σ w_j(v_j + ω_j ẑ×(r−r_j)) / (D0 + Σ w_j)`、ω_j は E2。
- 方向更新は従来どおり(フェルマー則)、位置更新を `r += ĉ·dl + u·(n/cLight)·dl` に変更。

### B4. 表示の改修

- **色マップのキー**: `T_i = ½·m_i·R_i²·s_i²`(= I s²)。正規化は全粒子 T の
  **90パーセンタイル**(EMA 平滑、係数 0.05)とし、`t = min(1, √(T/p90))` を
  既存 tempColor に渡す(単一の大質量天体がスケールを飽和させないため。
  パレット自体は変更しない)。
- **温度ヒストグラム**: T の定義を同じ I s² に変更、ラベルを「平均温度(Is²)左/右」に。
- **選択粒子の情報行**: `T=… λ=…`(λ = bM/T、T=0 のときは「λ=—」)を追記。
- **保存量モニタ**: 表示グループに「保存量モニタ」トグル(既定オフ)を追加。
  オンのとき HUD 2行目に `P=(Px,Py) L=… | 帳簿 P=(…) L=… 放射E=…` を15フレーム毎に更新表示。
  P・L は B2 の恒等式の左辺(粒子系のみ)、帳簿はリザーバ変数を表示する。

### B5. 検証フック(HP.verify)と受け入れ条件の追加

`window.HP.verify = { v1(), v2(), v8(), all() }` を実装する。各関数は
`{id, pass:boolean, detail:string, value:number}` を返す(all は配列)。UI には出さない。

- **v1(T7 保存則)**: makeSim で独立に組み立てる。physics: D₀=0, kFrame=1, G=1, kRep=1,
  muF=0.5, gammaN=0.4, kappaS=0.05, etaRad=0, softening=2, boundary none。
  粒子: 疑似乱数(seed 固定)で disk 状に 48 個(m 0.5〜2, |v|≤1.2, spin −2〜2)+
  m=30 の単体1個。1000 ステップ(dt=0.016)実行し、
  `|ΔP|/P_scale < 1e-3` かつ `|ΔL|/L_scale < 1e-3` で合格。
  スケール: P_scale = Σ m|v|(初期)、L_scale = Σ|m(r×v)| + Σ I|s|(初期)。
- **v2(D1 分光温度計)**: 検証粒子(m=2, R=radiusScale√2, s=1.7)につき
  T = Is²、λ = bM/T を計算し、`s' = √(bM/(I·λ))` で逆算。相対誤差 < 5% で合格
  (色マップと HUD が同じ T 定義を共有していることの配線検査を兼ねる)。
- **v8(T8 非対称レンズ)**: 中央に pinned 単体(m=1500)を置き、spin = +0.5 と −0.5 の
  2 ケースで、y = ±60 の対称な平行光線を traceRay で 320 ステップ積分。
  各ケースの上下光線の湾曲角差 Δθ(spin) を測り、`sign(Δθ(+)) ≠ sign(Δθ(−))` かつ
  `|Δθ| > 1e-4 rad` で合格(v1.7: Kt=500=旧 kLens 0.004 と湾曲係数が厳密等価, cLight=60)。
- **V3〜V7(銀河 r_flat・環の方向選択・𝒟効果・歳差・軌道移動)は長時間実行を要するため
  自動テスト列に含めない。** DERIVATIONS.md §6 の手順どおり手動実験として実施し、
  結果は THEORY SYNTHESIS / DERIVATIONS 側に記録する(実装フェーズの受け入れ条件外)。

受け入れ条件(既存リストに追加):
- [ ] `HP.verify.all()` で v1, v2, v8 が全て pass
- [ ] 既存6プリセットが v1.1 エンジンでも 120 フレーム console error 0・NaN なし
- [ ] etaRad=0(既定)のとき、v1.0 と比べてデモの見た目の破綻がない(目視)

### B6. 禁止事項(付録B 固有)

- 実行時LLMプロンプト・few-shot・HANDOFF_RUNTIME_LLM.md の変更(B1 の方針どおり不要)。
- tempColor のパレット変更(キーの変更のみ)。
- 旧 E5/E6/E10 の式を kFrame 等のフラグで残すこと(改訂式へ完全置換する。
  ニュートン退化は従来どおり kFrame=0 で得られる)。

---

## 付録C: v1.2 熱・放射系サンプル4種+スペクトルオーバーレイ(2026-07-15)

対象ファイルは `index.html` のみ。物理式は変更しない(E1〜E11 をそのまま使う)。
以下の数値は設計値であり、**QA(C4)の挙動指標を満たす範囲でのみ**
physics 値・質量・スピン・配置の調整可。調整結果は本付録に書き戻す。

### C1. スペクトルオーバーレイ(新規)

- プリセットスキーマの `overlays` に `spectrum:boolean` を追加(既定 false)。
  `validatePreset` の ov 既定値・表示タブのトグル(ラベル「スペクトル」)にも同キーを追加する。
- `drawSpectrum()`: 右下 150×92 パネル(rotationCurve/tempHistogram と同じ位置・配色。
  同時使用は想定しない)。10 tick 毎に再計算:
  1. 各粒子の T=½mR²s²。T>1e-9 の粒子につき λ=bM/T、l=log10(λ)、光度 Λ=T^pRad。
  2. l を 28 ビンに分ける。軸範囲 [lLo,lHi] は現在の l の min/max を ±5% パディングし
     EMA(係数0.1)で平滑。全粒子が同値なら ±0.5。対象粒子が 0 個なら「放射なし」とだけ表示。
  3. ビンの棒高 = max(2px, (ΛBin/ΛMax)^0.15 × (H−30))。指数 0.15 は QA 書き戻し
     (旧 0.25 では Λ∝T⁴ の比が ~10⁶ になる spectrum プリセットで低温の輝線が不可視)。
     棒の色 = ビン内平均 T を粒子色と同じ正規化(√(T/tempP90EMA)、1 で飽和)で tempColor に渡す。
  4. ラベル: 上「放射ピーク λ=b_m/T」、下端左「短λ=高温」右「長λ=低温」、
     軸両端に λ の実数値(fmt)。λ は右向きに増加(=左が高温)。
  5. 軸範囲の EMA 状態はモジュール変数のため、`sim.t` が前回より小さくなったら
     (⏮・プリセット切替=再構築)リセットする(QA 書き戻し: 前のプリセットの軸を引きずる)。
- render() 内の呼び出しは既存2オーバーレイと同列に `sim.overlays.spectrum` で分岐。

### C2. 内蔵プリセット4種(BUILTIN_PRESETS へ追記。スキーマは契約1と同一)

| id | 名称 | 主要設定 |
|---|---|---|
| spectrum | 🌈 スピンとスペクトル | scale260 / none / {G:0, kFrame:0, kRep:0, muF:0, gammaN:0, kappaS:0, etaRad:7e-11, pRad:4, timeScale:1} / single×6: m=8, y=0, x=−200,−120,−40,40,120,200, spin=0.6,1.2,1.8,2.4,3.0,3.6, 全て pinned:false / overlays{spectrum} |
| convection | ♨️ 加熱の対流 | scale240 / box 190 / {G:4, D0:40, kFrame:0.2, kRep:1, muF:0.5, gammaN:0.3, kappaS:0.4, etaRad:1.5e-5, pRad:4, radiusScale:3, softening:4, timeScale:2} / single(m5000, (0,900), pinned)=重力源 + ヒーター18個: single m=1 pinned y=186, x=−170..170(20刻み), spin=**全て+8** + box(n230, cy−10, 360×330, m0.3, spin0..0.3, v0.4) / なし |
| phase | 🧊 固体・液体・気体 | scale260 / box 240 / {G:0.7, D0:30, kFrame:0.3, kRep:0.8, muF:0.6, gammaN:0.85, kappaS:0.02, radiusScale:1.6, timeScale:2} / disk固体(n70, cx−160, r40, m1.2, spin0, none) + disk液体(n70, cx0, r36, m1, spin1.8..2.2, random v0.3) + disk気体(n60, cx160, r38, m0.15, spin8..10, random v3) / なし |
| radcool | 🌠 放射で冷えて固まる星 | scale320 / circle 300 / {G:1, D0:5, kFrame:0.5, kRep:1, muF:0.5, gammaN:0.5, kappaS:0.08, etaRad:3e-4, pRad:4, timeScale:2} / disk(n170, r110, m0.8..1.2, spin2..3, random v0.8) / overlays{spectrum} |
| accretion | 💫 降着円盤の摩擦発光(2026-07-15 追加) | scale260 / none / {G:1, D0:2, kRep:0.3, muF:0.9, gammaN:0.5, kappaS:0.02, etaRad:0.02, pRad:4, timeScale:4} / single(m1200, spin0.05, pinned) + ring順行(n250, 60..180, kepler, vNoise0.05, dir+1) + ring**逆行**(n70, 70..160, kepler, vNoise0.05, dir−1) / overlays{spectrum} |
| collision | 💥 二星団の衝突加熱(2026-07-15 追加) | scale260 / none / {G:2, D0:2, kRep:0.5, muF:1, gammaN:0.3, kappaS:0.02, etaRad:1e-4, pRad:4, timeScale:4} / disk×2(各n85, 中心(∓120,∓40), r36, m2..2.5, spin0, none)= 相互重力で自由落下衝突 / overlays{spectrum} |

**QA 書き戻し(2026-07-15、初期値安定化ループの結果。上表は反映済みの確定値)**:
1. **ヒーターのスピン符号は全て同符号にする(±交互は不可)** — スピンは符号付きで拡散
   (E10′)するため、±交互の熱浴では加熱がバルクで打ち消され T≈0 になる(対照実験:
   ±10 交互で成層比 1.3〜1.8 / 同符号 +8 で 8〜11.5。2シードで確認)。同符号による
   正味角運動量の注入は D₀=40・壁反射の下で軽微(時間平均の平均速さ 1.13)。
2. **対流の η_rad は 1.5e-5 の桁** — 当初の 4e-4〜1e-2 では Λ∝T⁴ が強すぎ、浮力に必要な
   スピン(s≈4〜6、斥力加速度 ≳ g≈0.04)に達する前に冷えて対流が立たない。
   床の加熱・放射の釣り合いで床面 s≈5.8 になるよう κs=0.4 と対で選定。
3. **固体クラスタは「粗な冷雲(r40)→重力収縮で結晶化」で開始する** — 初期半径 32 の密な
   配置は生成時の粒子間重なりがばね解放で運動エネルギーを注入し(法線減衰 γn は接近時
   vn<0 のみ作用し、初期圧縮の解放は減衰されない)、クラスタが飛散する。
4. **気体クラスタは m=0.15・spin8..10・v3** — 旧値(m0.4, spin2.2..2.8, v1.2)では摩擦・
   減衰が初期速度を先に食い、自己重力束縛が勝って蒸発しない(t77 で拡散 2%)。
   スピン斥力主導の蒸発に変更(t115 で拡散 67%)。
5. **radcool の収縮判定は RMS でなく中央値半径** — RMS は境界まで飛んだ少数の外れ値に
   支配される。η_rad は 3e-4(1.3e-4 では t600 の冷却 41% は合格するが収縮が遅い)。
6. **accretion は逆行流(2割)の混合で発光を持続させる** — 閉じた一方向円盤は
   内側から円軌道化して静穏・低温になり、温度勾配が逆転する(vNoise を上げても、
   中心天体のスピンによる撹拌・境界層加熱でも解消しない: いずれも一過性)。
   順行 250 + 逆行 70 のすれ違い摩擦は角運動量を対で打ち消し、
   **本物の降着**(中央値半径 118→43)と持続発光(radE 562)を生む。
   なお「内側ほど高温」はこの宇宙では**不成立** — 落下後の高密度地殻は接触摩擦で
   冷えて固まる(三態デモと同じ帰結)ため、受け入れ条件からも除外した。
7. **collision の初期間隔は ±120** — 星団は相互落下(t_ff≈200)より先に内部崩壊
   (t≈25)するため、±80 では崩壊の閃光と衝突の閃光が混ざる。±120 なら
   崩壊→静穏(平均T<0.003)→加速落下→衝突(T が30倍スパイク+radE 1.39倍)→
   合体残骸の冷却、と各フェーズが分離する。回転支持(vMode rigid)で内部崩壊を
   防ぐ案は、すれ違い散乱して合体しないため不採用。

**v1.5 追補(付録F)**: E1′(Plummer 型決定力)起因の回帰調整として collision の
gammaN を 0.3→0.25 に変更(kFrame=1 のため場の変化で衝突ジオメトリが変わり、
T峰が判定値を 2% 割った。詳細は `docs/HANDOFF_FIX_V15.md` 書き戻し欄)。

設計根拠(調整時に保つべき不変条件):
- **spectrum**: 相互作用を全て切り(G=kRep=muF=κs=0)、放射冷却だけを残す。
  T=I s²(I=46.08)で λ=b_m/T が s の2乗に反比例すること、Λ∝T⁴ により
  高スピン端から順に冷える(選択的冷却)ことが見どころ。η_rad は「s=3.6 の粒子が
  t≈40 で目に見えて冷え、s=0.6 の粒子はほぼ不変」の桁(≈7e-11)。
- **convection**: 重力は遠方の大質量(box から d≈700〜1300)で box 内をほぼ一様な下向き
  g≈0.017〜0.04 にする。加熱は床の pinned ヒーター(スピン固定=温度一定の熱浴。
  接触摩擦+スピン拡散で伝熱)、冷却は η_rad·T⁴(上昇して床から離れた粒子が冷える)。
  ヒーターのスピンは**全て同符号**(QA 書き戻し1参照。±交互は拡散で打ち消され加熱不能)。
- **phase**: 同一法則下で温度(スピン)だけを変えた3クラスタ。束縛条件は
  v_esc=√(2GM/r) に対し、固体: v≪v_esc かつ s=0(接触ばね+摩擦で結晶化)、
  液体: v<v_esc(束縛だが流動)、気体: 実効速度>v_esc(蒸発して箱に充満)。
- **radcool**: 提案サンプル(タスク④)。初期は圧力(kRep)と乱雑速度で支えた高温雲。
  E11 で放射→T低下→圧力喪失→重力収縮→接触・摩擦で結晶化(気体→液体→固体の逆行)。
  スペクトルのピークが長波長側へ移動し続けるのが見どころ。

### C3. 変更許可箇所(これ以外は変更禁止)

1. `BUILTIN_PRESETS` 配列への4要素追加(既存6要素は変更しない)。
2. `validatePreset` の ov 既定値への `spectrum:false` 追加。
3. `drawSpectrum()` の新設と `render()` からの分岐呼び出し1行。
4. 表示タブのオーバーレイトグル配列への1行追加。
5. 説明タブ・SYSTEM_PROMPT は変更しない(新プリセットの説明は description で完結させる)。

### C4. 受け入れ条件(QA スクリプトで機械判定)

回帰: 既存6プリセット各120フレームでコンソールエラー0・NaNなし、`HP.verify.all()` 全pass、
few-shot 4例が validatePreset 合格。新プリセットは各項目とも headless Chromium(390×844)で判定:

(判定値は 2026-07-15 の QA 実測に合わせて確定。スナップショット1点の判定はシード雑音
(N=230 で σ≈0.07)に埋もれるため、対流は**時間平均**で判定する)

- [ ] 4プリセットとも下記フレーム数を実行してコンソールエラー0・NaNなし
- [ ] **spectrum**(1200フレーム): s=3.6 の粒子のスピンが 20% 以上減少し、s=0.6 の粒子は
      2% 未満の変化(Λ∝T⁴ の選択的冷却)。全粒子の位置が初期値から不動(|Δr|<0.01)
      〔実測: 22.0% / 0.000% / Δr=0〕
- [ ] **convection**(暖機300フレーム後、50フレーム毎×18サンプルの時間平均):
      下1/3 の平均T / 上1/3 の平均T > 3(温度成層)、corr(T, −vy) > 0.15(対流熱輸送:
      熱い粒子ほど上昇)、平均速さ < 2.5(噴水化していない)
      〔実測: 成層比 11.08 / corr 0.524 / 平均速さ 1.13〕
- [ ] **phase**(1800フレーム): 固体: 重心からの平均距離 <40 かつ 平均T <0.1、
      液体: 平均距離 <75(束縛)、気体: 60% 以上が初期中心 (160,0) から 100 超に拡散、
      温度序列 T固 < T液 < T気
      〔実測: 31.5 / T固0.019 / 液53.2 / 気67% / 0.019<0.144<1.416〕
- [ ] **radcool**: 600フレームで平均|s| が初期の 60% 未満(放射冷却)、
      1200フレームで**中央値**半径が初期の 70% 未満(収縮。RMS は外れ値支配のため不使用)
      〔実測: |s| 2.48→1.06 / 中央値半径 83.3→41.1〕
- [ ] **accretion**(600フレーム毎×4サンプル=2400フレーム): 中央値半径@1200 < 初期の60%
      (降着)、radE > 300 かつ全サンプルで非減少(摩擦発光)、平均T の峰 > 終値×2
      (光って冷える)、生存率(20<r<300)≥ 95%
      〔実測: 半径118→43 / radE562・単調増加 / T峰0.030→終値0.005 / 生存100%〕
- [ ] **collision**(80フレーム毎×30サンプル): 衝突前(サンプル8〜13)の平均T峰 < 0.01
      (静穏な落下)、衝突後の平均T峰 > max(10×静穏時, 0.05)(衝突加熱)、
      radE 跳躍 > 1.2倍(衝突閃光)、終状態: 重心間 < 40(合体)かつ 平均T < 峰/3(残光減衰)
      〔実測: 静穏0.0025 / 峰0.091 / radE 1.39倍 / 重心間29・終値T 0.0098〕
- [ ] スペクトルオーバーレイ描画パスを通した状態で(spectrum/radcool/accretion/collision)
      描画例外が出ない
- [ ] `node --check` 合格、横スクロールなし(既存条件の維持)

2026-07-15 QA 結果: 回帰10項目+新規16項目 = **26/26 PASS**(headless Chromium 390×844)。
同日追補(accretion/collision): 回帰込み **35/35 PASS**。

### C5. 禁止事項(付録C 固有)

- 物理式・エンジン(makeSim/step/traceRay/HP.verify)の変更。
- 既存6プリセットの数値変更。tempColor パレット変更。
- SYSTEM_PROMPT / few-shot / HANDOFF_RUNTIME_LLM.md の変更(overlays.spectrum は
  バリデータ既定値マージで補完される。B1 と同方針)。

---

## 付録D: v1.3 サンプル3種 — 理想気体・摩擦熱と熱伝導・星の形成(2026-07-15)

対象ファイルは `index.html` のみ。物理式・エンジン・オーバーレイは一切変更しない
(E1〜E11 と既存 tempHistogram / spectrum をそのまま使う)。
以下の数値は設計値であり、**QA(D4)の挙動指標を満たす範囲でのみ**
physics 値・質量・スピン・配置の調整可。調整結果は本付録に書き戻す。

### D1. 変更許可箇所(これ以外は変更禁止)

1. `BUILTIN_PRESETS` 配列への3要素追加(既存12要素は変更しない)。
2. 冒頭コメント等のバージョン表記。説明タブ・SYSTEM_PROMPT・validatePreset は変更しない
   (新プリセットの説明は description で完結させる。physics の新キーは無い)。

### D2. 内蔵プリセット3種(BUILTIN_PRESETS へ追記。スキーマは契約1と同一)

**下表は 2026-07-15 の初期値安定化ループ(QA 書き戻し 1〜8)反映後の確定値**。

| id | 名称 | 主要設定 |
|---|---|---|
| idealgas | 🎈 理想気体の自由膨張 | scale200 / box 160 / {G:0, D0:50, kFrame:0, kRep:2, muF:0, gammaN:0, kappaS:0, etaRad:0, timeScale:2} / disk(n200, r55, m0.5, spin2.5(一様), random v6) / overlays{tempHistogram} |
| grinder | 🛞 摩擦熱と熱伝導 | scale190 / box 150 / {G:0, D0:50, kFrame:0, kRep:0, muF:0.9, gammaN:0.4, kappaS:2, etaRad:0, softening:4, timeScale:3} / ring(n14, 中心(−79,133), r15, m3, vMode omega, omega0.2, pinned)=回転ドラム + single(m20, (−79,141), vx0.4)=ミルボール + single(m4)×27 x=−49..133(7刻み), y147 =伝導鎖 / overlays{tempHistogram} |
| starform | ⭐ 星の形成 | scale300 / circle 280 / {G:3, D0:5, kFrame:0, kRep:1.5, muF:0.2, gammaN:0.05, kappaS:0.2, etaRad:0.002, **pRad:1**, radiusScale:1.5, timeScale:4} / single(m300, spin0.65)=種(斥力クッション+熱源) + ring内(n165, 50..95, m1..1.6, spin0..0.1, vMode none) + ring外(n55, 118..140, **m2..3**, spin0..0.1, vMode none) / overlays{spectrum} |

**QA 書き戻し(2026-07-15、初期値安定化ループの結果。上表は反映済みの確定値)**:

1. **乱流摩擦加熱は伝導可能な熱を作らない** — 砂利ピットを研削輪で撹拌する構成(設計初案)は、
   churn 衝突がスピン符号を±混合にするため、E10′ で運べる正味の熱がゼロになる
   (付録C 書き戻し1の「±交互ヒーター」と同じ帰結)。持続する同符号の摩擦熱源は
   **幾何学的に拘束された単一の研削接触**でしか作れない → ドラム(レール輪)内に
   閉じ込めたボールミルを採用。ボールは格子の隙間(2.9 < 直径10.7)から脱出できず、
   接触のたび同方向に擦られ、T≈22 の定常熱源になる(実測: f100〜f2400 まで 22.1 で一定)。
2. **密に接触した物質は熱を保持も伝導もできない(ギア冷却)** — E9 の接線摩擦は接触対を
   転がり整合(s_i·R̃_i+s_j·R̃_j → v_t)へ駆動するため、静止した接触網では同符号スピンを
   系統的に破壊する。堆積・柱・クレードルを伝導路にする構成は全て失敗した。
   伝導路は**互いに接触しない疎な粒の列**(間隔7 > 接触距離4.8)にする。
   E10′ は g(d)=(ΣR/(ΣR+d))² の裾で非接触でも拡散する(=この宇宙の伝導は遠達的)。
3. **pinned 粒子との持続接触はスピンを破壊する** — pinned は熱浴(スピン固定)であり、
   接触摩擦は自由粒子側のスピンを一方的に奪う(ドレイン率は質量に依らず Δs≈−0.6s/サブステップ)。
   非接触の吸熱(E10′)は I 重みで抑制できる(軽い粒 m1 なら I 比 0.015)が、
   接触は防げない。**伝導鎖は何にも触れさせない**こと。
4. **ばね力上限は高速衝突で「トンネル」を許す** — 接触ばねの対称キャップ(軽い側 a≤8)により、
   運動エネルギー ½mv² が貫通仕事(≈2·8m·δ)を超えるとボールが格子を突き抜ける。
   ドラムは rIn15・n14(隙間1.2)まで小型化し、内部落下距離を絞って KE < 貫通仕事とした。
5. **grinder は G=0・kRep=0 の「純粋な熱実験室」とする** — 研削輪の質量 m6×20 でも、
   摩擦のない床では水平引力が蓄積して伝導鎖がドラムへ滑り落ちる(結合 gg と引力 G が
   同じ 1/d² スケールで、距離では分離できない)。また遠方大質量プラーは R=1.2√m が
   巨大化(m4.9e6 → R2657)して箱全体を「内部」に飲み込み、全粒子を接触ばねで吹き飛ばす。
   重力を全廃すれば鎖は完全静止し、ボールは無重力ピンボールとしてケージに擦られ続ける。
6. **starform の種は「重力の焦点」ではなく「斥力クッション+熱源エンジン」** — 種 m300 に
   spin0.65 を与えると、E5′ の保存力クッション(散逸しない=ギア冷却と無縁)が崩落を
   受け止め、キネティックな気体圏(終状態 mean|v|≈3.4。凍結地殻なら <0.2)を支える。
   さらに種の熱(T=I·s²≈4.3万)が κs 伝導で外殻へ流れ、ギア摩擦で運動に変換されて
   対流を駆動する(恒星の中心エネルギー源の類似)。kFrame=0 で「ハリケーン」
   (PHYSICS.md §5 安定化(d))を封じる。
7. **pRad=1(ニュートン冷却)を採用** — p=4 では種(T≈4.3万)と塵(T≈1)の T⁴ が
   17桁差になり、種が数サブステップで放射死してクッションが消滅する(radE≈2万の正体)。
   p=1 なら冷却時定数 τ=1/η_rad が全粒子共通になり、種は τ=500t で緩やかに冷えて
   星は徐々に縮む=「冷えて釣り合いへ」の物語そのもの。
8. **動径崩壊はバー(m=2)不安定** — 一様円盤・完全対称リングいずれから始めても、
   無回転の動径崩壊は軸比 2.3〜3.1 の永続バーに落ちる。回転による抑制は
   クッションとの合算支持が重力を超えて崩壊自体を止める(掃引で確認)。
   採用解は**二段雲**: 内環(質量75%、落下距離が短くバー増幅が小さい)が円形の
   外層を先に組み上げ、外環(25%、m2..3)が全方位から遅れて爆撃して等方化しつつ
   閃光加熱(T峰2.65)を担う。外環なしの対照実験は軸比 3.1(=外環の爆撃が円形化に必須)。

設計根拠(調整時に保つべき不変条件):

- **idealgas**: 散逸チャネルを全て切る(muF=γn=κs=η_rad=kFrame=0、G=0)。
  残る相互作用は弾性の接触ばねとスピン斥力 E5′ のみ。E5′ は F ∝ ω²+ω² ∝ s² で、
  T=Is² より **F ∝ T** — 状態方程式 P∝nT の類似がこの宇宙で成立する。
  スピンを変えうる経路(E9摩擦・E10′拡散・E11冷却・E6′残余トルク)が全て 0 なので
  **スピン温度は厳密に不変** — 中央の高密度塊が箱へ自由膨張しても温度が変わらない
  「ジュールの自由膨張」の類似が機械精度で成り立つ(D4 の等温判定 <1e-6 の根拠)。
  壁反射の 0.95 減衰は並進速度のみに効き、温度(スピン)には触れない。
- **grinder**: 熱源はドラム内ボール1個の幾何拘束された研削(書き戻し1・4・5)。
  伝導鎖は非接触・完全静止(書き戻し2・3・5)。左右比のヒストグラムが
  「左が先に熱く右が遅れて追う」を表示する。遠端は鎖のホップ伝導より
  E10′ の長距離裾(直接拡散)が先に効くため、勾配は立ち上がり期(f200〜400)に
  最大で、その後全体が平衡へ向かう(それ自体が熱力学的に正しい絵)。
- **starform**: 4段階 = 崩落(内環→外環の二段)→ 閃光加熱(クランチ窓で T×231)→
  気体圏の形成(相変化: クッション+動圧で収縮停止・微反跳 ×1.10)→
  放射冷却と釣り合い(半径トレンド 1.000、軸比 1.23 の真円、束縛 100%)。
  「中心に巨大質量を仮定」はタスクの選択肢を採用したもので、
  この宇宙で唯一発見できた「触れずに支え、冷めずに熱する」恒星構造である(書き戻し6)。

### D3. 説明文(description)の要点(実装済み。200 字以内)

- idealgas: 散逸ゼロ=理想気体 / 圧力はスピン斥力 F∝T(P∝nT の類似)/ 自由膨張で
  一様充満しても温度不変(ジュールの実験)/ 右下: 左右の平均温度が等しく一定。
- grinder: 無重力ボールミル(回転ドラム内のボールが擦られ続けて発熱, A9)/
  熱は接触しない粒の列へスピン拡散 E10′ で飛び移り端から順に伝わる /
  右下: 左が先に熱く、右が遅れて追う。
- starform: 雲が種へ崩落 → 密集の摩擦で閃光加熱 → 気体圏へ相変化しクッションと動圧で
  収縮停止 → 種の熱が対流を駆動、放射で冷えて釣り合った球状の星として輝き続ける。

### D4. 受け入れ条件(QA スクリプトで機械判定。判定値は 2026-07-15 実測に合わせて確定)

回帰: 既存12プリセット各120フレームでコンソールエラー0・NaNなし、`HP.verify.all()` 全pass、
SYSTEM_PROMPT 内 few-shot 4例が validatePreset 合格、横スクロールなし。
新プリセットは headless Chromium(390×844)で判定:

- [ ] 3プリセットとも下記フレーム数を実行してコンソールエラー0・NaNなし
- [ ] **idealgas**(900フレーム, 30毎サンプル): 等温性 |ΔΣIs²|/初期 < 1e-6(厳密不変)、
      充満: 終盤の中央値半径 > 90(初期≈40)かつ4象限の粒子数が各 ≥15%、
      弾性: 並進運動エネルギーの終値 > 初期の 50%
      〔実測: ΔT=0.00e+0 / medR 40.2→130.5・象限最小22% / KE 104%〕
- [ ] **grinder**(2400フレーム, 100毎サンプル): 摩擦加熱: ボールT@100f > 1 かつ
      定常T@600f・@2400f > 5、伝導の遅延勾配: f200〜400平均の 近seg/遠seg > 1.8、
      熱到達: 遠seg終値 > 0.05 かつ > 3×@300f、平衡化: 終盤の3セグ比 < 1.5、
      鎖の静止: x順序 26/26 保存・平均速さ < 0.5・|Δy| < 3
      〔実測: ballT 31.3/22.1/22.1 / 勾配2.37 / b3 0.023→0.101 / 比1.13 / 26/26・v0.000・Δy0.0〕
- [ ] **starform**(2400フレーム, 40毎サンプル, 雲粒子=種以外で判定):
      収縮: 中央値半径が初期の 55% 未満へ、
      圧力加熱: クランチ窓(medR<60)の峰T > 0.5 かつ > 30×初期、
      相変化=気体圏: 終状態 mean|v| > 1.5(凍結地殻は <0.2)・崩壊停止(min ≥ 0.9×終値)・
      閃光後の再膨張 ≥ 1.02、冷却と釣り合い: 終|s| < 峰×0.6・末1200fの平均半径が
      初期の 25〜55%・無トレンド(末600f/前600f 比 0.9〜1.1)、
      球状: 終状態の共分散主軸比 √(λ1/λ2) < 1.4、束縛: r<150 に 90% 以上
      〔実測: medR 78.2→33.7 / T峰2.649(×231)@480f / mean|v|3.42・停止1.00・再膨張×1.105 /
      |s| 0.408→0.102・半径43%・トレンド1.000 / 軸比1.229 / 束縛100%〕
- [ ] スペクトル/温度ヒストグラムの描画パスを通した状態で描画例外が出ない
- [ ] `node --check` 合格、横スクロールなし(既存条件の維持)

2026-07-15 QA 結果: 回帰17項目+新規17項目 = **34/34 PASS**(headless Chromium 390×844)。

### D5. 禁止事項(付録D 固有)

- 物理式・エンジン(makeSim/step/traceRay/HP.verify)・オーバーレイ関数の変更。
- 既存12プリセットの数値変更。tempColor パレット変更。
- SYSTEM_PROMPT / few-shot / HANDOFF_RUNTIME_LLM.md の変更(physics 新キーなし。
  B1 と同方針)。
- プリセット調整の名目で安全上限(|v|≤100, |s|≤40)や境界反射係数を変えること。

---

## 付録E: v1.4 サンプルラインナップ改訂 — 「塊は熱で分かれる」(2026-07-15)

対象ファイルは `index.html` のみ。物理式・エンジン・オーバーレイ関数は一切変更しない。
タスク: サンプルのラインナップと内容を効果的に改訂し、初期値を適切化する。
現状の欠陥(機械計測 2026-07-15): **塊になると分離しない** —
galaxy は f1800 で塊が粒子の27%を飲み込み中心が崩壊(medR 184→105)、protodisk は
種が単調に合体(クラスタ13→6・増加し続ける最大塊)、phase は液体が f900 で凍結
(|s| 2.0→0.20)、collision は残骸が完全凍結(T≈0.011・|v|≈0.11)する。

### E1. 改訂方針(設計判断の要約)

1. **可逆性の直接デモを新設** — 新プリセット `snowline`(雪線)。同一の塵の環を
   「冷たい星」と「熱い星」に与える対照実験で、冷たい環は小衛星に集積し、
   熱い環は生まれかけた塊が熱でほどける(=塊は熱で分かれる、の一次証拠)。
2. **初期値の適切化** — galaxy(中心崩壊と塊化の抑制)、protodisk(種の複数持続)。
3. **物理的に不可能な改訂はしない** — phase の「凍結しない液体」と collision の
   「熱く在り続ける残骸」は E2 の書き戻し 1〜3 のとおり公理系の帰結として不可能。
   両プリセットは確定値を維持し、description で帰結を明示して雪線デモへ誘導する。
4. **ラインナップの再編** — プリセットに `group` フィールドを追加し、選択UIを
   「空間と光 → 熱の実験室 → 天体の物語」の3グループ(教育的順序)で表示する。

### E2. QA 書き戻し(2026-07-15、設計試作ループの結果)

1. **束縛接触クラスタは初期スピンをどこに置いても t≈20〜30 で凍結する** —
   液体クラスタ(m1, R1.6, G0.7)の初期スピンを 1.0〜1.3(釣り合い直上)/1.8〜2.2/
   2.0〜2.4/2.4〜2.8 のいずれから始めても、|s| は t≈19 までに 0.4〜1.1 へ落ち、
   f900〜1200 で凍結(fCl≥0.5)する。釣り合い(s*≈0.9)超過分は沸騰チャーン
   (E9 のスピン→速度変換)で捨てられ、残りはギア冷却(付録D 書き戻し2)が奪う。
   **熱源のない液体はこの宇宙では準安定にすらならない。**
2. **pinned ヒーターの埋め込みは液体を救えない** — ヒーター(m4, s4)+κs0.15 は
   プールを保温する前に沸騰させ(far 0.94)、同時に κs 経由で気体クラスタの熱を
   吸って三態の分離自体を壊す。よって phase は v1.2 確定値を維持し、
   「やがて凍る」ことを description の物語に組み込む。
3. **collision の「熱い残骸」はナイフエッジでしか作れない** — muF 0.45/0.55 では
   衝突加熱がギア冷却に食われ残骸が凍結(s≈0.06)、muF 0.7 では
   スピン→斥力→速度→摩擦→スピンの正帰還で全量飛散(|v|85・far 1.00)。
   臨界 muF≈0.6 はシード敏感のため採用しない(collision は v1.2 確定値を維持)。
   q<2 かつ kRep≥2 の組は崩壊クランチだけで爆発する(kRep2.5・q1.3 で確認)。
4. **galaxy の中心崩壊の原因は vMode "flat" のサブケプラー初期化** — flat v=1.6 は
   r<230 でケプラー速度未満のため内側円盤が中心へ落ち、散逸を muF0.02 まで
   下げても中心塊(in-zone fCl 0.78)は防げない。**ケプラー初期化
   (vMode kepler, vScale 1.05 — 円盤自己質量の補正込み)に変更**すると中心は
   崩壊せず(in-zone medR 103 で安定)、フレーム引きずりによる回転曲線の平坦化が
   「時間とともに起こる」形で見える(vt_out 1.82→2.51@f1200、ケプラー予想を超過)。
5. **protodisk はケプラー×1.0+低散逸で種が複数持続する** — 旧値(×0.75, muF0.5,
   γn0.6)は種が単調合体する。×1.0・muF0.35・γn0.45・kRep0.8・κs0.05 では
   種が最大12個形成→4〜5個で安定し、円盤は潰れず、種は温かい(実測は E6)。
   f2600 以降は種が残存塵を散乱で外へ掃き出し円盤が膨張する(惑星系の
   ガス円盤クリアリングの類似。far@f3000=0.09)ため、判定窓は f2400 とする。
6. **snowline は kRep=0 で設計する** — 熱い星(スピン s*)の E5′ 斥力の尾
   ω(d)=s*(R/(R+d))^q は環の公転重力に匹敵し(kRep>0 だと環を吹き飛ばす)、
   融解は kRep ではなく **E9 摩擦チャーン**(接触対の s·d が対の脱出速度を超えると
   分離)が担う。融解閾値 s_crit≈0.5 に対し、熱い環の平衡スピンは
   κs·gg·s*/(κs·gg+η_rad) ≈ 1.1〜1.3(κs1.2, η_rad0.15, pRad1)。
   η_rad(pRad=1)は冷たい環の衝突加熱も即座に捨てるため、冷側の集積を助ける。
7. **蒸発した塵は雪線の外側で再凝縮する** — 熱い環内(d≈65)で f450 に生まれた塊は
   f600 までに融解(=「ほどける」の実測)する一方、チャーンで外へ運ばれた塵は
   加熱圏の外(実測 d≈163、平衡スピン≈0.23 < s_crit)で持続する塊を作る。
   これは欠陥ではなく雪線現象そのもの(温度勾配が凝縮線を決める)であり、
   description に明記し、E6 の「塊になれない」判定は加熱圏 r<150 に限定する。

### E3. 確定プリセット(v1.4)

**値を変更する2種**(それ以外の既存13種は数値変更禁止):

| id | 変更 | 改訂後の主要設定 |
|---|---|---|
| galaxy 🌌 | physics と disk の vMode/vScale | scale300 / none / {D0:1, muF:0.02, gammaN:0.05, kRep:1.0, softening:4, timeScale:4} / single(m600, spin0.05) + disk(n380, r260, m0.5..1.2, **vMode:"kepler"**, aroundMass:600, **vScale:1.05**) / rotationCurve |
| protodisk 🌀 | physics と disk の vScale | scale260 / none / {muF:0.35, gammaN:0.45, kappaS:0.05, kRep:0.8, timeScale:4} / single(m400) + disk(n320, r220, m0.2..1.5, spin−0.2..0.2, kepler around 400, **vScale:1.0**) / なし |

**新設1種**:

| id | 名称 | 主要設定 |
|---|---|---|
| snowline | ❄️ 雪線 — 熱は塊を融かす | scale380 / none / {G:1, D0:2, kFrame:0, q:2, **kRep:0**, muF:0.6, gammaN:0.5, kappaS:1.2, etaRad:0.15, pRad:1, timeScale:4} / single(m400, (−260,0), spin0, pinned)=冷たい星 + single(m400, (260,0), **spin3.5**, pinned)=熱い星 + ring×2(各n110, rIn50, rOut85, m0.3..0.6, kepler around 400, vNoise0.05)を各星に / overlays{tempHistogram}(左右バー=冷側/熱側の平均温度) |

**description のみ改訂する2種**: phase(液体はやがて凍る=熱源のない宇宙の帰結を明示)、
collision(残骸は冷えて固まる — 融かす熱は雪線を参照)。他プリセットの description は
groupフィールド追加を除き変更しない。

### E4. グループ化UI(ラインナップ再編)

1. 各内蔵プリセットに `group` フィールド(文字列)を追加する。値と並び順:
   - **空間と光**: mach → galaxy → lensing → spectrum
   - **熱の実験室**: idealgas → gas → grinder → convection → phase
   - **天体の物語**: saturn → snowline → protodisk → accretion → collision → radcool → starform
2. `BUILTIN_PRESETS` 配列を上記の順に並べ替える(数値・説明は E3 のとおり)。
3. `rebuildPresetSelect` は内蔵プリセットを `group` ごとの `<optgroup>` で表示する
   (group 未定義は「内蔵」に落とす)。AI生成の optgroup は従来どおり末尾。
4. 起動時プリセットは従来どおり `saturn`(視覚的フック優先。配列先頭には依存しない)。
5. `group` はUI専用フィールド — validatePreset・SYSTEM_PROMPT・スキーマ契約には
   追加しない(LLM生成プリセットは従来どおり「AI生成」グループに入る)。

### E5. 変更許可箇所(これ以外は変更禁止)

1. `BUILTIN_PRESETS`: 並べ替え、`group` 追加、galaxy/protodisk の E3 の値変更、
   snowline の追加、phase/collision の description 文字列。
2. `rebuildPresetSelect` の optgroup 化。
3. 冒頭コメントのバージョン表記。
4. 説明タブ・SYSTEM_PROMPT・validatePreset・エンジン・オーバーレイは変更しない。

### E6. 受け入れ条件(QA スクリプトで機械判定。headless Chromium 390×844。
判定値は 2026-07-15 の QA 実測に合わせて確定)

回帰: 全16プリセット各120フレームでコンソールエラー0・NaNなし、
`HP.verify.all()` 全pass、SYSTEM_PROMPT 内 few-shot 4例が validatePreset 合格、
横スクロールなし、optgroup 構成が E4 と一致(空間と光4・熱の実験室5・天体の物語7)、
起動プリセットが saturn のまま。

改訂・新設プリセット(クラスタ=接触距離 R_i+R_j+0.5 の連結成分、粒子数≥5):

- [ ] **galaxy**(1800フレーム, 200毎): 中心崩壊なし: 内側(初期r<130)の中央値半径
      @f1800 ≥ 70、デモ窓の塊化抑制: f1200 まで内側クラスタ所属率 ≤ 0.15 かつ
      外側 ≤ 0.05、平坦化: 外側(初期r≥130)の平均接線速度 vt@f1200 ≥ 1.25×vt@f0、
      終状態も生きている: mean|v|@f1800 ≥ 1.5・クラスタ所属率(内側) ≤ 0.55、
      飛散: far(>400) ≤ 0.05
      〔実測: medR119.7 / maxFcl 0.08・0.02 / vt比1.43 / |v|2.83・fCl0.31 / far0.00〕
- [ ] **protodisk**(2400フレーム, 200毎。f2600以降は散乱掃き出し期のため窓外=E2-5):
      種の形成: クラスタ数の最大 ≥ 8、複数種の持続: f1400 以降の全サンプルで
      クラスタ数 ≥ 3 かつ @f2400 ∈ [3,12]、全合体しない: 最大クラスタの粒子比 ≤ 0.5、
      円盤の維持: 中央値半径@f2400 ∈ [90,230]、飛散: far(>400) ≤ 0.05、
      種は温かい: 平均T@f2400 ≥ 0.5
      〔実測: max12 / min5・end5 / fLargest0.28 / medR197.9 / far0.02 / meanT3.18〕
- [ ] **snowline**(1800フレーム, 150毎, ゾーン=初期x符号・アンカー=各星):
      冷たい環の集積: クラスタ所属率@f1800 ≥ 0.4 かつ クラスタ数 ≥ 2、
      熱い環の加熱圏は塊になれない: 熱い星から現在位置 r<150 の粒子群のクラスタ
      所属率が全サンプルで ≤ 0.06 かつ 終値 0(雪線の外の再凝縮は許容=E2-7)、
      熱コントラスト: f300〜f1200 の平均で mean|s|(熱) ≥ 3×mean|s|(冷)、
      デモ窓で環が保つ: far(>220)@f1200 熱側 ≤ 0.35・冷側 ≤ 0.15
      〔実測: cold fCl0.76・nCl7 / max fClNear0.045・終値0(融解イベント: 環内d≈65
      の塊が f450→f600 で消滅) / |s| 1.17 vs 0.08(14.6倍) / far 0.17・0.00〕
- [ ] 3プリセットとも上記フレーム数でコンソールエラー0・NaNなし
- [ ] rotationCurve(galaxy)・tempHistogram(snowline)の描画パスで例外なし
- [ ] `node --check` 合格

2026-07-15 QA 結果: 回帰25項目+新規17項目 = **42/42 PASS**(headless Chromium 390×844)。

### E7. 禁止事項(付録E 固有)

- 物理式・エンジン(makeSim/step/traceRay/HP.verify)・オーバーレイ関数の変更。
- E3 に列挙した以外の既存プリセットの数値変更。tempColor パレット変更。
- SYSTEM_PROMPT / few-shot / HANDOFF_RUNTIME_LLM.md / validatePreset の変更
  (group はUI専用。B1 と同方針)。
- 安全上限(|v|≤100, |s|≤40)・境界反射係数の変更。

---

## 付録I: v1.9 サンプル全面刷新 — 実行指示書と QA(2026-07-16)

設計フェーズ(Fable 5)の確定仕様。裁定の背景は DERIVATIONS §13、計画は MODEL_ROUTING v1.9。

### I1. 設計原則

1. **1サンプル=1効果**: 各プリセットは単一の法則・帰結を実証し、説明文の冒頭で言明する。
2. **初期値安定**: 全プリセットが既定パラメータで起動直後から意図した挙動を示し、
   600フレーム(以上)で NaN・発散・コンソールエラーなし。挙動は機械指標で判定する。
3. **熱デモは単一粒径**(L9: 異粒径では s 均一と T 均一が両立しない)。
4. **主張の誠実性**: 第4次裁定に整合(🎈旧理想気体の「P∝nT」主張は撤去、🧊は形態の
   アナロジー、♨️は定性デモと明記)。

### I2. エンジン変更(2点のみ・物理不変)

- **軌道トレイル**: `overlays.trail` — 自由粒子の経路を最大3000点(2フレーム毎サンプル
  ≈100秒)保持し、粒子描画の**後**(グローの上)にポリライン描画。loadPreset/⏮でリセット。
- **V10b**: 検証フック追加(固定スピン+kRep>0 の K+U_rep 保存。DERIVATIONS §13.3)。

### I3. 新ラインナップ(4グループ13種)と受け入れ指標・QA実測

| ID | 1効果 | 機械指標(受け入れ条件) | 実測(2026-07-16) |
|---|---|---|---|
| mach 🪣 | 空間の引きずり | 起動600f・NaNなし(挙動はv1.0からの継承値) | PASS |
| twin 🕰(新) | E7R 運動時間遅れ | 2000步後 τ/t が解析値 1.00/0.866/0.60 と ±0.02 | 1.000/0.874/0.611 PASS |
| drag 💫(新) | D4a′ 近点後退 | 近点角ドリフト ∈[−25°,−5°]/周(後退)・近点4回検出 | −11.34°/周 PASS |
| galaxy 🌌 | T4 平坦化 | 起動600f(因果は V11 が恒久検証) | PASS |
| lensing 💡 | ψ統一レンズ+光子捕捉 | 光線ファン NaN 0(較正は v1.7 §11.3) | PASS |
| spinlens 🌗(新) | T8 非対称湾曲 | 上下対称光線(±120)の湾曲和 |Δθ| > 0.05 rad | 0.754 rad(上33.3°/下9.9°) PASS |
| gas 🔥 | T6 温度平衡 | 起動600f(挙動はv1.0からの継承値) | PASS |
| pressure 🎈(新) | A10 圧力の創発 | 熱コア90粒の平均半径が600fで1.4倍超 | 34.5→108.8(3.2倍) PASS |
| conduction 📏(新) | E10′ 距離依存伝導 | t≈15 で密鎖の輸送熱 Σs > 1.8×疎鎖・前線A#3>1.5・全粒子完全静止(vmax<1e-9) | Σs 19.1 vs 7.6(2.5倍)・A#3=2.03・vmax=0 PASS |
| phase 🧊 | 形態の統制実験 | 起動600f(挙動はv1.5 F4aの継承値) | PASS |
| convection ♨️ | 浮力の創発(定性) | 起動600f(挙動はv1.2 付録Cの継承値) | PASS |
| saturn 🪐 | T3 整列 | 起動600f(継承値) | PASS |
| snowline ❄️ | 可逆性の対照実験 | 起動600f(挙動はv1.4 付録Eの継承値) | PASS |

回帰: `HP.verify.all()` 11項目(V10b 込み)全PASS・few-shot 4例合格・`node --check` 通過・
コンソールエラー0。継承8種の深い挙動指標(付録C/D/E)は数値未変更のため有効。

### I4. 廃止8種と理由(DERIVATIONS §13.4 の要約)

idealgas(主張が裁定#1で不正確)/ grinder(📏に置換)/ spectrum・radcool・accretion・
collision(放射系の重複・効果が判別しにくい)/ protodisk(🪐と重複)/ starform(複雑)。
放射・降着の物理はパラメータとして全プリセットで有効化可能。

### I5. 実装知見の書き戻し

- 軌道トレイルは粒子グローの**上**に描かないと近点側が隠れる(m=600 の表示グロー半径は
  世界座標≈80 に達し、近点60を覆う)。
- 💫の歳差視認性: spin 0.09 で −11.3°/周 ≈ トレイル4.5周分で約50°の花びら開き。
  spin 0.06(−9.2°/周)では開きが浅い。
- 📏の指標は平衡前(t≈15)で測る — E10′ の長距離カーネル(L16)により t≳60 では
  両鎖とも熱浴に直接加熱され差が消える(この遠達性自体を説明文の見どころに転用)。
- 🕰 は Kt=10⁴ で重力項を消し cLight=12 で運動項を可視域に(v=6, 9.6)。レール粒子
  (pinned+omega)はスピン・速度が固定なので理想時計として安定。

---

## 付録J: v1.10 — i18n・AIプロバイダ柔軟化・♨️🪐再設計(2026-07-16)

第5次レビュー裁定(DERIVATIONS §14)と原仮定者タスク(日英切替/AI追加の柔軟化/
♨️対流の欠陥修正/🪐安定化)の実装記録。実装は完了済み — 本付録は将来の改修者向けの正本。

### J1. 日本語/英語切り替え(i18n)

- 実装: `I18N` 辞書(ja/en)+`T(key)`+`applyLang()`。`LANG` は `localStorage.hp_lang` に永続
  (既定 ja)。ヘッダーの `#btnLang` でトグル。
- 対象範囲: 静的UI(タブ・ボタン・placeholder)/動的文字列(通知・エラー・AIステータス)/
  パラメータラベル(`I18N.en.params[key]`、ja は PARAM_DEFS.label が正)/
  プリセット名・説明(各内蔵プリセットの `en:{name,description}` フィールド。無ければ ja に
  フォールバック — AI生成プリセットは生成時の言語のまま)/グループ名(`I18N.en.groups`)/
  ヘルプ/キャンバス内ラベル(HUD・回転曲線・温度・スペクトル)/`document.title`・`<html lang>`。
- AI生成の言語: `getSystemPrompt()` が LANG=en のときのみ「name/description を英語で」の
  上書き節を付加(SYSTEM_PROMPT 本体・few-shot は不変=few-shot 回帰に影響なし)。
- 追加時の規約: **新しいUI文字列は必ず I18N.ja/en の両方へ**。内蔵プリセット追加時は
  `en:{name,description}` を必須とする(QA が機械検査: 全内蔵プリセットに en があること)。
- テストフック: `HP.setLang("en"|"ja")`、`HP.lang()`、`HP.getSystemPrompt()`。

### J2. AIプロバイダ柔軟化

仕様の正本は HANDOFF_RUNTIME_LLM.md v1.10 改訂節。実装要点:
- `AI_PROVIDERS`(anthropic/openai/gemini)+`aiCfg()`+`callLLM(cfg,model,messages)`。
- モデルIDは `#aiModelIn`(text+datalist)の自由入力。localStorage キー:
  `hp_provider` / `hp_model_{prov}` / `hp_key_{prov}` / `hp_baseurl_openai`。
- キー保存は `#keyPersist` チェック時のみ localStorage、既定はメモリ(`sessionKeys`)のみ。
  旧 `hp_key`/`hp_model` は anthropic 用へ自動移行(裁定 #13)。
- フォールバック連鎖は anthropic のみ(haiku→sonnet-5、sonnet→opus-4-8)。
  全プロバイダ共通: JSON抽出→validatePreset→失敗理由付きリトライ1回。

### J3. ♨️加熱の対流 — 片側ヒーター再設計(機械実験 conv-E1 採用)

**欠陥の機械診断(旧構成)**: 全面ヒーター+m=0.3×230粒。t≈128 で最大塊比率
maxFrac 0.51→0.99(全粒子が1塊に凍結)、循環は符号不定。原因は
①ガス自己重力(総質量69が自己束縛系)と②塊内での E9/E10′ 散逸の加速(塊=冷却器)の正帰還。
**棄却した対策(いずれも凍結)**: 片側ヒーター化のみ(t=190で0.97)/散逸低減
muF0.3・γn0.2(0.96)/冷天井+冷壁の追加(0.63〜0.9)— 自己重力が残る限り冷側から崩壊する。
**採用構成(conv-E1)**: 粒子を m=0.05 に軽量化(自己重力を1/36化)+天空質量13000で
外部重力は維持+床左半分のみヒーター(spin12×9)+右壁を冷熱浴(spin0固定×17)+
G1.5/kRep2/muF0.3/γn0.2/κs0.8/ηrad1e-5(p=2)/radiusScale4。
**実測(24000步=t384)**: maxFrac ≤0.26(凍結なし)、循環は全12サンプル同符号
(+61〜+99、片側加熱の定常セル)、TL/TR ≥2(左右温度勾配の持続)、NaNなし。
**書き戻し知見**: 本モデルの「ガス」は自己重力に対して常に凍結し得る(雪線の物理)。
対流デモの成立条件は「冷側の枝が塊化閾値を下回らないこと」であり、
軽量粒子化(自己重力消去)が唯一頑健だった。冷壁は塊化を遅らせ(E2: 壁なし0.49 vs E1: 0.26)、
muF増(E3: 0.45)は加熱よりも粘着を増やす。

### J4. 🪐土星の環 — 安定化(機械実験 saturn-pinned-far 採用)

**欠陥の機械診断(旧構成)**: 中心星が非固定のため環の反作用・近接遭遇で漂流
(t=384 でドリフト49.3、環残存 inAnn 55%、束縛率が0.6台まで振動、vrRms≈2.3)。
rIn=60 は星の物理半径 41.6 の1.44倍で近接しすぎ。「AI生成で作り直すと安定した」の正体は
few-shot 例3が中心天体を pinned:true にしていたこと。
**採用構成**: 中心星 pinned:true+環 rIn75〜rOut160・mMax0.2・vNoise0.03(他は不変)。
**実測(24000步=t384)**: inAnn 99.7%、束縛率100%、vrRms 0.41、ドリフト0、NaNなし
(対照: pinned のみで rIn60 のままだと 91.2%)。

### J5. 付随変更: 温度グラフの pinned 除外

`drawTempHistogram()` は pinned 粒子(熱浴・恒星)を平均から除外する(v1.10)。
理由: ♨️のヒーター(T=Is²≈1152)や❄️の恒星が平均を桁で支配し、物質側(ガス・塵)の
温度変化が読めなかった。除外により「熱浴が物質をどう温めるか」を測る計器になる。
視覚確認: ♨️ t≈190 で左右バーがガス温度スケールになり左>右が読める。

### J6. 受け入れ条件(v1.10 QA — 全PASS を機械確認すること)

1. `node --check`(script 抽出)通過、13プリセット起動120フレーム NaN・コンソールエラー0
2. `HP.verify.all()` 11項目 PASS(物理エンジン不変 — 数値まで一致すること)
3. i18n: 全内蔵プリセットに en あり/EN切替で title・タブ・optgroup・ヘルプ・SYSTEM_PROMPT
   上書き節が切替/JA復帰
4. AIプロバイダUI: プロバイダ切替で既定モデル・datalist・URL行が追随/自由入力モデルIDが
   `aiCfg().model` に反映/`#keyPersist` 既定OFF
5. few-shot 4例 validatePreset 合格(警告なし)、kLens付き旧プリセット互換ロード
6. ♨️: 24000步で maxFrac<0.35・循環同符号率≥90%・NaNなし
7. 🪐: 24000步で inAnn≥0.95・ドリフト<5・NaNなし

---

## 付録K: v1.11 — ♨️天井冷却への改良・AI生成の表現力拡張(2026-07-16)

原仮定者フィードバック「側面の冷たい壁で膠着してしまっている。冷たい壁は上面が良いのでは」
「AI生成で各サンプルを再現できるように。加熱冷却が出現するように」「ブラックホールも生成
できるように」への実装記録。Phase 0 残作業(LICENSE/CITATION/HANDOFF_PAPER_V2)も本版で完了。

### K1. ♨️対流 — 側壁冷却→天井冷却(機械実験 conv-G4 採用)

**膠着の定量化(v1.10 側壁構成)**: 新指標 stuck(冷 pinned 粒子から24以内かつ速度<0.2 の
自由粒子率)で t=384 に **23.1%** — 原仮定者の観察「冷壁で膠着」を機械再現。
**比較(24000步=t384)**:

| 構成 | stuck | maxFrac(最大塊率) | 塊内平均速度 | 循環符号 |
|---|---|---|---|---|
| 側壁冷却(v1.10) | 0.231 | 0.146 | —(壁面凍結) | 12/12 |
| 天井全幅冷却 | 0.223 | 0.477 | — | 12/12 |
| 天井右2/3 | 0.146 | 0.369 | 0.06(凍結) | 12/12 |
| ヒーター延長+天井右2/3 | 0.388 | 0.069 | 0.12 | 12/12 |
| **ヒーター延長+疎な天井右側(採用=G4)** | **0.042** | 0.388 | **1.37(流動)** | **12/12** |

知見: ①上昇流の真上まで冷やすと冷層が天井に滞留する(全幅は不可)。②冷却板は**疎に**
(間隔40)置くと粒子が貼り付かず、熱だけ抜ける。③maxFrac は「流れている冷たい枝」も
拾うため、凍結判定には塊内平均速度(clSpd)を併用する — 採用構成の大塊は clSpd 1.4〜1.8 の
対流ストリーム自身である。
**採用構成**: ヒーター11点(x=-170..30、spin12)+天井冷却板5点(x=10..170 間隔40、spin0)。
物理は v1.10 のまま。内蔵プリセット実測(固有シード): 循環 12/12 正(+51→+151 に成長)、
stuck 3.8%、NaN なし。

### K2. AI生成の表現力拡張(SYSTEM_PROMPT v1.11)

仕様の正本: HANDOFF_RUNTIME_LLM.md(写し同期済み)。要点:
- 物理要約に追加: pinned熱浴(高スピン=ヒーター/スピン0=冷却板)、放射冷却 etaRad/pRad、
  光子捕捉レシピ、overlays 4種の説明、色=温度。
- 既定値・値域に cLight/bM/etaRad/pRad を追加(physics 全キー出力の既定に整合)。
- ルール5に「加熱・冷却するガスは軽い粒子(m 0.05〜0.1)+kRep 2 — 重いガスは自己重力で
  凍結」(付録J J3 の知見の資産化)。
- few-shot 5例に改訂: 例3=ブラックホール光子捕捉、例5=床加熱・天井冷却の対流(K1 採用構成と同一)。

### K3. ブラックホール(光子捕捉)の成立条件 — 機械掃引の結果

| m | Kt | spin | 捕捉(8本中) | 挙動 |
|---|---|---|---|---|
| 3000 | 60 | 1 | 0 | 全脱出 — **スピンの媒質随伴が光を外へ流す** |
| 3000 | 60 | 0 | 7 | 中心へ螺旋落下 |
| 3000 | 40 | 0 | 8 | 中心へ螺旋落下 |
| **2000** | **40** | **0.5** | **8**(うち7が半径≈38の円軌道) | **光子球様のリング巻き付き(採用)** |
| 3000 | 30 | 0.5 | 0 | 引きずり優勢で脱出 |

書き戻し知見: 捕捉は Kt(屈折)と spin(随伴)の**競合**で決まる。プロンプトには
「質量2000〜3000・Kt40〜60・スピン0〜0.5」のレシピと、スピン過大で捕捉が消える警告を明記。
例3(m2000/Kt40/spin0.5)は 5/5 光線捕捉を回帰QAで機械検証。

### K4. 受け入れ条件(v1.11 QA — 全PASS 機械確認済み)

1. `node --check` 通過・13プリセット起動 NaN/エラー0・`HP.verify.all()` 11項目 PASS(数値一致)
2. few-shot **5例** validatePreset 合格(警告なし)
3. 例3(BH): 光線5本中5本が340步後も画面内(捕捉)
4. ♨️内蔵: 24000步で stuck<0.08・循環の後半6サンプル同符号・NaNなし
5. i18n・プロバイダUI・kLens互換の v1.10 回帰全PASS

---

## 付録L: v1.12 — 第6次提案(サンプル+アプリ改善)の裁定と Phase 1 CI 基盤(2026-07-16)

入力: 第6次外部AI提案4件(ChatGPT 2件=提案書+インポートJSON / Grok / Gemini。テーマ:
**新規サンプルとアプリ改善**)+ロードマップ Phase 1(再現性・CI)。

### L1. 裁定表(サンプル)

| 提案(出典) | 裁定 | 根拠・実測 |
|---|---|---|
| ⏱重力による時計の遅れ(ChatGPT P0-1) | **内蔵化** | τ/t=0.931/0.965/0.979 が解析値 e^{−ψ} と 1.1e-5 で一致・順序正(機械検証)。運動時間遅れ(🕰)と対をなす欠落領域 |
| 🌈放射冷却 — 高温ほど速い(ChatGPT P0-2) | **内蔵化** | 初期冷却速度比 8.0/7.8(理論 s³ 則=8)・位置ドリフト0(統制実験として成立) |
| 💥順行・逆行リングの衝突(ChatGPT P0-3) | **内蔵化** | muF=1→平均|s|=0.76 / muF=0→0.000 の完全対照・NaNなし。A9 の単独デモの欠落を補完 |
| 連星+周連星円盤 / 8の字三体 / 連星レンズ(ChatGPT P1) | **任意パック収録** | `presets/SAMPLE_PACK.json`。全て12000步スモーク合格。8の字は最大半径87に有界(数値法でも周期が保たれることを確認) |
| 銀河合体 / 雲崩壊(Grok #1/#3) | **任意パック収録** | スモーク合格(overlays.trail は本版でスキーマ化され保持される) |
| スピン非対称レンズ(Grok #2) | 棄却 | 内蔵🌗回る星のレンズと同一効果(重複) |
| 回転する対流セル(Grok #4) | 棄却 | m=1 の重いガスは自己重力で凍結する(付録J J3 の機械知見)。既存🔥/♨️と重複 |
| 鉄球と壁の破壊(Gemini) | 棄却 | ジョイント拘束・剛体箱・反発係数・3D は DFM エンジンの物理系外(別エンジンの提案) |

### L2. 裁定表(アプリ改善 — ChatGPT §7 / Grok §3)

| # | 提案 | 裁定 |
|---|---|---|
| 7.1 | インポート4形式の自動判別+結果レポート | **実装** — 単独/配列/{customPresets}/バックアップ全体を `coerceImport()` で判別。追加/ID重複スキップ/不正の件数を通知 |
| 7.2 | ID重複・再インポート防止 | **実装**(スキップ方式+件数報告。上書き/別名の選択UIは将来)。セーブは savedAt+presetId+name の完全一致を除外 |
| 7.3 | schemaVersion/appVersion | **実装** — 書き出しは schemaVersion:2+appVersion+exportedAt。v1 も受理(coerceImport が吸収) |
| 7.4 | seed の正式スキーマ化 | **実装** — preset.seed 最優先、無指定は従来の id/name ハッシュ(既存レイアウト完全互換)。決定性を機械検証 |
| 7.5 | overlays に trail/monitor | **実装** — バリデータの保持キーに追加(💫と同等の軌跡・保存量モニタ既定ONをカスタムでも指定可) |
| 7.6 | JSONファイル保存/共有 | **実装** — Web Share API(iPhoneは共有シート)→ 非対応時 .json ダウンロード |
| 7.7〜7.11 | A/B比較・experimentメタ・グラフ汎用化・ラベル・ギャラリー | **記録**(P1後続候補。A/B比較は次版の第一候補) |
| 7.12〜7.14 | Web Worker・Barnes-Hut・APIプロキシ | **記録**(P2。近似導入時も保存則検証は厳密全対を維持する条件を承継) |
| 7.15 | 文書正本の一本化(PRESET_SCHEMA.md) | **部分対応** — 本書冒頭に現行個数の注意書きを追加。スキーマ集約文書は Phase 1 後続 |
| Grok | CSVエクスポート・時系列グラフ・理論解説パネル・ダークモード等 | **記録**(教育価値順に P1 後続へ)。マルチプロバイダは v1.10 で実装済み |

### L3. Phase 1 CI 基盤(ロードマップ本体)

- **`tests/qa.mjs`**: 1コマンド機械QA(`npm test`)。構文 / verify 11項目 / 全16プリセット
  スモーク / i18n / few-shot 5例 / BH捕捉 / kLens互換 / インポート4形式+重複 / seed決定性 /
  新サンプル3種の挙動 / ♨️🪐長時間挙動(`QA_FAST=1` で省略可)= **計42項目**。
  結果は `tests/out/qa-results.json` にコミットハッシュ付きで保存。
- **`.github/workflows/ci.yml`**: push/PR ごとに実行し結果JSONを artifact 化。
- **`package.json`**: devDependency は playwright のみ(アプリ本体は依存ゼロのまま)。
- ローカルで playwright 未インストールでも `PLAYWRIGHT_CORE_DIR`+`CHROMIUM_PATH` で実行可。

### L4. 受け入れ条件(v1.12 QA — 全PASS 機械確認済み)

`node tests/qa.mjs` が 42/42 PASS(2026-07-16 実測)。加えて `presets/SAMPLE_PACK.json` の
実ファイルインポートで 5件追加・再インポート重複0・en/trail 保持・図8起動 NaNなしを確認。

---

## 付録M: v1.13 — A/B比較モード・理論解説パネル・パック内蔵化・♨️再調整(2026-07-16)

原仮定者タスク: ①A/B比較モード(第6次裁定 7.7 の筆頭候補) ②Grok提案の理論解説パネル
③任意パック5本の内蔵化 ④♨️対流「加熱が強すぎてうまく対流しない」の再調整(不可なら撤去)。

### M1. A/B比較モード

- パラメータタブ最上部の「A/B比較」: パラメータ(timeScale除く — 両者は同じ座標時間を刻む前提)
  とB側の値を指定して開始。**同一プリセット=同一シードで2シムを構築**し、上下分割で同時駆動。
- 描画は比較ビュー(粒子・境界・光線・A/Bラベル)— オーバーレイ・タップ選択・トレイルは
  単独ビュー専用。実行中のパラメータ編集は A のみに効く(仕様として明記)。
- ⏮はA/B両方を初期配置へ(A側の編集とB差分は保持)。プリセット切替で解除。
- 実装: `ab={key,valB,simB}` / `abStart/abStop` / `drawWorldInto(S,y0,hgt,label)`(クリップ+
  局所変換。温度色は各シム独立の90パーセンタイル正規化)/ ループで B へ同数の substep。
- テストフック: `HP.abStart(key,valB)` / `HP.abStop()` / `HP.ab()`。
- QA: 同一初期条件(座標一致1e-9)・パラメータ差・同期前進(t差<1e-6)・軌道差の発生・解除を機械検証。

### M2. 理論解説パネル(Grok提案)

- 一行用語集 `LAWS`(ja/en): A1〜A13(A7′/A12′含む)・E4/E5′/E6′/E7R/E8R/E9/E10′/E11・T1〜T9。
- `extractLawRefs(description)` がプリセット説明文から法則参照を正規表現抽出
  (用語集にあるものだけ・出現順・重複なし)→ 説明タブに「この現象の法則」節を自動表示。
- **規約: 内蔵プリセットの説明文には法則参照を最低1つ含める**(QA `theory.refs-all` が機械強制)。

### M3. パック5本の内蔵化(計21種)

binary(⭐連星系と周連星円盤・天体)/ fig8(♾️8の字・天体)/ blens(🔭連星レンズ・光)/
merger(🌠銀河合体・天体)/ collapse(🌫️雲崩壊・天体)。ID は pack_* から短縮形へ変更
(既にパックをインポート済みの端末では AI生成グループに旧IDが残るが、削除すればよい)。
`presets/SAMPLE_PACK.json` は撤去(内蔵化に伴い役目を終えた)。

### M4. ♨️対流の再調整(機械実験 conv-H0〜H8)

**過熱の定量化**: 現行(spin12×11)は中央占有率(|x|,|y|<95 の自由粒子率)が **0.00〜0.07** —
ガスが壁沿いの弾道循環になり中央が空洞化(=「うまく対流しない」の実体)。
**掃引**: spin7/6ヒーター/spin8+重力増/spin5 → いずれも**冷たい山が凍結**(maxFrac 0.5〜0.65・
塊速度≈0)。spin11 → 再び空洞化(0.019〜0.031)。**spin10 が唯一の窓**:
中央占有 0.13〜0.17・膠着1.5%・循環同符号・t=256まで塊ゼロ。
**採用**: ヒーター spin 12→10(内蔵+few-shot例5を同期)。QA に空洞化検査
(後半の中央占有最小値 ≥0.08)を追加し再発を機械防止。内蔵実測: stuck 3.1%・sign 6/6・
center 11.2%。**撤去は不要と判断**(窓は狭いが機械検証で固定できたため)。

### M5. 受け入れ条件(v1.13 QA — 全PASS 機械確認済み)

`node tests/qa.mjs` **54/54 PASS**(21プリセット・theory 2項目・ab 5項目・
♨️空洞化検査込み)。A/B比較の視覚確認: 🌌galaxy kFrame=1/0 で上半分のみ加熱と
平坦化が進む対比が一画面で見える(スクリーンショット確認済み)。

---

## 付録N: v1.15 — 第7次提案(ChatGPT/Grok/Gemini 精査レポート3件)の裁定と整合性修正(2026-07-17)

原仮定者より外部AIレビュー3件(ChatGPT 精査報告書・Grok サンプル精査・Gemini 精査レポート)を
受領し、model-routing 指名で裁定・実装した。方針: **ChatGPT の P0(整合性修正)を軸に採用**し、
「サンプルを増やすより主張を測る」という3レビュー共通の提言に従う。

### N1. 裁定表(採用分)

| # | 提案(出所) | 裁定 | 実装 |
|---|---|---|---|
| P0-1 | APP_VERSION が 1.13 のまま(ChatGPT 5.1) | **採用** | 1.15 に同期+QA `version.sync`(package.json と major.minor 一致を機械強制) |
| P0-2 | スライダー範囲と内蔵値の不一致(ChatGPT 5.2) | **採用** | κs 0.5→2・Kt 2000→10000・etaRad 0.1→1(CLAMPS と同一に)。QA `slider.covers-builtins` が全内蔵値の範囲内を機械強制 |
| P0-3 | few-shot例5 の m13000 が質量上限5000超・無警告クランプ(ChatGPT 5.3) | **採用** | m5000・G3.9(G×m=19500 不変で外場を保存)。24000步の機械実験で対流挙動の維持を確認(N3) |
| P0-4 | 粒子値クランプの無警告(ChatGPT 5.3) | **採用** | validatePreset の CO/LEN/MASS/VEL/SPN・aroundMass/omega/vNoise/vScale クランプが warnings に記録。fewshot.validate(警告ゼロ)が few-shot の値域整合を恒久強制 |
| P0-5 | 固定拘束・熱浴の明示(ChatGPT 6)| **採用** | 外部要素バッジ — bodies から固定粒子/レール/熱浴を**自動判定**(externalTags。理論パネルと同じ保守コストゼロ方針)し説明タブに表示。QA `ext.*` 2項目 |
| P0-6 | 主要主張の定量QA(ChatGPT 5.6/10.1) | **採用(銀河)** | `claim.galaxy-flatten` — 実プリセット・同一初期条件で kFrame=1/0 を3000步同時駆動し外縁帯 r∈[156,286] の平均接線速度比を判定。縮小同型は verify.V11 で既存担保 |
| P1-a | AIプロンプトの軌道系 pinned 指針の矛盾(ChatGPT 5.4) | **採用** | ルール4を目的別分岐に改訂(閉鎖系=pinned:false / 展示系=pinned:true+description 明記)。RUNTIME_LLM 写し同期 |
| P1-b | 対比プリセットのワンタップ化(Grok 4.1/ChatGPT P1-5) | **採用** | `abSuggest:{param,value}` を7プリセット(mach D0=50・gclock Kt=150・galaxy kFrame=0・pressure kRep=0・counterring muF=0・merger kFrame=0.3・collapse etaRad=0)に宣言し「おすすめA/B」ボタンで abStart。値は各プリセットの説明文が推奨する対比のみ(発明しない)。QA `absuggest.*` 2項目 |

### N2. 裁定表(見送り・却下分と理由)

| 提案(出所) | 裁定 | 理由 |
|---|---|---|
| QuadTree/Spatial Hash・Web Workers・WebGL/WebGPU(Gemini 1A) | **却下** | N_CAP=600 は設計上限(モバイル・単一ファイル・ゼロ依存)。O(N²)+pairD キャッシュで現行負荷は達成済み。「数万粒子」はアプリの目的(統制実験)と別方向 |
| 降着円盤ジェット・ロシュ限界・宇宙膨張の新サンプル(Gemini 2B) | **保留** | 3レビュー共通の「新サンプルより測定」提言と衝突。ロシュ限界は次期パック候補として記録 |
| Visual Regression(ピクセル比較)(Gemini 1B) | **保留** | 数値判定(保存則・解析一致・挙動指標)の方が環境非依存で強い。現行QAを優先 |
| スリングショット投入・Time Warp スライダー(Gemini 1C) | **却下/済** | 速度倍率 ×0.25〜×8 は実装済み。投入機能は統制実験の再現性(シード固定)と干渉 |
| プリセットセレクタのグループ化(Grok 4.2) | **実装済み** | optgroup 4分類は v1.10 から存在(QA i18n が検査) |
| スキーマv3(kind/claim/metrics/controls)・サンプル固有コントロール・3階層パラメータ(ChatGPT 7/8/12) | **保留(次期 Phase B)** | 大規模改修。今回の abSuggest はその最小前方互換(プリセット宣言→UI 生成)として設計 |
| 保存量モニタへの拘束帳簿追加(ChatGPT 6) | **保留** | バッジ(定性明示)を先行。帳簿の実装はエンジン変更を伴うため単独フェーズで |
| モバイル寸法・a11y・性能QA群(ChatGPT 10.2/10.3) | **保留** | ロードマップに記録。CI 時間との兼ね合いで別フェーズ |

### N3. 機械実験(¥0)

- **例5修正の等価性**: G×m を保存(1.5×13000=3.9×5000=19500)して外場不変。v1.13 の対流検査
  ロジック(24000步: 循環同符号・膠着・中央占有)で G を掃引した実測:
  | G(m=5000) | 循環同符号 | 膠着 | 中央占有(最小) | 判定 |
  |---|---|---|---|---|
  | 3.0 | 6/6 | 10.8% | 4.6% | 空洞化帯・膠着悪化 |
  | 3.5 | 6/6 | 10.8% | 6.9% | 空洞化帯 |
  | **3.9(外場保存)** | **6/6** | **4.2%** | **7.7%** | **採用** — 空洞化帯(≤7%)の外・警告ゼロ |
  発見: 旧 few-shot は m13000 がクランプで 5000 に落ち、実行時は G×m=7500(内蔵の38%)に
  劣化していた。修正版は本来の外場を初めて few-shot で再現している。
- **銀河平坦化の閾値校正**: 外縁帯 vφ 比(kF1/kF0)の時間発展を実測 — 3000步1.020・
  6000步1.082・9000步1.120・12000步0.986(円盤進化で反転)。単調でないため固定シードの
  決定論に依拠して **6000步時点・閾値>1.04(実測1.082 の2倍マージン)** を採用。

### N4. 受け入れ条件

`node tests/qa.mjs` 全PASS(新規: version.sync / slider.covers-builtins / ext.detect / ext.panel /
absuggest.valid / absuggest.one-tap / claim.galaxy-flatten の7項目)+外部要素バッジと
おすすめA/Bボタンの視覚確認。
