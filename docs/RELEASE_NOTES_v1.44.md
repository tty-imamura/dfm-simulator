# RELEASE NOTES v1.44 検証版(RC)— NS 連星の数値精度を検証中

> **「完成」には 2 つの意味があり、この版は片方だけを目指す**(第260便d・第52報の読み)。
> 第52報は「**早期にコンパクト天体連星サンプルと銀河サンプルを完成させる**」と
> 「**中性子星連星までの現実較正を終える**」を並べて指示している。**この 2 つは別の完成である**:
>
> | | (1) **原理サンプルの展示としての完成** | (2) **現実較正としての完成** |
> |---|---|---|
> | 何を満たせば完成か | 展示の条件(既定で見える・帳簿が閉じる・宣言が揃う)を列挙して 1 つずつ潰す | `docs/CALIBRATION_VERDICT_v1.44.md` §5′ の**完了定義 4 条件** |
> | いまの状態 | 🪟🎠 は条件のうち**数項が済み・1 項が未**(〔第259便〕統括節) | **4 条件のうち 1 つも満たしていない**(§5′) |
> | この版が名乗るもの | **名乗る**(展示として出す) | **名乗らない** |
>
> **(1) が済んでも (2) は済まない。** 版名に「較正」を入れないのはそのためである ——
> **「NS 連星までの現実較正を終えた」「NS 較正完了版」とは書かない。**


> **版名**(第258便d/第50報・3 審査 v16 一致で改題): 「**v1.44 検証版(RC)— NS 連星の数値精度を検証中**」。
> 第257便d の副題「NS 連星までの数値収束・観測一致は主張しない」は、**否定形で書かれていたために
> 「収束はしている」と読めた**。実際に足りていないのは**数値精度**であり、⚡ では h と h/4 の差
> 72.704 s が門の予算 0.3σ(7.517×10⁻⁸ s)の **9.67×10⁸ 倍**である。**検証中である**と書く。
> **版名**(第257便d/第49報・3 審査 v15 一致): 「**v1.44 検証版(RC)**」。
> 「NS 連星までの現実較正完了」とは書けない —— NS 連星で 3σ の門を通った量は **0 件**、
> 恒星連星で門が出した合否 4 件のうち **2 件は DFM 版の不合格(✴️ 26σ・💫 210σ)**である。
> **root 昇格も APP_VERSION 変更も、第257便d でも行っていない**(版を切るかどうかは統括の裁定)。

> **この文書の位置づけ**
> 第48報の原仮定者指示「中性子星連星までの現実較正を、近点移動込みで完了させる。この時点で、一旦
> Release する」への**起草**である。3 審査 v14 の Grok が言うとおり、ここでいう Release は
> 「**較正状態を台帳に固定して版を切ること**」であって、「理論が正しいと示すこと」ではない。
> **本便では root への昇格も APP_VERSION の変更も行っていない**(版を切るかどうかは統括の裁定)。
> 本稿は「何を固定し、何を主張しないか」を先に書き切ることだけを目的とする。

---

## 1. 何を Release と呼ぶのか(範囲の宣言)

**固定するのは「較正の状態」である。** 具体的には次の 4 つが台帳に載っていること:

| 固定するもの | 値 | どこにあるか |
|---|---|---|
| 慣性(質量)の一次則の不動点 f | **1.999942269345993**(⚡ J0737 で自己無撞着に解いた値・**fit ノブ 0**) | `psrDoubleABDFM.massCalibration`(`law:"inertia-law-lin-v1"`) |
| pull 重みの参照 D₀ᵖ | **3.24204×10⁻⁷ 単位**(=3.24204×10⁸ kg/m²) | 4 系の `physics.D0pull` |
| κ(=G/c₀²) | **7.415555555555556×10⁻⁷**(相対論的連星族 c₀=3×10³) | 4 系の `physics.kappaT` |
| 数値の宣言 | `framePrecision:"double"`・`stateCarry:"double"`・`cmGauge:"barycentric"`・`coupleSink:"core"` | 4 系の `physics` |

**較正に使った観測量は 1 つだけである** —— ⚡(PSR J0737−3039A/B)の**公転周期**。
他の 3 系(🧮 J1757−1854・🩺 J1946+2052・🧶 B1534+12)は**この処方を 1 か所も動かさずに当てた
hold-out** であり、係数をこの 3 系で決め直したことは一度もない(🧶 は第251便a 以降 **凍結 hold-out**)。

## 2. hold-out で何が起きたか(**値は測ったものだけ**)

| 系 | 量 | DFM | 観測換算 | 残差 |
|---|---|---:|---:|---:|
| 🧮 J1757−1854 | 近点間 P | 15767.87 s(h/4) | 15857.669019168 s | −0.566% |
| 🧮 | e | 0.606889(h/4) | 0.6058142 | +0.18% |
| 🩺 J1946+2052 | 近点間 P | 6757.54 s(h/4) | 6781.366656 s | −0.351% |
| 🩺 | e | 0.066265(h/4) | 0.063848 | +3.8% |
| 🧶 B1534+12 | 近点間 P | 36306.00 s(h/4) | 36351.702644 s | −0.126% |
| 🧶 | e | 0.274376(h/4) | 0.2736775 | +0.26% |

**近点移動は hold-out ではない**(→ 3.)。

## 3. Negative Claims(**この版が主張しないこと**)

1. **NS 連星について 3σ の観測一致は主張しない。** 棚卸しの機械門(`|y−y_obs| ≤ 3σ+ε_num` かつ
   `ε_num ≤ 0.3σ`)を通った NS 連星の量は **0 件**である(第256便d の実測)。止まっているのは 2 か所で、
   どちらも「合わない」ではなく「**まだ言えない**」: (i) 同じ定義の検出器 A/B の広がりが観測 σ_rel より
   大きい、(ii) 既定刻みの離散化誤差 ε_num が観測 σ の予算(0.3σ)を **3×10²〜9.7×10⁸ 倍**超える。
   **dt を 4 段・8 段にしても埋まらない**(1 次収束なら 10⁹ 倍の細分が要る)。
   なお (i) は第256便d ③ の合成軌道検定で「**定義の不一致ではなく軌道の雑音**」と同定された ——
   門の名前(`definitionMatches`)と条件を分けるかは統括の決断事項である。
   **恒星連星(この Release の範囲外)では門が初めて合否を出した**: 観測版 ✨ α Cen AB・🌟 Sirius AB の
   周期が「合(3σ)」(残差 0.096σ / 0.274σ)、**DFM 版 ✴️・💫 が「否(3σ)」(残差 26σ / 210σ)**である。
   **DFM 版が 3σ で否になった事実をこの Release は隠さない。**
2. **近点移動が観測と合ったとは書かない。** DFM/観測の比は 4 系とも **約 2**
   (h/4・窓 20 近点で 2.0014 / 2.0020 / 2.0023 / **2.0258**)。これは較正質量 fM(f≈2)が
   E12 の 1PN チャネルへそのまま入る**較正の従属量**であって、独立な予言ではない。
   **比は obsCard の別欄に出す**(第256便d)—— 数値収束の記録であって一致の主張ではない。
   🧶 の +1.2% の食い違いは離散化誤差(ε_num=0.33%)より大きく、離散化では説明できない
   (第252便a: その差の全部が λ_PN=0 の基線 A0 である)。
3. **`spaceMesh`(空間メッシュ)は本 Release のどのプリセットも宣言していない。**
   第253〜255便の空間メッシュは原理サンプル(🪟🫂🎠)と診断であって、現実較正の経路には入っていない。
4. **λ_PN=1/f 等の経験補正は本体へ自動採用しない**(3 審査 v14 ChatGPT)。候補は variant
   (🪶🪃🪀🪤)としてだけ存在し、本体 4 系の宣言は 1 bit も動かしていない。
5. **周期短縮(ΔP/P)を重力波の再現とは呼ばない。** 既定刻みで測れる短縮は dt にほぼ線形の
   **離散化仕事**で、連続極限で消える(Peters 式との比 8.8×10⁸ は記録であって比較ではない)。
6. **ブラックホール以降はトイモデルである**(原仮定者 第48報)。観測値と「類似」しつつ安定すれば
   よく、**証明しない**。「観測質量を補正した時点で元の計算式を否定している」以上、証明を強めるほど
   反感が増す —— だから BH・星団・銀河の展示は「類似と安定」だけを書く。
7. **星団は本 Release の範囲外である。** 第256便d の対照(C0/C1/Cn)は「中心に BH を置けば安定する」を
   **否定した**(→ 6. と同じ流儀で、否定結果をそのまま載せる)。
8. **「現実較正完了」とは書かない**(第257便d で版名に固定した)。この版が固定するのは
   **較正の状態**(§1 の 4 項目)であって、観測との一致ではない。**DFM 恒星版の 3σ 不合格
   2 件(✴️ α Cen AB 25.95σ・💫 Sirius AB 210.3σ・いずれも「同方向 1 周の公転周期」・
   ε_num は予算内なので数値誤差では説明できない)を台帳に載せたうえで版を切る。**
9. **第256便a の「f=1(観測質量)+ 空間メッシュの織り込み(pairPN)で質量補正が不要になり近点移動も
   解決する」という検証仮説は、実測で不成立である。** f=1・weave off では 4 系とも軌道が成立せず
   (⚡ の e が 0.088→0.823)、f=1・pairPN では Δϖ の DFM/観測比が **6.8 / 11.0 / 3.9 / 108**(⚡🧮🩺🧶)
   と本体の約 2 より遠ざかった。**これは「相対慣性則一般の否定」ではない** —— 否定されたのは
   **この 1 つの実装(対ループへの織り込み・χ 混合・1PN 速度依存項の引数だけを差し替える版)**であり、
   3 審査 v15 が書いた作用・反作用・エネルギーを定義する相対慣性則は**まだ実装していない**。
10. **ブラックホール以降はトイである**(6. の具体化)。この版が BH 以降に求めるのは 3 つだけ:
   **(a) 帳簿が閉じること**(E_tot=K+U+E_shell+E_core+E_mesh+Q+E_escaped・ΔE_tot=W_ext)、
   **(b) 観測されていないエネルギーを許容するが容量は有限であること**、
   **(c) 質量以外の観測量と「類似」すること**。**証明はしない**(観測一致の主張もしない)。

11. **`orbitNoiseIndicator`(検出器 A−B の相対差)を「軌道雑音」と同定しきらない。** この欄は
   **門に入らない**(`gate:"none"`)。読みは ⚡ 近点移動 **1.1×10⁴**・🩺 近点移動 **1.2×10³**・
   恒星連星 **≤7×10⁻⁷**(いずれも σ_rel を 1 とした目盛り)である。第256便d ③ の合成軌道検定は
   「検出器 B は位置雑音 ε≈10⁻¹⁰〜10⁻⁸ に敏感である」ことを示したが、**シミュレータ側の A−B の
   すべてが軌道雑音だと同定されたわけではない** —— 積分器の系統誤差・窓の取り方・近点の密集など、
   同じ欄に入りうる原因を切り分けていない。**この欄を根拠に「雑音だから無視してよい」とは書かない。**
12. **証拠付き予測は 0 件である**(第258便d)。3σ の門を通った 2 量(✨🌟 の周期)も、
   `usedForFit:false` / `validation:"held-out"` / `dataset` / `frozenProtocol` の 4 つを
   **宣言していない**ので予測に数えない。第257便d までの弱い数え方(「③ のうち較正の従属量でないもの」)
   では 2 件だった —— **2 → 0 である**。**「予測が成立した」とは書かない。**
13. **条件不一致 8 行を隔離した**(第258便d)。🟠 木星衛星 4・🌇 金星・🥔 火星衛星の対照差・
   ❄️ 冥王星/カロン・🌊 海王星の「**kFrame=0 対照**」の行に、**kFrame=1 の走行の測定値**が
   割り当てられていた(プリセットの physics は 1 つで、1 回の走行から 2 つの条件の行へ同じ数値が
   配られていた)。**合っていたのでも合っていなかったのでもなく、対照条件の走行が行われていない。**
   元の実測は `conditionRejectedEvidence` に残してある(捨てていない)。
   **この 8 行を「対照が合っていた」と読んではならない。**
14. **現実較正 37 本の 4 値判定は 合 0 / 量限定合 2 / 否 2 / 保留 33 である**(第258便d 起草 →
   第259便d 更新・`docs/CALIBRATION_VERDICT_v1.44.md`)。**完成は 0 本**であり、**保留 33 は否定ではない**
   (σ 未接続・写像未確定・数値精度・条件不一致という手続き上の状態である)。
   **40 本 → 37 本は分類変更によるものである**(第259便d — 参照が理論値の ⭕📶📐 を原理実証へ移した)。
   **分類を変えても不足は 1 つも解決していない**(数える母集団を変えただけである)。
15. **NS 連星の近点移動は deg/yr で読んでも観測の約 2 倍である**(第258便d)。
   実測/観測の比は deg/yr で **2.0574(⚡)/ 2.1142(🧮)/ 2.1215(🩺)/ 2.0414(🧶)**、
   °/周 で 2.0292 / 2.0292 / 2.0839 / 2.0308 —— **単位を変えても比 2 は直らない。**
   (🧿 だけ 1.0385 だが、🧿 の近点移動は **fit 対象**であって予測ではない。)
   なお**この「約 2」と較正質量 fM の f≈2 は同じ「2」ではない**。f≈2 は
   **⚡🎻 の一次則・pull 重み・D₀ᵖ=0.324204 単位という特定条件での較正結果**であって、
   **全恒星に共通する値ではない**(同じ一次則でも 💫 は f=1.0003769・✴️ も 10⁻⁴ 級である)。
16. **💫 Sirius AB DFM について、観測質量そのものが σ 予算を超えているとは書かない**(第259便d)。
   正しい言い方は「**補正質量 f=1.000377 を維持すると kFrame=0 でも 3.61σ**」であり、
   **f=1(観測質量)+ kFrame=0 の対照は 0.27σ** で 3σ の内側に入る(診断コピーの実測・
   `tests/exp-w259d-kf0.mjs`)。**これは「💫 を直した」ではない** —— プリセットは f=1.0003769 を
   宣言したままで、f=1・kFrame=1 は 206.16σ である(**引きずりの大きさという足りない要素は消えていない**)。
17. **NS 連星の数値精度について「dt を細かくすれば合う」とは書かない**(第259便d)。
   **⚡ の近点移動は dt/16 でも漸近域に居ない**(観測次数が段をずらすと 2.66 → 0.51 → 1.17 と動く)。
   周期の Richardson 外挿は ⚡ が観測から **−0.2190 s(8.74×10⁵σ)**、🧶 が **−15.8579 s(1.84×10⁷σ)**で、
   **外挿は走行ではない**(推定値である)。ε_num が σ 予算に対して足りない量は**約 10⁸ 倍(約 8 桁)**であって、
   「10⁸ 桁」ではない。
18. **「質量を Float64 にすれば精度が足りる」とは書かない**(第259便d)。⚡🧶 は `framePrecision`/
   `stateCarry` が double だが `S.m` は `Float32Array` のままで、**質量の丸めだけの寄与は
   ⚡ 215.5σ・🧶 251.7σ** —— 離散化誤差(⚡ で 2.90×10⁸σ)の**約 10⁻⁶ 倍**である。
   **精度を決めているのは離散化であって質量の型ではない。**
19. **`S._core` の JIT 崖(第258便e)を解決したとは書かない**(第259便d)。
   第259便d が入れたのは**数え方を固定した lint 予算(36000 字)**と、
   冷たいブロック 1 件の逐語移動による **−432 字**(現在 35197 字・残量 803 字)、
   そして **informational の A/B JIT probe** だけである。**しきい値そのものは依然として測っていない。**

20. **「複素決定力場を慣性の場として使える」とは書かない**(第260便a — Negative Claim の固定)。
   `lawVersion:"complex"` の **A=Σmᵢvᵢ/(rᵢ²+ε²)^{p/2} は速度ではない**(単位 M·L^{1−p}·T^{−1})。
   同じ p の **W=Σmᵢ(rᵢ²+ε²)^{−p/2} で割って無次元化すると、それは D₀=0 の正規化平均そのもの**
   (= `lawVersion:"scalar"`)になる。すなわち **A/W = D₀=0 の scalar** であり、
   「複素重ね合わせを慣性の場として使う」道は**正規化を別に宣言しない限り閉じている**(未導出)。
   トイ積分器へは接続していない(`S.geoToyStop="complexNotVelocity"`)。
   **「複素場が慣性の正体」とは書かない。**
21. **蓄積格子が共通場 API を読めるようになったことを「表示と力が同じ場になった」とは書かない**
   (第260便a)。読むのは **全源 scalar/local・背景 static・時間微分は不完全**(源の ax/ay を
   捏造しない)の**診断場**であって、銀河の既存表示(disk/affine の u_n)ともエンジンの
   E6′/座標慣性とも**別の場**である。同じ 81 交点での \|u_cur−u_api\| は 🪟 2.2002・🎠 1.7454
   (相対で 1 に届く)—— 格子には注記 `API diagnostic: <law> / all / static (not disk-affine)` が付く。
   **「格子が同じ関数になった」「絵が正しくなった」とは書かない。**
22. **有限容量(`meshEnergyCapacity`)は力を制限しない**(第259便・第260便)。
   容量は**帳簿の欄**であって、粒子のキックを制約する機構ではない。
   **「有限容量で距離減衰が実現した」とは書かない。**
23. **拡張天体(`body.layers`)どうしが重なる配置は unsupported である**(第260便)。
   点源取消 + 層重力の補正が**対ごとに二重に入る**ため、重なった 2 体の重力チャネルは
   **外向き**になりうる。保証するのは**非重複時の単極子等価**だけである。
   **第261便b の追記**: 対ごとの ΔF は非重複対にも広がり、近接域の桁落ち(d=10⁻⁶ で斥力 +2.4×10⁻⁴ を返していた)は
   推定誤差の小さい分岐(級数/安定積分/単極子)で直った(級数と相対差 ≤6.4×10⁻¹⁶)。d=0 は**対称性による力 0**
   として定義され(`layerStopMode:"define"` 既定・`"halt"` は opt-in で時刻を進めない)、**unsupported は
   「ΔF が定義できない対」だけ**に狭まった。交替点の ΔF は相対 10⁻⁶ 級が精度の上限であり、
   d≳200 では `S.vx` の Float32 分解能で頭打ちになる。**「層付き融合は Release-ready」とは書かない。**
24. **NS 連星の現実較正について「終えた」とは書かない**(第260便d)。完了定義は
   `docs/CALIBRATION_VERDICT_v1.44.md` §5′ の 4 条件(ε_num ≤0.3σ / 近点移動が漸近域で比≈2 を
   較正従属量として置く / eProxy ⇄ e_T の写像 / R の決定手続き)で、**4 つとも満たしていない**。
   **距離は ⚡ で 9.67×10⁸ 倍・🧮 で 2.00×10⁸ 倍・🧶 で 3.45×10⁸ 倍**(0.3σ 予算に対して)である。
   **空間メッシュを優先することは、この 4 条件の延期と同値である。**
25. **「🧮 は漸近域だから精度が足りている」とは書かない**(第260便d)。🧮 は h/8 まで走らせて
   **4 量とも漸近域**に居るが(観測次数が段をずらしても 0.1 以内)、
   **h/4 → h/8 の差だけで 1.01×10⁷σ** である。**収束次数の合格と観測誤差内の合格は別の合格である。**
26. **`S._core` の lint 予算(36000 字)は崖のしきい値ではない**(第259便d → 第260便d)。
   第260便d は **A/B ゲートを 2 系統**にした(同一 html の 2 ページ自己対照 + 凍結基準 html 対 候補)が、
   **どちらも informational のままである**。**「JIT 崖を解決した」とは書かない。**
27. **`geoPN=3`(トイの測地線モード)の慣性則を較正候補から外す**(第261便・統括の裁定)。
   〔第260便a〕⑤ の p 掃引(`lawVersion` scalar/local × p=1/2/3)で、⚡ の周期と近点移動は
   **どの組み合わせでも観測の σ 予算に入らなかった**。したがって **geoPN=3 のトイは較正の自由度として
   使わない**(トイ自体は器として残す)。**「geoPN=3 で近点移動が直った」とは書かない**し、
   **「複素場を含む将来の場の構成則全体を反証した」とも書かない**(反証したのは**この 3×3 の掃引**だけである)。
   **第262便a の追記(f=1 でも交差しない)**: 第54報「空間メッシュなら、質量補正が『1』に近くても成立するのではないか」
   に対して、**観測質量そのもの(f=1)・kFrame=0** の 📻 幾何で **D₀ 8 点 × η(toyGain)3 点 × lawVersion 2 種 = 48 構成**を掃いた
   (`tests/exp-w262a-geotoy.mjs`・`tests/out/geotoy-w262a.json`)。**トイを強くするほど近点間 P は長くなり**、
   指示された 6 点(D₀ ≤ 0.6)の範囲では**観測周期をまたぐ構成は 0 件**である(最も近いのが本便の新プリセット 🩻 の構成で **+7.92 s = 3.16×10⁷σ**)。
   D₀ を 6000 まで延長すると形式的な交差は現れるが、**そこは χ≈5×10⁻⁷ = トイが実質的に切れている点**である
   (交差の 4 段: local η=0.25・D₀=2.785×10³ で dt/8 の残差 +1.19×10⁻⁴ s = **474σ**・近点移動 **0.6367 °/yr = 観測 16.899323 の 3.8%**)。
   **トイが釣り合わせているのではない**: 同じ幾何の χ→0 極限(geoPN=0 の Newton・f=1)が既に −2.12×10⁻³ s(8.44×10³σ)にいて、
   交差はその極限に戻る途中の 1 点にすぎない。したがって **「空間メッシュなら f≈1 で成立した」とは書かない**し、
   **この交差を較正則にもしない**(本項の主文は維持する)。
   **D₀=0 は別の理由で使えない**: 二体・D₀=0 では u_A=v_B(χ=1 厳密)・∂ₜu_A=g_B となって **r̈=0 が厳密に成立**し、
   遠点発の連星は直線で離脱する(r>4r₀ が解析値 √15 r₀/|v_rel|=6468.576 s へ dt 一次で収束・質量に依らない)。
   **χ=1 は共回転の証拠ではない**(D₀=0・単一源では u は位置に依らず ∇u=0 である)。
   **第263便a の追記(kFrame>0 を開けても交差しない)**: 第55報「コンパクト連星では空間メッシュの影響が強まるので
   geoPN=3 の適用となり、引きずりが弱まって kFrame≈0.7 などの結果となる」に対して、明示キー
   `physics.spaceMesh.toyAllowDrag:true` で **geoPN=3 ∧ kFrame>0** を許す診断経路を作り、
   **kFrame∈{0,0.25,0.5,0.7,1} × η(toyGain)∈{0,0.5,1} × lawVersion∈{scalar,local} = 30 点**を掃いた
   (`tests/exp-w263a-dragoverlay.mjs`・`tests/out/dragoverlay-w263a.json`)。
   **近点間 P を合わせる f* は 29/30 点で出る**が、**P と ω̇ が同時に観測へ入る点は 1 点も無い** ——
   f* での ω̇/観測は測れた 19 点で **−4.22×10³ 〜 +1.77×10⁴**、残り 11 点は近点方位の unwrap が通らず**測れない**。
   最接近は scalar・η=1・kFrame=0.7 の **2.18×10³ 倍**で、反対の端は kFrame=0・η=0 の **−6.3×10⁻⁴**(= Newton)である。
   **「geoPN=3 にすると引きずりが弱まる」は成り立たない**: 同じ f=1・kFrame=0.7 の対照 3 走行で、
   📻/⚡ の D₀ 宣言(D0pull=3.24204×10⁻⁷・χ≈0.9998)では **E12 が切れた分は +80.9 秒(0.19%)**しかなく、
   **トイを入れると連星が離脱する**。弱く見えるのは **D₀ を 0.006 に替えて χ を下げたから**であって、
   **法則を 3 にしたからではない**(🩻 の D₀ では E12 が切れた分 +3697.9 秒・トイの分 +11034.1 秒)。
   **本項の主文は維持する** —— **geoPN=3 のトイは kFrame>0 を許しても較正候補ではない。**
   明示キーは `sampleClass:"calibration"` では geoPN の値に依らず拒否し、受理しても
   **「E6′ とトイが同じ步で重なる(二重計上の可能性)」の警告 1 行と HUD の `overlay:drag`** を必ず出す。
28. **空間メッシュの歪みの規格化(第261便a)は表示の正規化であって物理ではない。**
   gain=1 の最大変位を**セル幅の κ=0.25 倍**に揃えたのは絵の安定化であり、u も χ も 1 bit も変えていない。
   **「gain=1 が物理的に正しい引きずりである」「メッシュの見た目が静止したこと=物理的解決」とは書かない。**
   **「歪みを質量の合計に反比例させる」案は測って採らなかった**(113 本中 14 本が gain=1 で折返す)。

29. **有限容量の器(`HP.dfmMeshCapacityStep`)を Release の要件にしない**(第261便d)。
   この器は **箱だけの opt-in** であり、**力へは 1 バイトも接続していない**(22. の再掲ではなく位置づけの宣言である)。
   **τ・χ・u・容量(密度 × 面積)はすべて宣言値**で、**観測から導出したとは呼ばない**。
   **`τ = τ_drag` は仮説である**(引きずりの緩和時間と容量の緩和時間が同じ数だと決まっていない)。
   **容量密度が一様であることも宣言であって導出ではない**(位置の関数にしていない = 距離は 1 つも入っていない)。
   **「有限容量で距離減衰が実現した」とは書かない。**

30. **帳簿の相対残差は「どの分母で割ったか」を書かずに引用してはならない**(第261便d)。
   🎠 galaxyMeshSpiral の T=96・h=0.016 の残差は、**従来分母(\|K₀\|+\|U₀\|+\|E_core₀\|)では 3.18×10⁻⁴**、
   **pinned 核の一定スピン E を除いた活動部分では 1.09×10⁻²** で、**34.28 倍違う**
   (分母の **97.083%** が固定バルジ核のスピン E である)。**本 Release はどちらか一方を正本にしない** ——
   帳簿 API が**両方を返す**(`denom.legacy` / `denom.active`)。**「帳簿が 1e−3 を通った」と単独で書かない。**

31. **`inertiaRemoval:"inStep"` の既定昇格はしていない**(第261便d)。
   `coupleSink` を宣言した宇宙の残余トルクを**同段階で受け先へ送る**経路は入ったが、
   **既定は `post` のままで、内蔵プリセットは 1 本も `inStep` を宣言していない**。
   測れたのは「clampSN=0 が 15 構成で保たれた」ことと「状態が基点とビット同一」ことまでで、
   **支持範囲の出入り(戻し)は本便の帯では 1 度も起きていない**ので、
   **「出入りでも壊れないことを確かめた」とは書かない。**

32. **R(支持半径)の決定手続きは「決まらない」**(第261便c)。
   完了定義の条件 4(`docs/CALIBRATION_VERDICT_v1.44.md` §5′)が求める
   「R を観測や理論から決める手続き」は**本便でも作れなかった**。R は宣言値のままである。
   **ただし条件 4 は R を使う較正系にだけかかる**: `inertiaSupportR` を宣言していない系
   (NS 4 系 ⚡🧮🩺🧶 を含む)は**条件 4 の対象外**であり、
   **「R が決まらないから NS 較正が未完」とは書かない**(NS が未完なのは条件 1 と条件 3 による)。
33. **NS 連星に「精度向上で合格見込み」は 1 件も立っていない**(第261便c)。
   第53報の「数値精度が上がれば合格する事が予測出来る段階に達したら…完了とする」に対して、
   本便が出したのは**器だけ**である(プリセット鍵 `calibrationForecast`・純関数 `HP.dfmForecastGate`・
   チップ・QA `behavior.calibrationForecast`)。**門 5 つの機械判定で 4 系とも不合格**で、
   落ちたのは**門(4)(別積分法/別抽出法による独立推定が無い)と門(5)(距離)**である
   (距離は 🩺 8.96×10⁴σ・🧮 6.17×10⁵σ・⚡ 8.75×10⁵σ・🧶 1.84×10⁷σ)。
   **門(3)(周期の漸近域)は 4 系とも通る —— 漸近域の門と 3σ の門は別の門である。**
   収束先(P∞)に対して質量係数 1 本で合わせ込んでも、**隣接外挿差(包絡)だけで 8.5×10³〜1.9×10⁵σ** 残る。
   **「合格見込みを NS に立てた」「収束先較正で NS が合った」とは書かない。**
34. **拡張体対の層差分 ΔF は「厳密」ではない —— 交替点の相対精度は 10⁻⁶ 級が上限で、
   遠方ではエンジン側が Float32 の床に当たる**(第261便b の実測・第262便b で採番)。
   `HP.dfmLayerPairForce` は 3 分岐(ε=0 の閉形式 / ε>0 の安定形 / 遠方の多重極 3 項)で近接域の
   桁落ちを直したが、**分岐の切り替え点(`dFmethod` が `stable`↔`multipole` を跨ぐあたり)が最も悪く、
   独立な求積との相対差は全域で ≤3×10⁻⁷**である(〔第261便b §1〕)。さらに**エンジンの側**では
   Δv=ΔF·dt が `S.vx` の 1 ulp に届かなくなるため、**d≳200 では純関数の ΔF と実測 Δa が桁で食い違う**
   (d=200 で純関数 +1.97×10⁻¹² 対エンジン +1.73×10⁻¹²・約 12% 差 —— 〔第261便b §2〕の表)。
   したがって **「ΔF は厳密になった」「層差分は遠方でも正しく効いている」とは書かない**。
   多拡張体の距離早期打ち切り(U2-6)は**規約だけを PHYSICS〔第262便b §7〕に置き、実装していない**
   —— 打ち切り予算を相対誤差で決めると d→∞ でいくらでも落とせてしまうためで、
   絶対加速度の予算・対称性・U の扱い・帳簿残差の門を先に測る必要がある。

35. **A/B ゲートを FAIL 化したことを「JIT 崖を解決した」とは書かない**(第262便d)。
   第262便d が入れたのは**判定の適用**だけである —— しきい値は〔第261便d〕が宣言した
   **(a) 自己対照 >4 / (b) 凍結基準 html 対 候補 >1.5** のままで、**1 つも動かしていない**。
   **`root-fallback` は凍結扱いしない**(その行は `judgement:"informational"` のままで fail を増やさない)、
   **取得失敗を root-fallback の合格に置き換えない**。**崖のしきい値そのものは依然として測っていない**
   —— Negative Claim 19・26 は取り下げない。**`tests/ci-frozen-baseline.sh` は夜間/手動ジョブの候補**であって
   PR ゲートではない(**`.github/workflows` は第262便d でも 1 文字も変えていない**)。
36. **太陽系 16 本の σ を「繋いだら判定が出た」とは書かない**(第262便d)。
   対応表(`SIGMA_TARGET_BODY`)を足して **target ごとに CSV の行へ繋いだ**が、
   **3σ の判定は 1 件も出ない**(合 0 / 量限定合 0 / 否 0 / **保留 16**)。
   切断点は**対応表ではなかった**: 139 量のうち
   **CSV に天体の行が無い 66 量**(月・地球・水星・ガリレオ衛星 4・D68・C 環内縁・ミマス・タイタン)・
   **行はあるが sigma 列が空の 33 量**(Venus・Phobos・Deimos・Charon・Miranda・Ariel・Umbriel・Titania・Oberon・Triton)・
   **天体はあるが量の行が無い 14 量**・門に入らない来歴の欄 26 量である(器 `tests/exp-w262d-solarsigma.mjs`・
   出力 `tests/out/solarsigma-w262d.json`)。**CSV の 18 行の σ はすべて恒星連星と NS 連星の行**であり、
   **太陽系の天体には σ が 1 つも登録されていない**。**保留は否定ではない**(手続き上の状態である)。
   **±1% の目安を σ に昇格させない。**
37. **帳簿で「活動部分を正本にした」ことを「帳簿が閉じた」とは書かない**(第262便d)。
   第262便d が決めたのは**どちらを正本にするか**だけである(値もしきい値も動いていない):
   正本は**活動部分**・従来分母は**併記**・**両方通過を「合」と呼ばない**・
   **「否」を名乗れるのは帳簿が閉じた宇宙で門を超えたときだけ**である。
   未定義項が残る宇宙の超過は **`"undefined-terms"`(門外)**として記録する ——
   超過が未定義項の中にあるのかを切り分けられないからで、**FAIL ではない**。
   実測(窓 T=96・h=0.016・6000 步)は **🎻 gw150914DFM 8.462×10⁻⁴ = `within`**(未定義項 2 件)・
   **🎠 galaxyMeshSpiral 活動 1.0905×10⁻² = `undefined-terms`**(従来分母では 3.181×10⁻⁴)・
   **🫐 tuc47DFM 3.098×10⁻¹ = `undefined-terms`**(未定義項 5 件)。**門は廃していない。**
38. **`frameWeightPow` の番兵整理(0 → 1)は規則の変更ではない**(第262便d)。
   `share` と未知名で **0** を返していた番兵を廃し、**p=1**(核 w=m/(d²+ε²)^{1/2})をそのまま返すようにした。
   「pull 族かどうか」は `frameWeightIsPull` **だけ**が答える(`pw>0` を真偽に使う箇所は 0 になった)。
   **全内蔵 121 本 × 600 步の状態は基点 4ccaa74 とビット同一**であり、**既定値も署名も 1 bit も動いていない**。
   **「フレーム重みの規則を変えた」「share の物理が変わった」とは書かない。**

39. **釣り合い曲線 f=1+kFrame を「較正則」とは書かない**(第263便a)。
   これは**測定の記録**である: 現行経路(geoPN=2 + E6′/E12)で近点間 P を観測に合わせる質量係数 f は
   **f = 1 + kFrame**(8 点で (f*−1)/kFrame = 0.99982〜0.99995・dt 外挿・〔第262便a ①〕)という直線に乗るが、
   **これは「質量の性質」でも「χ-law」でもない** —— 同じ帯で χ_A=χ_B=0.9999 は kFrame と一緒に動かないので、
   χ ではこの直線を説明できない。**どの kFrame を選ぶかは、この曲線からは決まらない**:
   **P だけを合わせるなら kFrame=0(f*=1.0000153 = 観測質量そのもの = 📻)**・
   **P と ω̇ を同時に合わせるなら kFrame≈0.70**(族と刻みで 0.68〜0.70 に散る)である。
   **後者を観測安定則に採らない** —— kFrame は 0 か 1 のどちらかを宣言する量であり(2026-08-25 裁定・
   QA `preset.kframe-binary01` が内蔵の kFrame∈{0,1} を機械固定する)、中間値を「合ったから」で選ぶのは fit である。
   **「⚡/📻 の kFrame を 0.7 にした」とも「kFrame≈0.7 を観測安定則にした」とも書かない**
   (⚡ と 📻 の JSON は第262便a・第263便a を通じて 1 bit も動いていない)。
   **ω̇(kFrame) は単調でもない** —— 0.7 を「代表値」と呼ぶ前に曲線の形を見ること(第263便a §③ の谷の表)。
40. **近点間 P の残差 10⁻²〜10⁻¹ s を「模型と観測の差」に算入しない**(第263便a)。
   位相制限(1.5π)の近点抽出器は、質量係数 f をなめらかに動かしたときに
   **窓の 20 個目が 1 公転ぶん入れ替わる点**を持ち、そこで近点間 P が **10⁻²〜10⁻¹ 秒の段差で跳ぶ**
   (〔第262便a ①5〕)。観測の σ_P=2.5056×10⁻⁷ 秒に対して **5〜6 桁粗い**ので、
   **これは 3σ 判定の分解能の床**である。根の位置 f は 10⁻¹² まで挟めており、
   **表に出る |ΔP| はこの段差の高さであって根の精度ではない**。
   したがって **|ΔP| が 10⁵σ 級に留まることを「模型が外れた大きさ」として引用しない**し、
   **段差より細かい許容で根を追い込んだことを「精度が上がった」とも書かない**
   (第263便a の根の許容 10⁻² 秒は**この床の上**に置いた宣言である)。


## 4. 何が新しく固定されたか(第256便d)

- **観測レコードの補完**: `paper/data/solar-observations.csv` に ⚡ の ω̇ 行
  (**16.899323(13) °/yr**・Kramer et al. 2021 PRX 11 041050)を追加し、🧶 B1534+12 の 3 行に
  **タイミングモデル(DD・Damour & Deruelle 1986)**と**エポックが未取得であること**を明記した。
  棚卸し `tests/exp-w249b-calaudit.mjs` は 🧶 の σ を読めるようになった(第251便c から CSV には
  あったのに、対応表に行が無くて「σ 無し」に見えていた)。
- **検出器の合成軌道検定**(`tests/exp-w256d-detector.mjs`・QA `behavior.periDetectorSynthetic`):
  真値が閉じた式で分かる軌道では検出器 A/B は真値を **10⁻¹⁰ 台**で復元する。したがって
  シミュレータ側の A−B(4.5×10⁻⁶〜2.9×10⁻⁴ °/周)は**定義差でも推定器誤差でもない**。
- **星団の対照器**(`tests/exp-w256d-cluster.mjs`): C0/C1/Cn を M・r_h・L を揃えて 3 seed・50 交差時間
  走らせた結果、**3 群とも半質量半径が 3.9〜7.5 倍に膨張**し、**複数 BH の Cn が 3 seed すべてで最も速く崩れる**。
- **恒星 4 量 + 📻 の 3 段**(`--dt3`): 「数値未解決 5」が **1** になり、代わりに **合 2 / 否 2** が出た
  (`converged` は 14 → **29** 量)。

## 4′. 第257便d(第49報)で足したもの

- **棚卸しの門を 4 段で読む**(`tests/exp-w249b-calaudit.mjs`): ①数値収束 ②観測量対応 ③観測適合(3σ)
  ④予測。**`definitionMatches` は A を正本にした「定義が宣言されているか」だけになり**、
  検出器 A−B は `orbitNoiseIndicator`(軌道側の相対 1e−10〜1e−8 の雑音に対する検出器差の感度)
  として別欄に出る(第256便d ③ の同定に合わせた命名)。第 4 の状態 **`mapping-unresolved`**
  (観測量対応が未確定 — 例: タイミングの e_T と距離極値の eProxy)を足した。
  **3σ の合否の数は前後で変わっていない(合 2 / 否 2)。**
- **⚡ の ω̇ 行の来歴**: 一次表(Kramer et al. 2021 PRX 11 041050 Table IV・arXiv:2112.06795 p.20・TDB)を
  直接確認した。値・σ は既存の転写と一致し **数字は 1 つも動いていない**。
  **P_b・e_T は Hu et al. (2022) の別解のまま**で、時刻系・エポック・タイミング解は混ぜない。
- **🧶 の dt/4 の計算時間予算を既定 80 s に**(精度条件の緩和ではない)。
- **Float32 質量の記録**: `S.m` は `stateCarry:"double"` の系でも Float32Array である。
  NS 4 系+🎻 の保持質量・最小刻み・宣言値との差と、massCalibration の f の分解能を台帳に入れた。
- **星団の N/ε 掃引**(`tests/exp-w257d-cluster2.mjs`): 初期の 2K/|U|・E_tot・λ・外縁条件を
  3 群で揃えてから N × ソフトニング ε を掃いた(→ PHYSICS〔第257便d〕)。**星団は本 Release の範囲外のまま。**

## 4″. 第258便d(第50報)で足したもの

- **`docs/CALIBRATION_VERDICT_v1.44.md`**(新規): 現実較正 40 本の**再判定台帳**。
  4 値(合/量限定合/否/保留)・3σ 門の内訳・保存済み代表残差・**足りない要素**・**対策**・
  **分類変更の提案**(5 本 ⭕📶📐⏰💿 —— 提案だけでプリセット JSON は 1 bit も変えていない)。
  QA `docs.calibration-verdict-sync` が 40 行の ID 集合と判定を機械固定している。
- **条件不一致の隔離**(`tests/lib-w258d-evidence.mjs` の純関数 + 棚卸し): 8 行を `条` へ。
  全 339 量に `requiredContext` / `measurementContext` が立ち、同じ取り違えは機械検出される。
- **deg/yr の門**: 近点角を unwrap して**実時刻**に線形 fit し、CSV の deg/yr と**換算せずに**比べる。
  時系・年の長さ(ユリウス年 = 単位の約束)・**傾きの共分散**を記録。°/周 は別欄に残す。
  **判定は 1 件も動かない**(5 行とも `数値未解決`)。CSV の ⚡ ω̇ 行にこの旨を注記した(**値は不変**)。
- **h8 検査点**(⚡ 1 系・dt/8=0.002・11,083,772 步・19.5 s): 周期と離心率は観測次数が
  1.000 のまま(**漸近域に居る**)だが、**近点移動は 2.05 → 0.88 と動く(漸近域に居ない)**。
  **他の 3 系は未走行である。**
- **ε_num の推定誤差欄**: `|Q_h−Q_{h/4}|` は**上限ではない**。漸近形なら `/(1−4^−p)` 倍
  (p≈1 で ×4/3)であり、⚡ の周期では 72.704 s → **96.951 s** になる。**門は緩めも締めもしていない。**
- **✴️💫 の感度**(`tests/exp-w258d-stellar.mjs`): kFrame 4 段 × D0pull 3 段。
  **✴️ は kFrame ≤ 0.0885 で 3σ に入る**が、**💫 は kFrame=0 でも 3.61σ で入らない**。

## 4‴. 第260便d(第52報)で足したもの

- **🧮 J1757−1854 の h8**(`tests/exp-w260d-precision.mjs`・28,819,761 步・65.8 s):
  4 量とも漸近域(周期 0.9867→0.9933・離心率 1.0136→1.0068・deg/yr 1.0433→1.0214・°/周 1.1045→1.0538)。
  **それでも ε_num は 0.3σ 予算の 2.00×10⁸ 倍**である。**未走行は 🩺 の 1 系だけになった。**
- **Float32 の ULP と質量丸めの感度を純関数へ**(`tests/lib-precision-diagnostics.mjs`・QA `lint.precisionUlp`):
  対象質量付近の ULP は **0.00048828125(=2⁻¹¹)**。旧記録「~0.0004」は**探索版が ULP ではなく
  「丸めが倒れるまでの距離」を返していた**ためである(真の ULP の 0.23〜0.81 倍にばらつく)。
- **A/B JIT ゲートを 2 系統へ**(`tests/perf.mjs`・QA `lint.perfAbJit`):
  (a) **同一 html の 2 ページ自己対照**(素の走行 対 A/B 走行・**基準値が要らない**ので CI で再現できる)と
  (b) **凍結基準 html 対 候補**(両ページ共通の遅化を捕まえる)。**どちらも informational のまま**である。
- **💫 の 3 対照を正式行に**(台帳 §4.1)・**NS 較正の完了定義 4 条件と距離**(台帳 §5′)・
  **証拠付き予測の宣言書式 9 欄**(台帳 §6′・**記録は 0 件**)。

## 4⁗. HOLD 解除条件(この 6 項が揃うまで版を切らない・第260便d)

**この版は HOLD である。** 解除の条件を**先に列挙**しておく(どれも「測れば決まる」形にした)。

| # | 条件 | いまの状態 |
|---:|---|---|
| **1** | **Q1〜Q3 の実機確認**(原仮定者の実機での 3 点)が消化されている | **未消化**(第52報は「実機確認: 済み」だが、Q1〜Q3 の個別消化は台帳に無い) |
| **2** | **R の決定手続きが「無い」ことが Negative Claims に載っている** | 本便で §5′ 条件 4 として明記。**Negative Claims への転記は統括の統合時** |
| **3** | **入場条件 (v)「表示が同じ関数を読む」の恒等 QA が通っている**(fieldApi:false でビット同一 / true で 81/81 非 null / 同一点で差 0) | **通っている**(第260便a・QA `behavior.fieldApiIdentity`)。ただし読むのは**全源 scalar/local・背景 static の診断場**で、表示の disk/affine とも力学の E6′/coordinate とも**別の場**である(同じ 81 交点の \|Δu\| は 🪟 2.2002・🎠 1.7489)。**「表示と力が同じ場になった」とは書かない**(Negative Claim 21)。🪟🎠 の obsCard には第261便a で**限定つきの 1 行**を入れた |
| **4** | **🪟🎠 の展示完成条件がすべて済んでいる**(蓄積格子が既定で見える・帳簿・宣言・格子が同じ場 API を読む) | 〔第259便〕統括節の一覧で**1 項が未**(格子が同じ場 API を読む) |
| **5** | **「NS 連星で 3σ を通った量は 0 件」が Release Notes の先頭に出ている** | §3-1 にある。**先頭へ繰り上げるのは統括の判断**(本便は §1 の直前に「完成の 2 定義」を置いた) |
| **6** | **`S._core` の規約と A/B 自己対照が 2 便続けて安定している** | 第260便d が 2 系統にした **1 便目**である(informational)。**もう 1 便必要** |

**この 6 項は「版を切る条件」であって「理論が正しいことの条件」ではない。**

## 4⁵. 第261便d(第53報)で足したもの

**足したのは器・規約・文書だけである。** `beta/index.html` の**既定値・力学・プリセット JSON は 1 bit も動いていない**
(全内蔵 **121 本 × 600 步**の状態も正準形も基点 09899d2 と**ビット同一**・NaN 0・`S._core` は **35197 字**のまま)。

- **帳簿の規格化に「両分母」を入れた**(`HP.dfmToyLedger(…).denom` と純関数 `HP.dfmLedgerRelative`・
  QA `behavior.ledgerNorm`): **従来分母**(\|K\|+\|U\|+\|E_core\|)と、**pinned 核の一定スピン E を除いた活動部分**を
  **併記**する。活動部分が ≈0(宣言値 `floorRel`=1e−6)なら比は **null** にして**絶対残差**を出す。
  **毎時刻の E_tot を分母にしない。** —— **🎠 では両分母で 1e−3 の合否が分かれる**(3.18×10⁻⁴ / 1.09×10⁻²)。
  **p_obs(収束次数)は両分母で同じ**なので、**規格化では 🎠🍇 の「分離できない」は 1 つも解けていない**(否定結果)。
- **E_shell 契約を照合した**(「T_int だけ・回転 E は K」): **6 本すべてで既存実装と一致**したので
  **署名便を作らない**(`thermal:"tint"` を新たに宣言するプリセットは 1 本も無い)。
- **`residualDragState` / `EshellState` / `denom` を正式 API にした**(値は 1 bit も変えていない)。
  **1e−3 の門を「窓 T=96・h=0.016(6000 步)・分母(従来/活動)・seed・読む欄・判定 informational」まで宣言した。**
- **coupleSink × inStep の 3 行**: ②「宣言時は sink へ送ったあと 0」を実装(**送り先も規則も
  `applyCoupleSinkAlt` と同一なので状態は 1 bit も変わらない**)・③ の記帳欄を足した。
  **15 構成で clampSN=0 が保たれた**。**既定は `post` のまま**(昇格は次便の署名便)。
  **③ の「支持範囲の出入り」は本便の帯では 1 度も発火しなかった**(**否定結果** —— 次便の宿題)。
- **A/B JIT ゲートの 2 便目**(`tests/exp-w258e-jitprobe.mjs` を基点 09899d2 対 本便で走行):
  (a) 自己対照 **0.707〜0.858**(第260便d の 0.722/0.814 と同じ帯)・(b) 凍結基準対候補 **0.95〜1.00**(門 1.5 の内側)。
  **FAIL 化の基準**((a) >4 = 非常ベル・(b) >1.5・**root-fallback は凍結扱いしない**・雑音床 ×0.95〜0.99)を
  QA `lint.perfAbJit` に宣言した。**適用は次便であり、判定は 2 系統とも informational のままである。**
- **凍結基準 html の部分 clone を試した**(`tests/ci-frozen-baseline.sh`・QA `lint.frozenBaseline`):
  `git clone --filter=blob:none --no-checkout` + `git show <凍結SHA>:beta/index.html` で **1.56〜1.79 s**・
  `.git` **2.6 MB**(全履歴 448 MB の 0.6%)・取り出した html は凍結 SHA と**バイト同一**。
  **`.github/workflows` は 1 文字も変えていない**(有効化は次便の裁定)。**手元の秒数は CI の上限ではない。**

## 4⁶. HOLD 解除条件 6 項の現在地(第261便 時点)

**§4⁗ の 6 項は解除されていない。** 本便(d)が動かせるのは 1・6 だけで、2〜5 は a〜c と統括の統合結果で決まる。

| # | 条件 | 第260便d 時点 | **第261便 時点** |
|---:|---|---|---|
| **1** | **Q1〜Q3 の実機確認**が消化されている | 未消化 | **未消化 —— ただし §4⁷ に「実機で見る項目」を列挙した**(消化そのものは原仮定者の実機待ち) |
| **2** | **R の決定手続きが「無い」ことが Negative Claims に載っている** | 台帳 §5′ にあり・転記は統括 | 〔統括記入〕 |
| **3** | 入場条件 (v) の恒等 QA が通っている | 第260便a で通った(**ただし診断場**) | 〔統括記入〕(第261便a の obsCard 限定行の裁定を含む) |
| **4** | **🪟🎠 の展示完成条件がすべて済んでいる** | 1 項が未(格子が同じ場 API を読む) | 〔統括記入〕 |
| **5** | **「NS 連星で 3σ を通った量は 0 件」が Release Notes の先頭に出ている** | §3-1 にある・繰り上げは統括 | 〔統括記入〕 |
| **6** | **`S._core` の規約と A/B 自己対照が 2 便続けて安定している** | **1 便目**(informational) | **2 便目が揃った**(自己対照 0.70〜0.86 が 2 便・凍結対候補 0.95〜1.00)。**FAIL 化は次便** —— **本項はまだ「安定した」であって「解除」ではない** |

**〔統括記入〕の枠は埋めるまで空のままにする。** 埋まっていない枠を「済」と読まない。

### 4⁶′. HOLD 解除条件 6 項の現在地(**第262便 時点**)

第262便d が動かせるのは **1(Q1〜Q3)と 6(A/B)**、および 2〜5 の枠に**第261便の実測を転記する**ことだけである。
**転記は「済」の宣言ではない** —— 何が測れて何が測れていないかを、枠に**数と限定つき**で入れる。

| # | 条件 | **第262便 時点** | 解除? |
|---:|---|---|---|
| **1** | **Q1〜Q3 の実機確認**が消化されている | **済**(第54報 2026-09-14「**実機確認: 済み**」)。§4⁷ の個別チェックボックスは原仮定者が見る欄なので空のままにする —— **「済み」は報告の文言であって、個別項目の消化記録ではない** | **解除** |
| **2** | **R の決定手続きが「無い」ことが Negative Claims に載っている** | **載った**(第261便c → §3 の **Negative Claim 32**)。あわせて**条件 4 は R を使う較正系にだけかかる**こと(NS 4 系は対象外)も同じ項に書いた | **解除** |
| **3** | 入場条件 (v) の恒等 QA が通っている | **通っている**(第260便a・QA `behavior.fieldApiIdentity`)。ただし読むのは**診断場**で、同じ 81 交点の \|Δu\| は 🪟 2.2002・🎠 1.7489 —— 第261便a で 🪟🎠 の obsCard に**限定つきの 1 行**を入れた。**Negative Claim 21 は取り下げない** | **限定つきで解除**(「表示と力が同じ場」とは書かない) |
| **4** | **🪟🎠 の展示完成条件がすべて済んでいる** | **1 項が限定つきで済み**: 格子は共通場 API を読める(第260便a)が、それは**全源 scalar/local・背景 static の診断場**であって disk/affine でも E6′/coordinate でもない。**「絵が正しくなった」とは書かない** | **限定つきで解除** |
| **5** | **「NS 連星で 3σ を通った量は 0 件」が Release Notes の先頭に出ている** | §3-1 にあり、冒頭の「完成の 2 定義」表が §1 の直前にある。**先頭段落そのものへの繰り上げは第262便d でも行っていない**(版名の決定と同時に統括が行う) | **未** |
| **6** | **`S._core` の規約と A/B 自己対照が 2 便続けて安定している** | **3 便目**。第262便d で **FAIL 化した**((a) >4 = FAIL・常に gate / (b) >1.5 = FAIL・**frozen-file のときだけ**・root-fallback は informational のまま)。`S._core` は **35197 字**のまま(予算 36000・残 803) | **解除**(ただし**崖のしきい値は依然として未測定** — Negative Claim 19・26・34) |

**6 項のうち解除は 4 項(うち 2 項は限定つき)・未は 1 項(#5)である。** 残る #5 は**文書の配置**の問題であって測定ではない。
**「HOLD が解けた」とは書かない** —— 解除は**統括が 6 項すべてを確認して宣言する**ものである。

## 4⁷. Q1〜Q3 実機チェックリスト(原仮定者向け・**実機で見る項目**)

> **第54報(2026-09-14)で「実機確認: 済み」と報告された** —— したがって HOLD 解除条件 #1 は**済**である(§4⁶′)。
> **下のチェックボックスは空のままにする**: 「済み」は報告の文言であって、**個別項目の消化記録ではない**。
> **この表に「済」を書くのは原仮定者だけである**という第261便d の取り決めは変えない。

第53報の指示のうち**表示・編集専用**の 6 件(空間メッシュ 3・親子コア 3)と、本便で入った器は
**機械 QA では「壊れていないこと」しか言えない**。**見え方・触り心地は実機でしか判定できない**ので、
見る項目を先に列挙しておく(**この表に「済」を書くのは原仮定者だけである**)。

**Q1 — 空間メッシュ(表示・入力のみ。presetSig・S.params・力学には入らない)**

- [ ] 「引きずりの度合」を **1** にしたとき、**どのサンプルでも**空間メッシュの歪みが**破綻せずに描かれる**か。
- [ ] 「引きずりの度合」のスライダーが **0→10 を指数的に**動くか。**直値で 100 のような大きな値**も入るか。
- [ ] **「τ 固定」を切り替えても歪みが変わらない**か。
- [ ] 空間メッシュの**原点が質量上位 2 粒子の重心**になっているか。**2 位と 3 位が同質量のときは 1 位だけ**が原点か。

**Q2 — 親子コア(編集・表示・受け入れのみ。内蔵プリセットの JSON・力学は不変)**

- [ ] 「選択粒子の編集」で**タブ切り替え**により**それぞれの層(粒子)を編集**できるか。
- [ ] 親子コアの**見た目がコア V2 に準拠**しているか(同じ絵に見えるか)。
- [ ] **コア V2 の JSON を親子コアで受け入れ**られるか(変換の**警告が出る**か)。
      **V2 は「将来廃止予定」と宣言しただけで、本便では消していない。**

**Q3 — チップ・A/B・帳簿(本便で器だけを置いたもの)**

- [ ] 「**精度向上で合格見込み**」チップの**器が見えるか**(**中身は 0 件である** —— 5 つの状態のうち
      NS 4 系は「収束確認・未完」「測定再検証」「較正検証中」であって、**合格見込みは 1 件も立てていない**)。
- [ ] A/B(「くらべる」)の**体感が前の版と変わっていない**か(器の実測は (a) 0.70〜0.86・(b) 0.95〜1.00)。
- [ ] 帳簿の相対残差を読むとき、**どちらの分母か**が画面・記録から分かるか(🎠 は 34 倍違う)。

## 4⁸. 3 便計画と「いつ版を切るか」の 2 案

**第261便** = 修正 + 器(中身 0 件)+ 限定つきの 1 行 + 🧅 署名便 + **A/B 2 便目**。**Release は HOLD 継続。**
**第262便** = 収束先(P∞ 等)に対する最小ノブ較正・eProxy ⇄ e_T の写像・独立な計算法・
`inStep` 既定昇格(coupleSink の受け先が決まったら)・**A/B の FAIL 化**・凍結基準の CI 有効化。
**第263便** = **新しい完了基準で判定して Release**(下の 2 案のどちらかを選ぶ)。

**第262便d 時点の更新**(**この段落は計画の書き換えであって成果の宣言ではない**):

| 第262便の予定 | 第262便d の実績 |
|---|---|
| **A/B の FAIL 化** | **入れた**(§4⁹)。(a) は常に gate・(b) は frozen-file のときだけ gate |
| **凍結基準の CI 有効化** | **入れていない** —— **夜間/手動ジョブの候補として文書化しただけ**である(`.github/workflows` は 1 文字も不変)。有効化は第263便の裁定 |
| 収束先較正・e 写像・独立な計算法・`inStep` 昇格 | **第262便d の担当外**(a〜c の結果と統括の統合で決まる) |

**第263便 = RC 判定**である: **HOLD 6 項のうち #5(「3σ を通った量は 0 件」を先頭へ繰り上げる)だけが残り**、
これは**文書の配置**であって測定ではない(§4⁶′)。したがって第263便の分岐は
「**測り足りないから待つ**」ではなく「**案 A で版を切るか、別の手続き(収束先に対する較正)を先に入れるか**」である。
**それでも「NS 連星までの現実較正を終えた」とは名乗らない**(案 A でも案 B でも同じ)。



| | **案 A: v1.44 を「NS 較正検証中」として出す** | **案 B: NS 完了版を待つ** |
|---|---|---|
| 版名 | 「**v1.44 検証版(RC)— NS 連星の数値精度を検証中**」(現行のまま) | 「NS 連星の現実較正 …」(**完了定義 4 条件を満たしてから命名する**) |
| 出す条件 | §4⁗ の HOLD 6 項がすべて解除されていること(**3σ の合格は条件にしない**) | それに加えて `CALIBRATION_VERDICT_v1.44.md` §5′ の **4 条件**を満たすこと |
| いまの距離 | HOLD 6 項のうち **6 は 2 便目が揃った**・1 は実機待ち・2〜5 は統括の統合待ち | **4 条件とも未達**(ε_num は 0.3σ 予算の 2.0×10⁸〜9.7×10⁸ 倍・写像 未着手・R の手続き 無) |
| 分岐の判断材料 | 「**較正の状態を台帳に固定して版を切る**」ことが目的なら案 A で足りる(§1) | 「**観測と合ったことを示す**」つもりなら案 A では足りない —— **案 A はそれを主張しない** |
| **この版が名乗らないもの** | **どちらの案でも**「NS 連星までの現実較正を終えた」「NS 較正完了版」とは名乗らない | 同左 |

**分岐条件を 1 行で**: **完了定義 4 条件のうち条件 1(ε_num ≤0.3σ)が「刻みでは埋まらない」ままである限り、
案 B は「待つ」ではなく「**別の手続き(収束先に対する較正)を先に入れる**」ことを意味する** ——
**待てば届く差ではない**(条件 1 の距離は**約 8 桁**である)。**この区別を付けずに「あと少し」と書かない。**

## 4⁹. 第262便d(第54報)で足したもの

**足したのは規約・表示・台帳だけである。** `beta/index.html` の**既定値・力学・プリセット JSON は 1 bit も動いていない**
(全内蔵 **121 本 × 600 步**の状態・NaN・クランプ数・署名長が基点 **4ccaa74** と**ビット同一**・
`S._core` は **35197 字**のまま〔予算 36000・残 803〕・`S._core` には 1 命令も足していない)。

- **A/B JIT ゲートを FAIL 化した**(`tests/perf.mjs`・QA `lint.perfAbJit`・定数 `AB_FAIL_CRITERIA_W262D`):
  **(a) 自己対照 >4 = FAIL**(基準値が要らないので**常に gate**)/ **(b) 凍結基準 html 対 候補 >1.5 = FAIL**、
  ただし **`frozen-file` を取得できたときだけ**。**`root-fallback` は凍結扱いしない**
  (`judgement:"informational"` のままで fail を増やさない)。
  **しきい値は〔第261便d〕が宣言した値そのままで、1 つも動かしていない**(`activatedAt` が null から便名になった)。
  **`.github/workflows` は 1 文字も変えていない。**
- **凍結基準 script の位置づけを固定した**(`tests/ci-frozen-baseline.sh`・QA `lint.frozenBaseline`):
  **夜間/手動ジョブの候補であって PR ゲートには入れない**。**取得失敗を root-fallback の合格に置き換えない。**
  **手元の 1.56〜1.79 s は CI の上限ではない**(ランナーの回線も GitHub 側の応答も測っていない)。
- **`frameWeightPow` の番兵 0 を廃した**(`frameWeightIsPull` を新設・QA `behavior.framePull` の `sentinel`):
  `share` と未知名は **p=1 をそのまま返す**。`pw>0` を真偽に使う箇所は **0 になった**
  (監査した 14 か所 —— 判定 9・指数 4・インライン複製 1)。**121 本 × 600 步でビット同一**。
- **格子消失の警告**(`SPACE_MESH_LOSS_FRAC=0.25`・`#smGainReset`・QA `ui.spaceMeshGain` ⑨⑩):
  凡例に **`nOk/nodes`** を出し、**`nOk < nodes × 0.25` で琥珀 1 行**。**g は自動で戻さない**
  (戻す操作はパラメータ行の ⟲)。実測: 🎠 は **g=1 で 61/81(警告なし)・g=100 で 5/81(警告)**。
  **presetSig にも S.params にも入らない。**
- **太陽系 16 本の σ を繋いだ**(`SIGMA_TARGET_BODY`・器 `tests/exp-w262d-solarsigma.mjs`・
  出力 `tests/out/solarsigma-w262d.json`): **3σ の判定は 1 件も出ない**(合 0 / 量限定合 0 / 否 0 / **保留 16**)。
  切断点は対応表ではなく **CSV** である(139 量: 天体の行が無い 66・sigma 列が空 33・量の行が無い 14・
  門に入らない来歴 26・**接続 0**)。**CSV の σ 18 行はすべて恒星連星と NS 連星の行**である。
- **帳簿の正本を活動部分にした**(`HP.dfmLedgerGateState`・`LEDGER_GATE_THRESHOLD=1e−3`・HUD 2 行・
  QA `behavior.ledgerNorm` ⑦⑧⑨): 従来分母は**併記**。**両方通過を「合」と呼ばない**・
  **「否」を名乗れるのは帳簿が閉じた宇宙の `"over"` だけ**・未定義項が残る宇宙の超過は
  **`"undefined-terms"`(門外)**。実測は 🎻 8.462×10⁻⁴=`within`・🎠 活動 1.0905×10⁻²=`undefined-terms`・
  🫐 3.098×10⁻¹=`undefined-terms`。**門は廃していない。**
- **HOLD 6 項の現在地を第262便 時点で書き直した**(§4⁶′)・**Q1〜Q3 は第54報で「実機確認: 済み」**(§4⁷)・
  **Negative Claims 35〜38 を足した**(§3)。


## 5. 版の切り方(統括への申し送り)

- **名前**(第257便d で確定): 「**v1.44 検証版(RC)— NS 連星までの数値収束・観測一致は主張しない**」。
- **名前の候補**(第256便d の記録): 「**NS 連星までの現実較正(数値収束)**」/「**検証用 beta**」。
  **「現実較正完了」とは書かない**(NS 連星では 3σ の門が 1 件も開いていない。恒星連星で開いた 4 件のうち
  2 件は **DFM 版の「否(3σ)」**である)。
- **root 昇格**: 本便では行わない。昇格するなら `index.html` と `beta/index.html` の差分を
  統括がフルゲートで通してから。`APP_VERSION` も同時に上げる。
- **恒星 4 量(✨✴️🌟💫 の周期)**: 「NS まで完了」を名乗るなら未判定を残さない、という
  ChatGPT の条件への回答は第256便d ④ の実測にある(PHYSICS〔第256便d〕参照)。
  **「数値未解決 5」は「数値未解決 1・合 2・否 2」になった** —— 未判定量は減ったが、
  **減った先の 2 件は DFM 版の 3σ 不合格**である。**この 2 件を先に台帳へ載せずに版を切ってはならない。**
