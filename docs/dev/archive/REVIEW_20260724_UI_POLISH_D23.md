# 第23便 裁定・実装記録 — 第22次レビュー反映+UI改善(DFM Lab・タップ説明)(2026-07-24)

- 起点: 第22便 PR #63 マージ済み(main b50046d)。第22次外部AIレビュー3系統(ChatGPT 検証
  報告書 / Gemini 評価 / Grok 差分分析 — いずれも v3 実測・記録と整合する良質なレビュー)と
  原仮定者の UI 指示 2件を受けた beta スプリント
- 対象: `beta/index.html` / `beta/sw.js`(`dfm-beta-v1.30-b4`)。ルート `index.html` は無変更

## 1. 原仮定者指示の実装

| 指示 | 実装 |
|---|---|
| 左上のタイトルを「DFM Lab」に短縮 | ヘッダ `#appTitle` を **DFM Lab**(ja/en 共通)へ。**document.title(ページタイトル)と OGP は従来のまま**(QA i18n.toggle が document.title を検査するため・検索/共有時の説明性維持) |
| 「パラメータ」の「表示」でパラメータ名タップ→説明 | 表示トグル8行(残像・線の軌跡・決定力マップ・光線・回転曲線・温度・スペクトル・保存量モニタ)+文字サイズ・粒子サイズ表示の全行に、既存の品質行と同じ **pdesc 開閉方式**のタップ説明を追加(ja/en 各10件の説明文を新設。時間倍率は既存 mkParamRow の説明を継承) |
| 「選択粒子の編集」でパラメータ名タップ→説明 | 全12ラベル(m・s・mc/m・sc/s・R・Rc/R・To×・掻出・TcO×・vx・vy・固定)をタップで **beNote 欄に説明を表示**(同じ名前を再タップで元の注記へ復帰。ja/en 各11件の説明文を新設 — 式・公理番号つき) |

## 2. 第22次外部AIレビューの主張別裁定

### 2.1 ChatGPT(v3 検証報告書 — CI 成果物まで確認した最も厳密なレビュー)

| # | 主張 | 裁定 |
|---|---|---|
| §1-§4 | beta/root QA 一致・bit 等価・v3 実測値の完全一致・現行内蔵値を推奨基準値に | **確認** |
| §7.2 | 中心 R=60/spin0.15 は円盤膨張(第22便走査と同一)— R45/s0.12 を超える変更を標準にしない | **確認**(較正記録どおり) |
| §8 P1 | **main の qa-results.json が旧 v2(9dda4b1・142項目)のまま** | **事実確認・本便で解消** — PR #63 が QA結果更新コミット(a2b8ab8)より前の時点でマージされたため。ブランチ再作成時に規約どおり未マージコミットを新基底へ載せ直し(cherry-pick 159ce70)、本便 PR で main が最新化される |
| §8 P1 | lightSweep は現象論的観測属性(光の行き先は未計算) | **確認**(PHYSICS.md 主張域に記載済み) |
| §8 P1 | 中心 pinned 依存は閉鎖系と区別 | **確認**(説明文・記録に明記済み) |
| §8 P2 | 時間依存 u_φ の追加計測・main head での CI 再実行 | **記録**(前者は 4-47 Phase C の計測項目へ・後者は昇格手順の注意点へ) |

### 2.2 Gemini(評価レポート — 第19次の過大記述から改善し、今回は正確)

| # | 提案 | 裁定 |
|---|---|---|
| A | kFrame=0 対照 | **確認**(説明文の おすすめA/B と同一) |
| B | q を 1.5〜1.0 へ/D0 を 0.3 へ下げて到達範囲を拡大 | **採用** — activeParams を ["kFrame","D0","G"]→**["kFrame","D0","q"]** に変更し、説明文に「qやD0を下げると引きずりの届く範囲が広がる(実験)」を追記 |
| C | coreMR=−0.1(空洞殻)の思考実験 | **記録**(粒子編集で実施可能 — 専用プリセット化はしない) |
| D | 掻出=1→0 で正体(高温核)を可視化 | **採用** — 説明文に「掻出を0にすると隠れていた高温核が見える」を追記 |

### 2.3 Grok(差分分析)

| # | 主張 | 裁定 |
|---|---|---|
| 全体 | 差分の核心・現状維持推奨・バリエーション表・推奨しない変更(coreMR≥0.5 等) | **確認**(実験系は粒子編集で可能 — 記録) |
| §1.2 | 「lightSweep は root にも○」の表記 | **誤記の記録**(lightSweep は beta 専用 — root は v1.29 で未搭載) |

## 3. QA・版数

- root フル **136/136** / beta フル **141/141 ALL PASS**(本便はエンジン無変更 — UI・文言のみ。
  タイトル変更は document.title 不変のため i18n.toggle も従来どおり)
- beta SW キャッシュ `dfm-beta-v1.30-b4`・起動通知の版数表示も b4 へ

## 4. 実機チェックリスト追加分(第23便)

- [ ] 左上タイトルが「DFM Lab」(EN 切替でも同じ)・ブラウザのタブ名は従来どおり
- [ ] パラメータタブ「表示」の各行名をタップ → 説明が開閉する(品質行と同じ挙動)
- [ ] 粒子をタップ → 編集パネルの「R」「掻出」等の名前をタップ → 下の注記欄に説明が出る・
      再タップで元の注記に戻る・EN でも英語説明
- [ ] 🕶️ の主役スライダーに q が入っている(kFrame/D0/q)— q を下げて引きずり範囲の拡大を確認
- [ ] 🕶️ 中心をタップ → 掻出を 0 に → 高温核が見える(説明文どおり)
- [ ] beta 起動通知「beta v1.30-b4」・SW `dfm-beta-v1.30-b4`

## 5. 残課題

- 4-48(光線終端状態・名称整合)・4-47 Phase C・4-51 — 従来どおり(ロードマップ参照)
- v1.30 昇格判断: beta の第20〜23便分の実機確認後(CHANGELOG は昇格時一括)

---

*第22次レビュー3系統はいずれも実測・記録と整合しており、指摘の採否は本記録 §2 が正。*
