# 導出集: 決定力場モデル(DFM)の解析的帰結

担当フェーズ: 設計(最上位 Tier / Claude Fable 5)/ 作成日: 2026-07-15
入力: docs/PHYSICS.md、THEORY_SYNTHESIS.md、原仮定者の補足2点(2026-07-15)、
外部AIレビュー3件(ChatGPT / Grok / Gemini、PDF提供)
位置づけ: 本書 §0〜§1 は **PHYSICS.md への正式な差分(v1.1 改訂)** を兼ねる。
§2 以降は改訂後の公理系から導いた解析的帰結であり、論文の "Analytical consequences" 節の正本となる。

---

## 0. 公理の改訂と追加

### A13(相互性)— 原仮定者による改訂 ★最重要
> 空間の引きずりは、厳密には反作用を生じ、運動量を保存する。
> リンゴが地球に落ちるとき、地球もリンゴに引っ張られている。

**公理文**: すべての相互作用(重力・スピン斥力・フレーム引きずり・接触・スピン拡散)は
対ごとに作用反作用対称であり、系全体の線運動量と全角運動量
L_tot = Σ m_i(**r**_i×**v**_i) + Σ I_i s_i を厳密に保存する。
背景決定力 D₀ との運動量交換は、静止した無限慣性リザーバ(遠方宇宙)への移譲として
帳簿づけされる — リンゴに引かれた地球が、感知できないほど僅かに動くのと同じである。

これにより旧 THEORY_SYNTHESIS §7 の限界 **L2(保存則の破れ)は解消**され、
「保存則を厳密に満たす関係論的力学」として理論の格が一段上がる(新定理 T7)。
実装上の改訂は §1 の E5′/E6′/E10′。

### A11(光の伝播と同時性)— 原仮定者による改訂
> 光は「決定した空間」を進み、観測者によるパラドックスは発生しない。

**公理文**: 光は各点の局所決定フレーム **u**(**r**) に対して等方的に速さ c₀/n(**r**) で
伝播する。座標時間 t は宇宙で一意(絶対的同時性)であり、固有時間 τ の遅れ(E7)は
時計の**動的な**遅れであって、運動学的な相対性ではない。
したがって同時性の相対性に由来するパラドックス(双子・同時刻の不一致)は原理的に生じない。
DFM は「優先フレームを持つ理論」であることを明示する — 決定力場そのものが媒質である。
帰結: 光は媒質 **u** に運ばれる(フレネル随伴の完全版、E8′)。回転体の左右で
光の湾曲が非対称になる(新帰結 T8 — Gemini が独立に提案したブラックホール実験と一致)。

### A12(放射)— 設計者による補完(**原仮定者承認済み 2026-07-15**)
D1(§2)の導出根拠と、旧限界 L3(法線減衰 γ_n の散逸先が不明)の解消のため追加する。

**公理文**: 温度 T_i を持つ質点は光量子を放射する。スペクトルのピーク波長は
ウィーン型則 λ_peak = b_m / T_i に従い(b_m はモデル定数)、光度は
Λ_i = η_rad · T_i^p(既定 p=4、シュテファン=ボルツマン型)。
E9 の法線減衰 γ_n で失われる並進エネルギーは、衝突時の閃光として放射される。
これによりエネルギー収支が閉じる: 並進 → スピン(摩擦)or 放射(法線減衰)、スピン → 放射(冷却)。

### 温度定義の精密化
2次元剛体円盤の回転自由度は1つであり、エネルギー等分配 ½I_i s_i² = ½k_B T_i から

> **T_i ≡ I_i s_i² / k_B**(シミュレータ単位系では k_B = 1、T_i = I_i s_i² = ½ m_i R_i² s_i²)

PHYSICS.md の略記 T ∝ s²R² は質量因子を欠くため、本定義で置き換える。
実装注: 粒子の色マップは s ではなく I s² をキーにする。

---

## 1. 計算式の改訂(E5′ / E6′ / E8′ / E10′)と新定理

### E5′ スピン斥力(対称化)
旧 E5 は i←j と j←i の力が等大逆向きにならない(m_i ω_j² ≠ m_j ω_i²)。改訂:

> **F**_rep(i←j) = k_rep · μ_ij · [ω_i(d_ij)² + ω_j(d_ij)²] · (**r**_i − **r**_j)、
> μ_ij = m_i m_j/(m_i + m_j)、加速度は **a**_rep,i = **F**_rep/m_i

中心力なので角運動量も自動保存。**遠方漸近(q=2)**: ω(d) ≈ sR²/d² より
F ∝ μ(s_i²R_i⁴ + s_j²R_j⁴)/d³ — **1/d³ 斥力。Gemini が独立に選んだ形と厳密に一致**
(q=2 の妥当性を外部AIが偶然裏書きした形になる)。

### E6′ フレーム引きずり(輸送+反作用)— A13 の中核実装
設計判断の記録: 「u へ向けた速度緩和(粘性型)」は静的中心天体まわりの軌道を
数周で崩壊させるため不可(Grok の β·S·(V_space−v) 形式はこの理由で不採用)。
旧 E6 の **Du/Dt 輸送形式は正しい力学**を与えるので、これを保ったまま反作用を追加する。

各ステップ、粒子 i について(旧 E6 と同一): Δ**v**_i = k_F · (u_i(今回) − u_i(前回))。
追加(反作用の分配): Δ**p**_i = m_i Δ**v**_i とし、u_i を構成した各ソース j の寄与率
φ_ij = w_j(**r**_i)/W(**r**_i)、背景の寄与率 φ_bg = D₀/W(**r**_i) を用いて

> **反作用**: Δ**v**_j −= (φ_ij/m_j) · Δ**p**_i (全ソース j について)
> **背景分**: φ_bg · Δ**p**_i は無限慣性リザーバ(遠方宇宙)へ移譲(帳簿に記録)

> **残余トルクのスピン移譲**: 各対の残余トルク n_ij = [(**r**_i − **r**_j) × φ_ij Δ**p**_i]_z を
> Δs_i = Δs_j = −n_ij/(I_i + I_j) としてスピンへ等角加速度で移譲。
> 背景分のトルク [**r**_i × φ_bg Δ**p**_i]_z はリザーバへ。

**性質**: (i) D₀=0 のとき P・L とも機械精度で厳密保存、D₀>0 ではリザーバ勘定込みで保存。
(ii) 引きずられる側の運動は旧 E6 と同一 — 既存デモ(銀河・土星・マッハ)の挙動は不変。
(iii) 反作用が新しい物理を生む: 銀河で外縁星を加速させた分、内側の星(u のソース)が
減速する = **角運動量の外向き輸送**(実在の降着円盤・潮汐進化と同型の構造)。

### E8′ 光線(媒質随伴)— A11 の実装
> d**r**/dt = (c₀/n(**r**)) ĉ + **u**(**r**) (光は決定フレームに運ばれる)
> 方向更新は E8 と同じフェルマー則を、**u** と共動する局所フレームで適用する。

帰結 T8: 中心天体のスピンによる u_φ が、順行側の光を加速し逆行側を減速するため、
**左右非対称な光の湾曲**(カー時空の光学類似)が生じる。

### E10′ スピン拡散(角運動量保存形)
旧 E10 の質量比重みでは Σ I s が保存しない(I ∝ m² のため)。慣性モーメント比に改訂:

> ds_i/dt = κ_s Σ_{j≠i} [ I_j/(I_i + I_j) ] (s_j − s_i) g(d_ij)

これで対ごとに I_i ds_i = −I_j ds_j、スピン角運動量が厳密保存。
注: 平衡点は「s の均一化」であり、粒径が異なると T = Is² は完全一致しない
(異粒径系の温度平衡の厳密化は未解決問題 L9 として登録)。

### 新定理
- **T7(保存則)**: E4(中心力)+E5′(中心力)+E6′(反作用つき輸送)+E9(接触対力)
  +E10′(I 重み拡散)の下で、D₀=0 なら線運動量と全角運動量(軌道+スピン)は厳密に保存する。
  D₀>0 では保存量の欠損は全てリザーバ勘定と一致する。✓A13
- **T8(非対称レンズ)**: A11+E8′ の下、スピン s_c の中心天体を挟む対称な平行光線は
  非対称に湾曲し、湾曲差は k_F・u_φ に比例する(Kerr 型フレームドラッグの光学類似)。

---

## 2. D1: 温度と光の波長からスピン速度の方程式を導く

A12(ウィーン型則)と温度定義 T = I s²/k_B を連立する。
λ_peak = b_m/T = b_m k_B/(I_i s_i²) をスピンについて解いて:

> **s_i(λ) = √( k_B b_m / (I_i λ_peak) ) = (1/R_i)·√( 2 k_B b_m / (m_i λ_peak) )**

表面(接線)スピン速度はさらに簡潔になる:

> **v_surf = s_i R_i = √( 2 k_B b_m / (m_i λ_peak) )** — 半径に依らず、質量と観測波長だけで決まる

**帰結**:
1. 青い(λ 小)天体ほど速く自転する: s ∝ λ^{−1/2}。
2. 同じ色の2天体では、重い方がゆっくり自転する: s ∝ 1/(R√m) ∝ m^{−1}(R=r_R√m のとき)。
3. 観測波長 → スピンの**分光温度計**が成立し、シミュレータで放射色から s を逆算できる。
4. 気体デモの平衡(E10′: s 均一)では、大粒子ほど T = Is² が高く赤外側から可視側へずれる —
   実装の色マップ改修(§0)と整合する検証項目になる。

シミュレータ単位系(k_B=1): λ = b_m/(I s²)、s = √(b_m/(I λ))。b_m は UI パラメータ化する。

---

## 3. D2: 楕円銀河・レンズ状銀河・渦巻銀河の形状方程式

### (G1) 回転則 — DFM の銀河基本式
軸対称・準定常の円盤で、慣性は局所フレーム基準(A5)。旧 E6 の輸送項は、定常な
u_φ(r) 場の中を周回する星に **向心加速度 k_F u_φ v_φ/r** を与える(導出: 経路に沿う
Du/Dt = v_φ ∂_θ(u_φ θ̂)/r∂θ = −(u_φ v_φ/r) r̂)。よって円軌道条件は

> v_φ²/r = g_eff(r) + k_F u_φ(r) v_φ/r、 g_eff = G ∂_r D − a_rep,rad

これを v_φ について解くと **v_φ(r) = ½[ k_F u_φ + √( k_F² u_φ² + 4 r g_eff ) ]**。

### (G2) フレーム場の自己無撞着方程式と平坦域の長さ
u_φ 自身は内側の星々の回転から作られる(不動点方程式):

> u_φ(r) = [ ∫ Σ(r′) w(|**r**−**r**′|) v_φ(r′) dA′ + Σ_c w_c ω_c r ] / W(r)

円盤質量 M_d、平均回転速度 V̄ に対し外縁では w 総和 ≈ M_d/r なので
u_φ(r) ≈ V̄ · M_d/(M_d + D₀ r)。したがって:

> **平坦域**: r ≪ r_flat では v_φ ≈ k_F V̄(平坦)。 **r_flat ≈ M_d / D₀**
> r ≫ r_flat では u_φ → 0 となり v_φ → √(GM_d/r)(ケプラー減衰に復帰)

平坦部の自己無撞着条件 v_φ ≈ k_F v_φ + (重力項) は **k_F = 1 が特異的に自己整合的**で
あることを示す(k_F < 1 では平坦レベルが内側平均より下がる)。
**モデル内予言**: 回転曲線は無限に平坦ではなく、半径 M_d/D₀ で折れる。D₀ を上げると
平坦域が縮む — シミュレータで直接検証可能。

### (G3) 形態分類 — 3つの無次元数
- 回転パラメータ: **λ_DFM = L_tot / ( M √(G M R_h) )**(R_h は半質量半径)
- 熱分率: **h = Θ / |W_grav|**、Θ = Σ I_i s_i²(スピン熱の総量)、W_grav = −ΣΣ G m_i m_j/d_ij
- 円盤安定数: **Q_DFM = σ_eff κ_eff / (π G Σ)**、σ_eff² = σ_trans² + c_spin²
  (c_spin² = ∂P_spin/∂Σ はスピン斥力の「音速」、κ_eff は (G1) の回転則から作る
  エピサイクル振動数 κ_eff² = r dΩ_eff²/dr + 4Ω_eff²、Ω_eff = (v_φ − k_F u_φ)/r)

**分類則**(境界値 h_c, Q_c は数値校正、初期見積り h_c ≈ 0.3, Q_c ≈ 1.5):

| 条件 | 形態 | 機構 |
|------|------|------|
| h > h_c(λ_DFM 小) | **E(楕円)** | スピン圧支持。摩擦が整列を作る前に加熱が勝った系(高温初期・ランダム軌道) |
| h < h_c かつ Q_DFM > Q_c | **S0(レンズ状)** | 回転支持だが円盤が熱い。腕を立てる自己重力不安定が起きない滑らかな円盤 |
| h < h_c かつ Q_DFM < Q_c | **S(渦巻)** | 冷たい回転円盤。差動回転 Ω(r) = v_φ/r が摂動を巻き込み腕を形成 |

### (G4) 楕円銀河の平衡形状(静水圧ODE)
等方支持の E 型は、面密度 Σ(r) が

> G Σ(r) ∂_r D(r) = ∂_r P_spin(r)、 P_spin ≈ c_P · k_rep · Σ² · ⟨μ s² R⁴⟩ · 𝓛

を満たす(𝓛 は 1/d³ 対斥力の2次元長距離補正の対数因子、c_P·𝓛 は数値校正)。
A12 の放射冷却 dΘ/dt = −Σ_i η T_i⁴ により **E 型は放射しながらゆっくり収縮する**
(進化の矢: 冷却 → 収縮 → 摩擦再加熱の準静的系列)。

### (G5) 渦巻腕の巻き込み
平坦域では Ω(r) = v_φ/r ∝ 1/r の差動回転により、動径方向の摂動線は
ピッチ角 tan i(t) ≈ 1/( t |dΩ/d ln r| ) で巻き込まれる(トレーリング渦巻)。
腕が持続する条件は Q_DFM ≲ Q_c(冷たい円盤での自己重力増幅)。
密度波の完全な分散関係の導出は未解決問題 L10 として登録する。

---

## 4. D3: 土星の環を安定形成する方程式

### (R1) 軌道則と回転方向の選択
中心天体(質量 M、スピン S_c、半径 R_c、静止)まわりのリング粒子の円軌道は (G1) の特別な場合:

> v_φ(r) = ½[ k_F u_c(r) + √( k_F² u_c(r)² + 4GM/r ) ]、
> u_c(r) = [ w_c/W ]·ω_c(r)·r = [ M/(M + D₀r) ]·S_c (R_c/(R_c+r))^q · r

u_c > 0(順行)側は必要な |v − u| が小さく、逆行粒子は大きなフレーム逆風を受けて
衝突散逸(E9)が激しい。ゆえに **環は中心天体のスピンと同方向に淘汰される**
(実在の土星の環が自転と同方向である事実の、モデル内での説明)。

### (R2) DFM ロッシュ条件(引きずりせん断つき)
粒塊(質量 m、密度 ρ)が潮汐+フレームせん断で引き裂かれない条件。
引きずりせん断数を **𝒟(r) ≡ k_F · | r ∂_r( u_c/r ) | / Ω_K(r)**、Ω_K = √(GM/r³) と定義して:

> **ヒル半径(2D)**: r_H = r · √( m / (3M) ) · [ 1 + c_R 𝒟(r) ]^{−1/2}
> 粒塊サイズ δ < r_H なら生存(衛星化)、δ > r_H なら破砕(環に留まる)
> 等価な臨界密度形: ρ_crit(r) = ρ_Roche,Newton(r) · [ 1 + c_R 𝒟(r) ]

(c_R は O(1) の校正係数。3D 移植時は指数 1/2 → 1/3。)
**帰結**: フレームせん断は実効ロッシュ限界を**外へ押し広げる** — DFM の環は
ニュートン力学より広い半径帯で安定に存在できる。

### (R3) 熱平衡幅と安定窓
衝突加熱と放射冷却(A12)の釣り合いが速度分散 σ_eq を決める:

> μ_f · n(r) σ_coll ⟨Δv²⟩ · μ̄ / 2 = η_rad Σ_i T_i⁴ / N ⇒ σ_eq(r)
> 環の厚み(動径幅): w_ring = σ_eq / Ω_K

**環の安定窓**(3条件の同時成立):
1. 重力凝集しない: Q_ring = σ_eq Ω_K/(πGΣ_ring) > 1 ⇔ σ_eq > πGΣ_ring/Ω_K
2. 衛星化しない: ρ_ring < ρ_crit(r) (R2)
3. 粘性拡散が遅い: t_spread = (Δr)²·Ω_K/σ_eq² ≫ 観測時間

1〜3 を同時に満たす半径帯 [r_in, r_out] が「安定な環」。σ_eq は自己調整的
(σ が上がる→衝突減→加熱減→σ 低下)なので、窓の中では **環は熱的アトラクタ**である。

---

## 5. D4: 追加の例(提案と導出)

### (a) 近日点移動 — レンズ=サーリング類似の歳差
(G1) の引きずり項は離心軌道に対して 1/r² 型の速度依存摂動として働き、
近点は順行軌道で前進(逆行で後退)する。摂動論の骨格から:

> **Δϖ(1周あたり) ≈ 2π c_p k_F · ⟨ u_c(r) / ( r Ω_K(r) ) ⟩軌道平均**(c_p = O(1)、数値校正)

近傍領域(M/r ≫ D₀、r ≫ R_c)では u_c ≈ S_c R_c²/r なので

> Δϖ ∝ k_F S_c R_c² / √( G M r³ )

これは一般相対論のレンズ=サーリング歳差 Δϖ_LT ∝ G J /( c² √(GM) r^{3/2} )(J = I s)と
**同一の r^{−3/2}・スピン×半径² スケーリング**を持つ。対応 k_F ↔ 2Gm_c/c²·(規格化) は
弱場写像表(THEORY_SYNTHESIS §4.2)の第4行を埋める。遠方領域(D₀r ≫ M)では
u_c ∝ 1/r² に折れ、Δϖ ∝ 1/(D₀ r^{3/2})·M^{1/2} — 背景が歳差を遮蔽する(DFM 独自の予言)。

### (b) 自転同期と軌道移動 — 潮汐進化の DFM 類似
点粒子の定常円軌道では、定常軸対称 u 場の輸送項は純粋な向心成分となり
永年トルクは生じない(∮(u×dr)_z = −∮u_φ dr = 0 — 実在の LT が円軌道を移動させないのと同型)。
**移動には散逸が要る。** DFM の散逸チャネルは、広がりを持つ衛星(接触結合した粒塊)内部の
E9 摩擦である:
1. フレームせん断 ∂_r u_c が粒塊の内外に速度差 Δu = δ·|∂_r u_c − u_c/r| を与える
2. 内部摩擦がこれを散逸し、粒塊は局所フレームと同期回転へ向かう(**自転同期の起源**)
3. E6′ の反作用により、トルクは中心天体のスピン S_c と往復する

**共回転半径** r_co を [w_c/W]·ω_c(r_co) = Ω_K(r_co) の解として定義すると:

> r > r_co(フレームが軌道より速い): 粒塊は前方へ引かれ dL_orb/dt > 0 → **外側へ移動**、
> 反作用で中心天体は減速(月が遠ざかり地球の自転が遅くなる関係の類似)
> r < r_co: dL_orb/dt < 0 → **内側へ落下**(フォボス型)
> 移動率: dr/dt = (2 F_t/(m v_K))·r、 F_t ≈ c_t k_F μ̄ γ(r)·δ·|∂_r u_c|(c_t 校正)

### (c) スピン気体の状態方程式(数値測定の提案)
E5′ の 1/d³ 対斥力+接触圧から、2次元スピン気体の EOS
P = Σ T_eff·[ 1 + B(T) Σ + … ] を仮の形として、ビリアル経由で B(T) をシミュレータで測る。
理想気体からのずれ(長距離斥力による正の第2ビリアル係数)が予言される。
旧限界 L4 の解消手順として §6 の検証プランに含める。

---

## 6. 数値検証プラン(受け入れテスト)

| ID | 検証対象 | 手順 | 合格条件 |
|----|----------|------|----------|
| V1 | T7 保存則 | D₀=0、全相互作用ON、1000 ステップ | |ΔP|/P_scale と |ΔL|/L_scale < 10⁻³(Float32) |
| V2 | D1 分光温度計 | 既知 s の粒子の放射色から s を逆算 | 相対誤差 < 5% |
| V3 | (G2) r_flat | D₀ ∈ {1,2,4,8} で銀河プリセット | 平坦域終端が ∝ 1/D₀ でスケール |
| V4 | (R1) 方向選択 | 順逆混合リング初期条件 | 生存粒子の 9 割以上が S_c と同方向 |
| V5 | (R2) 𝒟 効果 | k_F ∈ {0, 0.5, 1} で粒塊生存率 | k_F 増で衛星化半径が外へ単調移動 |
| V6 | D4a 歳差 | 離心軌道1体+中心スピン | Δϖ の符号=順行前進、r^{−3/2} 則で回帰 |
| V7 | D4b 移動 | 2粒子粒塊を r_co の内外に配置 | 外側→遠ざかる/内側→落ちる、S_c 反作用符号一致 |
| V8 | T8 非対称レンズ | ±S_c で平行光線束 | 左右湾曲差が S_c に比例 |

## 7. 実装への影響(次フェーズへの引き継ぎ要点)

index.html への差分(実装フェーズ、Sonnet 担当。HANDOFF_IMPLEMENTATION.md に本節を添付):
E5′ の対称化 / E6′ の反作用ループ(φ_ij 分配・スピン移譲・リザーバ帳簿変数)/
E10′ の I 重み化 / 色マップを T = Is² キーに変更 / b_m・η_rad・p の UI 追加 /
E8′ の光線随伴項 / 保存量モニタ(P, L, リザーバ勘定)の表示 / §6 の V1〜V8 をテスト列に追加。
受け入れの一次条件は V1(保存則)である。

## 8. 未解決問題の更新
- 解消: L2(→A13/E6′/T7)、L3(→A12)、L4(→D4c の測定手順を定義)
- 新規: **L9** 異粒径系の温度平衡(s 均一と T 均一の不一致)、**L10** 渦巻密度波の分散関係、
  **L11** A12 の放射反作用(光量子の運動量)と T7 の整合(現状は放射をスカラーエネルギーとして扱う)

---

## 9. v1.5 改訂記録(2026-07-16)— 外部AIレビュー3件の裁定と整合性修正

入力: ChatGPT / Grok / Gemini の3レビュー(相互に矛盾)。全指摘を v1.4 402df57 の現物コードで
検証して裁定した(裁定表は `docs/HANDOFF_FIX_V15.md` §0 が正本)。実装は同書 F1〜F5。

### 9.1 L5 の解消 — E1 の Plummer 型統一

旧: w = m/(d+ε)、∇w = −m·(**r**−**r**_j)/(d(d+ε)²) ≠ E4 の (d²+ε²)^{−3/2} 型
→ A6「a = G∇D」が有限 ε で不成立(d=ε 近傍で最大 ~26% 乖離。V9 修正前実測 2.66e-1)。

新(E1′): w = m/√(d²+ε²)、∇w = −m·(**r**−**r**_j)/(d²+ε²)^{3/2} = E4/G(厳密)。
ε→0 で原仮定 m/d に一致し、d=0 の値 m/ε・遠方漸近は旧式と同一。中間域で W は最大 +41%
(d=ε 時)増える。traceRay の屈折率 n は元々 Plummer 型であり、本修正で
**n・∇n・W・E4 の4者が単一の場に統一**された。エンジン・E6′ 反作用 φ・決定力マップ・
光線フレーム重みの5サイトを同時改訂(1サイトでも残すと Σφ≠1 で V1 が FAIL する)。

### 9.2 L3 の再開と完全閉性 — A12′

v1.1 の「第一法則が閉じた」は不完全だった(ChatGPT 指摘 #2・#3 を採用):
- **接線摩擦のすべり散逸が帳簿外** — 反例: 等質量2円盤(m=1, R=1, 接線速度 ±0.5, μ_f=1)の
  全付着で E: 1/4 → 1/6、**ΔE = −1/12 が消滅**していた(v1.5 実装で radE 増分 = 1/12 を検算)。
- **E10′ 同期の回転エネルギー損が帳簿外** — L 保存の代償に、対ごとの損失率
  κ·g·I_iI_j(s_j−s_i)²/(I_i+I_j) ≥ 0 が消滅していた。

A12′(v1.5): 両散逸を「変更 DOF の運動エネルギー差」として radE に厳密計上。
P・L の経路には触れないため V1 は不変。V10(修正前 1.87e-1 FAIL → 修正後 2.48e-4 PASS)で
機械検証し、恒久ゲート化した。
既知の未計上チャネル(記録): box/circle 境界反射の 0.95 減衰は帳簿対象外。
V10 は boundary "none" で構成すること。

### 9.3 裁定 #4 の記録 — A9「スピン=熱」の温存(U/T 分離案の棄却)

ChatGPT はスピンと熱を分離し内部エネルギー U を導入する再設計を提案した。棄却する。
根拠: (i) A9 は原仮定そのもの=本プロジェクトの同一性であり、U/T 分離は「別の理論」になる。
(ii) 本プロジェクトの品質バーは「実在物理への一致」ではなく「帳簿の閉性」であり、
A12′ で達成できる(V10)。(iii) L9(s 均一 ≠ T 均一)は既知の未解決問題として明示維持する。

### 9.4 検証フックの追加(§6 の表形式)

| ID | 検証対象 | 手順 | 合格条件 |
|----|----------|------|----------|
| V9 | A6 厳密性(L5) | pinned 3質点(m50@原点・m20@(18,−7)・プローブ m0.01)を3プローブ位置で5ビルド(X, X±h·x̂, X±h·ŷ、h=0.01)し、sumW の中心差分勾配と E4 加速度を比較 | 3点全てで |a−G∇W|/|a| < 1e-3(修正前 2.66e-1 FAIL、修正後 2.38e-4) |
| V10 | 第一法則の完全閉性(L3) | G1/D₀0/kFrame0/kRep0・μ_f0.5・γ_n0.4・κ_s0.3・η_rad2e-4(p=1)の37粒子系を1000ステップ。E_総(運動+回転+Plummer重力ポテンシャル+区分ばねポテンシャル)+radE の変化を測る | |ΔE_総+radE|/E_scale < 7.5e-4(=実測 2.48e-4 ×3。修正前 1.87e-1 FAIL) |
| V11 | T4 の因果性 | 縮小銀河(m600+disk n140 r130 kepler×1.05、D₀0.5)を同一初期条件で kFrame=1/0 の A/B 実行。外側帯 [85,130] の平均接線速度の差を 1000〜2000 步で時間平均 | 時間平均 gain ≥ 0.03 かつ kFrame=0 側のドリフト ≤ 0.10(実測 gain 0.055±0.007、ドリフト 0.012) |

V11 は当初仕様(R260・800步・終値判定)が T4 連鎖(引きずり減速→落下→スピンアップ)の
力学時間に対して窓が短く、ゲインが負になる(実測 −0.032)ことが判明したため、
縮小系+時間平均に改訂した(`docs/HANDOFF_FIX_V15.md` 追記1)。

### 9.5 v1.6 候補(今回は保留として記録のみ)

- Gemini 提案2: 自転惑星の大気循環(コリオリ)— 2D で実現可能な新規サンプル。
- Gemini 提案3: 間欠泉の自励振動 — 加熱と圧力の周期解。2D で実現可能。
- 種なし星形成(重力不安定からの創発)— starform の上位互換。バー不安定(付録D 書き戻し8)の
  回避策が未設計。
- EXPLAINER_HIGHSCHOOL.md の v1.5 追従(用語・E1′)。

---

## 10. v1.6 改訂記録(2026-07-16)— 「時間パラドックス=惑星逆行」の観測者転換と第2次レビュー裁定

入力: 外部AIレビュー第2次3件(Grok / Gemini / ChatGPT、いずれも 2026-07-16、v1.5
コミット 1ecc7fb 時点。MD 提供)+ 原仮定者の指定アナロジー
「**相対性理論における時間のパラドックスは、天動説における惑星の逆行と同等であり、
観測者を変えることでよりシンプルな法則に帰結する**」。
本節はこのアナロジーを公理系の定理(T9)として定式化し、第2次レビューの全指摘を裁定する。

前提注記: Gemini レビューはリポジトリへのアクセス失敗を自認しており(名前解決エラーを明記)、
内容は DFM を読まない一般理論の構築である。DFM 固有の診断としては使えないが、
アナロジーの構造化(周転円=γ因子の対応表)と仮説モデルA(後述 #11)は独立形式化として扱う。
Grok は v1.2/v1.5 の内容を正確に反映しており(第1次の Gemini と異なり)事実関係は信頼できるが、
全面肯定ゆえ欠陥検出力は低い。診断の主力は ChatGPT レビューである。

### 10.1 第2次レビューの裁定表

| # | 指摘/提案(出典) | 現物での検証 | 裁定 |
|---|---|---|---|
| 1 | A2(絶対空間なし)と A11(優先フレーム)の緊張(ChatGPT §4.1) | ○ 両公理の文言が未接続(A11 は「優先フレームを持つ」とだけ宣言) | **採用 P0 → §10.3**(A11 追補: 優先フレーム=物質共動フレームの創発。実装変更なし) |
| 2 | 時間パラドックスの座標化・基礎表示/観測表示の二層構造(ChatGPT §5〜§8) | §10.2/§10.5 で導出を検証 — 成立 | **採用 P0 → 新定理 T9**。原仮定アナロジーの定理化そのもの |
| 3 | 運動による時間遅れ(v−u 依存)の欠如(ChatGPT §4.2) | ○ E7 は W のみに依存(`tauUpdate`) | **採用 P1(設計確定・実装保留)→ §10.4** E7R/E8R。原仮定 A7 の公理拡張であるため原仮定者承認後 v1.7 で実装(A12 承認の前例に従う) |
| 4 | K_t と k_lens の独立性 — 単一場からの統一(ChatGPT §4.4。Grok 弱み1と収束) | ○ 独立パラメータ(既定 Kt=60、kLens=0.002。統一すると k_lens ↔ 2/K_t ≈ 0.033) | **採用 P1(同上)→ §10.4**。統一で lensing デモの湾曲が約16倍になるため再校正込みで原仮定者判断 |
| 5 | 自己場 m_i/ε が自分の時計に混入する懸念(ChatGPT §4.5) | ✕ 実装は既に自己除外(`sumW[i]` は j≠i のみ蓄積。E7 コメント「外部の決定力のみで評価」) | **文書修正のみ採用** → PHYSICS.md E7 に「W は自分自身を除いて評価」を明記 |
| 6 | 一様 D₀ のゲージ化(W−W_ref で時計を定義)(ChatGPT §4.5) | D₀ は DFM では観測可能量である(マッハのダイヤル: D₀→∞ で時計差・フレーム差が消える設計。T2/G2) | **棄却(仕様)**。ψ 統一(§10.4)では W_ref を弱場写像の基準に使う選択肢のみ記録 |
| 7 | E6′ は基礎法則というより数値アルゴリズム(ChatGPT §4.6) | 既知 L1(作用原理の欠如)と同族 | **L1 に併合**。連続式 a_frame = κ_F[∂u/∂t+(v·∇)u] を L1 の中間目標として追記 |
| 8 | D₀ リザーバは保存則の証明でなく境界条件(ChatGPT §4.7) | 表現は「帳簿づけ」だが閉/開の区別が不明瞭 | **表現修正採用** → THEORY_SYNTHESIS L2 に「D₀=0 は閉鎖系の保存則 / D₀>0 は開放系の収支」を明記 |
| 9 | スピン=熱の平均/分散分離(J=I·s̄ と T=I·σ_s²)(ChatGPT §4.8) | v1.5 裁定 #4 で棄却済み(A9 は原仮定=プロジェクトの同一性) | **棄却維持**(README 限界節が位置づけを明示済み) |
| 10 | A11 は「天動説→地動説」級の転換であり有望(Grok §2・§4) | アナロジーの枠組みとしては本節が採用するもの | **参考採用**。ただし主張水準は ChatGPT の限定を正とする — 標準相対論の内部でも双子問題は固有時間積分で一意に解けるため、T9 は「反証」ではなく「表示の単純化」である(§10.2 の限定(i)) |
| 11 | 4次元ユークリッド超球 U_w²+U_x²+U_y²+U_z²=c²、全物体が絶対速度 c で運動(Gemini モデルA) | dτ/dT=√(1−v²/c²) の幾何化。E7R の ψ=0・u=0 特殊形と同値(§10.4 性質(c)) | **収束として記録**(採用は E7R 経由) |
| 12 | 光子基準系(ヌル座標・固有時ゼロ観測者)(Gemini モデルB) | τ≡0 の縮退座標であり質点力学の記述に不適 | **棄却** |
| 13 | 情報熱力学的創発時間(Gemini モデルC) | 古典力学である DFM の公理系の枠外(量子情報) | **範囲外** |
| 14 | 検証項目 V12〜V22・逆行比較デモ・GPS型デモ(ChatGPT §10〜§11) | V17 相当(自己場除外)は現行実装で成立済み(#5)。V12/V13/V16 は E7R 実装後に意味を持つ | **設計採用・実装保留 → §10.6** |

### 10.2 アナロジーの定理化 — T9(時間パラドックスの座標化)

構造対応(裁定 #2・#10・#11 と Gemini の対応表を統合):

| 天動説 | 相対論の観測者中心表示 | DFM 基礎表示 |
|---|---|---|
| 静止した地球(観測者中心座標) | 各慣性観測者のアインシュタイン同期座標 | 宇宙で一意の座標時間 t(A11)と決定フレーム u(r) |
| 惑星の見かけの逆行 | 相互的時間遅れ・折返し時の遠方時刻割当のジャンプ | (生じない — 時刻の割当が一意) |
| 周転円・従円の積み上げ | 同時性の相対性(−Vx/c² の同期項)を観測者ごとに適用 | (不要) |
| 太陽系重心座標への転換 | — | 物質共動フレーム(u, t)への転換 |
| 単純な楕円軌道 | 世界線に沿う固有時間積分 | E7 の積分(各時計で一意) |

> **T9(時間パラドックスの座標化)** — 再会する二時計の経過時間差は、各世界線に沿う
> E7 の積分として一意に決まる。観測者ごとの「相互的時間遅れ」と折返し時の遠方時刻割当の
> 不連続は、観測者中心の同期座標が生む**表示効果**であり、天動説の座標に現れる惑星の逆行と
> 同型である。時計の物理的な逆行・矛盾は、どの表示を選んでも生じない。

**限定(ChatGPT §5.2 の裁定を正とする)**:
(i) T9 は「相対性理論の反証」ではない。標準相対論の内部でも双子問題は世界線の固有時間積分で
一意に解ける。T9 の主張は「DFM の基礎表示では、同時性の相対性という表示装置そのものが
最初から不要になる」という**表現の単純化**である。
(ii) 惑星の逆行では座標転換により力学法則そのものも単純化した(周転円→楕円)が、
時間パラドックスで単純化するのは表示であり、実測(移動時計の遅れ・光の往復等方性)を
再現する物質側の動力学の複雑さは残る(→ 新規 L12)。複雑さは消えるのではなく
「同時性の相対性」から「時計・物差し・光の動力学」へ移る。

### 10.3 A11 追補 — 優先フレームは絶対空間ではなく物質共動フレーム(裁定 #1、v1.6 採用)

A2 と A11 の緊張を解消する公理の明確化(実装変更なし。u・W の定義は不変):

> 【A11 追補】A11 の優先フレーム(座標時間 t と局所フレーム u)は、A2 が否定する
> 「物質から独立した絶対空間」ではない。それは A3/A4 により**物質分布そのものから創発する
> 共動フレーム**である — u(r) は決定力加重の物質運動の平均であり、D₀ は遠方物質の粗視化、
> 座標時間 t はこの共動状態に静止する時計の族が刻む時間である。宇宙から質点を全て除けば
> フレームも消える(T1)。ゆえに A2 と A11 は矛盾しない: 優先フレームは空間の性質ではなく
> **物質配置の性質**である。地動説の「太陽」が絶対的中心ではなく質量分布の重心であるのと
> 同じ意味で、DFM の優先フレームも力学的に定義された基準である。

### 10.4 観測者転換後の「よりシンプルな法則」— 単一ポテンシャル ψ の統一時空則(v1.7 実装候補・確定設計)

現行の基礎表示では時間(E7: K_t)と光(E8′: k_lens)が独立係数を持つ。観測者を物質共動
フレーム(u, t)に固定すると、両者は単一の無次元決定ポテンシャルから生成できる(裁定 #3・#4):

> ψ(r) = W_ext(r)/K_t (W_ext は自分自身を除く決定力。§10.1 #5)
> N = e^{−ψ}(時計率)、 A = e^{+ψ}(空間率)
> **E7R(時計)**: dτ/dt = √( N² − A²·|v−u|²/c₀² ) (根号内 ≤ 0 は局所光速超過として拒否)
> **E8R(光)**: |dr/dt − u| = c₀·N/A、 実効屈折率 n_eff = A/N = e^{2ψ} ≈ 1 + 2ψ

性質:
(a) 弱場・低速で dτ/dt ≈ 1 − ψ − |v−u|²/(2c₀²)。現行 E7 の一次形(1 − W/K_t)に
**共動フレームに対する運動の時間遅れ**が加わる(第2次レビューの欠落指摘 #3 の解消)。
(b) k_lens が独立係数でなくなり k_lens ↔ 2/K_t に固定される — 自由係数が1つ減る。
原仮定「時間差が光の屈折率になる」(法則10)が文字どおり単一の場 ψ で実現する。
これが本節の表題「よりシンプルな法則」の実体である(公理の数ではなく独立係数の数が減る)。
(c) Gemini モデルA の超球条件(#11)は E7R の ψ=0・u=0 特殊形
(dτ/dt)² + (|v|/c₀)² = 1 と同値 — 「空間方向に速度を配分した分だけ時間方向の進みが減る」
というピタゴラス的読み替えが、DFM 基礎表示でそのまま成立する。
(d) τ は世界線の汎関数なので T9 は E7R の下でも成立する(表示不変)。

実装を v1.7 に保留する理由:
(i) A7 の原仮定「決定力が高いほど時間が遅い」に運動依存項を加える**公理拡張**であり、
原仮定者の承認事項(A12 承認の前例に従う)。
(ii) k_lens = 0.002 → 2/K_t ≈ 0.033 の統一で lensing デモの湾曲が約16倍になり、
プリセット再校正(K_t または c₀ の再設定)が必要。
(iii) どこまで実測再現を求めるかの方針判断が先行する: 教育用・解釈モデル(ChatGPT 方針A)
なら E7R/E8R で十分であり、光共振器実験の等方性まで求める(方針B)なら物質側の
ローレンツ応答(L12)が別途必要。本プロジェクトは README「位置づけと限界」のとおり
**方針A を採用**する。

### 10.5 双子問題の DFM 表示(T9 の適用例)

基礎表示: 共動静止時計 A は τ_A = ∫ dt/(1+W/K_t)(E7。E7R 採用後は
τ_B = ∫ dt·√(N²−A²V(t)²/c₀²) < τ_A)。どちらが年を取るかは積分で一意に決まり、
折返しの「ジャンプ」はどの物理量にも現れない。
観測表示: B が外向き/内向きのアインシュタイン同期座標を切り替えると、遠方の A に割り当てる
「現在」が不連続に動く — これが天動説表示の逆行開始点と同型の**座標写像の折返し**であり、
時計(天体)の運動ではない。

### 10.6 検証・デモ設計(実装は v1.7、E7R 承認後)

| ID | 検証対象 | 手順 | 合格条件 |
|---|---|---|---|
| V12 | E7R 運動時間遅れ | W_ext=0、v/c₀ ∈ {0, 0.3, 0.6} の時計を同一 t だけ進める | τ/t = √(1−v²/c₀²) ±10⁻³ |
| V13 | 重力+運動の合成 | 異なる W と |v−u| を持つ時計を比較 | E7R の解析積分と数値時計が一致 |
| V14 | 表示不変性 | 同一イベント列を t / 絶対同期 t_A / アインシュタイン同期 t_E で表示 | 再会時計差・光の往復時間が表示に依らず同一 |
| V16 | 双子問題 | 往復時計 B vs 共動静止時計 A | τ_B = ∫√(1−V²/c₀²)dt、時計値に不連続なし |
| — | 逆行比較デモ | 左: 太陽系(地球中心表示⇄重心表示で逆行が現れ/消える)、右: 双子(B の同期表示⇄共動表示で遠方時刻割当のジャンプが現れ/消える) | 両者が同一の図形構造(座標写像の折返し)であることの対比表示 |

(ChatGPT 提案の V17 相当=自己場除外は現行実装で成立済み(#5)。V15/V20/V21 の
一方向光速・Sagnac・共振器異方性は方針B に進む場合のみ意味を持つため設計保留)

### 10.7 未解決問題の更新

- 新規 **L12(物質側ローレンツ応答の欠如)**: E8′/E8R は光を媒質随伴させるが、移動する
  物差し・共振器・原子遷移の応答は未定義であり、Michelson–Morley / Kennedy–Thorndike 型
  実験の等方性は再現されない。README「位置づけと限界」の理論的正体。方針A では仕様、
  方針B に進む場合の最重要課題。
- 新規 **L13(K_t と k_lens の独立性)**: ψ 統一(§10.4)で解消可能。v1.7 実装候補。
- **L1 追補**: E6′ の連続極限 a_frame = κ_F[∂u/∂t + (v·∇)u] を先に定義してから離散化する
  経路を、作用原理探索(L1)の中間目標とする(裁定 #7)。
- 中性子星合体+極軸ジェット(Gemini 提案1)は 2D で原理的に不能のため恒久棄却(L7)。
