# 導出集: 決定力場モデル(DFM)の解析的帰結

担当フェーズ: 設計(最上位 Tier / Claude Fable 5)/ 作成日: 2026-07-15
入力: docs/PHYSICS.md、THEORY_SYNTHESIS.md、原仮定者の補足2点(2026-07-15)、
外部AIレビュー3件(ChatGPT / Grok / Gemini、PDF提供)
位置づけ: 本書 §0〜§1 は **PHYSICS.md への正式な差分(v1.1 改訂)** を兼ねる。
§2 以降は改訂後の公理系から導いた解析的帰結であり、論文の "Analytical consequences" 節の正本となる。

---

## 0. 公理の改訂と追加

### A13(相互性)— 原仮定者による改訂 ★最重要
> 空間の引きずりは、厳密には反作用を生じ、運動量を保存する。
> リンゴが地球に落ちるとき、地球もリンゴに引っ張られている。

**公理文**: すべての相互作用(重力・スピン斥力・フレーム引きずり・接触・スピン拡散)は
対ごとに作用反作用対称であり、系全体の線運動量と全角運動量
L_tot = Σ m_i(**r**_i×**v**_i) + Σ I_i s_i を厳密に保存する。
背景決定力 D₀ との運動量交換は、静止した無限慣性リザーバ(遠方宇宙)への移譲として
帳簿づけされる — リンゴに引かれた地球が、感知できないほど僅かに動くのと同じである。

これにより旧 THEORY_SYNTHESIS §7 の限界 **L2(保存則の破れ)は解消**され、
「保存則を厳密に満たす関係論的力学」として理論の格が一段上がる(新定理 T7)。
実装上の改訂は §1 の E5′/E6′/E10′。

### A11(光の伝播と同時性)— 原仮定者による改訂
> 光は「決定した空間」を進み、観測者によるパラドックスは発生しない。

**公理文**: 光は各点の局所決定フレーム **u**(**r**) に対して等方的に速さ c₀/n(**r**) で
伝播する。座標時間 t は宇宙で一意(絶対的同時性)であり、固有時間 τ の遅れ(E7)は
時計の**動的な**遅れであって、運動学的な相対性ではない。
したがって同時性の相対性に由来するパラドックス(双子・同時刻の不一致)は原理的に生じない。
DFM は「優先フレームを持つ理論」であることを明示する — 決定力場そのものが媒質である。
帰結: 光は媒質 **u** に運ばれる(フレネル随伴の完全版、E8′)。回転体の左右で
光の湾曲が非対称になる(新帰結 T8 — Gemini が独立に提案したブラックホール実験と一致)。

### A12(放射)— 設計者による補完(要承認)
D1(§2)の導出根拠と、旧限界 L3(法線減衰 γ_n の散逸先が不明)の解消のため追加する。

**公理文**: 温度 T_i を持つ質点は光量子を放射する。スペクトルのピーク波長は
ウィーン型則 λ_peak = b_m / T_i に従い(b_m はモデル定数)、光度は
Λ_i = η_rad · T_i^p(既定 p=4、シュテファン=ボルツマン型)。
E9 の法線減衰 γ_n で失われる並進エネルギーは、衝突時の閃光として放射される。
これによりエネルギー収支が閉じる: 並進 → スピン(摩擦)or 放射(法線減衰)、スピン → 放射(冷却)。

### 温度定義の精密化
2次元剛体円盤の回転自由度は1つであり、エネルギー等分配 ½I_i s_i² = ½k_B T_i から

> **T_i ≡ I_i s_i² / k_B**(シミュレータ単位系では k_B = 1、T_i = I_i s_i² = ½ m_i R_i² s_i²)

PHYSICS.md の略記 T ∝ s²R² は質量因子を欠くため、本定義で置き換える。
実装注: 粒子の色マップは s ではなく I s² をキーにする。

---

## 1. 計算式の改訂(E5′ / E6′ / E8′ / E10′)と新定理

### E5′ スピン斥力(対称化)
旧 E5 は i←j と j←i の力が等大逆向きにならない(m_i ω_j² ≠ m_j ω_i²)。改訂:

> **F**_rep(i←j) = k_rep · μ_ij · [ω_i(d_ij)² + ω_j(d_ij)²] · (**r**_i − **r**_j)、
> μ_ij = m_i m_j/(m_i + m_j)、加速度は **a**_rep,i = **F**_rep/m_i

中心力なので角運動量も自動保存。**遠方漸近(q=2)**: ω(d) ≈ sR²/d² より
F ∝ μ(s_i²R_i⁴ + s_j²R_j⁴)/d³ — **1/d³ 斥力。Gemini が独立に選んだ形と厳密に一致**
(q=2 の妥当性を外部AIが偶然裏書きした形になる)。

### E6′ フレーム引きずり(輸送+反作用)— A13 の中核実装
設計判断の記録: 「u へ向けた速度緩和(粘性型)」は静的中心天体まわりの軌道を
数周で崩壊させるため不可(Grok の β·S·(V_space−v) 形式はこの理由で不採用)。
旧 E6 の **Du/Dt 輸送形式は正しい力学**を与えるので、これを保ったまま反作用を追加する。

各ステップ、粒子 i について(旧 E6 と同一): Δ**v**_i = k_F · (u_i(今回) − u_i(前回))。
追加(反作用の分配): Δ**p**_i = m_i Δ**v**_i とし、u_i を構成した各ソース j の寄与率
φ_ij = w_j(**r**_i)/W(**r**_i)、背景の寄与率 φ_bg = D₀/W(**r**_i) を用いて

> **反作用**: Δ**v**_j −= (φ_ij/m_j) · Δ**p**_i (全ソース j について)
> **背景分**: φ_bg · Δ**p**_i は無限慣性リザーバ(遠方宇宙)へ移譲(帳簿に記録)

> **残余トルクのスピン移譲**: 各対の残余トルク n_ij = [(**r**_i − **r**_j) × φ_ij Δ**p**_i]_z を
> Δs_i = Δs_j = −n_ij/(I_i + I_j) としてスピンへ等角加速度で移譲。
> 背景分のトルク [**r**_i × φ_bg Δ**p**_i]_z はリザーバへ。

**性質**: (i) D₀=0 のとき P・L とも機械精度で厳密保存、D₀>0 ではリザーバ勘定込みで保存。
(ii) 引きずられる側の運動は旧 E6 と同一 — 既存デモ(銀河・土星・マッハ)の挙動は不変。
(iii) 反作用が新しい物理を生む: 銀河で外縁星を加速させた分、内側の星(u のソース)が
減速する = **角運動量の外向き輸送**(実在の降着円盤・潮汐進化と同型の構造)。

### E8′ 光線(媒質随伴)— A11 の実装
> d**r**/dt = (c₀/n(**r**)) ĉ + **u**(**r**) (光は決定フレームに運ばれる)
> 方向更新は E8 と同じフェルマー則を、**u** と共動する局所フレームで適用する。

帰結 T8: 中心天体のスピンによる u_φ が、順行側の光を加速し逆行側を減速するため、
**左右非対称な光の湾曲**(カー時空の光学類似)が生じる。

### E10′ スピン拡散(角運動量保存形)
旧 E10 の質量比重みでは Σ I s が保存しない(I ∝ m² のため)。慣性モーメント比に改訂:

> ds_i/dt = κ_s Σ_{j≠i} [ I_j/(I_i + I_j) ] (s_j − s_i) g(d_ij)

これで対ごとに I_i ds_i = −I_j ds_j、スピン角運動量が厳密保存。
注: 平衡点は「s の均一化」であり、粒径が異なると T = Is² は完全一致しない
(異粒径系の温度平衡の厳密化は未解決問題 L9 として登録)。

### 新定理
- **T7(保存則)**: E4(中心力)+E5′(中心力)+E6′(反作用つき輸送)+E9(接触対力)
  +E10′(I 重み拡散)の下で、D₀=0 なら線運動量と全角運動量(軌道+スピン)は厳密に保存する。
  D₀>0 では保存量の欠損は全てリザーバ勘定と一致する。✓A13
- **T8(非対称レンズ)**: A11+E8′ の下、スピン s_c の中心天体を挟む対称な平行光線は
  非対称に湾曲し、湾曲差は k_F・u_φ に比例する(Kerr 型フレームドラッグの光学類似)。

---

## 2. D1: 温度と光の波長からスピン速度の方程式を導く

A12(ウィーン型則)と温度定義 T = I s²/k_B を連立する。
λ_peak = b_m/T = b_m k_B/(I_i s_i²) をスピンについて解いて:

> **s_i(λ) = √( k_B b_m / (I_i λ_peak) ) = (1/R_i)·√( 2 k_B b_m / (m_i λ_peak) )**

表面(接線)スピン速度はさらに簡潔になる:

> **v_surf = s_i R_i = √( 2 k_B b_m / (m_i λ_peak) )** — 半径に依らず、質量と観測波長だけで決まる

**帰結**:
1. 青い(λ 小)天体ほど速く自転する: s ∝ λ^{−1/2}。
2. 同じ色の2天体では、重い方がゆっくり自転する: s ∝ 1/(R√m) ∝ m^{−1}(R=r_R√m のとき)。
3. 観測波長 → スピンの**分光温度計**が成立し、シミュレータで放射色から s を逆算できる。
4. 気体デモの平衡(E10′: s 均一)では、大粒子ほど T = Is² が高く赤外側から可視側へずれる —
   実装の色マップ改修(§0)と整合する検証項目になる。

シミュレータ単位系(k_B=1): λ = b_m/(I s²)、s = √(b_m/(I λ))。b_m は UI パラメータ化する。

---

## 3. D2: 楕円銀河・レンズ状銀河・渦巻銀河の形状方程式

### (G1) 回転則 — DFM の銀河基本式
軸対称・準定常の円盤で、慣性は局所フレーム基準(A5)。旧 E6 の輸送項は、定常な
u_φ(r) 場の中を周回する星に **向心加速度 k_F u_φ v_φ/r** を与える(導出: 経路に沿う
Du/Dt = v_φ ∂_θ(u_φ θ̂)/r∂θ = −(u_φ v_φ/r) r̂)。よって円軌道条件は

> v_φ²/r = g_eff(r) + k_F u_φ(r) v_φ/r、 g_eff = G ∂_r D − a_rep,rad

これを v_φ について解くと **v_φ(r) = ½[ k_F u_φ + √( k_F² u_φ² + 4 r g_eff ) ]**。

### (G2) フレーム場の自己無撞着方程式と平坦域の長さ
u_φ 自身は内側の星々の回転から作られる(不動点方程式):

> u_φ(r) = [ ∫ Σ(r′) w(|**r**−**r**′|) v_φ(r′) dA′ + Σ_c w_c ω_c r ] / W(r)

円盤質量 M_d、平均回転速度 V̄ に対し外縁では w 総和 ≈ M_d/r なので
u_φ(r) ≈ V̄ · M_d/(M_d + D₀ r)。したがって:

> **平坦域**: r ≪ r_flat では v_φ ≈ k_F V̄(平坦)。 **r_flat ≈ M_d / D₀**
> r ≫ r_flat では u_φ → 0 となり v_φ → √(GM_d/r)(ケプラー減衰に復帰)

平坦部の自己無撞着条件 v_φ ≈ k_F v_φ + (重力項) は **k_F = 1 が特異的に自己整合的**で
あることを示す(k_F < 1 では平坦レベルが内側平均より下がる)。
**モデル内予言**: 回転曲線は無限に平坦ではなく、半径 M_d/D₀ で折れる。D₀ を上げると
平坦域が縮む — シミュレータで直接検証可能。

### (G3) 形態分類 — 3つの無次元数
- 回転パラメータ: **λ_DFM = L_tot / ( M √(G M R_h) )**(R_h は半質量半径)
- 熱分率: **h = Θ / |W_grav|**、Θ = Σ I_i s_i²(スピン熱の総量)、W_grav = −ΣΣ G m_i m_j/d_ij
- 円盤安定数: **Q_DFM = σ_eff κ_eff / (π G Σ)**、σ_eff² = σ_trans² + c_spin²
  (c_spin² = ∂P_spin/∂Σ はスピン斥力の「音速」、κ_eff は (G1) の回転則から作る
  エピサイクル振動数 κ_eff² = r dΩ_eff²/dr + 4Ω_eff²、Ω_eff = (v_φ − k_F u_φ)/r)

**分類則**(境界値 h_c, Q_c は数値校正、初期見積り h_c ≈ 0.3, Q_c ≈ 1.5):

| 条件 | 形態 | 機構 |
|------|------|------|
| h > h_c(λ_DFM 小) | **E(楕円)** | スピン圧支持。摩擦が整列を作る前に加熱が勝った系(高温初期・ランダム軌道) |
| h < h_c かつ Q_DFM > Q_c | **S0(レンズ状)** | 回転支持だが円盤が熱い。腕を立てる自己重力不安定が起きない滑らかな円盤 |
| h < h_c かつ Q_DFM < Q_c | **S(渦巻)** | 冷たい回転円盤。差動回転 Ω(r) = v_φ/r が摂動を巻き込み腕を形成 |

### (G4) 楕円銀河の平衡形状(静水圧ODE)
等方支持の E 型は、面密度 Σ(r) が

> G Σ(r) ∂_r D(r) = ∂_r P_spin(r)、 P_spin ≈ c_P · k_rep · Σ² · ⟨μ s² R⁴⟩ · 𝓛

を満たす(𝓛 は 1/d³ 対斥力の2次元長距離補正の対数因子、c_P·𝓛 は数値校正)。
A12 の放射冷却 dΘ/dt = −Σ_i η T_i⁴ により **E 型は放射しながらゆっくり収縮する**
(進化の矢: 冷却 → 収縮 → 摩擦再加熱の準静的系列)。

### (G5) 渦巻腕の巻き込み
平坦域では Ω(r) = v_φ/r ∝ 1/r の差動回転により、動径方向の摂動線は
ピッチ角 tan i(t) ≈ 1/( t |dΩ/d ln r| ) で巻き込まれる(トレーリング渦巻)。
腕が持続する条件は Q_DFM ≲ Q_c(冷たい円盤での自己重力増幅)。
密度波の完全な分散関係の導出は未解決問題 L10 として登録する。

---

## 4. D3: 土星の環を安定形成する方程式

### (R1) 軌道則と回転方向の選択
中心天体(質量 M、スピン S_c、半径 R_c、静止)まわりのリング粒子の円軌道は (G1) の特別な場合:

> v_φ(r) = ½[ k_F u_c(r) + √( k_F² u_c(r)² + 4GM/r ) ]、
> u_c(r) = [ w_c/W ]·ω_c(r)·r = [ M/(M + D₀r) ]·S_c (R_c/(R_c+r))^q · r

u_c > 0(順行)側は必要な |v − u| が小さく、逆行粒子は大きなフレーム逆風を受けて
衝突散逸(E9)が激しい。ゆえに **環は中心天体のスピンと同方向に淘汰される**
(実在の土星の環が自転と同方向である事実の、モデル内での説明)。

### (R2) DFM ロッシュ条件(引きずりせん断つき)
粒塊(質量 m、密度 ρ)が潮汐+フレームせん断で引き裂かれない条件。
引きずりせん断数を **𝒟(r) ≡ k_F · | r ∂_r( u_c/r ) | / Ω_K(r)**、Ω_K = √(GM/r³) と定義して:

> **ヒル半径(2D)**: r_H = r · √( m / (3M) ) · [ 1 + c_R 𝒟(r) ]^{−1/2}
> 粒塊サイズ δ < r_H なら生存(衛星化)、δ > r_H なら破砕(環に留まる)
> 等価な臨界密度形: ρ_crit(r) = ρ_Roche,Newton(r) · [ 1 + c_R 𝒟(r) ]

(c_R は O(1) の校正係数。3D 移植時は指数 1/2 → 1/3。)
**帰結**: フレームせん断は実効ロッシュ限界を**外へ押し広げる** — DFM の環は
ニュートン力学より広い半径帯で安定に存在できる。

### (R3) 熱平衡幅と安定窓
衝突加熱と放射冷却(A12)の釣り合いが速度分散 σ_eq を決める:

> μ_f · n(r) σ_coll ⟨Δv²⟩ · μ̄ / 2 = η_rad Σ_i T_i⁴ / N ⇒ σ_eq(r)
> 環の厚み(動径幅): w_ring = σ_eq / Ω_K

**環の安定窓**(3条件の同時成立):
1. 重力凝集しない: Q_ring = σ_eq Ω_K/(πGΣ_ring) > 1 ⇔ σ_eq > πGΣ_ring/Ω_K
2. 衛星化しない: ρ_ring < ρ_crit(r) (R2)
3. 粘性拡散が遅い: t_spread = (Δr)²·Ω_K/σ_eq² ≫ 観測時間

1〜3 を同時に満たす半径帯 [r_in, r_out] が「安定な環」。σ_eq は自己調整的
(σ が上がる→衝突減→加熱減→σ 低下)なので、窓の中では **環は熱的アトラクタ**である。

---

## 5. D4: 追加の例(提案と導出)

### (a) 近日点移動 — レンズ=サーリング類似の歳差
(G1) の引きずり項は離心軌道に対して 1/r² 型の速度依存摂動として働き、
近点は順行軌道で前進(逆行で後退)する。摂動論の骨格から:

> **Δϖ(1周あたり) ≈ 2π c_p k_F · ⟨ u_c(r) / ( r Ω_K(r) ) ⟩軌道平均**(c_p = O(1)、数値校正)

近傍領域(M/r ≫ D₀、r ≫ R_c)では u_c ≈ S_c R_c²/r なので

> Δϖ ∝ k_F S_c R_c² / √( G M r³ )

これは一般相対論のレンズ=サーリング歳差 Δϖ_LT ∝ G J /( c² √(GM) r^{3/2} )(J = I s)と
**同一の r^{−3/2}・スピン×半径² スケーリング**を持つ。対応 k_F ↔ 2Gm_c/c²·(規格化) は
弱場写像表(THEORY_SYNTHESIS §4.2)の第4行を埋める。遠方領域(D₀r ≫ M)では
u_c ∝ 1/r² に折れ、Δϖ ∝ 1/(D₀ r^{3/2})·M^{1/2} — 背景が歳差を遮蔽する(DFM 独自の予言)。

### (b) 自転同期と軌道移動 — 潮汐進化の DFM 類似
点粒子の定常円軌道では、定常軸対称 u 場の輸送項は純粋な向心成分となり
永年トルクは生じない(∮(u×dr)_z = −∮u_φ dr = 0 — 実在の LT が円軌道を移動させないのと同型)。
**移動には散逸が要る。** DFM の散逸チャネルは、広がりを持つ衛星(接触結合した粒塊)内部の
E9 摩擦である:
1. フレームせん断 ∂_r u_c が粒塊の内外に速度差 Δu = δ·|∂_r u_c − u_c/r| を与える
2. 内部摩擦がこれを散逸し、粒塊は局所フレームと同期回転へ向かう(**自転同期の起源**)
3. E6′ の反作用により、トルクは中心天体のスピン S_c と往復する

**共回転半径** r_co を [w_c/W]·ω_c(r_co) = Ω_K(r_co) の解として定義すると:

> r > r_co(フレームが軌道より速い): 粒塊は前方へ引かれ dL_orb/dt > 0 → **外側へ移動**、
> 反作用で中心天体は減速(月が遠ざかり地球の自転が遅くなる関係の類似)
> r < r_co: dL_orb/dt < 0 → **内側へ落下**(フォボス型)
> 移動率: dr/dt = (2 F_t/(m v_K))·r、 F_t ≈ c_t k_F μ̄ γ(r)·δ·|∂_r u_c|(c_t 校正)

### (c) スピン気体の状態方程式(数値測定の提案)
E5′ の 1/d³ 対斥力+接触圧から、2次元スピン気体の EOS
P = Σ T_eff·[ 1 + B(T) Σ + … ] を仮の形として、ビリアル経由で B(T) をシミュレータで測る。
理想気体からのずれ(長距離斥力による正の第2ビリアル係数)が予言される。
旧限界 L4 の解消手順として §6 の検証プランに含める。

---

## 6. 数値検証プラン(受け入れテスト)

| ID | 検証対象 | 手順 | 合格条件 |
|----|----------|------|----------|
| V1 | T7 保存則 | D₀=0、全相互作用ON、1000 ステップ | |ΔP|/P_scale と |ΔL|/L_scale < 10⁻³(Float32) |
| V2 | D1 分光温度計 | 既知 s の粒子の放射色から s を逆算 | 相対誤差 < 5% |
| V3 | (G2) r_flat | D₀ ∈ {1,2,4,8} で銀河プリセット | 平坦域終端が ∝ 1/D₀ でスケール |
| V4 | (R1) 方向選択 | 順逆混合リング初期条件 | 生存粒子の 9 割以上が S_c と同方向 |
| V5 | (R2) 𝒟 効果 | k_F ∈ {0, 0.5, 1} で粒塊生存率 | k_F 増で衛星化半径が外へ単調移動 |
| V6 | D4a 歳差 | 離心軌道1体+中心スピン | Δϖ の符号=順行前進、r^{−3/2} 則で回帰 |
| V7 | D4b 移動 | 2粒子粒塊を r_co の内外に配置 | 外側→遠ざかる/内側→落ちる、S_c 反作用符号一致 |
| V8 | T8 非対称レンズ | ±S_c で平行光線束 | 左右湾曲差が S_c に比例 |

## 7. 実装への影響(次フェーズへの引き継ぎ要点)

index.html への差分(実装フェーズ、Sonnet 担当。HANDOFF_IMPLEMENTATION.md に本節を添付):
E5′ の対称化 / E6′ の反作用ループ(φ_ij 分配・スピン移譲・リザーバ帳簿変数)/
E10′ の I 重み化 / 色マップを T = Is² キーに変更 / b_m・η_rad・p の UI 追加 /
E8′ の光線随伴項 / 保存量モニタ(P, L, リザーバ勘定)の表示 / §6 の V1〜V8 をテスト列に追加。
受け入れの一次条件は V1(保存則)である。

## 8. 未解決問題の更新
- 解消: L2(→A13/E6′/T7)、L3(→A12)、L4(→D4c の測定手順を定義)
- 新規: **L9** 異粒径系の温度平衡(s 均一と T 均一の不一致)、**L10** 渦巻密度波の分散関係、
  **L11** A12 の放射反作用(光量子の運動量)と T7 の整合(現状は放射をスカラーエネルギーとして扱う)
