# 仮想物理ラボ — 仮定物理法則シミュレータ

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](LICENSE)
[![Docs: CC BY 4.0](https://img.shields.io/badge/Docs-CC%20BY%204.0-lightgrey.svg)](docs/LICENSE)
[![QA](https://github.com/tty-imamura/dfm-simulator/actions/workflows/ci.yml/badge.svg)](https://github.com/tty-imamura/dfm-simulator/actions/workflows/ci.yml)
[![DOI](https://zenodo.org/badge/DOI/10.5281/zenodo.21454188.svg)](https://doi.org/10.5281/zenodo.21454188)

「空間座標は質点の相対位置で決まる」「スピン=熱」といった**仮定の物理法則**を体系化し、
その帰結(回転方向の整列、銀河外縁の回転速度増強、熱平衡、光の湾曲、空間の引きずり)を
iPhoneのブラウザで観察できるシミュレータです。

> 🚀 **オンラインで試す**: https://tty-imamura.github.io/dfm-simulator/
> (GitHub Pages 有効化後に利用可能 — Settings → Pages → Branch: `main` / root)

## これは何か・何ではないか(最初にお読みください)

- **これは**: 少数の仮定(公理 A系・拡張 E系 — 決定力場モデル DFM)から出発した**トイ宇宙**の
  物理エンジンです。同じ一組のルールが、分子の熱平衡からビーカーの対流、惑星の軌道、恒星・銀河の
  構造、膨張する箱宇宙まで、7つのスケールタグにわたって何を創発させるかを観察します。主張は
  「このルール系がこの現象を創発させる」という**モデル内の因果**であり、各サンプルは対照実験
  (ノックアウト・用量反応・多seed・摂動回復)と機械QA・CIで検証されます。
- **これは違う**: 実在の宇宙・ダークマター・ブラックホール・銀河などの**定量的な再現主張では
  ありません**。数値の一致(GPS +38.5μs/日 等)は較正(対応写像 Kt=c²/G)の帰結であって証拠では
  ありません。実在の天体名・現象名を使うサンプルにはタイトル直下の「⚠ しない主張」行が、
  初回起動時の3枚ガイド(DFMとは/できること/しない主張)とともに境界を明示します。
  画面のスケール換算(m/s² や m/s への換算表示)もアナロジーの目安であり、モデルは実宇宙の
  定数に束縛されていません。
- **反証できます**: 定量主張は宣言(claims)として機械可読化され、固定seedの窓検査を CI が毎回
  照合します。窓から外れれば FAIL がそのまま公開されます。負の結果も隠しません — ☿水星は
  「Base DFM: FAIL → E12 ON: PASS」を本文より先に表示し(Failure First)、成立しなかった主張は
  説明文・PHYSICS.md に「撤回」として残します。
- 正本ドキュメント: 物理仕様 [docs/PHYSICS.md](docs/PHYSICS.md) /
  箱宇宙 [docs/BOX_UNIVERSE.md](docs/BOX_UNIVERSE.md) / 変更履歴 [CHANGELOG.md](CHANGELOG.md)

## 使い方(iPhone)

`index.html` を開くだけで動きます。ホスティング不要の方法:

1. **GitHub Pages(推奨)** — リポジトリの Settings → Pages → Branch を `main`(root)にして保存。
   数分後に `https://tty-imamura.github.io/dfm-simulator/` をSafariで開く。
   共有→「ホーム画面に追加」でアプリのように使えます。
2. **ファイルとして開く** — `index.html` をiCloud Drive等に保存し、
   「ファイル」アプリからSafariで開くことも可能です。

## 機能

- **内蔵シミュレーション**(**原則「1サンプル=1主題」**の11グループ構成(第273便a 時点) — 統制実験型の
  サンプルは変える要素を1つに絞るが、🌑🐚⏳⚫ のように複数の機構を組み合わせて構造の創発を
  見る複合型のサンプルもある。v1.31 で「銀河」を新設、v1.32 で「箱宇宙」(📦🌀📈🫧🪢🫁🔦 —
  7サンプル)を新設。v1.42 でグループを再編し「銀河」→「銀河の物語」・「光」→「光の物語」へ
  改名、観測較正・監査系をまとめた**「現実との照合」を新設**した(表示・分類のみ — 物理は不変。
  分類チップ「現実較正」・現実準拠チップ「📏 現実較正」と紛らわしくない呼称に揃えたもので、
  チップ側の呼称は不変)。さらに v1.42 で**「ローターの物語」を新設**し、ダークローター系の
  9サンプル(🕶️🕳️⚫🪜🌑🐚⏳🥚🌱)を銀河の物語・天体の物語から移した(表示・分類のみ — 物理は不変。
  ファミリーがグループを跨がなくなり、既定表示から変種が落ちる経路が無くなった)。
  総数は入れ替わるため記載しない — **現物はアプリのプリセット選択が正本**で、
  以下は主要サンプルの抜粋。v1.9 全面刷新、以後追加・整理を継続。
  **v1.45-b1・第272便d でカテゴリの表示順を論文の順番へ合わせました**〔空間と時間(論文1 実験1 相対回転・
  実験4 時計)→銀河の物語(実験2)→天体の物語(実験3 近点)→光の物語(実験5)→熱の実験室(実験6)→
  箱宇宙(論文2)→ローターの物語(論文2 付録・減光)→現実との照合・太陽系(論文3)→現実との照合・太陽系外→
  **実在天体のアナロジー**〕。**表示順だけの変更**で、サンプルの中身・物理・保存 JSON は 1 bit も変わりません
  (グループ内の並びも従来どおり)。同じ便で**新グループ「実在天体のアナロジー」**(11 サンプル —
  🎐🎻⏰⚛️🍇🫐🌃🛞🥀🌹🦀)を新設し、「現実との照合・太陽系外」から移しました。**実在天体の模型**を
  比較器の状態(comparable / inside / not-applicable など)で観測と並べる群で、**観測一致版ではありません**
  (この群に入っていることは合否を意味しません)。移動したのは group の宣言だけで、
  較正サンプルの母集団(37 本)・比較器の導線・旧較正の履歴はそのままです。
  **v1.45-b1・第273便a でグループ名を改め、「空間と時間」を 2 つに分けました**(原仮定者の裁定・第63報)。
  空間と時間→**運動と時空** / 銀河の物語→**銀河の力学** / 天体の物語→**天体の機構** /
  光の物語→**光の伝播** / 熱の実験室→**スピンと熱** / 箱宇宙→**箱宇宙の実験** /
  ローターの物語→**自転と減光** / 現実との照合・太陽系外→**現実との照合・連星**
  (現実との照合・太陽系と実在天体のアナロジーは据え置き)。あわせて時計・弱場GR較正の 5 本
  (⏱️🛰️🕰️🌟⏲️)を**新グループ「時計と重力」**へ分けました。表示順は
  運動と時空 → 銀河の力学 → 天体の機構 → 時計と重力 → 光の伝播 → スピンと熱 → 箱宇宙の実験 →
  自転と減光 → 現実との照合・太陽系 → 現実との照合・連星 → 実在天体のアナロジー です。
  **表示だけの変更**で、サンプルの中身・物理・保存 JSON・署名は 1 bit も変わりません
  (内蔵 124 本・較正 37 本のまま)。開閉の記憶に使う内部 id も据え置きで、新群の
  `clocksGravity` だけが増えています。旧名を宣言した保存・AI 生成サンプルは新しい群に出ます
  (宣言の文字列は捨てません)。**群の名前は合否を意味しません**:
  - **運動と時空**(第273便a で「空間と時間」から改名):
    ⚾自由落下と放物運動(一様重力場 gravityY の力学実験室。等価原理の簡易デモ) /
    ⏪時間の矢 — 巻き戻る宇宙と巻き戻らない宇宙 / 🪗コンパクト力の符号 /
    🫂箱宇宙と連星 — 等質量だと背景に対する移動が消える / 🪟空間メッシュの窓
  - **時計と重力**(v1.45-b1・第273便a 新設 — 「空間と時間」から 5 本を分割。ファミリーは分断していません):
    ⏱重力による時計の遅れ(距離だけを変えた統制実験。解析値と1e-5で一致) /
    🛰️弱場GR較正(Kt=c²/G の1本で GPS +38.5μs/日・光偏向 1.7512″・シャピロ ≈281μs が
    定量一致する較正デモ) /
    🕰️GPS時計差 — 重力項と速度項の分解(v1.42 新設。🛰️の焦点版その1。地上時計・周回衛星・
    衛星と同半径の静止参照時計の3本立てで、引き算だけで重力項と運動項が分離する。画面の
    半径比を実GPSに合わせてあるので、画面上の重力項:運動項の比も実GPSと同じ6.3) /
    🌟太陽縁の光偏向 1.75″(v1.42 新設。焦点版その2。α=4GM☉/(c²R☉)=1.7512″。
    まっすぐ狙って撃った光子が太陽に引かれて観測者を外す — 日食観測が測ったずれ) /
    ⏲️シャピロ遅延(v1.42 新設。焦点版その3。Δt=(4GM☉/c³)ln(4r₁r₂/b²)≈281μs。
    ワンタップ対照 κ=0 と並べると、時空係数のある宇宙の信号だけが遅れて着くのが見える) /
    ☿水星の近日点前進(v2.0 測地線1PNモード E12。時計と同じψが軌道を回す —
    実値換算42.98″/世紀。機械検証V18/V19/V20) /
    ⚾自由落下と放物運動(一様重力場 gravityY の力学実験室。等価原理の簡易デモ)
  - **箱宇宙の実験**(v1.32 に「箱宇宙」として新設 → 第273便a で改名): 📦並進する箱(壁と共動する時計は速度1.2でも遅れず、箱に対する
    固有運動を持つ時計だけが遅れる — 箱の並進は内部から検知できない) /
    🌀回る箱と空間の支え(内部フレームの利得は中心近傍で理論値どおり約1/2。G=0でも空間の
    引きずり(E6′)だけで円軌道を支える — ニュートンのバケツの解) /
    📈膨張する箱・質点リング(離散壁が内部へ運べる膨張は壁の半分 — √a に比例した追随) /
    🫧UniverseBoxの完全共動と膨張時計(静的な背景決定力D₀を位置・速度・尺度a(t)を持つ
    動的背景UniverseBoxへ昇格し、完全共動χ=r/a保存を実現。決定力の希釈で時計が加速) /
    🪢束縛系は膨張に逆らえるか(膨張への追随は決定力の持ち分φ_Bで決まる則 — 箱支配の
    連星はd∝a^{2φ_B}で拡大する) /
    🫁呼吸する宇宙と時計(振動する尺度因子。時計率の変調と一周期の可逆性) /
    🔦光の赤方偏移(A・B・C)(光子は放出波長λ_emitと放出時尺度a_emitを状態として保持し
    1+z=a_obs/a_emitで波長が伸びる — 意味論であり光子力学からの導出ではない)。
    膨張則(exp/lin/sin/pow — 係数・指数・揺らぎ)は実行中に編集でき、膨張グラフ
    (a(t)・H(t)・赤方偏移カラーバー)で確認できる。実宇宙の特異速度減衰と比較する
    FLRW摩擦比較モードも用意。正本は[docs/BOX_UNIVERSE.md](docs/BOX_UNIVERSE.md)。
  - **銀河の力学**(v1.31 に「銀河」として新設 → v1.42「銀河の物語」→ 第273便a で改名): 🌌銀河の回転曲線(中心大質量星+恒星380体+光線24本。kFrame=1 で
    外縁速度が実測約1.3倍に増強 — グラフは実測 vs バリオン基準 v_bar(質量分布だけから
    期待される回転)。光線は中心の強い場に捕捉され巻き付く) /
    🌠銀河合体と潮汐効果(核と円盤が一体で並進) /
    🌫️自己重力雲の崩壊と原始星形成(微小な初期回転から円盤形成) /
    🍳ディスクとバルジ — 回転支持と分散支持(v1.37 新設。同じ質量分布を「円運動で回る円盤190体」と
    「等方ランダム速度のバルジ190体」に分けた統制実験。空間引きずり〔A8/E6′〕の残余トルクだけを
    スピン経路に残してある) /
    🪁銀河の空間メッシュ — geoPN=3 原理コピー(v1.45-b1・第265便b 新設。🎠 の幾何・粒子・乱数種はそのままに
    **法則だけ**を geoPN=3 のトイ測地線モードへ替えたコピー。**較正ではありません** — 観測回転曲線は 1 つも入れていません) /
    🎋銀河の空間メッシュ・軽量コピー(**v1.45-b1・第274便c 新設**。🪁 と**物理の宣言が 1 文字も違わず**、
    **指数円盤の粒数だけ** 260→80 に減らした本です。geoPN=3 のトイの費用は粒数の二乗で効くので、
    **法則を 1 バイトも変えずに軽くする**道を 1 本置きました。**同じ走行ではありません** —— 粒数が違うので
    保持率も V_rms も 🪁 と一致しません。**どちらが正しいとは言いません**〔較正ではありません〕)
    スピン経路に残してある)
    🔮球状星団トイ / 🥏腕なし回転円盤トイ / 🧵腕単体トイ(v1.45-b1・第274便d 新設 —— **指定した
    3D 正規分布を定常分布に持つ参照モデル**〔`physics.shapeToy`〕。復元・減衰・**供給(外部熱浴)**の
    3 作用を宣言し、離散化は厳密〔F=e^{Bh}・Q_h=C−F C Fᵀ〕。**較正ではありません**: 観測された星団・
    銀河・渦状腕の量は 1 つも入力していません。**重力で安定したのでもありません** —— 対象粒子は
    規定運動で、重力 G を上げても 600 步の状態は 1 bit も変わりません。円盤の扁平は σ_z の宣言
    であって回転の帰結ではなく、腕は指定した形状であって「創発した模様」ではありません。
    **第275便d(v1.45-b1)で既定値を見直しました**: **成長の宣言を外し**〔τ=0〕、σ を**初期配置の
    RMS 半径に一致する値**へ置きました〔🔮 36・🥏 44/σ_z 14・🧵 7.2・腕の長さ 120 固定〕。
    🥏 は Ω/ω₀ を 0.5 → 5 に上げ、**角運動量が平均と逆向きの粒子の割合を 27.9% → 1.1% へ**下げています。
    **平坦な回転曲線は出ません** —— この写像は線形なので回転は剛体回転だけです〔否定結果〕)
    🎱球状星団トイ・中心天体つき / 📀腕なし回転円盤トイ・中心天体つき / 🧹棒トイ・中心天体つき
    (**v1.45-b1・第275便d 新設 → 第276便d で法則を入れ替えました** —— 上の 3 本に**位置固定
    〔pinned〕の中心天体を 1 個**足した本です〔`center:"pinned"`〕。第275便d までは**規定運動**で、
    中心天体の質量もスピンも 600 步の状態を 1 bit も変えませんでした。**第276便d で、粒子を
    「中心天体のスピンが作る速度場を読む力学」で動かすようにしました**〔`physics.shapeToy.law:"coreField"`〕:
    L=½m|v−u|²−mΦ・u=ω_m〔s×r〕・Φ=½κ₀r²+½ω_m²{αr⊥²+βr∥²}・**ω_m=W_c/(W_c+W₀)·ω_c**。
    **これは新しい構成仮説であって、現行 DFM から一意に導出された法則ではありません**。
    束ねているのは**中心スピン**です —— **ω_c を 0 にすると力が 1 つも残らず、雲は散ります**
    〔指紋も変わります。実測〕。伝播子は**厳密**〔λ±=ω_m±√(ω_m²+K⊥) の 2 モード + 調和〕で、
    保存量 h=½|v|²+½K⊥r⊥²+½K∥r∥² は 20,000 步で相対 1e−12 級しか動きません。
    交換・散逸は共回転する熱浴〔厳密 OU〕で、**供給は有限のコア熱容量から出て、尽きれば止まります**。
    中心は pinned のままの**外部支持版**で、反作用は帳簿に負号で記帳するだけです〔**閉じた系では
    ありません**〕。**重力を二重に足しません**: 中心重力をどちらが持つかを
    `coreField.centreGravity:"phi"/"engine"` で宣言し、二重になる組み合わせは門で止めます。
    円盤の薄さ σ_z/σ_R と逆行の割合、棒の細長さは、いずれも**宣言した α・β・Ω_p の帰結**です。**較正ではありません**〔観測された軸比・回転曲線・中心質量は 1 つも入力していません〕。
    **平坦な回転曲線は出ません**・**腕が創発したのでもありません**・**中心ブラックホールを実装した
    のでもありません**。ω Cen の IMBH 観測は**参照**として名前を挙げるだけです)
  - **自転と減光**(v1.42「ローターの物語」として新設 → 第273便a で改名 — ダークローター〔高スピンで自光を掻き出す天体〕を中心に
    据えた系を銀河の物語・天体の物語から集約した。※実在のダークマターについての主張ではない。
    **v1.45-b1・第272便d で群の位置付けを明文化しました**: これは**自転状態による減光を確認する実験群**で、
    表示輝度・脱出光・吸収・放射エネルギーを比べます。**銀河の不足質量や観測ブラックホールの再現は、
    この群の達成条件に含めません**〔暗さを「隠れた質量の証拠」として読まない — 暗さと質量は別々に測ります〕。
    群の 10 サンプルはいずれも `notClaim` に "dm" を宣言しています):
    🕶️ダークローターの銀河(v6・全粒子自由の閉鎖系。中心BHとx=±200で向かい合う2個の
    ダークローター(スピン2.0)、恒星380体。ローターの空間引きずり(A8/E6′)が恒星円盤に
    m=2の後行渦状腕(ピッチ≈26°)を立てる。有効窓は2本立てで、恒星の安定はt≈384(保持
    87.9%)まで・腕の分離はt≈96まで。※対向2個という配置そのものがm=2の質量四重極なので、
    スピン0の対照にも配置由来の弱い腕が残る(帯平均0.156)— スピンを入れると0.560=3.6倍に
    増強され、スピン0→1→2→3で単調に増える(用量反応)ことがスピン起因分の分離の根拠。
    ※実宇宙のダークマターや渦状腕理論の主張ではない) /
    🌑ローター星雲 — 暗黒星雲と散光星雲(v1.37 新設。高スピンの小ローター54個のクランプが自光を
    掻き出して暗く、低スピンのエンベロープ44個は放射がそのまま漏れて明るい — ひとつの雲に
    「暗黒星雲」と「散光星雲」が同居する) /
    🐚重殻ローター星雲 — 束縛と暗さの両立(v1.37 新設。「重い外殻+高速スピンの小コア」の2層構造で、
    重力の束縛と光学的な暗さを両立させる。これが本モデルの**ダークローターの正式定義**) /
    ⏳双極星雲 — 暗黒トーラスと極方向ローブ(v1.37 新設。赤道を暗黒アークで塞ぐと、スピン圧の
    出口が極方向だけになり上下2つの光るローブになる) /
    ⚫DFM版ブラックホール — 5層の中心(v1.37 新設。層0コア〔独立 J_core・初期Ω=20〕/層1重い内殻/
    層2擬似地平面〔2層減光で真っ暗〕/層3決定フレーム/層4降着円盤。τ_cs でコアの角運動量が外殻へ
    流れ、**中心が真っ暗なまま**外縁速度が1.52倍に増強される〔可視の🌌標準 1.26 超〕。
    コアなし対照 1.05 で因果を分離。※実在のブラックホール物理の再現主張ではない) /
    🪜隠れ質量ラダー — 暗い中心の力学質量(v1.38 新設。減光autoの重い中心核 m=2500/5000/10000 を
    3系並べ、同じ半径の測定リングの速度だけで質量比を読む。3中心とも観測温度 T_obs=0
    〔=光度から推定される質量は0〕なのに、力学質量 M_dyn=⟨v²r⟩/G の比は 1.96 と 3.84 で
    真の比 2:4 を回復する。超過分〔+7〜11%〕は E6′ 引きずり由来で kFrame=0 で消える。
    ※実在のダークマター観測の再現主張ではない) /
    🥚自己形成ダークローター — 種から育つ暗い中心(v1.38 新設。**目標の形を初期条件に置かない**
    創発サンプル。一様な180粒(全部おなじ重さ・自転ゼロ・自由)+弱い全体回転+各粒の微小な
    コアの種だけを置き、あとは「近づきすぎた2粒は1粒になる」融合とコア継承則に任せる。
    9000步で25体まで融合し、最大の1体が全質量の26.7%を占める暗い中心天体に育つ〔実効減光
    0.589 に対し周囲は0.159〕。中心天体の J_core は種48個ぶんちょうどで、飲み込んだ質量48と
    1粒単位で一致する。ノックアウト対照: 融合を切ると最大天体は1粒のまま〔0.56%〕・コア種を
    外すと J_core は恒等的に0。自己重力が駆動因であることは G の用量反応〔2/4/6/8/12 →
    1.7/6.1/12.8/26.7/44.4%〕で分離した。※暗さそのものは融合の有無を判別しないこと、
    kFrame=0 対照に系統差が出ないことも説明文に正直に記載している) /
    🌱星の種ローター — 圧縮とパワーボール(v1.37 新設・v1.42 で天体の物語から移動。
    星の種がダークローターへ育つ過程の原理実験で、**コアv2**(中心コアを J_core を主変数とする独立サブシステムにした拡張)の実演。
    主要因は自己重力圧縮による角運動量保存(A13)、副機構はパワーボール原理 — 軸の自由な
    差動コア(種A)は軌道の揺すりで加速し、軸を固定した剛体コア(種B)は同じ揺すりで減速する。
    ※2Dでは歳差運動そのものは表現できないため、入出力の対応だけを写したアナログである)
  - **光の伝播**(v1.42 で「光」→「光の物語」・第273便a で改名): 💡重力レンズと光子捕捉(時間と光は単一のψ) / 🌗回る星のレンズ(非対称湾曲 —
    カー型フレームドラッグの光学類似) / 🔭連星重力レンズ(2つの決定力井戸の複合レンズ) /
    🌆暗黒雲の赤化 — 体積吸収と波長依存(v1.38 新設。背景の高温星群の手前を冷たい暗黒雲が
    横切る。雲は光線を曲げないほど軽いのに、通り抜ける光を波長ごとに違う強さで吸うので
    青が先に消える。雲中心の透過率は青(450nm)0.0044 / 赤(650nm)0.288、雲の脇は厳密に素通り。
    光学輸送は観測層のみで、軌道・スピン・時計は1bitも変わらない。
    ※実在の星間減光則〔R_V 等〕の再現主張ではない)
  - **スピンと熱**(第273便a で「熱の実験室」から改名): 🔥気体の熱平衡(初期スピンだけを変えた統制実験。温度グラフ付き) /
    🎈スピン圧 — 熱は押し広げる / 📏熱伝導 — 近いほど速く伝わる /
    🌈放射冷却 — 高温ほど速い(E11 Λ∝T^p・既定 pRad=2 の **T²則**の統制実験。
    初期の冷却速度比 ≈1:16:256) /
    🧊固体・液体・気体(一様重力下の形態統制実験 — 液体は底に溜まる) /
    ♨️加熱の対流 — 床加熱と天井冷却(一様重力+スピン圧だけで循環する要因分離デモ) /
    🧪重力分離と浮力(重い粒子が沈み、熱い軽い粒子が浮く)
  - **天体の機構**(第273便a で「天体の物語」から改名。物語・トイ系。実単位で現実へ較正したサンプルは下の「現実との照合」へ移動):
    🪐土星の環(実験)(氷粒の3環帯 — kFrame=1 の下での長時間安定。v1.28 で
    旧「土星の環」を置換) /
    🎯土星の環(主星2層)(コアの差動回転が環に効く引きずりの統制実験) /
    🌍地球と月(潮汐固定の公転 — コアは PREM/LLR 観測値) /
    🌕地球と月(自由二体・物理比 — kFrame=0 の純ニュートン閉鎖系・現実の自転比 27.4×) /
    ⭐連星系と周連星円盤 /
    ♾️三体問題 — 8の字軌道 / 💥順行・逆行リングの衝突(公転→熱の変換) /
    🌪️収縮とスピン加速(圧力を切った自己重力雲。内側半質量=コアの半径が3.06倍縮む間に
    コアの回転が8.50倍速くなる — 角運動量保存の予測(R₀/R)²=9.36倍に一致90.8%。おすすめA/B
    kRep=1で圧力が収縮を支える。※主張はコア限定で、系全体では逃走粒子のせいで回転はむしろ
    遅くなる。実在の星形成の再現主張ではない)
    (🌱星の種ローターは v1.42 で「ローターの物語」へ移動)
  - **現実との照合**(v1.42 新設 — 天体の物語から観測較正・監査系14本をまとめた。
    スケール換算込みで数値を現実の対応対象へ較正したサンプルと、その較正を監査する対照群):
    ☄️水星(実単位)— 43″/世紀 / 🪨水星(実単位・kFrame=1)— 共通補正で 43″/世紀成立 /
    🌙地球と月(実単位)— 恒星月 27.32日 / 🌘地球と月(実単位・kFrame=1)— 近点回転 8.85年の較正再現 /
    💍土星の環(実単位)— 見えないのが正しい / 💿土星の環(実単位・kFrame=1)— 表面随伴と自動算出q /
    📡土星の近点移動(実単位・D68精密較正 — E13 帯状重力補正+較正係数で Cassini 実測へ精密一致。
    v1.29 で旧🧭を一本化) / 🌞太陽系 — 内惑星(実単位) /
    🟠木星とガリレオ衛星(実単位)— 規則を再フィットしない hold-out /
    ⭕🧲🔆地球と月・機構判別A/B/C(「数値一致 ≠ 機構同定」の実験化 — 二体ニュートン/二体DFM較正/
    太陽+地球+月の三体) / 📶📐qLock 半径方向監査とその q=3 対照
- **A/B比較モード**(v1.13)— 同一の初期配置で宇宙を2つ複製し、選んだ1つのパラメータだけを
  変えて上下に並べて同時再生(例: 🌌銀河で kFrame=1 vs 0 → 外縁増速が起きる宇宙と起きない宇宙が
  一画面で見比べられる)。パラメータタブから開始。**おすすめA/B**(v1.15)— 説明文が推奨する
  対比(🌌kFrame=0・💥muF=0・⏱Kt=150 など)を1タップで開始
- **外部要素バッジ**(v1.15)— 説明タブに、そのサンプルの固定粒子(外部拘束)・レール駆動
  (規定運動)・熱浴を bodies から自動判定して表示。閉鎖系(保存則が系内で閉じる)と
  外部駆動の展示系を混同しないための表示
- **理論解説パネル**(v1.13)— 説明タブに、そのサンプルが示す法則(A8・E7R・T4 など)の
  1行解説を自動表示。日英対応
- **創発モニタ(v1.38)** — 「グラフ」カテゴリのトグル。「構造がどれだけ立ったか」を4つの実測量の
  時系列で読む診断グラフ: ①塊の数(接触距離ベースの連結成分数 — i–j を繋ぐ条件は
  d<1.2·(R_i+R_j))②最大塊の質量比 ③スピン整列度 Σ I_i s_i ÷ Σ I_i |s_i|(+1=全部同じ向き)
  ④回転支持比 V/σ=⟨v_φ⟩²÷σ²(1より大きければ回転が支え、小さければランダム運動が支えている)。
  表示・診断のみで物理には一切影響せず、計測は表示ONのときだけ数フレームに1回走る(OFFではゼロコスト)
- **E水準タグ(v1.38)** — 説明タブのバッジ。そのサンプルの「創発の水準」の宣言:
  E0=規定 / E1=外部駆動下の自己組織化 / E2=閉鎖系の創発(目標の形を初期条件に埋め込んでいない) /
  E3=多seed頑健性確認済み。sampleClass と同じ**宣言専用**のメタで自動判定はせず、物理には一切影響しない
  (宣言のあるサンプルにだけ表示: 🧬🧊⛓️🥚=E2 / ♻️=E1)
- **回転曲線グラフの基準線 v_bar(v1.32)** — 実測(実線)に加え、質量分布だけから期待される
  回転 v_bar(r)(バリオン実加速度基準 — 円盤の寄与も含む)を点線で表示。中心質量だけの
  ケプラー基準から置き換え、中心天体1個に縮退する構成では旧基準と厳密に一致する
- **UI・表示(v1.31)** — 左上タイトルのタップでアプリ説明(操作・法則要約。キーボード
  操作対応)/グラフと「選択粒子の編集」の最小化ボタン/文字サイズ 小・標準・大(既定=標準)/
  粒子サイズは半径準拠(描画時のみ品質連動の上限 — 正確=無制限・自動40px・軽量30px。
  物理半径と数値表示は不変)
- **タブパネルを広げる/狭くする(v1.45-b1・第275便f)** — **縦画面**でタブを開くと、パネル右上の
  「✕ 閉じる」の左に「**⤢ 広げる**」(英語UIは `⤢ Expand`)が出ます。押すとパネルが
  **画面の高さからヘッダー・操作列・タブバーを引いた分**まで伸びてシミュレーション画面が隠れ、
  ボタンは「**⤡ 狭くする**」(`⤡ Shrink`)に変わります。**「✕ 閉じる」は広い状態でもそのまま効き**、
  閉じても広い/狭いの選択は保たれます(次にタブを開くと同じ状態で開きます)。
  **既定は「狭い」**で、選択は端末の localStorage 鍵 **`hp_panel_wide`**(`"1"`=広い / `"0"`=狭い)に
  残ります — 表示専用の設定で、サンプルのパラメータにもセーブJSONにも書き込みません。
  **横画面の2カラムレイアウト**(landscape かつ幅 900px 以上)では、パネルが元から右カラムの
  残り高さを全部使うためボタンは表示されません(縦画面の判定はこの2カラム化の条件の裏を
  そのまま使っています)。引く高さは文字サイズ(小/標準/大)や言語でヘッダー段の高さが
  変わるため、定数ではなく**実測して** CSS 変数 `--panelWideReserve` へ渡しています
- **説明タブの並び・概要・畳み(v1.45-b1・第276便f)** — 「説明」タブの並びを
  **チップ → タイトル → ID → 概要 → 注意書き → 仲間 → 監査ビュー → 較正台帳 →
  本文(要約・観察・操作)→ 観測結果カード → 失敗から見る → 数値主張 → 標準試験 →
  理論解説 → 外部要素** に整理しました。
  - **「🔖 概要」**(英語UIは `🔖 In brief`)は ID 行の直下に出る**数行の要約**です。
    サンプルが `descStruct.brief` を宣言していればその文、していなければ「📌 要約」の
    **先頭 1〜2 文を機械抽出**したものを **120 字以内**で表示します(切ったときだけ末尾が「…」)。
    表示専用の宣言で、`description`・`presetSig`・セーブJSON には一切入りません。
  - **「🧪 失敗から見る」と「観測結果カード」は折り畳み**になりました(既定は畳む —— 見出しを
    タップすると開きます)。**「🔎 数値主張」と同じ規約**で、開閉は記憶しません(サンプルを
    選び直すと畳んだ状態に戻ります)。中身・行数は宣言のままで変わっていません。
  - **「🔬 監査ビュー」の導線はトグル**になりました。開いたあとに**もう一度タップすると閉じます**
    (✕ と Escape も従来どおり効きます)。
  - **タブを広げたときに、シミュレート画面の文字が操作列・タブ・パネルの上へはみ出さなくなりました**
    (広げた状態でだけ `#canvasWrap` をクリップします。狭い状態の見た目は 1px も変わりません)
- **親子コア(同心層)の編集 UI(v1.44・第261便b / 第262便b)** — 層を宣言した粒子を選ぶと
  「選択粒子の編集」の **`m` 欄の上に「親子コア(層)を開く」ボタン**が出ます。押すと
  **粒子の編集(m・s・R・…)と根/層の情報が丸ごと入れ替わり**ます(下に積み増さないので、
  縦に長くなって見切れることがありません。もう一度押すと戻ります)。
  タブは「**根 / 層1 / 層2 …**」で、**「根」は最も内側の層**です。各層の
  **役割・質量 m・半径 r・スピン角運動量 J** を編集できます(層の追加・削除も同じブロックから)。
  **半径は昇順(同心・非重複)が必要**で、それを壊す編集は**適用されず**理由が下に出ます。
  **層があるあいだ、根の質量は層の合計に追随します**(層が主・根が従)。**層の J は宣言値で、
  力には接続していません**(表示・保存の対象)。画面では層が**同心円**で描かれ(コア V2 と同じ線種・
  透明度は質量比)、タブの左帯の色が役割ごとの凡例になります。
  **コア内訳(v2)は廃止予定です** —— コア V2 を持つ粒子には「廃止予定」の告知と
  「**親子コアへ移行**」ボタンが出ます(**押すまで何も変わりません**)。移行すると
  **『コア』が「根」に・『外殻』が「層1」に**なります。移行しても**遠方の重力は厳密に同じ**で、
  **コア半径の内側だけ**が変わります(コアの内側は包含質量 0 になるため)。
  移行できない粒子(空洞コア・コア半径が観測半径以上・負の質量)では**ボタンが押せなくなり、
  理由がその場に出ます**。質量比 1 の「裸コア」は層が 1 つになり観測半径が層に載らないので、
  **1 回目の押下で警告・2 回目で移行**します。
  既存のサンプル・セーブのコア V2 は従来どおり動きます(**消していません**)。
  (v1.44・第263便b)説明は**ボタンと同じ行に押し込まず 1 行を占有**し(狭い場所で 1 行 2.4 文字
  まで痩せていたのを 13.0 文字にしました)、「V2 は消さない・元の宣言を保持する・遠方は同じで
  コア半径の内側だけ変わる・裸コアは 2 段確認・移行できない粒子は理由を出す」の規約文は
  ボタンの下の「**この移行について**」に畳んであります(既定は閉)。
  また「選択粒子の編集」を**最小化すると「親子コア(層)」も畳み**、**再展開すると基本行(m・s・R・…)
  から開きます**(層を見るには「親子コア(層)を開く」をもう一度押します)。
  (v1.45-b1・第264便c)層モードに**読み取り専用の数値行**が出ます —— **回転場の源 Q(と、その所有者)・
  ω・傾き θ・|J|・Jz・Jx・Jy・回転エネルギー E_r・I_c・M_c・M_s・K_cs・観測半径**。
  **コア V2 を持つ粒子は「コア内訳(v2)」で見えていた値そのもの**、**層だけの粒子は層(根+層)から
  導いた値**が出ます(K_cs は層に無いので「—」)。同じ規約が回転場の源にも当たっていて、
  **コア V2 があれば V2・無ければ層の J** が源になります(**両方あっても二重には数えません**)。
  層の J の**面内成分 Jx/Jy** は**数値として持つだけ**で、力(重力・引きずり・歳差・熱)には
  接続していません。**これは「コア V2 を層で置き換えられる」という意味ではありません** ——
  置き換えの可否は 6 項(回転場の源・傾き・K_cs・能動系・保存/復元・移行)で別に測っています。
  (v1.45-b1・第265便c)層の **`J` に明示的に 0 を書いた層**は「**Q=0 を宣言した層**」として扱われます
  —— 何も書かない層(従来式に落ちる)とは別物で、この区別が**読込・編集・⏮ の保存/復元・A/B の複製・
  融合・粒子の詰め替え**のすべてで残ります(`Jx`・`Jy` も同じです)。また層が
  **慣性倍率 ζ(`inertiaScale`)**を持てるようになりました —— コア内訳(v2)の ζ と同じ意味で、
  **回転エネルギー・慣性 I と、回転場の源 J/ζ の両方**に効きます(**既定は 1**)。
  数値行では **ζ と「殻項の質量則」は既定(ζ=1・総質量)のとき行に出さず**、
  行にマウスを載せたときの**ツールチップから読めます**(宣言されているときは行にも出ます)。
  (v1.45-b1・第271便c)数値行に **ζ_eff の行**が増えました —— 層の状態は
  **J(角運動量)と Q(回転場の源)の 2 つ**で、ζ_eff=J/Q は**そこから読むだけの診断値**です。
  **一意に決まらないときは 0 や 1.0 で埋めず、「—」と理由**を出します(J=Q=0 は「**不定**」であって
  「不可」ではありません / J=0・Q≠0 は源だけ / Q=0・J≠0 は角運動量だけ / 符号が違う / 有限でない)。
  同じ行に **J・Q・ζ** が並ぶので、何が宣言されていて何が導出なのかがその場で読めます。
  また層の **Q には J の上限(±10¹²)が掛からなくなりました** —— 融合では Q を足し合わせるので、
  切ってしまうと**半径をひとつ動かしただけで保存したはずの源が縮む**ためです(**有限の数値だけを受け付け、
  そうでない値は 1 bit も書かずに断ります**)。**J=0 の層に独立の Q があるとき J だけを動かす編集**は、
  「ζ 一定」の意味が決まらないので **Q か ζ を一緒に指定しないと断られます**(黙って据え置きません)。
- **実行・停止・再開・初めから**、速度倍率(×0.25〜×8)
- **パラメータはいつでも編集可**(重力G、背景決定力D₀、空間引きずりkFrame、スピン斥力、摩擦、時空係数Kt(時間・光)…)— 実行中に即反映
  (v1.44・第257便c: **背景決定力 D₀ のスライダーは左端が 0** になりました〔重力 G・時空係数 κ と同じ〕。
  0 は「遠方の錨がない」状態で、近くに質量がひとつでもあればそれだけが基準になります。
  この値はもともと入力として受け付けていたもので〔⏱️🛰️📦🌀🔦🧭 など内蔵14本が D₀=0 です〕、
  スライダーから触れるようにしただけです)
- **「パラメータ」タブのカテゴリ構成(v1.44・第254便c)** — 並びは
  **時空 → 引きずり・測地線 → スピン・熱 →(実験箱/箱宇宙/相変化)→ シミュレーション → グラフ → 表示 → スケール → 共通設定**。
  「シミュレーション」にはソフトニング ε・時間経過倍率・摂動注入に加えて**ステップ診断を表示**・
  **物理ベンチ(200步)**・**コア軸の投影線を表示**(v1.45-b1・第275便e)が入り、
  「共通設定」には**保存量モニタ(P・L・帳簿)**・描画品質・文字サイズ・
  カメラ追従・上下反転が入ります(いずれもサンプルに依らない設定として端末に永続)。
  「表示」には**粒子表示倍率**(表示専用の虫めがね)・線の軌跡(表示時間を同じ行で直接編集)・
  決定力マップ・**空間メッシュ(「引きずりの度合」を同じ行に併設 — v1.44・第258便c で
  1 行にまとめ、数値を直接編集できるようにしました)**・
  **光線(基準波長 nm のスライダーを同じ行に併設)**が入ります。
  空間メッシュを ON にすると、その下に診断の**「τ 固定」**行が出ます(v1.44・第258便c。
  v1.44・第261便a から、この行を動かしても**描かれる歪みは変わりません** — 凡例の数だけが変わります)。
- **コア軸の投影線(v1.45-b1・第275便e — 表示専用)** — 親子コア(コア V2)のうち
  **`core.axisMode:"prescribed"` を宣言した粒子**だけ、コア回転軸を公転面へ**正射影**した線分を描きます
  (+J 側の端に丸)。傾き角 `core.tilt` が小さいほど線は短く(軸が画面手前を向く)、90° で最長になり、
  宣言した歳差角速度 `core.precessionRate` で方位が回ります。方位の初期値は `core.azimuthDeg` です。
  **3D 表示ではありません** — 面外成分は線の長さにしか現れず、エンジンの力学が読むのは従来どおり
  z 射影 J_z だけです。宣言の無い粒子には何も描かないので、**内蔵サンプルの画面は 1 ピクセルも変わりません**。
  **既定は「宣言した粒子だけ ON」です**(v1.45-b1・第276便c で言葉を統一) — トグル自体は ON ですが、
  内蔵サンプルは 1 本も `core.axisMode` を宣言していないので、線は 1 本も描かれません。
  「シミュレーション」カテゴリのトグルで消せます(既定 ON・端末に永続)。
  「表示」カテゴリのチェックボックスは **「⏮ 初めから」では保持**され、
  **「プリセット既定値に戻す」でサンプルの既定へ戻ります**(共通設定は端末の永続設定なのでどちらでも変わりません)。
  「スケール」の**ベースのスケール**は、内蔵サンプルで実際に使われている指数の組み合わせを
  一括で設定する選択肢です(表示換算のみ — 物理には一切入りません)。v1.44・第255便c で
  選択肢の名称を**三つ組表記**(距離/時間/質量)へ統一し、v1.44・第258便c で**2 行**に分けました
  — **1 行目=用途の短い名**(「🪐 衛星系」「🪐 巨大惑星系」「🪐 太陽系」「🪐 惑星・標準」など)・
  **2 行目=三つ組**(e6/e2/e25 など)です。行の小さなボタンで**表示形を切り替えられます**:
  **プルダウン**(選択肢は 1 行目だけ・2 行目は下の説明行に出る)と**2 行ボタン**
  (1 つのボタンに 2 行を積む)。どちらを使うかはこの端末に記憶されます。
  v1.44・第258便c で、**天体以上のスケールなのに「現実較正」のどれも使っていない選択肢を廃止**しました
  — 廃止したのは「☀️ 球状星団(e16/e12/e35)」と「🌌 実単位銀河(e19/e15/e38)」の 2 つで、
  どちらも**代替は「🌌 銀河(e19/e14/e34)」**です(残った選択肢のうち指数がいちばん近いものを機械で選びました)。
  この 2 つを使っていたのは 🍇🫐(球状星団 47 Tuc)と 🌃🛞(NGC 3198)の 4 本で、いずれも較正外です。
  **4 本の宣言指数は 1 文字も書き換えていません** — 観測転写サンプルでは指数そのものが
  「1単位=10^L m / 10^T s / 10^M kg」という実単位の宣言なので、代替を機械で書き込むと
  単位換算の表示が桁で狂うからです。廃止行に一致する指数のサンプル・セーブを開いたときは、
  説明行に「廃止: … — 代替は …」と出して**黙って「なし(個別指定)」にはしません**。
  **保存量モニタ**(共通設定)の永続はカメラ追従と同じ方式です — 一度でも自分で切り替えたら
  以後はその設定が常に勝ち、サンプルの `overlays.monitor` 宣言を既定に読むのは
  まだ一度も触っていない場合だけです(v1.44・第255便c)。
  **v1.44・第262便d から、保存量モニタを ON にすると「帳簿分母」と「帳簿残差」の 2 行が増えます。**
  相対残差は**割った分母によって桁が変わる**ので(固定した天体のスピンのエネルギーが分母の大半を
  占めるサンプルでは 30 倍以上変わります)、**どちらの分母で割ったかを画面に必ず出します**。
  **正本は「活動部分」**(固定天体の一定スピンぶんを除いた分母)で、**従来の分母も併記**します。
  行の末尾に出る状態名は `within`(活動分母で 1e−3 以下)/ `over`(**帳簿が閉じたうえで**超えた)/
  `undefined-terms`(**まだ数えていない項が残っているので、超えても合否を言えない**)です。
  **「通ったから合格」とは読まないでください** — この門は記録のためのものです(表示専用・物理不変)。
- **「サンプルを選ぶ」ウィンドウ(v1.44・第255便c / 第256便c)** — ヘッダの選択中サンプル名をタップすると開きます。
  絞り込みは**スケール / 分類 / E水準**の3次元+検索(名前 ja/en・ID・絵文字)で、
  **分類の「現実較正」は v1.44・第258便c で「現実較正(DFM)」と「現実較正(kF0)」の 2 つに分かれました**
  — 既定で空間引きずりが入っている版(physics.kFrame≠0)が **DFM 版 27 本**、切ってある対照版
  (kFrame=0)が **kF0 版 10 本**(🌙🔆☄️✨🌟📻📿🎐📡💍)で、合わせて **37 本**です。
  これは**宣言から導く表示だけの区別**で、サンプルの宣言・保存 JSON・署名には 1 bit も入りません。
  **v1.44・第259便d で 40 本 → 37 本になりました** — 参照が観測値ではなく**理論値**である 3 本
  (⭕ 地球と月・機構判別A / 📶 qLock 半径方向監査 / 📐 同・対照 q=3)を「**原理実証**」へ移したためです。
  理論値と突き合わせる対照器には観測誤差が付かないので、「現実との照合」の本数に数えません
  (**足りないものが解決したわけではありません** — 数える範囲を変えただけです)。
  分類は**表示だけの宣言**なので、力学・保存 JSON・署名は 1 bit も変わりません。
  **グループの絞り込みは廃止**しました。グループは**一覧の見出し**として残り、
  見出しのタップで開閉します。**既定は畳んだ状態**(いま読み込んでいるサンプルのグループだけ開く)で、
  開閉はこの端末に記憶されます。検索中・属性の絞り込み中は、一致があるグループが自動で開き、
  その間の開閉は一時的なもの(絞り込みを解くと記憶した状態へ戻ります)。
  v1.44・第256便c で、グループの説明行を**「グループ」**という名前にして**一覧のすぐ上**へ移し、
  開閉の記憶をグループの**表示名ではなく安定した id** で持つようにしました(グループ名が変わっても
  記憶した開閉が消えません。以前の記憶は自動で移行し、名前しか手がかりのないものも捨てずに残します)。
  「現実との照合」グループは v1.44・第220便から**太陽系 / 太陽系外**の 2 つだけで、
  サフィックスの無い「現実との照合」に残っているサンプルは 0 本です(v1.44・第258便c で機械確認)。
  v1.45-b1・第272便d に**新グループ「実在天体のアナロジー」(11 本)**を足し、
  v1.45-b1・第273便a で「現実との照合・太陽系外」を**「現実との照合・連星」**へ改名したので、
  現在の内訳は**太陽系 19 本 / 連星 19 本 / 実在天体のアナロジー 11 本**です
  (移したのは group の宣言だけで、較正サンプルの母集団 37 本は変わっていません)。
  一覧の開閉の記憶は**表示名ではなくグループ id** に付いているので、この再編でも記憶は飛びません
  (新グループだけ id `realAnalogy`・第273便a の「時計と重力」だけ id `clocksGravity` を足しました。
  第273便a の改名では**既存の id を 1 つも変えていない**ので、開閉の記憶はそのまま効きます)。
  v1.44・第258便c で 🌞**太陽系 — 内惑星(実単位)**と 🌇**太陽と金星(実単位)**を
  **それぞれ単独のファミリー**にしました(以前は ☄️水星のファミリーの変種でした)。
  単独ファミリーには「この仲間」の導線が出ません(他のメンバーがいないため)。
  表示専用 — 物理・保存 JSON には一切影響しません。
- **質量の由来チップ(v1.45-b1・第272便d)** — 「説明」タブのチップ列に、そのサンプルの
  bodies の質量が**どこから来たか**を 1 行で出します。**宣言専用**で、**自動判定はしません**
  (`massCalibration` が無いこと・f=1 であること・「📏 現実較正」チップが付いていることのどれも、
  「観測質量だ」と認定する根拠にしません)。表示は 3 つです ——
  **「観測解の質量を入力」**(公表された観測解から換算した質量。**「真の質量」ではありません** ——
  観測解はケプラー軌道・GR タイミング・波形推定などの**推定モデル**を通した量で、モデルが変われば
  値が変わります)/ **「DFM 質量補正 f=…」**(観測解の質量にこの模型の補正 f を掛けた値。
  **実在質量の主張ではありません**。f はそのサンプルの `massCalibration` 台帳の値をそのまま読み、
  **丸めずに**出します)/ **「観測解と模型値の混在」**。チップに触れると由来
  (観測記録の `record_id` / 公表解の `solution_id` / 推定モデル / 換算)が出ます。
  宣言はプリセットの**外**の表に置いてあるので、**署名(presetSig)・保存 JSON・力学は 1 bit も変わりません**。
  いま宣言があるのは **14 本**(観測解 6・DFM 版 7・混在 1)で、🌃 NGC 3198 のように
  **NFW ハローなど模型の質量が載っている系は「観測質量」と表示しません**(明示的に「宣言しない」と
  書いてあります)。**このチップは較正の合否とは無関係です。**
- **空間メッシュチップ(v1.45-b1・第272便d → 第273便d)** — 同じチップ列に、空間メッシュの
  **宣言**と**いま走っている状態**を分けて出します。scalar / local が実際に作動していれば
  「空間メッシュ: scalar(local)・実験」、宣言はあるが測地線トイを選んでいなければ
  「空間メッシュ: 宣言のみ」、**入場条件が立たない步・積分器が止めた步**では
  「**メッシュ未作動: 理由**」と出ます。**入場できたことと、実際に当たったことは別です** ——
  第273便d から、実行中は**走っている宣言**を先に読み、停止理由(`geoToyStop`)が立っていれば
  作動中とは書かず、**走行中に理由が変わればチップだけがその場で追い付きます**
  (説明パネル全体は作り直さないので、他のチップは 1 文字も動きません)。
  `physics.spaceMesh.toyAllowDrag` で引きずりと重ねた宇宙では
  「**+引きずり重畳(明示した実験 variant)**」が末尾に付きます(汎用の係数ではありません)。
  **`lawVersion:"complex"` は「複素決定力場」とは表示しません** —— これは**意図された未接続**で、
  complex の重ね合わせ A は速度ではないため積分器へ接続しておらず、**作動する步が 1 つもない**からです
  (停止理由 `complexNotVelocity` として出ます)。geoPN=3 であることも作動の根拠にしません。
  **表示だけの読み口**で、較正クラスの受理条件は 1 つも変えていません。
  **v1.45-b1 時点で `lawVersion:"complex"` を宣言している内蔵サンプルは 0 本です**
  (宣言があるのは 🪁 scalar・🎋 scalar・🩻 local・🪟 宣言のみ の 4 本 —— **v1.45-b1・第274便c で 🎋 が加わりました**)。
- **正式判定値の行(v1.45-b1・第273便c)** — 観測結果カードの**下**に、判定器の正本
  (`tests/out/calaudit-w249.json`)が出した**判定段の値**・判定段(h / h-4 / h-8)・σ 倍・
  門の状態(合(3σ) / 否(3σ) / 数値未解決 / mapping-unresolved / 未判定)を 1 行ずつ出します。
  **このページでは何も測り直していません** —— 正本の**転記**です。観測結果カードの行は
  宣言したときの**静的な文面**(多くは h 段)なので、**混ぜて読まないでください**。
  **σ 倍は合否ではありません**(門の状態のほうが判定です)。
  **正本を測ったときと入力の署名(presetSig)が違うサンプルでは数を出しません**。
  **鮮度の見方**: リポジトリの側では **① 表 ↔ 正本**(転記が 1 行ずつ一致)と
  **② 正本 ↔ 現行 html**(正本が記録した SHA-256 = いまの `beta/index.html`)の
  **2 段の鎖**で見ます。**エンジンだけが変わった場合も html が動けば ② が落ちる**ので捕まります
  (どちらも QA `ui.assessedValuePanel` と `lint.calauditMergeKeyHash` が突き合わせます)。
  **正直な限界**: 捕まらないのは**ページを開いて見ているとき**です ——
  ページは自分自身の SHA-256 を確かめられないので、実行時に効くのは **preset 署名の一致**だけです。
  刻んである hash は「この表を書き込む前の html」のもので、**刻めばその hash が動く**ため
  同じファイルの hash とは**原理的に一致しません**(「どの走行から転記したか」の記録です)。
- **較正の現在地チップ(v1.44・第261便c)** — 「説明」タブのチップ列と観測結果カードの先頭に、
  **その較正がいまどこに居るか**を 1 行で出します。状態は 5 つだけです ——
  **✅ 実測合格**(σ を持つ量が 3σ の門を実測で通った。範囲つきなら**その量だけ**)/
  **🟡 精度向上で合格見込み**(下の門を 5 つとも機械で通った)/ **⏳ 収束確認・未完** /
  **🔎 測定再検証** / **🔁 較正要再確認**(宣言したときの署名が変わると**自動で**立ちます)。
  **このチップに赤はありません** —— 「合わなかった(否)」は
  `docs/CALIBRATION_VERDICT_v1.44.md` の 4 値(合/量限定合/否/保留)の側にあり、
  **灰色の「収束確認・未完」「測定再検証」は否定ではなく「まだ言えない」** という意味です。
  「合格見込み」が立つ条件は 5 つで、(1) 対象量・観測版・単位・時系・窓・抽出法・補正係数を**固定**する
  (刻みごとに測り直さない)、(2) 刻みを **4 段以上**積み、重複検出・NaN・未完走・位相のつなぎ損ね・
  丸めの床を**除く**、(3) 隣り合う 3 段から読む収束の次数が**段をずらしても変わらない**、
  (4) 外挿を**段をずらして**比べ、**別の計算法による独立な推定**と突き合わせる、
  (5) **外挿値と観測値の差 + 不確かさの幅 ≤ 3σ**。
  **この不確かさの幅は数学的な上限ではありません**(3 つの寄与を足すという**宣言**です)。
  いま宣言があるのは 6 本で、**✨ αケンタウリAB と 🌟 シリウスAB が「実測合格(公転周期という部分量だけ)」**、
  **⚡🩺 が「測定再検証」・🧮🧶 が「収束確認・未完」**です。
  **「精度向上で合格見込み」は 1 本もありません。**
  表示専用の宣言で、**物理・保存 JSON・署名には 1 bit も入りません**。
- **空間メッシュ(蓄積格子)(v1.44・第257便c / 第258便c / 第261便a)** — 「表示」の**空間メッシュ**トグルの既定表示。
  **質量の大きい 2 粒子の重心を原点にした正方格子**を、その時刻の速度場が作る**歪み**で写して描きます。
  作り方は5段です — (1) **質量上位 2 粒子の重心**を原点にする(**2 位と 3 位が同じ質量のときは 1 位の粒子だけ**を
  原点にします — v1.44・第261便a。原点が決まらない系では描きません)
  (2) 原点まわりの**低解像度の標本格子**(17×17)へ速度場を**一度だけ**評価して**凍結**する
  (3) 凍結した場の中を進む**ひとつの写像**をルンゲ=クッタ法で解く
  (4) **原点に近い交点から順に**、隣の交点との差を**足し上げていく**
  (5) 画面へ映す。
  **辺ごとに角度を足していく作り方は採っていません** — その作り方だと格子を細かくするほど歪みが増え、
  同じ交点でも通ってきた道で値が変わってしまいます。ひとつの写像から差を足し上げるので、
  **どの道を通っても同じ交点は同じ場所**になります(一周して戻った誤差は計算機の丸めだけです)。
  「引きずりの度合」(既定 1.0)で歪みの強さを変えられます — v1.44・第258便c で
  **トグルと同じ 1 行**にまとめ、**数値を直接入力できる**ようにしました(「線の軌跡」と同じ作法)。
  v1.44・第261便a から、**スライダーは 0 から 10 までを指数目盛**(いちばん左だけが特別に 0)で動き、
  隣の欄には **100 などの直値**も入れられます(上限 10000 で頭打ちになります)。
  **歪みはセル(マス目)の幅で規格化してあるので、度合 1 のときの最大のずれは、
  どのサンプルでもマス目の 0.25 個ぶん**です(実測: 内蔵 113 本で 0.22〜0.28・折返し 0 本)。
  **これは絵の見やすさのための規格化であって、「度合 1 が物理的に正しい引きずり」という意味ではありません。**
  **完全に表示専用**で、サンプルの署名にも物理パラメータにも入りません(動かしても計算は 1 bit も変わりません)。
  上げすぎると格子のマス目が裏返ります(**折返し**)。そのときは**裏返ったマス目だけが赤く縁取られ**、
  格子全体が破線になって「折返し(表示の限界)— 赤いセル=折返し」と出ます(v1.44・第258便c:
  色が読めない環境でも破線と凡例で分かるようにしてあります) — **空間が反転したのではなく、
  表示の時間の幅と刻みの限界**です。
  **凡例には度合 g・歪み(マス目の幅に対する比)・v_ref・実際に使った τ と、τ が自動か固定かを出します**
  (v1.44・第261便a)。**v1.44・第262便d から、凡例に「交点 61/81」のように読めた交点の数も出ます。**
  度合を上げすぎると、折返しより先に**格子そのものが消えます**(場が読めない交点が増えます)。
  **読めた交点が全体の 1/4 を割ると、琥珀色で 1 行**「格子が読めない交点が多い — 引きずりの度合を下げると戻る」
  と出ます(実測: 🎠 銀河メッシュ渦巻は度合 1 で 61/81・度合 100 で 5/81)。
  **度合はこちらでは戻しません** —— 戻すときは同じ行の **⟲ ボタン**(度合を 1 に戻す)を押してください。
  この警告も**完全に表示専用**です(署名にも物理パラメータにも入りません)。
  **v1.44・第261便a から、「τ 固定」を入れても値を変えても、描かれる歪みは変わりません**
  (格子は 1 bit も動きません) — 歪みは度合とマス目の幅だけで決まり、**τ は凡例の診断表示**として残ります。
  **強さの違いは絵ではなく、凡例の v_ref と τ の数で読んでください。**
  **3 体以上で宣言の無い系**では、場を**上位 2 体**から作っているので、その旨も凡例に出します
  (v1.44・第258便c — 系全体のメッシュではありません)。
  **これは「凍結した場の診断図」であって、過去から運ばれてきた物質のメッシュではありません。**
  銀河のサンプルでは、場が**有効な範囲の外**(中心から遠すぎるところ)では交点を描きません。
- **空間線(v1.44・第255便c / 第256便c)** — 「表示」の**空間メッシュ**トグルの診断表示のひとつ。主要天体と**重心**から
  基準方向へ複数本の線を飛ばし、**凍結した時刻の場**を表示用パラメータ τ で追跡します
  (dx/dτ=u+c_line ê・dê/dτ=(I−êêᵀ)Bê を中点法で積分)。**光子でも物質線でもなく、c_line は
  描画規約であって光速ではありません**(光線の屈折率・局所時計・減光は呼びません)。
  本数は光線と同じ品質縮退で減り、線は上限つきのキャッシュから描きます。**粒子がちょうど2つの系**では
  2天体を結ぶ線の点が**重心**(⊙ の二重環。等質量では中点と一致)になります
  (v1.44・第257便c: 3つ以上の系では「対の中間地点」が意味を持たないので出しません)。
  v1.44・第256便c で **表示方式を1つだけに整理**しました(蓄積格子 / 空間線 / 参照ガイド /
  輸送された物質線 / 銀河の物質線 の**排他**。v1.44・第257便c から**既定は蓄積格子**で、
  残り4つはサンプルが明示宣言したときだけの診断です。**参照ガイド**は瞬時の写像の格子を
  χ の濃さだけで描く診断で、**座標は混ぜません**〔全域が同じ変換で一緒に動きます〕)。
  **線の長さは系の長さで決まります**(連星は2天体の分離の0.75倍・銀河は中心から外縁までの2.4倍)ので、
  **ズームしても線は伸び縮みしません**(ズームは映し方だけを変えます)。色は**琥珀**にして
  **線の軌跡(氷青)と混ざらない**ようにしました。銀河の物質線は**表示していない間は動かさず**、
  再表示のときに現在時刻から引き直します。**表示のみで力学・帳簿には一切影響しません**。
- **コメント付きセーブ/ロード**(localStorage保存、JSONエクスポート/インポート対応)。
  v1.12: インポートは**単独プリセット/配列/{customPresets}/バックアップ全体**を自動判別し、
  ID重複はスキップして件数を報告。**ファイルとして保存/共有**(iPhoneは共有シート)、
  `seed` 指定による初期配置の完全再現、overlays の trail(軌跡)/monitor(保存量モニタ)指定に対応。
  v1.32: セーブに箱宇宙(universeBox)パラメータを含めて保存・復元(旧セーブとの後方互換維持)
- **日本語 / English 切り替え**(v1.10)— ヘッダーのボタンでUI・サンプル名/説明・ヘルプ・
  グラフラベルまで全て切り替わり、選択は端末に記憶されます。英語UIではAI生成の名前・説明も英語になります。
- **AIでシミュレーション追加** — 既定は Claude API(**Sonnet 5** → 検証NGでリトライ →
  **Opus 5** フォールバック)。v1.10 からプロバイダを選択可能: **Claude / OpenAI互換 API
  (URL変更可、ローカルLLMにも接続可) / Google Gemini**。モデルIDは自由入力(候補は datalist で提示)。
  v1.31: **ベースのサンプル選択**(任意)— 既存プリセット(カスタム含む)のJSONを土台に
  改変生成できます。選択したベースのJSONは生成時に選択中のAIプロバイダへ送信されます。
  APIキーはプロバイダ別に管理し、既定では**端末に保存せずメモリのみ**(保存はチェックで明示オプトイン)。
  送信先は**選択・設定したAPIエンドポイント**です(OpenAI互換では入力したベースURLへ送信されるため、
  URLは必ず確認してください)。v1.32: **送信内容の事前確認** — 「送信内容を確認(実際に送るテキスト)」
  を開くと、要望文・ベース選択を反映した送信直前の実テキストをその場で確認できます。
  **v1.42(第178便): AIタブを「自由生成」へ一本化しました** — 第170便の
  「実天体カタログ」・第176便の「観測採取」という**生成モードの分岐は撤去**しました。実機で
  「コピーしたプロンプトがモードに連動せず、観測採取モードでも旧プリセット用プロンプトが
  配られる」「貼付先が分からず、観測レコード配列がプリセットのインポート欄へ入り
  『追加0件/不正9件』になる」という失敗が起き、原因が分岐そのものだったためです。
  いまのAIタブは **要望 + ベースのサンプル + プロバイダ/モデル + プロンプトのコピー(1系統)
  + 貼付欄1つ + アプリ内生成ボタン(常時有効)** だけです。

  - **配るプロンプトは1系統**。アプリ内生成・「生成用プロンプトをコピー」・「短縮版をコピー」の
    3経路とも「仕様 + **出力の使い分け** + **内蔵の実天体サンプル一覧** + **少数ショット例** +
    要望」という同じ骨格を運びます(短縮版は仕様を [docs/AI_SPEC.md](docs/AI_SPEC.md) への
    リンク参照に置き換えるだけ)。
  - **少数ショット例が必ず1つ入ります**。選択中の「ベースのサンプル」、未選択なら現在読み込んで
    いるサンプルのJSONです(例の質が生成JSONの質をいちばん左右する、という設計指針)。
  - **出力の使い分け**: ①要望が内蔵の実天体サンプルに合致 → そのサンプル名を案内して生成しない
    ②実在天体で観測値をウェブ出典から写せる → **ObservationRecord 配列を優先**(各行に url と
    取得日が必須・記憶で埋めるのは禁止)③それ以外 → プリセットJSON。
  - **貼付欄は1つだけで、形はアプリが自動判別**します。レコード配列なら第176便の決定的構築
    (単位換算・スケール指数・配置と初速・qLock の q・共有補正 D₀ + 周期 ±1% の自己診断)、
    プリセットJSONなら下の**整形・安定化パス**へ回ります。プリセットのインポート欄に
    レコード配列を貼った場合は「これは観測レコードです — AI追加タブの貼付欄へ」と案内します。
  - **整形・安定化パス(新設)**: 貼り付け・生成されたプリセットは決定的な後処理を通します。
    ①κ=G/c₀²(物理対応ロック)の整合 — 実較正を宣言したサンプルだけ再導出 ②`scaleExp` の
    規約(L−T=4・M+2T−3L=11)検査 — **警告と提案のみ** ③円軌道と判る配置の速度をケプラー
    再導出(既定ON・チェックボックスで解除可)— ただし**束縛楕円の帯 0<v/v_circ<√2 の中は
    作者の意図として1ビットも触りません**。直すのは脱出速度以上・中心へ落ち込む配置だけです
    ④`camera.scale`・`softening`・`timeScale` を明らかに壊れている場合だけクランプ
    ⑤複製状態で短い試走(NaN・発散・全粒子脱出)— **不合格は理由付きで差し戻し**、
    合わせ込みのフィットはしません。**変更した点は必ず全部画面に列挙**します(黙って書き換え
    ません)。内蔵サンプル73本はこのパスで**変更0件・自己診断 全PASS**(QA `ai.stabilize`)。
  - **取込の堅牢化**: `JSON.parse` に失敗したときだけ、スマート引用符(“ ” ‘ ’)・全角の構造記号
    (：，｛｝［］)・BOM・ゼロ幅文字を正規化して読み直し、「補正して読み込みました」と告げます
    (全角→ASCII は**文字列リテラルの外だけ** — 説明文は1文字も変わらず、**数値も一切変わりません**)。
    解析エラーは**行・列・不正文字**を表示します。**逆行自転**は `rotation_period` の**負値**で
    書けるようになりました(冥王星のような逆行天体。符号は spin へ決定的に反映し宣言します)。
  - 第170便の実天体カタログと第176便の観測レコード機構は**内部資産として温存**しています
    (プロンプトの内蔵一覧・貼付欄の自動判別・QA `ai.catalog-integrity` / `ai.placement-determinism`
    / `ai.obs-schema` / `ai.obs-build` はそのまま。生成物のビット一致も維持)。

  仕様の正本は [docs/AI_SPEC.md](docs/AI_SPEC.md)(英語先行の二言語構成・URLは従来どおり1本)で、
  アプリ内の文字列との逐語一致を QA `prompt.spec-sync` / `ai.obs-schema` / `ai.unified-prompt` が
  機械固定しています。

  > ⚠️ **APIキーの取り扱いに関する注意(BYOK方式)**: 本アプリはバックエンドを持たず、
  > キーはブラウザ内でのみ使用されます。ただし一般に、ブラウザへの長期APIキーの入力は
  > 各プロバイダが本番用途に推奨する方式ではありません(公式推奨はバックエンド経由)。
  > 利用する場合は **(1) 利用上限を設定した専用キーを使う (2) 「キーを保存」は共有端末では
  > 使わない (3) `localStorage` は暗号化保管庫ではないと理解する (4) エクスポートした
  > JSON・スクリーンショットにキーを含めない**、を推奨します。キーが漏えいした可能性が
  > ある場合は、プロバイダのコンソールから直ちに無効化してください。v1.11 で生成の表現力を拡張:
  **熱浴(ヒーター/冷却板)・放射冷却・対流・ブラックホール(光子捕捉)**が要望文から
  生成できます(例:「ブラックホールが見たい」「床で温めて天井で冷やす対流実験」)。
- 1本指ドラッグで移動 / 2本指ピンチで拡大縮小 / 粒子タップで固有時計(τ/t)表示

## 仮定物理法則(要約)

1. 空間は実体を持たず、質点同士の相対配置だけで決まる(絶対空間なし)
2. 空間座標の「決定力」= 質量÷距離。遠方の大規模構造は背景決定力 D₀ として効く
3. 決定力の勾配が重力(ニュートンの逆二乗則を自動再現)
4. 質点は「近くの空間」に対して慣性を持ち、空間ごと引きずられる(フレームドラッグ)
5. 決定力が高い場所の時計は遅く、空間(共動フレーム)に対して速く動く時計も遅れる。
   光は同じ ψ=W/Kt で屈折して曲がる(v1.7 で時間と光を単一の ψ に統一。n=e^{2ψ})
6. スピン=熱(温度 T=Is²)。衝突で速度がスピンに変換され、スピンは斥力(圧力)を生み、拡散して平衡に達する
7. すべての相互作用は作用反作用対称で、運動量・角運動量を厳密に保存する(遠方宇宙との交換は帳簿づけ)。温度 T の質点はウィーン則 λ=b_m/T の光を放射する

公理系(A1〜A13)・帰結(T1〜T9)・計算式(E1′〜E12)の全文は
[docs/PHYSICS.md](docs/PHYSICS.md) を、公理改訂の導出は
[docs/DERIVATIONS.md](docs/DERIVATIONS.md) を参照してください。

## 本プロジェクトの位置づけと限界

本シミュレータは「仮定の物理法則」(決定力場モデル: DFM)の帰結を観察する思考実験・教育用ツールであり、現実の物理学の代替理論を主張しません。

- A11 は優先フレームと絶対的同時性を仮定します。v1.7 の ψ 統一により、**時計と光の弱場一次効果** — GPS 時計の進み(+38.5 μs/日)・太陽縁の光偏向(1.7512″)・シャピロ遅延 — は写像 Kt=c²/G ひとつで定量再現します(第3次レビューで検算・機械検証。DERIVATIONS §12)。**物質軌道の 1PN** は二層です: **legacy 力学(E4+E6′、既定)では再現しません**(E6′ の引きずりは GR のレンズ=サーリングと同符号の微小な「後退」のみを与える)。一方、v1.20 の **E12 測地線1PN(geoPN=1)** は、静的・単一支配源・u=0 の限定条件下で水星近日点前進 42.98″/世紀を同じ写像 Kt=c²/G で定量再現します(機械検証 V18〜V20。☿サンプル)。連星パルサーの重力波減衰(瞬時遠隔作用 L8)・物質側のローレンツ応答(Michelson–Morley 型の光速等方性 L12)は引き続き再現せず、目的外です(THEORY_SYNTHESIS §6 Negative claims)。なお、シミュレータ上では Kt(時計・光)・cLight(E12・運動時間項)・G(重力)を独立に編集できるため、SI 対応の主張は**物理対応条件 Kt=cLight²/G** を満たす設定に限られます — 画面プリセットは見やすさ優先の誇張アナログでこの条件外です(v1.25 明記。PHYSICS §5「物理対応条件」)。v1.26 でパラメータタブに**物理対応ロック**を追加: オンで Kt=cLight²/G を自動維持し(Kt は導出値)、オフでは「一般化トイ設定(GR較正条件外)」のバッジを常設表示します。
- 「スピン=熱」(A9)とスピン斥力による圧力(A10)は、実在の分子運動論・等分配則とは異なる仮定上の機構です。個別質点のウィーン型放射(A12)も黒体放射(プランク分布)の簡略アナロジーです。熱部分の正確な位置づけは「角運動量を持つ2D散逸粒子系の熱力学類似モデル」であり、理想気体の状態方程式(スピン圧は P∝ρ²T)・潜熱を持つ相転移・対流の定量則は再現しません(第4次レビュー裁定。THEORY_SYNTHESIS Negative claims 6・L15〜L17)。
- その代わり本モデルは、運動量・角運動量の厳密保存(V1)とエネルギー帳簿の閉性(V10: kRep=0 域 / V10b: 固定スピン+kRep>0 域。スピンが変わる過程での斥力ポテンシャル変化は帳簿外=L17 として明示)を機械検証で保証します。「もし空間がこのように振る舞ったら何が起きるか」を、対比材料として安心して探索してください。

## ドキュメント(model-routing スキル成果物)

| ファイル | 内容 |
|---|---|
| [docs/PHYSICS.md](docs/PHYSICS.md) | 仮定物理法則の体系化・調整と補完・計算式 |

※開発時のモデル割当・実装/執筆の実行指示書・レビュー記録などの内部開発文書は、
2026-08-01 に本リポジトリの管理外(非公開)へ移した(旧 `docs/dev/` — 履歴は Git に残る)。

## 理論・論文化レイヤー(第5次レビュー裁定 #14 で導線追加)

| ファイル | 内容 |
|---|---|
| [docs/THEORY_SYNTHESIS.md](docs/THEORY_SYNTHESIS.md) | 論文として主張・防御できる理論構造(3層構造・中核主張 C1〜C8・Negative claims・限界 L1〜L17) |
| [docs/DERIVATIONS.md](docs/DERIVATIONS.md) | 解析的導出と外部AIレビュー全5次の裁定記録(数学的正本) |
| [docs/EXPLAINER_HIGHSCHOOL.md](docs/EXPLAINER_HIGHSCHOOL.md) | 高校生向け解説(英語版: [EXPLAINER_HIGHSCHOOL_EN.md](docs/EXPLAINER_HIGHSCHOOL_EN.md)) |

## 開発・QA(Phase 1 再現性基盤)

新規cloneから1コマンドで全機械QAを実行できます(項目数は増減するため固定数を記載しない —
現在数と結果は `tests/out/qa-results.json` が正。v1.25 で QA が機械強制):

```bash
npm ci && npx playwright install --with-deps chromium   # 初回のみ(lockfile 固定 — CI と同一手順。依存更新は明示的な PR で)
npm test          # 全QA(約5分)。結果は tests/out/qa-results.json
npm run test:fast # 長時間挙動テストを省略した高速版
node tests/perf.mjs # 動的性能ゲート(v1.31 — 同一Chromiumで root と beta を実測し中央値比 ≤1.10 を検査)
```

GitHub Actions([.github/workflows/ci.yml](.github/workflows/ci.yml))が push/PR ごとに
同じスイートを実行し、機械可読な結果JSONを artifact として保存します(QA は root/beta の
マトリクス、性能ゲートは独立ジョブ perf)。
アプリ本体(index.html)は引き続き依存ゼロの単一ファイルです。

### 公開サイトに影響を与えないテスト運用(v1.27 公開後の3層方式)

本番(GitHub Pages のルート)は **main のルート `index.html`** だけが正です。
変更のテストは以下の3層で行い、本番には最後のマージまで触れません:

1. **ローカル + 実機(推奨・第一選択)** — 開発ブランチで
   ```bash
   python3 -m http.server 8080   # リポジトリ直下で
   ```
   を起動し、同一 Wi-Fi の iPhone Safari から `http://<PCのIPアドレス>:8080/` を開く。
   `file://` では確認できない HTTP 挙動(保存・入出力・AI接続)も検証でき、
   公開には一切影響しない。機械QA(`npm test`)もブランチ上で完結する。
2. **公開ベータ(サブパス)** — 候補版を main の `beta/index.html` として置くと
   `https://tty-imamura.github.io/dfm-simulator/beta/` で共有できる(ルートは無影響)。
   ⚠ **同一オリジンのため localStorage(`hp_*` — セーブ・言語設定・APIキー)は本番と
   共有される**。ベータ試験の前にセーブを JSON エクスポートでバックアップし、
   スキーマを壊しうる実験は第1層で行うこと。試験後は `beta/` を削除してよい。
3. **完全分離が必要な場合** — 公開の別リポジトリ(例: `dfm-simulator-dev`)に
   Pages を有効化し、`git push <dev-remote> <branch>:main` で配備する。
   別オリジンになるため localStorage も完全分離される(大規模な UI/スキーマ変更向け)。

## ライセンス・引用

- コード(index.html ほか): [MIT License](LICENSE) — © 2026 Tetsuya Imamura
- 理論文書(docs/): [CC BY 4.0](docs/LICENSE)(表示: Tetsuya Imamura)
- 引用方法: [CITATION.cff](CITATION.cff)(GitHub の「Cite this repository」からも取得可)
- ソフトウェアアーカイブ(Zenodo): Concept DOI
  [10.5281/zenodo.21454188](https://doi.org/10.5281/zenodo.21454188)
  (全バージョン共通 — 常に最新版へ解決。`CITATION.cff` の `doi:` が使用)/
  v1.41.0 の Version DOI
  [10.5281/zenodo.22025091](https://doi.org/10.5281/zenodo.22025091)。
  いずれもソースコードのアーカイブであり、論文そのものの DOI とは別レコードである。

### 提出版タグとアーカイブ DOI(論文投稿時。第5次模擬査読 裁定 #14 — **2026-07-20 実施済み**)

論文の全数値を単一の不変コミットに固定する手順と、その実施記録:

1. **提出版タグ** ✅ — `paper-v1`(注釈付き、= paper v0.8 / release v1.27.0 系のコミット。
   タグ時点の `npm test` は 109/109 ALL PASS、`tests/out/qa-results.json` と全図 JSON が
   同一コミット SHA+実行環境を記録)
2. **Zenodo DOI** ✅ — GitHub 連携の Release 公開で自動アーカイブし、
   **Version DOI: doi:10.5281/zenodo.21454189**(paper-v1 当該コミット固定 — 論文が引用)と
   **Concept DOI: doi:10.5281/zenodo.21454188**(全バージョン共通 — `CITATION.cff` が使用)が
   発行された。(a) `paper/dfm-paper.tex` の Reproducibility 節、(b) `CITATION.cff` の
   `doi:`、(c) 本節に記載済み。
   DOI 挿入後のコミットには `paper-v1.1` を再タグする(**GitHub Release は作らない** —
   第2の DOI 発行を防ぐ。Zenodo は Release 公開で発火するため)
3. **補足資料**: `tests/out/qa-results.json`(全QA結果)と `paper/figures/*.json`
   (図の生成パラメータ・実測値)を論文の supplementary material として同梱する

## AI追加機能のコスト

プリセット生成1回 ≈ $0.03〜0.05(約5〜8円、Claude Sonnet 5 既定 — 第170便で Haiku 4.5 から引き上げ・第238便で Haiku は候補から撤去)。

## 技術メモ

- 単一HTMLファイル・外部依存ゼロ(CDNなし)。Canvas 2D + Vanilla JS。
- 物理: 全対 O(N²)、粒子上限600、セミ・インプリシット・オイラー+サブステップ。
- 品質ゲート: 構文検査 / headless Chromium(390×844)で全プリセット起動・コンソールエラー0・
  NaNなし・UI操作列・APIモックによる段構えE2E、計31項目 PASS(2026-07-12)。
- 版ごとの変更履歴は **[CHANGELOG.md](CHANGELOG.md)** を参照(2026-07-21 に本節から分離)。
